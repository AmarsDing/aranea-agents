# 13-AI智能运维 实施进度文档

## 总体完成度：约 15%（v2.0 新口径：文档重构完成，代码仅骨架与可复用资产就位，aranea 接入链路与 MCP-Server 未开始）

> 检查日期：2026-07-28（v2.0 文档重构后全量重置基线）。检查范围：TwinServer（api/app/pkg）、TwinWeb（pages/services/router）、DTRoomTwin（Source）。
> **基线变更说明**：2026-07-28 按《TwinMonitor-AI融合总体设计方案》D1 决议与 aranea-agents 底座决策（https://github.com/AmarsDing/aranea-agents），本模块三份文档（需求/概要/详细设计）全量重构为 v2.0——模块定位由"自研 AI 平台"调整为 **智能运维治理与接入层**，LLM 模型目录、Agent 运行时、编排引擎、五层记忆等 AI 内核能力全部委托 aranea-agents。原 v1.x 进度口径（2026-07-27 批次C 约 45%）作废，v1.x 已建后端资产按文末「v1.x 资产处置表」逐项裁定（复用/重构/废止），本文档自 v2.0 功能点重新计度。

## 功能点清单（v2.0 口径）

| 序号 | 功能点 | 文档定义 | 后端完成 | 前端完成 | UE5完成 | 状态 |
|------|--------|----------|----------|----------|---------|------|
| 1 | aiops 微服务骨架（HTTP:8100 / gRPC:9100，cmd/wire/configs/四层） | 概要设计 §5 | ✓（v1.x 已建 `app/aiops`，可复用；HTTP 服务注册组需按 v2.0 端点重组） | — | — | 已完成（待重组） |
| 2 | aranea 接入管理（实例登记/凭据加密/连通性探测/健康监测/ai_aranea_instances） | 需求 §5.1、详设 §1.1 | ✗（AraneaClient/HealthProber 未建） | ✗ | — | 未开始 |
| 3 | 种子同步与能力清单（12 预设 Agent 种子注册、ai_sync_records 差异高亮） | 需求 §5.2、概要 §9.1 | ✗（SeedSynchronizer 未建） | ✗ | — | 未开始 |
| 4 | 运维智能体目录（治理镜像 ai_agents：分类/风险/启停/使用统计/在线测试） | 需求 §5.2、详设 §1.2 | ◐（v1.x biz/agent + data/agent_repo 存在，需重构为种子源+镜像语义：增 aranea_agent_id/sync_status/is_preset/tool_whitelist） | ✗ | — | 待重构 |
| 5 | 场景模板管理（ai_scenario_templates 注册/参数契约/风险/版本/导入导出/立即执行） | 需求 §5.3、详设 §1.3 | ✗（v1.x biz/workflow 整体废止，替换为 scenario_template 域） | ✗ | — | 未开始 |
| 6 | 任务中心（ai_tasks 镜像/幂等键/状态回写/节点进度/日志透传/取消/ai_task_nodes） | 需求 §5.4、详设 §1.9/1.10 | ◐（v1.x biz/task + data/task_repo 存在，需重构为 aranea 执行镜像语义；WebhookReceiver/FallbackPoller 未建） | ✗ | — | 待重构 |
| 7 | aranea Webhook 回调端点（HMAC 签名校验 + event_id 幂等去重） | 需求 §5.4、概要 §7.13 | ✗ | — | — | 未开始 |
| 8 | 审批中心 HITL（ai_approvals：interrupt_id/Resume/超时自动拒绝/promotion 源） | 需求 §5.5、详设 §1.11 | ◐（v1.x biz/approval + data/approval_repo 存在，需扩展场景内审批语义与 aranea Resume 调用） | ✗ | — | 待重构 |
| 9 | 脚本库（ai_scripts CRUD/版本/参数化执行；启用自动注册 MCP 工具） | 需求 §5.6、详设 §1.4 | ◐（v1.x biz/script + data/script_repo 可复用，需增 mcp_risk_level 与 MCP 注册联动） | ✗ | — | 待改造复用 |
| 10 | 配置模板（ai_config_templates + config.render MCP 工具） | 需求 §5.7、详设 §1.6 | ◐（v1.x biz/config_template 可复用） | ✗ | — | 待改造复用 |
| 11 | 定时任务（ai_scheduled_jobs：Cron 调度场景模板） | 需求 §5.8、详设 §1.5 | ◐（v1.x biz/scheduled_job 存在，workflow_id 需改 scenario_id） | ✗ | — | 待重构 |
| 12 | TwinMonitor-MCP-Server（ai_mcp_tools/ai_mcp_clients/SSE 端点/安全校验层/调用历史） | 需求 §5.9、详设 §1.7/1.8 | ◐（v1.x biz/mcp + mcp_registry 为"外部 MCP 接入"方向，与 v2.0"能力对外开放"方向相反，需大改；mcp_external_servers 表废止） | ✗ | — | 待重构 |
| 13 | AI 根因分析（告警触发/aranea RCA run/症状证据时间线/置信度/误报/知识记忆引用） | 需求 §5.10、详设 §1.14 | ◐（v1.x biz/rca + data/rca_repo 存在，执行需委托 aranea，表增 aranea_run_id/引用字段） | ✗ | — | 待重构 |
| 14 | 告警自动诊断（诊断 Agent + 只读命令集经 MCP→16 执行） | 需求 §5.11 | ◐（v1.x biz/auto_analysis 存在，执行通道需改 MCP） | ✗ | — | 待重构 |
| 15 | AI 洞察看板（MTTR/修复成功率/准确率/降噪率/知识增长 + aranea 运行指标） | 需求 §5.12 | ◐（v1.x biz/insight 四源聚合+60s 缓存可复用，LLM 运行指标数据源改 aranea 配额 API） | ✗ | — | 待改造复用 |
| 16 | 知识沉淀联动（10 知识库写入 + aranea L3 记忆互链） | 需求 §5.13、概要 §9.7 | ✗ | ✗ | — | 未开始 |
| 17 | 数据库表 v2.0（twinmonitor 8 配置表 + twinmonitor_log 8 镜像/分析表） | 详设 §1.1-1.16 | ◐（v1.x ent 已建 aiagent/aimodel/aiprovider/aiscript/aiworkflow/aitask 等：4 表废止待删，6 表新增（aranea_instances/scenario_templates/mcp_tools/mcp_clients/task_nodes/sync_records），存量表按 v2.0 字段迁移） | — | — | 部分完成 |
| 18 | NATS 主题（发布 ai.task.events/ai.approval.pending/ai.analysis.completed/ai.aranea.health；订阅 alarm.events） | 概要设计 §8 | ◐（v1.x biz/publisher 已建，ai.aranea.health 与事件载荷规范待补齐） | — | — | 部分完成 |
| 19 | 前端页面（接入管理/Agent 目录/场景模板/任务中心/审批中心/脚本/定时/模板/MCP 治理/RCA/自动诊断/洞察看板） | 详设 §3.1 | — | ✗（TwinWeb/src/pages 无 aiops 目录） | — | 未开始 |

## v1.x 资产处置表（2026-07-28 裁定）

v1.x 批次C（2026-07-27 核实：`app/aiops` 13 组 HTTP 服务、biz 17 域、data 双 ent 客户端 + 10 仓储）按 v2.0 裁定如下：

| v1.x 资产 | 处置 | 说明 |
|-----------|------|------|
| cmd/wire + configs + server 骨架 | **复用** | 四层结构与中间件链不变；http.go 注册组按 v2.0 端点（aranea/agents/scenarios/tasks/approvals/scripts/scheduled-jobs/config-templates/mcp/rca/auto-analyses/insights/webhooks）重组 |
| biz/ai_model + provider_preset + fallback_chain + data/ai_model_repo + llm_tester | **废止删除** | LLM 模型目录/Provider/降级链归 aranea；ai_models 表废止 |
| biz/workflow + data/workflow_repo | **废止，功能替换** | 编排执行定义归 aranea Graph；以 scenario_template 域（注册/映射/治理）替代；ai_workflows 表废止 |
| biz/provider + data/provider_repo | **废止删除** | 工作流 Provider 归 aranea 工具生态；ai_providers 表废止 |
| biz/mcp + mcp_registry + data/mcp_repo（外部 MCP 服务器接入） | **大改** | 方向反转：由"接入外部 MCP"改为"对外开放 TwinMonitor-MCP-Server"；mcp_external_servers 表废止，新增 ai_mcp_tools/ai_mcp_clients |
| biz/agent + data/agent_repo | **重构** | 由运行时管理改为种子源 + 治理镜像（aranea_agent_id/sync_status/is_preset 等字段迁移） |
| biz/task + data/task_repo | **重构** | 由引擎执行记录改为 aranea 执行镜像（aranea_run_id/幂等键/trigger_type）；节点明细不重复落库 |
| biz/approval + data/approval_repo | **重构** | 扩展 interrupt_id/Resume/超时扫描/promotion 源语义 |
| biz/script + data/script_repo | **改造复用** | 增 mcp_risk_level；启用即注册 MCP 工具 |
| biz/config_template | **改造复用** | 增 config.render MCP 暴露 |
| biz/scheduled_job | **重构** | workflow_id → scenario_id |
| biz/rca + data/rca_repo | **重构** | 分析执行委托 aranea RCA run；表增 aranea_run_id/知识/记忆引用 |
| biz/auto_analysis | **重构** | 诊断命令执行通道改 MCP→16 |
| biz/insight + data/insight_repo | **改造复用** | LLM 运行指标数据源改 aranea 可观测/配额 API |
| biz/publisher | **复用扩展** | 补齐 ai.aranea.health 主题与统一信封 |
| proto：api/aiops/v1/* | **重编** | 按 v2.0 端点全集重排；Go stub 与 TS client 重新生成 |
| ent 表（aimodel/aiprovider/aiworkflow/mcp_external_servers） | **废止删表** | 删表前清理旧索引（索引名全局唯一约束，参照既往教训）；ent runtime 导入不得遗漏 |

## 未完成项说明

1. **aranea 接入链路全部未开始**（功能点 2/3/7）：实例管理、健康探测、种子同步、Webhook 回写是 v2.0 其余一切功能的前置，列为第一里程碑。
2. **MCP-Server 未开始**（功能点 12）：平台能力对外开放是 aranea 调用本平台的唯一通道，与接入链路并列第一里程碑。
3. v1.x 可复用资产（功能点 4/6/8/9/10/11/13/14/15）均需按 v2.0 语义重构，工作量按"改造"而非"新建"估算。
4. 前端 12+ 页面整体未开始（功能点 19），是后端就绪后的主要工作量。
5. UE5 侧精灵/场景指令通道归《TwinMonitor-AI融合总体设计方案》S1-S5 场景与 19-语音精灵模块，不在本模块计度。

## 风险与阻塞

- **aranea-agents 部署未就绪**：实例端点/服务凭据/Webhook 密钥是全部运行时能力的前置依赖；未部署前功能点 2-8、13-16 均无法端到端验证（与 14 一期"执行引擎未就绪（aranea）"决策同源）。
- **v1.x 废止代码删除窗口**：ai_model/workflow/provider/mcp（外部接入方向）四域删除涉及 proto、ent 表、wire 装配与测试联动，需一次性清理避免半迁移态；14 一期占位契约（13 CreateWorkflowTask 未接线）随二期执行下发段统一切换到 v2.0 `POST /tasks`（scenario_id）契约。
- **库表重构风险**：废 4 表、增 6 表、迁移 6 表，需手工清理旧索引（索引名 schema 内全局唯一）并保留 ent runtime 导入，防止 privacy 规则静默失效。
- **RAG/知识沉淀外部依赖**：依赖 10-业务管理知识库检索/写入接口（10 侧知识库 CRUD 已落地，检索端点需核实）与 aranea L3 记忆 API。

## 2026-07-28 v2.0 重构同步

- **文档重构**：需求/概要/详细设计三份全量重写为 v2.0（系统级架构视图、双系统职责切分、16 张库表重构、12 组 API、4 发布 1 订阅 NATS 主题、aranea 接入/场景执行/审批/智能分析/MCP 安全五段关键流程）。
- **完成度重估 45% → 约 15% 的理由**：v1.x 批次C 按 v2.0 口径裁定后，4 个 biz 域整体废止、9 个域需重构、仅骨架/脚本/配置模板/洞察/publisher 可直接复用；v2.0 新增的 aranea 接入、种子同步、Webhook、场景模板、MCP-Server、知识沉淀 6 大能力域均为零起点。按 v2.0 功能点 19 项计：1 项完成（骨架）、2 项部分完成（库表/NATS）、9 项待重构复用、7 项未开始。
- **下一里程碑**：M1「aranea 接入 + MCP-Server 只读面」——实例管理/探测/种子同步/Webhook + MCP 工具注册表/SSE 端点/只读工具放行，打通"aranea 能调平台、平台能收事件"最小闭环。
