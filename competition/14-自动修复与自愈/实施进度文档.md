# 14-自动修复与自愈 实施进度文档

## 总体完成度：后端约 70%（v2.0 口径：一期业务主体可复用，缺口 = 库表字段迁移 + 13 场景任务下发段 + ai.task.events / ai.analysis.completed 消费 + 兜底轮询）；前端 0%

> 检查日期：2026-07-28（v2.0 对齐重写；一期 2026-07-27 交付证据与验证记录保留于文末）。
> **基线变更说明**：2026-07-28 按《TwinMonitor-AI融合总体设计方案》D1 决议与 aranea-agents 底座决策（https://github.com/AmarsDing/aranea-agents），本模块三份文档（需求/概要/详细设计）升级为 v2.0——① 修复/验证/回滚动作的载体由"13 工作流"调整为 **13 场景模板**（aranea Graph 在 13 的治理注册项），`workflow_id` → `scenario_id`；② 执行状态回流由"轮询 13 任务 API"调整为 **NATS `ai.task.events` 事件驱动为主、30s 兜底轮询为辅**；③ 新增订阅 `ai.analysis.completed` 关联 AI 诊断结果；④ 手动触发 RCA 联动对齐 13 v2.0 rca-records 契约（`root_cause_task_id` → `rca_record_id`）；⑤ 明确双层审批（本模块策略级 / 13 场景内 HITL）与 141021 错误语义。模块业务规则（策略匹配四维、频率冷却、状态机、审批操作、验证回滚语义、工作台、仪表盘、手动触发分流）**不变**，一期已建后端资产大部分直接复用；变化集中在 13 集成契约与库表字段命名，按本文「二期 v2.0 改造点」统一实施。一期"execution_mode 仅 suggestion + approval、auto 400 拒绝"决策不变。

## 功能点清单（v2.0 口径）

| 序号 | 功能点 | 文档定义 | 后端完成 | 前端完成 | UE5完成 | 状态 |
|------|--------|----------|----------|----------|---------|------|
| 1 | remediation 微服务（独立 Kratos 服务，HTTP:8110 / gRPC:9110） | 概要设计 §5、详设 §2.10/2.11 | ✓（一期已建：cmd/main + wire_gen + configs + server/service/biz/data 全层接线；v2.0 无结构变化） | — | — | 完成（复用） |
| 2 | 修复策略管理（CRUD、触发条件、执行模式、关联**场景模板**、变量注入、启禁用、匹配测试） | 需求 §4.1、概要 §7.1、详设 §2.5/2.9 | ◐（一期已建 6 端点 + auto 400 拒绝 + 参数注入校验；v2.0 差异：字段 `workflow_id`→`scenario_id`、场景存在性校验缓存 `remediation:scenario:{id}`、新增 match-test 端点） | ✗ | — | 待迁移（E1/E2） |
| 3 | 频率限制与冷却控制（Redis 滑动窗口 + SETNX 冷却） | 需求 §4.1.5、详设 §2.3 | ✓（INCR + EXPIRE 频限、SETNX 冷却、Redis 降级放行；v2.0 无变化） | — | — | 完成（复用） |
| 4 | 修复执行管理（状态机、批准/拒绝/取消/查询） | 需求 §4.2、概要 §7.2、详设 §2.1 | ◐（一期已建 5 端点 + 状态机守卫 + Approve 落库发事件；v2.0 差异：批准后的下发段切换为 13 场景任务创建（E3），取消联动 13 任务取消（转发 aranea 取消语义）） | ✗ | — | 后端完成（一期口径）+ 二期下发段 |
| 5 | 修复后验证（验证**场景**、超时控制、手动重新验证） | 需求 §4.3、详设 §2.1.3 | ◐（VerifyExecution 端点保留，统一返回 141021；策略 verify_* 字段已落库待改名；二期切换为验证场景任务创建，idempotency_key phase=verify） | ✗ | — | 二期（等 aranea） |
| 6 | 自动回滚（回滚**场景**、失败自动回滚、手动回滚、回滚记录） | 需求 §4.4、详设 §2.1.4 | ◐（RollbackExecution 端点保留返回 141021；remediation_rollbacks 表 + Repo 已建待字段迁移；二期切换为回滚场景任务创建，phase=rollback + attempt 递增） | ✗ | — | 二期（等 aranea） |
| 7 | 自愈工作台（待审批队列、最近执行记录） | 需求 §4.5、概要 §7.3 | ✓（GetPendingQueue / GetRecentExecutions 纯查询聚合；v2.0 无变化，仅策略级审批待办） | ✗ | — | 完成（复用） |
| 8 | 修复仪表盘（统计卡、执行趋势、策略成功率排行、告警来源分布） | 需求 §4.6、概要 §7.4 | ✓（4 端点 + 统计缓存 TTL 5min；v2.0 无变化） | ✗ | — | 完成（复用） |
| 9 | AI 修复记录（诊断报告、命令清单、逐条结果） | 需求 §4.7、概要 §7.5、详设 §1.4 | ◐（remediation_ai_records 表 + List/Get 查询端点已建；v2.0 数据来源 = `ai.analysis.completed` 消费（E5）+ 13 任务节点摘要，生成链路二期接线） | ✗ | — | 查询完成/生成二期 |
| 10 | 手动触发（POST /remediation/manual/process，告警中心联动） | 需求 §4.8、概要 §7.6、详设 §2.4 | ◐（一期已建：自定义 handler、snake_case 冻结契约、按模式分流；v2.0 差异：响应根因字段 `root_cause_task_id`→`rca_record_id`（E7，与 04 侧同步升级）、RCA 创建改 13 v2.0 `POST /aiops/rca-records` 契约） | ✗ | — | 待契约升级（E7） |
| 11 | 数据库表（policies/executions/rollbacks/ai_records） | 详设 §1.1-1.4 | ◐（一期 4 表已建（ent 双库 + docker/init-remediation.sql）；v2.0 需统一字段迁移：workflow_id→scenario_id、workflow_task_id→ai_task_id、root_cause_task_id→rca_record_id，新增 idx_remediation_executions_ai_task_id 等索引，旧索引手工清理） | — | — | 待迁移（E1） |
| 12 | 告警事件订阅（alarm.events 消费自动触发） | 需求 §4.2.4、概要 §8、详设 §2.4.1 | ✓（NATS subscriber 统一信封解析，firing 才处理，trigger_type=auto 复用 ManualProcess 内部逻辑；v2.0 无变化） | — | — | 完成（复用） |
| 13 | **ai.task.events 消费**（TaskEventConsumer，状态回流主链路） | 概要 §8、详设 §2.6 | ✗（v2.0 新增：Queue Group remediation-task-events、event_id SET NX EX 24h 幂等、trigger_type=remediation 来源过滤、trigger_ref_id 匹配 execution_no、推进状态机 + waiting_approval 展示标记） | — | — | 二期（E4） |
| 14 | **ai.analysis.completed 消费**（AnalysisEventConsumer，AI 诊断关联） | 概要 §8、详设 §2.7 | ✗（v2.0 新增：RCA/自动诊断结果回写执行记录 rca_record_id 与 AI 修复记录"已诊断"关联） | — | — | 二期（E5） |
| 15 | **FallbackPoller 兜底轮询**（running 记录 30s 轮询 13 任务 API） | 需求 §6.4、详设 §2.8 | ✗（v2.0 新增：事件丢失/服务重启收敛，Redis 分布式锁多实例去重，60s 内收敛） | — | — | 二期（E6） |
| 16 | **13 场景任务下发段**（POST /api/v1/monitor/aiops/tasks） | 详设 §2.5 | ✗（一期 external_clients 13 CreateWorkflowTask/RCA 占位未接线；二期整体替换为 v2.0 契约：scenario_id + 渲染参数 + trigger_type=remediation + trigger_ref_id=execution_no + idempotency_key=remediation:{execution_no}:{phase}[:{attempt}]；13/aranea 不可用返回 141021） | — | — | 二期（E3，等 aranea） |
| 17 | 前端页面（策略编辑器、自愈工作台、执行详情、仪表盘、AI 记录） | 详设 §3.1-3.6 | —（TS client 一期已生成；E2 proto 重编后需重新生成） | ✗ | — | 未开始 |

## 二期 v2.0 改造点（统一实施清单）

| 编号 | 改造点 | 内容 | 依赖 | 阻塞 |
|------|--------|------|------|------|
| E1 | 库表字段迁移 | 4 表字段改名（workflow_id→scenario_id、workflow_task_id→ai_task_id、root_cause_task_id→rca_record_id，含 verify_/rollback_ 前缀字段）；新增 scenario_id/ai_task_id 索引；旧索引手工清理（索引名 schema 全局唯一约束）；ent schema + docker/init-remediation.sql 同步 | 无 | 否 |
| E2 | proto 契约重编 | api/remediation/v1 字段更名 + 新增 match-test RPC；buf generate 重产 Go stub 与 TS client | E1 | 否 |
| E3 | 场景任务下发段 | external_clients 替换为 13 v2.0 任务创建契约（trigger_type=remediation + trigger_ref_id + idempotency_key）；批准/重试/验证/回滚四条链路接入；13 场景校验缓存（5min） | 13 M1（aranea 接入 + 任务中心） | **是（aranea 未部署）** |
| E4 | TaskEventConsumer | ai.task.events 消费推进状态机（Queue Group + event_id 幂等 + 来源过滤 + execution_log 步骤追加） | 13 发布 ai.task.events | **是（13 未生产）** |
| E5 | AnalysisEventConsumer | ai.analysis.completed 消费关联 RCA/诊断结果 | 13 发布 ai.analysis.completed | **是（13 未生产）** |
| E6 | FallbackPoller | running 记录 30s 兜底轮询 GET /tasks/{id}，Redis 分布式锁，重启自动重挂 | E3 | 是 |
| E7 | manual/process 契约升级 | 响应根因字段 root_cause_task_id→rca_record_id；RCA 创建改 13 rca-records 契约；**04 侧冻结契约需同步升级** | E2 + 04 配合 | 否 |
| E8 | auto 模式开放 | 放开策略创建/更新的 auto 400 拒绝校验（alarm.events 链路一期已就绪） | E3-E6 就绪 | 是 |

实施顺序建议：E1 → E2（无外部依赖，可先行）→ E7（联动 04）→ E3（13 M1 就绪后）→ E4/E5/E6（并行）→ E8（收口）。

## 一期交付证据（2026-07-27，v1 口径留存）

> 以下为一期（W1/C1）验收记录，v2.0 对齐后其中 13 集成相关条目（CreateWorkflowTask 占位、字段命名）按上文改造点表处置，其余持续有效。

**契约与生成物**

- `api/remediation/v1/remediation.proto`（22 个 RPC）+ `remediation_error.proto`（业务错误码，含 141021 执行引擎未就绪）。
- `buf generate` 无错：Go stub `TwinServer/api/gen/go/remediation/v1/`（pb/grpc/http/errors/validate 7 文件）；TS client `TwinWeb/src/services/kratos/remediation/service/v1/index.ts`。

**服务实现（app/remediation，分层 cmd/server/service/biz/data，Kratos v2.9.2 + Wire）**

- server：http.go（JWT 认证 + 操作者注入中间件链、/metrics、/ready）、grpc.go、manual_process.go（自定义 handler 满足 04 snake_case 冻结契约，alarm_id/alert_id 双兼容）。
- service：remediation_service.go 全量 22 RPC。
- biz：policy（CRUD+Toggle+auto 拒绝+参数安全校验）、matcher（四维匹配 + 变量注入 {{alert.*}} 已知键替换/未知保留）、ratelimit（INCR 频限 + SETNX 冷却，Redis 降级放行）、execution（状态机 + Approve/Reject/Cancel + 141021 占位）、manual_process（告警详情拉取 04 → 匹配 → 按 mode 分流）、subscriber（alarm.events firing 消费）、dashboard（统计/趋势/排行/分布）、ai_record（查询）、publisher（remediation.events + remediation.approval.required 统一信封，失败仅记日志）、helper（策略/统计缓存）。
- data：ent 双客户端（主库 + 日志库）、4 Repo、redis_client（execution_no=REM+YYYYMMDD+6 位序号，INCR 原子计数，降级时间戳）、external_clients（13 任务/RCA 接口占位未接线，二期按 E3 替换为场景任务契约）。
- biz 不反向依赖 data：ExecutionNoGenerator 端口 wire.Bind、CacheHelper/RateLimiter 依赖 redis.Cmdable（NewRedisCmdable 注入），消除 import cycle。

**基础设施**

- 网关种子：`app/gateway/internal/data/main_db.go` 增加 remediation 服务（127.0.0.1:8110）与路由（/api/v1/monitor/remediation，priority 100）。
- Prometheus：`tools/prometheus/prometheus.yml` 与 `TwinServer/docker/prometheus/prometheus.yml` 双份均加 remediation job（:8110）。
- docker：`TwinServer/docker/init-remediation.sql` 4 表 DDL，已挂载 docker-compose.yaml `/docker-entrypoint-initdb.d/`（v2.0 字段迁移随 E1 同步）。

## 一期验证结果（2026-07-27 全绿，v1 口径留存）

| 验证项 | 结果 |
|--------|------|
| `buf generate`（TwinServer/api） | 无错，Go stub + TS client 均产出 |
| `go build ./app/remediation/...` | BUILD_EXIT=0 |
| `go test ./app/remediation/...` | TEST_EXIT=0（biz：匹配四维/边界、变量注入、频限窗口、冷却 SETNX、状态机非法迁移守卫、141021 占位、参数注入拒绝；data：execution_no 生成与降级） |
| `go build ./app/monitoralarm/... ./app/gateway/...` | REGRESS_EXIT=0（既有服务不受影响） |
| 网关种子改动后 `go build ./app/gateway/...` | GATEWAY_BUILD_EXIT=0 |

**manual/process 契约示例响应**（04 冻结契约，snake_case；v2.0 根因字段更名随 E7 升级）：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "matched_strategies": 1,
    "created_executions": 1,
    "matched_policies": [
      {"policy_id": 3, "policy_name": "磁盘清理建议", "execution_mode": "approval", "execution_id": 12}
    ],
    "process_id": 12
  }
}
```

**运行时 curl 冒烟未做**：本机无 docker 基础设施运行中（NATS/Redis/双库未整体拉起），且 04 monitoralarm 未在本轮启动，联动链路（告警详情拉取、审批事件）无法端到端验证；以编译 + 单测 + 契约静态对齐作为一期验收口径，运行时冒烟待环境齐备后补做。

## 未完成项与遗留（v2.0 口径）

1. **13 集成链路（二期，等 aranea/13 M1）**：场景任务下发段（E3）、ai.task.events 消费（E4）、ai.analysis.completed 消费（E5）、兜底轮询（E6）——端点已保留契约，统一返回业务错误 141021「执行引擎未就绪（aranea）」，不伪造执行结果。
2. **库表与 proto 迁移（E1/E2）**：字段改名 + 索引重建 + 生成物重产，无外部依赖，可先行实施。
3. **manual/process 契约升级（E7）**：根因字段更名需 04 侧同步，属跨服务冻结契约变更，需协调升级窗口。
4. **auto 执行模式（E8）**：一期创建/更新策略时 400 拒绝；alarm.events 自动触发链路已就绪，待 E3-E6 就位后放开校验。
5. **AI 修复记录生成**：ai_records 仅查询接口，生成链路 = E5 消费 + 13 任务节点摘要回填。
6. **前端页面**：TS client 待 E2 后重新生成，页面（策略编辑器/工作台/执行详情/仪表盘/AI 记录）未开始。
7. **运行时冒烟**：待 docker 基础设施与 04 服务同起后补 manual/process 404 告警、策略 CRUD、频限触发三项。

## 风险与阻塞

- **aranea 未部署 / 13 M1 未就绪**：E3-E6 唯一阻塞点，与 13 侧"执行引擎未就绪（aranea）"决策同源；一期已按决策隔离（端点占位 + 明确错误），解除阻塞不影响已有代码结构。
- **04 契约同步升级**：TriggerRepairMatch 冻结契约的根因字段更名（E7）需 04 monitoralarm 同步修改，存在跨服务协调成本；升级前保持 root_cause_task_id 现行口径。
- **库表迁移窗口**：E1 涉及 4 表字段改名与索引重建，需手工清理旧索引（索引名 schema 内全局唯一约束，参照既往教训），并保留 ent runtime 导入防止 privacy 规则静默失效。
- **事件驱动可靠性**：E4 主链路依赖 13 严格按统一信封（event_id/trigger_type/trigger_ref_id）生产 ai.task.events；E6 兜底轮询为最终一致性保险，两者需与 13 侧联合联调。
