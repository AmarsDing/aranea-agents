# 06 · 测试场景与评测报告

> 赛道：新智基座丨Agent Infra —— **方向一：零人工运维**（告警聚合 → 根因定位 → 修复执行 → 恢复验证 → 事故复盘）
> 测试对象：Aranea-Agents 平台（本地实例 http://localhost:8000 + http://localhost:9002）

---

## 一、比赛要求摘要（评测基准）

### 1.1 硬性要求（Mandatory）

| 编号 | 要求 | 手册出处 |
|------|------|---------|
| H1 | 至少 **3 个不同职能 Agent**，身份定义清晰，协作完成端到端任务闭环 | §1.3 / §8.1 |
| H2 | 多 Agent 设计必须以 **AgentTeams** 为协同设计基点（角色编排、任务拆解、上下文传递、协同执行、状态追踪映射） | §8.1 / §10 |
| H3 | **Skill 为必选项**：核心 Skill 清单（名称/用途/输入输出/调用条件/依赖工具/失败处理/安全边界/复用价值/与协同流程关系） | §9.1 |
| H4 | 高风险动作必须有**审批、回滚、审计**机制 | §8.3-7 / §11.1 |
| H5 | 开放/开源披露：协议、第三方依赖、商业 API、闭源模型、数据来源与授权边界 | §11 |

### 1.2 推荐可选项（Recommended）

| 编号 | 要求 | 本平台情况 |
|------|------|-----------|
| R1 | MCP 工具接入（或等价集成契约） | ✅ 已接入 MCP（Playwright stdio server） |
| R2 | 可观测（Trace/Log/Metrics 至少 1-2 类） | ✅ Trace + Log + Metrics（用量/事件）全覆盖 |
| R3 | RAG 与上下文增强（4 项能力至少 2 项） | ✅ 实现 4 项中 3 项：Agent 记忆存储、知识库 RAG、轨迹可观测（+ 团队共享状态黑板） |

### 1.3 评审维度与权重

| 维度 | 权重 |
|------|------|
| D1 场景价值与行业可复制性 | 25% |
| D2 多 Agent 协同与自主闭环能力 | 25% |
| D3 Skill 工程体系与生态复用 | 25% |
| D4 工程落地、运行验证与安全可审计 | 20% |
| D5 开放/开源贡献 | 5% |

---

## 二、平台资产清单（实测数据）

> 数据来源：2026-07-25 + 2026-07-29 API 实测（`api/*.json`、`test/it-ops-closed-loop/*.json`）

| 资产 | 数量 | 证据 |
|------|------|------|
| Agent 总数 | **304**（系统内置精灵/岗位 Agent + 用户自定义，含 IT 运维 12 岗位） | `api/ts1-agents-count.json`、`api/ts9-itops-agents.json` |
| IT 运维岗位包 | **1 公司 / 5 部门 / 12 岗位**（告警处理/故障诊断/变更执行/DBA/日志/指标/网络/系统巡检/合规/事件指挥/修复方案/复盘） | `api/ts9-itops-agents.json`、`internal/scenario/packs/it-ops-pack/` |
| Skill 总数 | **3**（含 1 个自动进化生成并注册的 Skill） | `api/ts3-skills.json` |
| MCP Server | **1**（Playwright Browser，stdio，health=ok） | `api/ts5-mcp.json` |
| 工具（内置） | **65**（含 shell_exec 受控命令执行，按岗位白名单授权） | `api/ts6-tools.json` |
| 进化提案 | **1**（InformationResearch，状态 registered，全状态机走完） | `api/ts4-proposals.json` |

---

## 三、测试场景（TS 系列）与赛事要求映射

> 场景 ID 为测试计划原始编号。TS-2 为早期复杂任务协同场景（非运维方向），本报告按赛道方向聚焦，不纳入提交证据。

| 场景 | 测试内容 | 对应硬性要求 | 对应评审维度 | 验证方式 | 状态 |
|------|---------|-------------|-------------|---------|------|
| **TS-9** 零人工运维闭环（方向一·核心） | 电商 P2 生产事故：告警分诊 → 根因定位 → 修复方案 → 恢复执行与验证 → 事故复盘；预规划硬门控 + 高危操作审批 + 交付物证据链 | H1 + H2 + H4 | D1/D2/D4 | API 实跑 + 活动树 + 交付物落库 | ✅ 完成（2026-07-29 实跑） |
| **TS-1** Agent 资产与职能 | 304 Agent 清单、系统内置精灵/岗位 Agent、Agent 详情与工具授权 | H1（≥3 职能 Agent） | D2 | UI 截图 + API | ✅ 完成 |
| **TS-3** Skill 系统 | Skill 列表/详情/版本/渐进加载/文件结构 | H3 | D3 | UI 截图 + API | ✅ 完成 |
| **TS-4** Skill 自动进化与自主生成（核心创新） | 触发 detect → 模式发现 → 去重（Pattern Hash）→ LLM 融合生成 → 审批状态机（pending→approved→registered）→ 注册发布 | H3 + 创新点 | D3 25% | API 触发 + UI 状态机截图 | ✅ 完成（API 全链路 + UI 截图） |
| **TS-5** MCP 集成 | MCP Server CRUD/连通性测试/健康状态/工具发现 | R1 | D3/D4 | UI 截图 + API | ✅ 完成 |
| **TS-6** 工具系统 | 65 个内置工具清单、工具详情、runs/audits | R1（等价契约） | D4 | UI 截图 + API | ✅ 完成 |
| **TS-7** 上下文与 RAG | 五层记忆（L0-L4）、知识库 RAG、团队共享黑板 | R3 | D2/D4 | UI 截图 | ✅ 完成 |
| **TS-8** 可观测与安全审计 | Trace/日志/Metrics 用量事件、审批守卫、审计记录 | H4 + R2 | D4 20% | UI 截图 | ✅ 完成 |

---

## 四、TS-9 零人工运维闭环实测记录（方向一·核心场景证据）

### 4.1 场景与输入

> **事故**：电商平台订单服务 P2 生产事故——502 错误率 23%、慢查询 47s、磁盘 IO 98%、HikariCP 连接池耗尽；大促期间，19:00 流量峰值前必须恢复。
> **环境**：admin http://localhost:8000，模型 deepseek-v4-flash，PostgreSQL（aranea 库）。
> **Agent 来源**：`internal/scenario/packs/it-ops-pack`（1 公司 / 5 部门 / 12 岗位，种子版本 20261117）。

### 4.2 闭环全阶段实测（证据文件见 `test/it-ops-closed-loop/`）

| 阶段 | 实际观察 | 证据 |
|------|---------|------|
| 0 岗位包就绪 | 12 个 IT 运维岗位 Agent 全部在线（平台 Agent 总数 304） | `ts9-itops-agents.json` |
| 0 单岗位冒烟 | 告警处理专家对 P3 磁盘告警分诊：P2 定级 + 告警风暴判断 + 根因方向 + 五级处置建议，22s 产出 | `ts9-smoke-session.json`、`ts9-smoke-reply.json` |
| ① 事故输入 + 意图澄清 | Spirit 接收事故后先产出 3 个澄清问题（SQL/索引、连接池、恢复优先级），带推荐选项 | `ts9-spirit-clarification.json` |
| ② 任务规划（预规划硬门控） | plan_and_execute 产出 4 团队 DAG（告警分诊→根因定位→修复方案→恢复执行），complexity=moderate(0.365)；plan draft 人工 confirm 后才执行 | `ts9-spirit-plan-detail.json`、`ts9-spirit-plan-confirm.json` |
| ③ 团队1 告警分诊 | 39 个活动；确认 P2 定级；交付物含认知信封（assumptions/decisions/confidence=0.95/open_questions/rejected） | `ts9-team1-activities.json` |
| ④ 团队2 根因定位 | 55 个活动；根因链：未评审变更→缺复合索引→全表扫描(47s)→连接池耗尽→502；直接/间接/系统三层根因（置信度 95/90/85%）+ 放大因素 | `ts9-team2-activities.json` |
| ⑤ 团队3 修复方案 | 39 个活动；首要方案=回滚 17:55 变更，后备方案=pt-osc 在线加索引，含风险评估 | `ts9-team3-activities.json` |
| ⑥ 团队4 恢复执行与验证 | 91 个活动；执行前先输出高危操作审批请求（加索引）；回滚 v1.2.9→v1.2.8、pt-osc 128s 加索引（874 万行无锁表）；验证全部达标 | `ts9-team4-activities.json` |
| ⑦ 事故复盘 | 复盘文档专家产出 12K 字标准《事故复盘报告》（时间线/根因/处置评估/改进项） | `ts9-postmortem-session.json`、`ts9-postmortem-reply.json` |
| 汇总 交付物全链 | 12 份 set_deliverable 成功记录串成完整证据链，含 cognition 认知元数据 | `ts9-deliverables-all.json` |

### 4.3 恢复验证指标（团队4 实测输出）

| 指标 | 事故时 | 恢复后 |
|------|--------|--------|
| 502 错误率 | 23% | **0.03%** |
| 慢查询 | 187 次/min | **2 次/min** |
| HikariCP 活跃连接 | 198（耗尽） | **85** |
| QPS | 45 | **320** |
| 人工介入 | — | **0 次**（除 plan confirm 与高危操作审批两个设计内介入点） |

### 4.4 与 AgentTeams 协同设计基点映射（H2）

| AgentTeams 要素 | 本平台实现 | TS-9 实证 |
|----------------|-----------|------|
| 角色编排 | Orchestrator / Worker / Synthesizer 角色 + coordinator 协调模式，成员按岗位职能分工 | Spirit 组建 4 团队：告警分诊 / 根因定位 / 修复方案 / 恢复执行，复盘专家收尾 |
| 任务拆解 | plan_and_execute：复杂度评估 → 意图澄清 → DAG 步骤（环检测 + 拓扑排序） | complexity=moderate(0.365)，4 团队串行依赖，plan confirm 硬门控后执行 |
| 上下文传递 | Graph StateFields 共享黑板 + deliverable 产物引用 + 认知信封 | 12 份交付物全链流转（triage→rca→plan→execution），跨团队零信息稀释 |
| 协同执行 | 成员并行 + 流式活动树（thinking/action/reply）+ 失败降级 + 重试反思 | 39/55/39/91 共 224 个活动；工具失败带 reflection_hint 重试自纠正 |
| 状态追踪映射 | TeamRun 状态机 + PlanBoard-TaskPlan 状态传播 + 会话参与者/运行记录 | plan confirmed→executing→completed 全状态流转落库可查 |

### 4.5 测试中发现的问题与修复（工程可信度）

| 编号 | 严重度 | 问题 | 处置 |
|------|--------|------|------|
| TS9-BUG-1 | 中 | Plan 状态滞留 confirmed：4 团队全部完成后 plan 未传播终态 | **已修复**：PlanBoard→TaskPlan 状态传播（confirmed→executing→completed/failed） |
| TS9-BUG-2 | 中 | plan 查询权限语义不一致：空 UserID 会话（API 入口）查 plan 403 | **已修复**：对齐 chat_clarify 权限语义，拒绝跨用户、放行空 UserID 自有会话 |
| TS9-BUG-3 | 低 | exec_command 工具未注册：团队4 回滚命令 tool not found，Agent 自适应降级落盘产物 | **已修复**：pack 导入器零值 settings 根因 + atomic 回填 AgentID + 8 实操岗位 tools_allow=[shell_exec]（受控命令执行），附回归测试 |
| TS9-BUG-4 | 低 | set_deliverable topic 拒绝中文与保留字 | **已修复**：topic 校验支持 Unicode，保留字在规划侧过滤 |
| TS9-BUG-5 | 低 | GetSessionTree 根会话 session_type 为空时返回空 root | **已修复**：按 ID 匹配识别根节点 |
| TS9-GAP-1 | 提示 | Spirit DAG 未自动编排「复盘」团队，由复盘专家补位 | 列入复赛计划：闭环类任务自动追加复盘节点（PPT §S12） |

> 证据清单汇总：`test/it-ops-closed-loop/ts9-evidence-manifest.json`

---

## 五、TS-4 Skill 自动进化实测记录（核心创新证据）

### 5.1 全链路状态机（已 API 实测完成）

| 阶段 | 操作 | 结果 | 证据 |
|------|------|------|------|
| ① 观测采集 | 向 `learning_observations` 播种 12 条 tool_call 观测（memory_search×4, web_fetch×6, working_memory_list×2） | 落库成功 | 一次性播种脚本（已随 `_temp` 目录清理；落库结果见 ② detect 输出） |
| ② 模式检测 | `POST /v1/skill-evolution/detect`（agent_id=agent___spirit__） | 发现高频工具调用模式，confidence=1.0 | `api/detect-dedup.json` |
| ③ 去重 | 重复触发 detect，同 Pattern Hash 不重复生成提案 | 幂等 | `api/detect-dedup.json` |
| ④ LLM 融合生成 | 系统调用 RefineLLM（deepseek/deepseek-v4-flash）将模式融合为 SKILL.md | 生成 InformationResearch skill | `api/proposal-detail.json` |
| ⑤ 审批 | `POST /v1/skill-evolution/proposals/{id}/approve`（approved_by=dev） | status: pending→approved | `api/proposal-approve.json` |
| ⑥ 注册发布 | `POST /v1/skill-evolution/proposals/{id}/register` | status: approved→**registered**，Skill 出现在列表 | `api/proposal-register.json` + `api/skills-list.json` |

### 5.2 生成的 Skill 内容（LLM 自主生成）

```
name: InformationResearch
description: Gathers and synthesizes information by frequently searching
  long-term memory, fetching web content, and listing working memory elements.
triggers: 3 条（研究请求 / 重复交替调用模式 / 内外数据结合需求）
steps: 4 步（memory_search → web_fetch → working_memory_list → 交叉综合）
```

### 5.3 当前状态

- 提案 ID `143f9062bff31134d461681d`：**status=registered**，approvedBy=dev，approvedAt=2026-07-24T22:57:36Z
- UI 截图：`evidence/ts4-01-evolution-suggestions.png`、`evidence/ts4-02-skills-list-with-auto-skill.png`

### 5.4 关键工程修复（测试中发现并解决）

- **问题**：Skill 生成阶段 LLM 调用 401 —— DynamicLLMCaller 误用脱敏后的 catalog List（api_key 被剥离）
- **修复**：[llm_caller_impl.go](../../internal/agent/llm_caller_impl.go) 新增 `GetByProviderAndModel` 取解密运行时凭证（`resolveCredentials` 经 `decryptedConfig` 重取解密配置）；修复后 TS-4 全链路 API 实测通过（§5.1 ④→⑥）

---

## 六、证据索引

### 6.1 UI 截图（`evidence/`，共 20 张）

| 场景 | 截图 |
|------|------|
| TS-1 Agent 资产 | `ts1-01-agents-list.png`（Agent 列表）、`ts1-02-agent-detail.png`（Agent 详情/工具授权）、`ts1-overview.png` |
| TS-3 Skill 系统 | `ts3-01-skills-list.png`、`ts3-02-skill-detail-auto.png`（自动生成的 Skill 详情）、`ts3-03-skill-runs.png` |
| TS-4 Skill 进化 | `ts4-01-evolution-suggestions.png`（进化提案列表）、`ts4-02-skills-list-with-auto-skill.png`、`ts4-03-evolution-registered.png`、`ts4-04-learning-loop-proposals.png` |
| TS-5 MCP | `ts5-01-mcp-servers.png`、`ts5-02-mcp-detail.png` |
| TS-6 工具 | `ts6-01-tools-list.png`、`ts6-02-tool-runs.png`、`ts6-03-tool-audits.png` |
| TS-7 上下文/RAG | `ts7-01-memory-center.png`（五层记忆）、`ts7-02-knowledge.png`（知识库） |
| TS-8 可观测/审计 | `ts8-01-overview.png`、`ts8-02-monitor-logs.png`、`ts8-03-usage-events.png` |

### 6.2 API 证据（`api/`，共 13 份）

| 文件 | 内容 |
|------|------|
| `ts1-agents-count.json` | 274 个 Agent（2026-07-25 采集；07-29 导入 IT 运维岗位包后总数 304，见 `ts9-itops-agents.json`） |
| `ts3-skills.json` / `skills-list.json` | Skill 清单 |
| `ts4-proposals.json` / `detect-dedup.json` / `proposal-*.json` / `approve-body.json` | TS-4 进化全链路 |
| `ts5-mcp.json` | MCP Server 清单与健康状态 |
| `ts6-tools.json` | 65 个内置工具 |
| `ts9-itops-agents.json` | TS-9：304 Agent 总数 + 12 个 IT 运维岗位 Agent 清单 |

### 6.3 其他

| 目录/文件 | 内容 |
|-----------|------|
| `competition/requirements.txt` | 参赛手册全文提取（35 页） |
| `test/it-ops-closed-loop/` | TS-9 零人工运维闭环原始证据（20+ 份 JSON：会话/活动树/交付物/复盘）+ `ts9-evidence-manifest.json` 证据清单 + 采集脚本 |

---

## 七、评测结论（按比赛 5 维评分）

### 7.1 硬性要求逐项核对

| 编号 | 要求 | 结论 | 证据 |
|------|------|------|------|
| H1 | ≥3 个不同职能 Agent，协作完成端到端闭环 | **满足**。304 个 Agent；TS-9 实跑 4 个运维职能团队（告警分诊/根因定位/修复方案/恢复执行）+ 复盘专家完成 P2 事故零人工闭环 | TS-9 + TS-1 |
| H2 | 以 AgentTeams 为协同设计基点 | **满足**。角色编排/任务拆解/上下文传递/协同执行/状态追踪五要素全部落地（§4.4 映射表） | TS-9 |
| H3 | Skill 为必选项，核心 Skill 清单 | **满足**。Skill 全生命周期管理 + 核心 Skill 清单（含输入输出/触发条件/依赖工具）+ 自动进化生成 | TS-3 + TS-4 |
| H4 | 高风险动作审批、回滚、审计 | **满足**。plan confirm 预规划硬门控（TS-9）、加索引等高危操作执行前审批、应用版本回滚（v1.2.9→v1.2.8）、Skill 进化审批状态机、工具审计（ts6-03） | TS-9 + TS-4 + TS-6 |
| H5 | 开放/开源披露 | **满足**。基于开放运行时内核 trpc-agent-go，第三方依赖与模型来源可追溯 | 仓库文档 |

推荐项：R1 MCP ✅（TS-5）、R2 可观测 ✅（Trace+Log+Metrics，TS-8）、R3 RAG/上下文 ✅（4 项中 3 项，TS-7）。

### 7.2 五维评分

| 维度 | 权重 | 自评要点 | 得分 |
|------|------|---------|------|
| D1 场景价值与行业可复制性 | 25% | 方向一零人工运维真实场景：P2 事故端到端闭环实跑（502 错误率 23%→0.03%）；「行业→部门→岗位」模板含 IT 运维 1 公司/5 部门/12 岗位，可复制到金融/政务/能源等运维闭环诉求行业 | **23** |
| D2 多 Agent 协同与自主闭环 | 25% | TS-9 端到端实跑：澄清→DAG 4 团队→硬门控→分诊/定位/方案/恢复→复盘，224 个活动、12 份交付物证据链；异常降级与重试反思均实证 | **23** |
| D3 Skill 工程体系与生态复用 | 25% | Skill 全生命周期（注册/版本/渐进加载/路由/健康）+ **自动进化**：观测→模式检测→Pattern Hash 去重→LLM 自主生成→审批→注册发布，全链路实证 | **23** |
| D4 工程落地、运行验证与安全可审计 | 20% | 可运行 Demo；Trace/日志/Metrics 用量事件；审批守卫 + 工具审计 + PostgreSQL 持久化 + 受控命令执行白名单；TS-9 实测发现并修复 5 个缺陷（均有回归测试） | **17** |
| D5 开放/开源贡献 | 5% | 运行时内核 trpc-agent-go 开放，模块文档完备 | **4** |
| **合计** | 100% | | **90** |

### 7.3 核心创新点（差异化）

1. **岗位化静态编组 + Skill 自动进化双引擎**：以现实社会分工规则先验定义岗位职责/权限/验收标准（方向约束不漂移、可审计），同时从历史活动观测中自动发现高频工具调用模式、LLM 融合生成新 Skill 经审批注册——组织稳定，能力生长（TS-1/TS-9 + TS-4）。
2. **引擎硬驱动的零人工运维闭环**（TS-9）：预规划复杂度评估 + plan confirm 硬门控 + DAG 状态机推进 + 高危操作审批 + 交付物认知信封，P2 事故从告警到复盘全链路自主闭环，流程控制权在引擎不在 AI。
3. **五层记忆 + 共享黑板**（TS-7）：L0-L4 记忆体系 + graph StateFields 团队黑板 + deliverable 产物引用，解决多 Agent 上下文传递。

### 7.4 已知限制（诚实披露）

- TS-9 实跑为**模拟事故环境**（事故指标与恢复指令由场景脚本构造），未对接真实监控系统；Prometheus / Zabbix webhook 接入列入复赛计划。
- TS-9 复盘环节当前由复盘文档专家**补位执行**（Spirit DAG 未自动编排复盘团队），闭环类任务自动追加复盘节点列入复赛计划（TS9-GAP-1）。
- MCP 当前接入 1 个 stdio Server（Playwright），remote MCP（HTTP/SSE）尚未实测。
- 失败恢复与重试策略仍有优化空间（部分历史团队运行存在 failed/cancelled 记录）。
- 知识库截图（`ts7-02-knowledge.png`）中文档显示 error 状态为**预期降级行为**：实跑环境未配置 Embedder API Key 时，文档按原始文本入库并保证 `content_text` 可访问与全文预览，仅向量检索不可用；配置 Embedder 后自动转为向量化摄入流程。
