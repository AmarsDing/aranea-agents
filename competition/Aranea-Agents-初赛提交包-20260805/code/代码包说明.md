# 代码包说明（可执行 AgentTeams 代码包 · 初赛可选提交）

> 快照：`aranea-agents-src-20260805.zip`（128 MB）
> 来源：Git HEAD `421e38aab30ef3eb6b96d55e0e1d6e91ccc15108`（2026-08-05），`git archive` 导出。已排除：提交包自引用目录、宣传视频素材、IDE/Agent 工具配置、运行时数据、临时排查目录；`test/` 仅保留 TS-9 零人工运维闭环证据目录。**含 TS-9 全部修复与 IT 运维岗位包**（`internal/scenario/packs/it-ops-pack`）。

## 一、代码包内容

| 目录 | 内容 |
|------|------|
| `cmd/` | 运行入口：`cmd/admin`（服务端）、`cmd/aranea`（CLI） |
| `internal/` | 业务核心：service / biz / data / agent / event 分层（45+ 模块） |
| `pkg/trpc-agent-go/` | Agent 运行时内核（Runner / Agent / Session / Memory / Tool / Skill / Graph / Team）——多 Agent 协同设计基点框架 |
| `api/kratos/` | Proto 定义（HTTP/gRPC/WebSocket 契约） |
| `web/` | 前端：Vue 3 + Quasar + Pinia + TypeScript |
| `configs/` | 配置文件样例（`config.yaml`） |
| `docs/` | 模块开发文档（需求/设计/开发计划三件套，65+ 篇）与架构决策记录 ADR |
| `internal/scenario/packs/` | 内置行业模板（IT 运维/金融/自媒体/软件开发：部门、岗位、团队种子） |
| `test/it-ops-closed-loop/` | TS-9 零人工运维闭环原始证据（20+ 份 JSON + 证据清单） |
| `Makefile` | 一键初始化 / 构建 / 测试 / lint |

## 二、依赖说明

- Go 1.25+ · Node.js 20+ · PostgreSQL 14+（pgvector 向量存储；不启用时自动降级）
- 完整依赖锁定于 `go.mod` / `web/package.json`

## 三、运行入口与配置

```bash
# 1. 初始化（生成 proto + wire + 前端依赖）
make all

# 2. 启动后端（免登录开发模式）
#    Linux/macOS:
DEPLOY_ENV=dev KRATOS_HTTP_AUTH_DISABLED=1 go run ./cmd/admin -conf ./configs/config.yaml
#    Windows PowerShell:
#    $env:DEPLOY_ENV="dev"; $env:KRATOS_HTTP_AUTH_DISABLED="1"
#    go run ./cmd/admin -conf ./configs/config.yaml

# 3. 启动前端
cd web && npm install && npm run dev   # http://localhost:9001

# 4. 健康检查
curl http://localhost:8000/healthz     # 本地账号 dev / dev
```

LLM Provider 在 Web 控制台「模型目录」中配置（支持 DeepSeek / Qwen / OpenAI / OpenRouter 等 12+ Provider，实跑使用 deepseek-v4-flash）。

## 四、样例输入输出（TS-9 零人工运维实跑任务）

**输入**（Web 聊天框，精灵助手）：
> 电商平台订单服务 P2 生产事故：502 错误率 23%、慢查询 47s、磁盘 IO 98%、HikariCP 连接池耗尽；大促 19:00 流量峰值前必须恢复。

**输出**：意图澄清 → plan confirm 硬门控 → 4 团队 DAG（告警分诊→根因定位→修复方案→恢复执行）→ 高危操作审批 → 回滚 v1.2.9→v1.2.8 + pt-osc 128s 在线加索引（874 万行无锁表）→ 验证达标（502 23%→0.03%、慢查询 187→2/min、QPS 45→320）→ 12K 字《事故复盘报告》。12 份交付物全链落库（含认知信封）。

完整证据：提交包 `evidence/ts*.png`、`api/ts9-*.json`、`../06-测试场景与评测报告.md` §五；原始活动树 JSON 见代码包内 `test/it-ops-closed-loop/`。

## 五、运行证据

| 证据 | 位置 |
|------|------|
| 20 张 UI 实跑截图（TS-1 ~ TS-8） | `../evidence/` |
| 13 份 API 实测记录（JSON，含 TS-9 运维岗位清单） | `../api/` |
| 评测报告（五维自评 90/100，含 TS-9 零人工运维闭环实测） | `../06-测试场景与评测报告.md` |
| TS-9 零人工运维闭环原始证据（20+ 份 JSON） | 代码包内 `test/it-ops-closed-loop/`（含 `ts9-evidence-manifest.json` 证据清单） |
| Skill 自动进化全链路记录 | `../api/proposal-*.json`、`../evidence/ts4-*.png` |

## 六、测试方法

```bash
go build ./...                  # 编译
go test ./internal/... ./pkg/...  # 后端单元/集成测试
cd web && pnpm lint && pnpm test && pnpm build   # 前端质量门禁
```
