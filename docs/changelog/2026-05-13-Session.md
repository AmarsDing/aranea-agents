# 2026-05-13 Session: 核心数据结构重构

**影响**：🔴 高 | **模块**：Session / Chat / Team

## 变更摘要

Session 核心数据结构全面重构，新增 12 个字段，重命名 2 个字段（provider/model → default_provider/default_model），新增元数据支持和 Kratos 错误处理统一。

## 关键变更

- 新增 12 字段（workspace_id, user_id, tags_json, visibility, avg_latency_ms, error_count 等）
- 重命名 provider/model → default_provider/default_model，新增 last_provider/last_model
- 新增 metadata_json、runner_snapshot_json 元数据支持
- 统一错误处理为 Kratos BadRequest

## 破坏性变更

1. `CreateSessionRequest` 的 `provider`/`model` 字段重命名为 `default_provider`/`default_model`
2. 需执行数据库迁移，新增 12 个字段
3. 现有数据需迁移重命名字段

> 详细实现记录见 [devlog/2026-05-13-Session.md](../devlog/2026-05-13-Session.md)
