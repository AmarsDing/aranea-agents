# M13: Knowledge 知识库 — 详细需求

> 对标 `pkg/trpc-agent-go/knowledge` 包，实现 RAG 知识库能力。
>
> 架构设计、Proto/API 契约、数据模型、代码分层详见 [37-knowledge.design.md](./37-knowledge.design.md)。
> 代码锚点、现状评估、任务清单、Phase 划分详见 [37-knowledge.development.md](./37-knowledge.development.md)。

---

## 1. 用户故事

### US-1：知识库管理员创建知识集合

**作为**知识库管理员，**我希望**创建一个命名的知识集合（Collection），指定嵌入模型和维度，**以便**将相关文档组织在一起并统一检索。

**验收标准**：
- 可通过 API 创建 Collection，指定 name、description、embedding_model。
- 创建后 Collection 状态为 `active`，维度默认 1536。
- name 和 embedding_model 为必填项。

### US-2：用户上传文档到知识集合

**作为**用户，**我希望**向知识集合上传文档（文本/Markdown），**以便**文档被自动分块、向量化并索引，供后续语义搜索使用。

**验收标准**：
- 可通过 API 上传文档，传入 base64 编码的文档内容和元信息。
- 文档创建后状态为 `pending`，后台异步完成分块和向量化。
- 成功后状态变为 `indexed`；失败则变为 `error` 并记录错误信息。
- 分块参数（chunk_size、chunk_overlap）可在请求中指定。

### US-3：用户搜索知识库

**作为**用户，**我希望**通过自然语言查询搜索知识集合，**以便**获取与查询语义相关的文档片段。

**验收标准**：
- 可通过 API 发起语义搜索，指定 collection_id、query、top_k、min_score。
- 返回按相似度排序的文档片段（Chunk），包含内容、分数、来源文档 ID。
- 支持通过 filter_json 进行元数据过滤。

### US-4：Agent 通过工具搜索知识库

**作为** Agent，**我希望**在对话中调用 `knowledge_search` 工具搜索知识集合，**以便**获取外部知识增强回答质量。

**验收标准**：
- Agent 启用 `knowledge_search` 工具开关后，工具自动装配到 Agent 工具集。
- 工具接收 collection_id、query、top_k、min_score 参数。
- 搜索结果以结构化 JSON 返回给模型上下文。

### US-5：用户管理知识集合和文档

**作为**用户，**我希望**列出、查看和删除知识集合及文档，**以便**管理知识库生命周期。

**验收标准**：
- 可列出所有 Collection（分页）。
- 可列出某 Collection 下的所有 Document（分页）。
- 删除 Collection 时级联删除其下所有 Document 和 Chunk。
- 删除 Document 时级联删除其下所有 Chunk。

### US-6：LLM 动态过滤搜索结果（AgenticFilter）

**作为** Agent，**我希望** LLM 根据查询意图动态决定过滤条件，**以便**精准检索相关知识片段。

**验收标准**：
- 启用 AgenticFilter 后，LLM 可在搜索时自动生成过滤参数。
- 过滤条件基于文档元数据字段。

### US-7：OCR 文档识别入库

**作为**用户，**我希望**上传图片或 PDF 文档后自动 OCR 提取文本并入库，**以便**非纯文本文档也能被语义搜索。

**验收标准**：
- 图片/PDF 上传后自动 OCR 提取文本。
- 提取的文本进入分块和向量化流水线。
- OCR 失败时文档状态标记为 `error`。
- OCR 提供者通过 `KNOWLEDGE_OCR` 环境变量配置。

### US-8：多租户知识库隔离（超越层）

**作为**系统管理员，**我希望**不同租户的知识库完全隔离，**以便**租户 A 搜索不到租户 B 的知识。

**验收标准**：
- 搜索请求自动注入租户 ID。
- 向量存储按租户分区。
- 跨租户搜索返回空结果。

### US-9：Agent 自校验检索质量（knowledge_reflect）

**作为** Agent，**我希望**在搜索知识库后评估检索结果是否充分，**以便**在结果不足时自动发起补充检索，提升回答质量。

**验收标准**：
- Agent 启用 `knowledge_reflect` 工具开关后，工具自动装配到 Agent 工具集。
- 工具接收 `collection_ids`（支持多个集合）、`query`、`top_k` 参数。
- 返回结构化评估结果：`sufficient`（是否充分）、`confidence`（置信度）、`supplement_query`（补充查询建议）、`chunks`（检索片段）。
- 当 `FederatedRetriever` 可用时，自动跨多个 Collection 并行搜索。
- 当 `RetrievalEvaluator` 可用时，自动评估检索质量。

### US-10：跨 Collection 联邦搜索

**作为**用户/Agent，**我希望**同时搜索多个知识集合，**以便**从不同知识源获取综合信息。

**验收标准**：
- `knowledge_reflect` 工具支持传入多个 `collection_ids`。
- 多 Collection 搜索并行执行，结果合并去重。
- 单 Collection 搜索时自动降级为标准检索路径。
- 部分集合搜索失败时不阻塞其他集合的结果返回。

### US-11：查询重写与混合检索

**作为**用户，**我希望**搜索时自动优化查询并使用混合检索策略，**以便**复杂查询也能获得高召回率结果。

**验收标准**：
- Search API 支持 `rewrite_strategy` 参数（hyde / decomposition / multi_query）。
- Search API 支持 `hybrid_search` 参数（auto / dense / sparse / rrf）。
- 自适应路由根据查询复杂度自动选择检索模式。
- 检索质量评估（CRAG）在结果不足时自动触发补充检索。

### US-12：拖拽上传文档并自动整理入库

**作为**用户，**我希望**将多个 PDF、Word、Markdown、XML 等文本类文档直接拖拽到知识库页面，**以便**系统自动提取内容、整理为结构化 Markdown 文档并完成分块向量化入库，无需手动逐个配置。

**验收标准**：
- 知识库文档面板提供拖拽区域，支持一次拖入多个文件。
- 每个文件生成独立上传任务，实时展示状态（pending → indexing → indexed / error）。
- 文本类（txt/md/json/csv/html/xml/yaml）与 Office 类（pdf/doc/docx/xlsx/pptx）文档均可正确提取文本。
- 默认开启「整理为 Markdown」：提取文本经 LLM 结构化为 Markdown 后再按标题层级分块入库。
- LLM 不可用或整理失败时自动降级为原文本入库，不阻塞流程。
- 整理后的 Markdown 全文可在文档列表中预览。
- 拖入图片等多模态文件时给出明确提示（多模态支持见 US-13），不静默失败。

### US-13：多模态资料入库（图片）

**作为**用户，**我希望**将图片（png/jpg/jpeg/webp）拖入知识库，**以便**系统通过多模态 LLM 理解图片内容并整理为 Markdown 描述文档入库，与文本文档一样可被语义搜索。

**验收标准**：
- 图片与文本文档共用同一拖拽入口与上传队列。
- 图片经多模态 LLM 输出结构化 Markdown 描述（含图中文字、表格、图表含义）后入库。
- 原始图片留存并与文档记录关联（血缘可追）。
- 检索结果与文本文档无差别返回；文档 metadata 记录模态与提取方式。
- 多模态 LLM 不可用时文档状态标记为 `error` 并给出明确错误信息。

### US-14：免选择知识库（存储分类、使用免选）

**作为**用户，**我希望**存入知识库的资料就是我的知识（可分门别类存放），但在会话中使用时**不需要我决定用哪个知识库**——系统自动在我的全部知识中检索，**以便**我无需理解"知识库/Collection"概念即可获得基于私有知识的回答。

**验收标准**：
- 上传不强制预选 Collection：未选中时自动落入系统「默认知识库」（首个上传自动创建），不静默丢弃文件。
- 会话中 Agent 检索知识时无需指定 Collection：默认在用户全部 Collection 中智能路由检索（按集合名称/描述与问题的匹配度取 top N 广播 + 结果合并）。
- `knowledge_search` / `knowledge_reflect` 工具的 Collection 参数改为可选，留空即全库检索。
- Agent 绑定特定 Collection 保留为高级配置（默认不绑定 = 全库可搜），用于「专属客服只搜产品文档」类精细化场景。
- 文档可跨 Collection 移动（整理：默认库收件箱 → 分类库归档），移动后检索立即可见于目标库。
- 调试搜索面板默认「全部知识库」。

---

## 2. 功能规格

### 2.1 知识集合管理

| 功能 | 说明 |
|------|------|
| 创建集合 | 指定 name、description、embedding_model |
| 列出集合 | 分页查询，支持 workspace 过滤 |
| 获取集合 | 按 ID 获取单个集合详情 |
| 删除集合 | 级联删除文档和向量块 |

### 2.2 文档管理

| 功能 | 说明 |
|------|------|
| 上传文档 | base64 + 可选 `chunk_strategy`；PDF/DOCX/HTML 自动解析 |
| 拖拽批量上传 | 文档面板拖拽区域，一次拖入多文件，逐文件生成上传任务 |
| 整理为 Markdown | 提取文本经 LLM 结构化为 MD 后按标题层级分块入库（默认开启，可关闭；失败降级原文本） |
| MD 全文预览 | 整理后的 Markdown 全文持久化，文档列表可预览 |
| 多模态入库 | 图片经多模态 LLM 输出 MD 描述入库（png/jpg/jpeg/webp），原图留存血缘 |
| 上传免预选 | 未选中 Collection 时自动落「默认知识库」（懒创建），不静默丢弃 |
| 文档跨库移动 | 文档连同 chunks 移动至目标 Collection，计数同步校正 |
| 列出文档 | 按集合分页查询 |
| 删除文档 | 级联删除向量块 |
| 文档状态 | pending → indexing → indexed / error |
| 进度可观测 | WS `knowledge_ingest` 事件 + 管理页文档 status 轮询 |

### 2.3 语义搜索

| 功能 | 说明 |
|------|------|
| 向量搜索 | 余弦相似度，ivfflat 索引 |
| 元数据过滤 | filter_json 通过 JSONB `@>` 操作符 |
| 最低分数过滤 | min_score 阈值 |
| TopK 限制 | 默认 5 |
| Reranker | topk / cohere / infinity（env + SearchRequest 覆盖） |
| 重排候选 oversample | `rerank_candidates` 或默认 topK×3（上限 50） |
| 查询重写 | HyDE / Decomposition / MultiQuery（`rewrite_strategy` 参数） |
| 混合检索 | Dense + BM25 + RRF 融合（`hybrid_search` 参数） |
| 自适应路由 | 查询复杂度分类 → 自动选择检索模式 |
| 检索质量评估 | CRAG 式自校验，不足时自动补充检索 |
| BM25 全文检索 | PostgreSQL ts_vector + pg_trgm 双路检索 + GIN 索引 |
| 全库免选检索 | Collection 留空 = 全库智能路由（名称/描述匹配取 top N 广播 + RRF 合并），使用时无需选择知识库 |

### 2.4 分块策略

| 策略 | 键 | 说明 |
|------|-----|------|
| 按字符 | `char` | 按 N 字符窗口分割，含重叠 |
| 按 Token | `token` | 空格分词，近似 Token 计数 |
| Markdown 按标题 | `markdown` | 按标题层级分块（trpc chunking） |
| JSON 结构 | `json` | 按 JSON 结构分块 |
| 递归分块 | `recursive` | 递归字符分割 |

### 2.5 嵌入提供者

| 提供者 | 说明 |
|--------|------|
| OpenAI 兼容 | `/v1/embeddings` 端点 |
| Ollama | `/api/embeddings` 端点 |
| Gemini | Google GenAI API |
| HuggingFace | TEI `/embed` 批量 |

### 2.6 Agent 集成

| 功能 | 说明 |
|------|------|
| knowledge_search 工具 | Agent 可调用搜索知识库 |
| knowledge_reflect 工具 | Agent 可评估检索质量 + 跨 Collection 搜索 |
| 工具开关 | Agent 工具配置中启用/禁用 |
| AgenticFilter | LLM 动态生成过滤条件（未实现） |
| code_search 工具 | 代码语义搜索（未实现） |

### 2.7 高级功能

| 功能 | 说明 |
|------|------|
| OCR 识别 | 图片/PDF 自动提取文本 |
| Reranker | 检索结果重排序（TopK/Cohere/Infinity） |
| SourceSync | 数据源增量同步（未实现） |
| 多租户隔离 | 租户间知识库完全隔离（未实现） |
| Extractor | 格式转换统一抽象：文本类（trpc reader 提取 → LLM 整理为 MD）+ 多模态（视觉 LLM 图片 → MD），产出物归一为 Markdown |
| 跨 Collection 联邦搜索 | 多集合并行搜索 + 结果合并（Broadcast + Route 策略） |
| Plan-Then-Retrieve | 首轮 BeforeModel 预检索段落（`## Retrieved Knowledge`）+ Collection 目录；工具循环不重复检索 |

---

## 3. 非功能需求

| # | 需求 | 说明 |
|---|------|------|
| NFR-1 | 需要 PostgreSQL + pgvector 扩展 | 无 Postgres 时模块不可用，API 返回明确错误 |
| NFR-2 | 嵌入维度每个集合固定 | 更改需重建集合 |
| NFR-3 | 文档内容必须可文本解码 | 图片/PDF 需 OCR 提取 |
| NFR-4 | 文档级 metadata_json 写入 Chunk | 供 filter_json 检索过滤 |
| NFR-5 | 查询重写和检索评估依赖 LLM | 无可用 LLM 时自动降级（透传原始查询 / 跳过评估） |
| NFR-6 | 联邦搜索支持 Broadcast 和 Route 策略 | Route 策略基于 Collection 名称/描述相关性评分 |
| NFR-7 | Plan-Then-Retrieve 通过 BeforeModel 钩子注入 | 高频场景下可能增加延迟 |
| NFR-8 | Embedder 配置优先级 | env `KRATOS_KNOWLEDGE_EMBED_*` > `system_settings` DB > 运行时 Knowledge API/UI |
| NFR-9 | 上传安全守卫 | 32MB 大小限制 + MIME magic 检测 + 白名单；OOXML（zip 容器）按声明 MIME/扩展名二次判定 |
| NFR-10 | Embedder 超时可配 | `KRATOS_KNOWLEDGE_EMBED_TIMEOUT_SEC` 环境变量，默认 60s |
| NFR-11 | LLM 整理降级 | 无可用 LLM 或整理失败时透传原文本入库，不阻塞摄取 |
| NFR-12 | 多模态入库依赖 | 图片理解依赖多模态 LLM Provider，未配置时图片上传返回明确错误 |
| NFR-13 | 模态归一 | 任何模态提取后归一为 Markdown 文本，下游分块/向量化/检索无模态差异 |

---

## 4. 验收标准总览

| # | 验收标准 |
|---|----------|
| 1 | 可创建/列出/获取/删除知识集合 |
| 2 | 可上传文档并异步完成分块和向量化 |
| 3 | 文档向量化后存储到 pgvector，可进行相似度搜索 |
| 4 | Agent 可调用 `knowledge_search` 工具搜索知识库 |
| 5 | 支持元数据过滤（filter_json） |
| 6 | 知识搜索支持动态过滤（AgenticFilter） |
| 7 | 图片/PDF 文档可 OCR 识别入库 |
| 8 | 多租户知识库隔离 |
| 9 | 摄取进度前端可观测（WS / 文档 status） |
| 10 | Embedder 配置从 conf/env 注入 + Admin 运行时更新 |
| 11 | 查询重写（HyDE/Decomposition/MultiQuery）可按请求指定 |
| 12 | 混合检索（Dense+BM25+RRF）可按请求指定模式 |
| 13 | 自适应路由根据查询复杂度自动选择检索策略 |
| 14 | 检索质量评估（CRAG）在结果不足时自动补充检索 |
| 15 | Agent 可调用 `knowledge_reflect` 工具评估检索质量 |
| 16 | 跨 Collection 联邦搜索（多集合并行 + 结果合并） |
| 17 | Plan-Then-Retrieve（Agent 系统提示注入 Collection 摘要） |
| 18 | 联邦搜索 Route 策略（基于相关性智能路由） |
| 19 | 拖拽批量上传文本类文档，自动整理为 Markdown 入库，状态实时可见 |
| 20 | 整理后 Markdown 全文持久化，可预览 |
| 21 | 图片经多模态 LLM 整理为 Markdown 描述入库，原图血缘可追 |

---

## 5. 交互规格（用户视角）

### 5.1 知识库管理页

- 路由 `/knowledge`，侧栏入口 `menu.knowledge`
- 页面级三 Tab 结构：**文档 | 检索 | 设置**
  - 文档 Tab：左侧集合列表 + 右侧操作区（顶部常驻拖拽上传区 → 上传队列 → 选中集合详情卡 + 文档面板）
  - 检索 Tab：调试搜索面板（见 5.2）
  - 设置 Tab：Embedder 配置（低频操作收纳，见 5.3）
- 集合列表：展示名称、嵌入模型、维度、文档数、块数、状态
- 文档列表：按集合分页展示，显示来源、MIME 类型、大小、状态、错误信息、入库时间、更新时间
- 入库进度：WebSocket 实时推送 `knowledge_ingest` 事件，文档状态自动刷新；存在 indexing 中文档且文档 Tab 激活时前端轮询兜底
- 服务不可用 / 加载错误时以条件横幅提示（非常驻），错误横幅附重试入口

### 5.2 搜索面板

- 位于「检索」Tab
- 输入：collection_id（默认「全部知识库」）、query、top_k、min_score、filter_json
- 高级控件：rewrite_strategy（hyde/decomposition/multi_query）、hybrid_search（auto/dense/sparse/rrf）、use_rerank，各控件附一行简要说明
- 结果展示：按相似度排序的 Chunk 列表，含内容、分数、来源文档 ID

### 5.3 Embedder 配置面板

- 位于「设置」Tab；面板内注明该配置与系统设置页共享同一配置源，并提供跳转链接
- 展示当前 Embedder 配置（provider/base_url/model/dim，API Key 脱敏）
- 运行时更新：修改 provider/base_url/api_key/model/dim 并写回 DB
- 系统设置页可写 `knowledge_embed` 字段

### 5.4 文档入库对话框（粘贴文本）

- 标题「粘贴文本入库」，仅支持文本粘贴模式；文件上传统一走拖拽上传区（文本输入框内提示引导）
- 输入：source（文件名/URL/描述）、mime_type、text、metadata_json
- 分块参数：chunk_size、chunk_overlap、chunk_strategy（char/token/markdown/json/recursive）
- 上传后立即返回，异步处理进度通过 WS 推送

### 5.5 拖拽上传区（文件统一入口）

- 位于文档 Tab 右侧操作区顶部，常驻，高亮提示可拖入文件
- 支持多文件同时拖入，自动推断 source（文件名）与 mime_type
- 上传队列逐文件展示：文件名、大小、状态（pending → indexing → indexed / error）、错误信息
- 默认参数：整理为 Markdown 开启、分块策略 markdown
- 图片与文本文档共用入口（多模态支持上线前图片给出明确提示）
- 文档列表提供「预览」入口查看整理后的 Markdown 全文

---

## 子模块：Vault 重设计（V2 需求，2026-07-25）

> 设计详见 [37-knowledge.design.md §子模块：Vault 重设计](./37-knowledge.design.md#子模块vault-重设计)；任务计划见 [37-knowledge.development.md §子模块：Vault 重设计 Phase 计划](./37-knowledge.development.md#子模块vault-重设计-phase-计划)。
> 方案论证：[2026-07-25-research-knowledge-vault-redesign.md](../reports/2026-07-25-research-knowledge-vault-redesign.md)；评审：[2026-07-25-review-knowledge-vault-redesign.md](../reports/2026-07-25-review-knowledge-vault-redesign.md)。
> 本节为 V2 需求，US-1~US-14（Collection 时代）在迁移完成前继续有效；V2 落地后 Collection 语义升级为 Vault。

### US-15：给定本地路径创建知识库（Vault）

**作为**用户，**我希望**指定一个本地文件夹路径即可创建知识库，**以便**我现有的文档目录（如 Obsidian 库、资料文件夹）直接成为可被 agent 检索的知识库，无需逐个上传。

**验收标准**：
- 创建 Vault 时指定 `root_path`（本地目录），系统校验目录存在且非系统根目录。
- 嵌入模型改为**可选**：不配置时知识库完整可用（词法检索 + 导航），配置后额外获得语义检索。
- 同一 `root_path` 不可重复挂载（规范化后唯一）。
- 仍可创建多个 Vault，每库一路径，检索以库为隔离边界。

### US-16：双向同步（文件夹 ↔ 知识库）

**作为**用户，**我希望**把文件放进/移出/修改该文件夹时知识库自动跟进，**以便**知识库永远反映文件夹真实内容，我不需要在系统里重复维护一份。

**验收标准**：
- 向文件夹新增/修改 .md 文件后，知识库在扫描周期内（≤5min）自动入库/更新索引。
- 从文件夹删除文件后，知识库将其移入回收站（`.aranea/trash/`）并下线索引，**不物理删除**。
- 文件移动/重命名被识别为移动（保留文档身份与索引），而非删除+新增。
- 系统回写（摘要卡、agent 笔记）不会造成自我重复摄取。
- 文件夹不可用（盘拔出/权限）时 Vault 标记 error，已有索引保持只读可检索。

### US-17：每文档摘要卡（大致内容一览）

**作为**用户，**我希望**每篇文档都有系统生成的摘要卡（摘要/标签/类型），**以便**我不用打开全文就知道这篇文档大致讲了什么，并能按标签/类型浏览。

**验收标准**：
- 文档入库后自动生成摘要卡（LLM 可用时），写入文档 frontmatter 与索引。
- 文档内容变化后摘要卡自动更新。
- LLM 不可用时文档正常入库，摘要卡留空待补，不阻塞。
- 摘要/标签/类型为系统受管字段，与用户手写内容互不覆盖。

### US-18：文档关联（双链 + 自动关联）

**作为**用户，**我希望**看到每篇文档与哪些文档相关，**以便**我能顺着关联浏览知识、发现没注意到的联系。

**验收标准**：
- 支持在 Markdown 中写 `[[文档名]]` 显式双链，详情面板展示出链与入链。
- 系统自动抽取实体生成隐式关联，并标注关联来源类型（显式/实体/语义）。
- 详情面板提供相关文档推荐（语义层可用时为向量近邻，否则为同目录+共享标签+共现）。

### US-19：资源管理器式知识库面板

**作为**用户，**我希望**知识库像资源管理器一样呈现（文件夹树 + 文件列表 + 详情面板），**以便**我按自己熟悉的文件夹结构组织与浏览知识。

**验收标准**：
- 左栏 Vault 切换 + 文件夹树；中栏当前目录文档列表；右栏选中文档的详情面板（摘要卡 + 关联 + 预览）。
- 统一搜索框：输入即时显示**文件名匹配**（搜文件，毫秒级），回车/停顿触发**内容检索**（搜知识）。
- 支持路径式搜索（如 `财报\q2` 命中 `财报/2026Q2.md`）与过滤器（文件夹/标签/类型/时间）。
- 拖拽上传到指定文件夹；支持面包屑导航与「在树中定位」。
- 文件可拖拽移动到其他目录（树目录节点/库根/面包屑段为落点，非法落点禁用）；同名冲突弹确认：覆盖 / 保留两份 / 取消。
- 搜索框可选定目录范围（范围选择器），选中后即时区与语义区均只在该目录内匹配，直至清除。

### US-20：Agent 导航与自编辑知识库

**作为** Agent，**我希望**能像人一样浏览知识库（看目录树 → 读摘要卡 → 按需读全文），并能创建笔记，**以便**我低成本定位知识（而非盲目检索）并沉淀会话中学到的内容。

**验收标准**：
- `knowledge_navigate` 工具提供 tree/card/read 三级能力，返回受 token 预算约束，超限给出缩小范围提示。
- `knowledge_grep` 工具提供内容字面/正则搜索（只读）。
- `knowledge_write` 工具可创建/追加笔记，写入后自动被索引；越权路径（库外目录）被拒绝并记录。
- 现有 `knowledge_search` / `knowledge_reflect` 行为不变（collection_id 语义 = vault_id）。

### V2 非功能需求（增补）

| # | 需求 | 说明 |
|---|------|------|
| NFR-14 | 文件系统即真相源 | 全部派生索引（检索/向量/关联）可无状态重建，提供 reindex |
| NFR-15 | 无 embedding 完整可用 | 默认配置端到端检索 <30ms；语义层为可选增强插件 |
| NFR-16 | 写入安全 | 一切写入经唯一出口；覆盖前备份；agent 写入限 vault root 内并记审计日志 |
| NFR-17 | 同步保守默认 | 冲突留双份、删除进回收站，任何场景不丢用户数据 |
| NFR-18 | 迁移不中断 | 存量 Collection 迁移为 Vault 期间检索可用，迁移幂等可重入 |

### V2 验收标准总览（增补）

| # | 验收标准 |
|---|----------|
| 22 | 给定本地路径可创建 Vault；同一路径不可重复挂载 |
| 23 | 文件夹内增/改/删/移文件，知识库在扫描周期内正确跟进，删除入回收站 |
| 24 | 每文档有摘要卡（summary/tags/type），内容变化后自动更新 |
| 25 | `[[双链]]` 出链/入链可见，自动关联标注来源类型 |
| 26 | 资源管理器面板可树形浏览；统一搜索区分搜文件（即时）与搜知识（回车） |
| 27 | 无 embedding 配置时知识库全功能可用（检索/导航/关联降级版） |
| 28 | agent 可 tree→card→read 导航下钻；可写笔记并被自动索引；越权写入被拒 |
| 29 | 存量 Collection 数据完整迁移为 Vault 文件，迁移期检索不中断 |

### V2 交互规格（增补，用户视角）

- **页面结构**：知识库页为「浏览 | 图谱 | 设置」三 Tab（检索能力融入浏览 Tab 顶部双区搜索，独立检索 Tab 已由图谱取代）。
- **浏览 Tab**：三栏（Vault 树 | 目录内容 | 详情面板），顶部统一搜索框 + 面包屑；上传入口在树节点 hover 菜单（新建目录/新建文档/上传文件到此）。
- **搜索框**：即时区（文件名，输入即出，150ms 防抖）+ 内容区（回车触发，结果按文件分组、命中高亮）；前缀「范围」按钮弹出迷你目录树，选中目录后两区均限定该目录。
- **拖拽移动**：中栏文件行可直接拖到左栏目录/库根或面包屑段完成移动；目标发光高亮、非法落点禁用；同名冲突弹「覆盖 / 保留两份 / 取消」。
- **详情面板**：摘要一行（hover 大号浮层卡）+ 关联计数 chips + 正文/媒体区（md/txt 可编辑，图片/音频/视频内联预览，word 下载）。
- **图谱 Tab**：左 3D 力导向图（节点按类型着色、大小=连接度；旋转/平移/缩放；hover 高亮一跳邻居；点击选中）；右操作台（库选择、边类型过滤、目录范围、节点搜索定位、节点列表、选中节点卡「在浏览中打开」）。**（G4 描述；G5 起被「深空星河 + HUD 操作台」取代，详见下方 G5 子模块交互规格）**
- **设置 Tab**：Vault 路径与同步开关、嵌入模型（可选增强）、重建索引入口。

---

## 子模块：图谱深空版与实体治理（G5 需求，2026-08-07）

> 设计详见 [37-knowledge.design.md §V12.8](./37-knowledge.design.md#v128-图谱深空版渲染层--实体治理g52026-08-07-评审通过)；任务计划见 [37-knowledge.development.md §子模块：图谱深空版与实体治理（G5）Phase 计划](./37-knowledge.development.md#子模块图谱深空版与实体治理g5phase-计划)。
> 调研依据：[2026-08-07-research-knowledge-graph-oss.md](../reports/2026-08-07-research-knowledge-graph-oss.md)。G4 图谱（3d-force-graph 默认视觉）视觉与性能均不达标，G5 重写图谱画布为「深空星河」并补实体消歧治理能力。用户已裁决：自研渲染层；深空星河 + HUD 操作台；粒子流完整复刻；旧 3d-force-graph 依赖完全移除；实体消歧与前端一轮做完。

### US-21：深空星河图谱浏览

**作为** 知识库用户，
**我希望** 图谱 Tab 打开即见深空星河式图谱——发光节点按目录/类型聚成星簇、星云与星空为背景、hover 时发光粒子沿边流向关联文档，
**从而** 一眼看清知识结构与聚类，浏览本身具有沉浸感与科技感。

**验收标准**：
- 节点为发光球体（加法混合 + 辉光），密集区域自然增亮为「热核」；同目录/同类型节点在空间中聚为可分辨的星簇而非一团结。
- 背景为深空：FBM 星云 + 分层星空 + 锚定核心节点的雾团；背景绝不喧宾夺主（不糊屏、不压过节点）。
- hover 节点：一跳邻居提亮、其余压暗、关联边高亮，且发光粒子沿边从该节点流向邻居（连续溪流感）。
- 布局收敛后静置不占 CPU/GPU；图谱 Tab 切走（不可见）时渲染暂停。
- 2 万节点规模下交互（旋转/缩放/hover）保持流畅（目标 60fps 量级）。

### US-22：局部图谱与节点直达

**作为** 知识库用户，
**我希望** 选中节点后能聚焦其 N 跳邻域（局部图谱），单击选中、双击直接在浏览 Tab 打开文档，
**从而** 在大图中快速下钻到关心的局部，并一步抵达内容。

**验收标准**：
- 局部图谱模式：以选中节点为根、按跳数（1-4）过滤展示子图；子图节点颜色与全局图一致；可一键返回全局。
- 单击节点 = 选中（联动操作台）；双击节点 =「在浏览中打开」；拖拽节点可钉住位置（松手后留在原处）。
- 滚轮缩放朝向光标位置（zoom-to-cursor）；节点标签按缩放级别/连接度阈值显示，密图不糊屏。

### US-23：实体治理（消歧合并）

**作为** 知识库管理员，
**我希望** LLM 抽取的实体自动归一化去重（"AI"/"ai"/"ＡＩ" 视为同一实体），并能一键合并系统识别出的高相似实体，
**从而** 图谱的实体边准确聚合，同一概念不碎裂为多个节点。

**验收标准**：
- 实体入库前经归一化（Unicode NFC + 大小写折叠 + 空白折叠）；归一化后相同的实体合并为同一字典条目。
- 系统给出合并建议列表（归一化冲突 + 配置 embedding 时高相似对）；管理员确认后一键合并，该实体的所有文档提及与图谱边引用被重写为保留实体，返回重写条数。
- 无 embedding 配置时归一化与手动合并全功能可用（语义建议为可选增强，对齐 NFR-15）。
- 合并后可观测：治理操作写流程日志，重写条数对用户可见。

### V3 功能需求清单（G5）

| # | 需求 | 说明 |
|---|------|------|
| FR-G5-1 | 自研 3D 渲染层 | 废弃 3d-force-graph：InstancedMesh 节点 + Worker 力导向物理 + lazy-render，支撑 2 万节点 |
| FR-G5-2 | 簇感知布局 | 同目录/同类型节点空间聚簇（簇凝聚/簇分离力），簇间可分辨 |
| FR-G5-3 | 深空视觉 | bloom 辉光 + FBM 星云 + 分层星空 + 核雾；加法混合节点热核 |
| FR-G5-4 | 粒子流 | hover/选中节点时发光粒子沿边流向邻居（相位均布连续流、时变彩虹色） |
| FR-G5-5 | 节点三层分级 | hub 检测（supernode/ultranode）映射尺寸与视觉层级 |
| FR-G5-6 | 微弯边 | 边为微弯曲线（方向稳定各异），hover 关联边瞬时高亮 |
| FR-G5-7 | 交互全集 | 单击选中 / 双击打开 / pin 拖拽 / zoom-to-cursor / 标签阈值 / auto-rotate 开关 / 局部图谱 N 跳 |

---

## 子模块：编辑器与笔记体验（SP2 需求，2026-08-10）

> 设计详见 [37-knowledge.design.md §SP2](./37-knowledge.design.md#sp2-编辑器与笔记体验深空液态玻璃工作台2026-08-10)；任务计划见 [37-knowledge.development.md §子模块：编辑器与笔记体验（SP2）Phase 计划](./37-knowledge.development.md#子模块编辑器与笔记体验sp2phase-计划)。
> 调研依据：[2026-08-08-research-pkm-obsidian-blueprint.md](../reports/2026-08-08-research-pkm-obsidian-blueprint.md) §5/§8/§9 + 视觉参考（透明流体卡片/3D 环形文件夹/粒子容器/3D 旋转）。
> **用户裁决（2026-08-10）**：目标是 Obsidian 级笔记处理能力；不止血、按最优方案大改；UI 推翻 Tab 管理后台为 Obsidian 工作台；编辑器选型 CodeMirror 6 Live Preview（非 TipTap——源文本即真相源哲学）；A1+A2 全量一轮交付；视觉要液态玻璃、科幻、炫酷。
> **范围**：纯前端重构 + 编辑器接入；后端零改动（SP1 内核 API 已就绪）。
> **增强轮（2026-08-11，用户反馈驱动）**：配色未跟随系统主题、液态玻璃质感不足、对标 Obsidian/Notion/VS Code 逐项核查功能差距——产出 P0 视觉同源（色彩令牌 + 真液态玻璃）、P1 功能补齐（全库内容搜索、标签页管理）、P2 体验增强（wikilink 标题段、命令面板 MRU/别名、未链接提及）。P2-7 未链接提及为 SP2 唯一后端新增（1 个只读 RPC）。

### US-24：Obsidian 式笔记工作台

**作为** 知识库用户，
**我希望** 打开知识库就是一个以笔记为中心的工作台——左侧 Vault 树，中间笔记（可多开标签页），右侧围绕当前笔记联动的反链/出链/大纲/属性/局部图谱面板，
**从而** 我的注意力始终在内容本身，而不是在管理后台的 Tab 之间跳来跳去。

**验收标准**：
- 知识库页不再是「浏览 | 图谱 | 设置」三 Tab 管理后台，改为三栏工作台（左导航 / 中笔记区 / 右联动面板）。
- 选中树中文档即在笔记区打开为标签页；已打开的标签页重复选中时激活而非重开；标签页可关闭、有未保存脏标记。
- 右栏五个面板（反向链接 / 出链 / 大纲 / 属性 / 局部图谱）全部跟随当前活动笔记联动刷新。
- 图谱从 Tab 变为全屏覆盖模式（顶栏进入、ESC 或按钮退出），深空视觉与交互保持 G5 现状不变。
- 设置入口收敛到顶栏，打开为浮层而非 Tab。

### US-25：Live Preview 编辑器

**作为** 笔记作者，
**我希望** 编辑 Markdown 时获得 Obsidian 同款 Live Preview——光标所在行显示源语法，其余行直接渲染标题/粗斜体/列表/代码块样式，
**从而** 写作时既能看到排版效果，又不丢失对源文本的精确控制。

**验收标准**：
- 编辑器基于 CodeMirror 6，`@codemirror/lang-markdown` 语法高亮。
- Live Preview：非光标行的 `#` 标题渲染为对应字号、`**粗体**`/`*斜体*`/`~~删除线~~` 渲染样式、代码块/引用/列表有视觉样式；光标进入行时该行回退为源码。
- 保存复用 CAS（`UpdateDocumentContent` + content_hash），并发冲突沿用「留双份 + 警告提示」既有行为；保存状态在标签页可见。
- 编辑/只读预览可切换；图片/音视频/下载等非 Markdown 文档仍走既有预览/下载路径，不出现编辑器。

### US-26：双链写作（`[[` 补全与跳链）

**作为** 笔记作者，
**我希望** 输入 `[[` 时弹出笔记名自动补全，文档中的 `[[双链]]` 渲染为可点击的链接芯片，未创建的目标灰显，
**从而** 像 Obsidian 一样自然地编织知识网络。

**验收标准**：
- 输入 `[[` 触发补全（150ms 防抖），候选来自文件名即时搜索（复用统一搜索即时区同一数据源）；键盘 ↑↓ 选择、Enter 确认插入 `[[名称]]`。
- 编辑器内 `[[目标]]`（含 `[[目标|别名]]`、`[[目标#标题]]`）渲染为链接芯片；Ctrl+点击（或阅读模式单击）打开目标笔记。
- 悬空链（目标未创建）芯片灰显 + 虚线 + hover 提示「目标未创建」，与浏览视图 dangling 视觉一致。
- 补全候选含当前库全部文档；跨库语法留待后续（B-1 遗留不在本轮）。

### US-27：快速导航与命令（⌘O / ⌘K）

**作为** 重度键盘用户，
**我希望** 用 ⌘O 按文件名秒开任何笔记、用 ⌘K 唤起命令面板执行全部常用操作，
**从而** 双手不离键盘完成知识库全部高频动作。

**验收标准**：
- ⌘O（Ctrl+O）打开快速切换浮层：输入即过滤（文件名即时搜索），Enter 在当前标签组打开，↑↓ 导航，ESC 关闭。
- ⌘K（Ctrl+K）打开命令面板：命令含新建笔记/新建文件夹/保存/切换编辑预览/打开图谱/切换 Vault/重建索引/晋升到团队库；支持模糊过滤 + 键盘导航；命令展示快捷键提示。
- 两浮层均为液态玻璃居中浮层（Raycast 风格），打开时背景模糊压暗。

### US-28：右栏联动面板

**作为** 笔记作者，
**我希望** 写笔记时右侧始终能看到当前笔记的反链、出链、大纲、属性和局部图谱，
**从而** 不离开编辑器就能感知这篇笔记在知识网络中的位置。

**验收标准**：
- 反向链接：块级粒度 + 上下文片段（复用既有反链 API 与分组展示），点击跳转来源笔记。
- 出链：当前笔记引用的文档列表（显式/实体/语义分区沿用既有口径），点击打开目标。
- 大纲：从当前内容解析标题树（H1-H6），点击滚动定位；编辑时随内容实时刷新。
- 属性：解析 frontmatter 键值只读展示（含 title/aliases/tags）；无 frontmatter 时显示「无属性」空态。
- 局部图谱：以当前笔记为根的 1-5 跳邻域迷你图，点击节点打开该笔记，可一键展开为全屏图谱。
- 无活动笔记时右栏显示空态而非报错。

### US-29：笔记生命周期入口

**作为** 知识库用户，
**我希望** 在树上或命令面板直接新建笔记/新建文件夹，并能重命名、删除、移动笔记，
**从而** 知识库的日常维护全部在 UI 内闭环，不需要去文件系统操作。

**验收标准**：
- 树节点 hover 菜单与命令面板均可「新建笔记」「新建文件夹」（对接既有 `CreateVaultDocument`/`CreateVaultDir` RPC）；新建笔记自动在笔记区打开并进入编辑态。
- 重命名/删除/移动沿用既有交互（拖拽移动 + 同名冲突「覆盖/保留两份/取消」不变）。
- 新建/重命名/删除后树、标签页、右栏面板状态一致（删除已打开笔记时关闭对应标签页）。

### US-30：深空液态玻璃视觉

**作为** 产品用户，
**我希望** 知识库界面是深空液态玻璃风——透明流体玻璃面板、粒子背景、3D 倾斜卡片、辉光微交互，
**从而** 使用时有科技感与沉浸感，愿意长时间停留。

**验收标准**：
- 全局深空底（`#0a0e1a`）+ 稀疏粒子漂浮背景 + 青紫极光光斑；视觉与 G5 深空图谱同一语言。
- 面板为液态玻璃：半透明 + backdrop blur + 高光描边 + 流体折射质感；卡片 hover 3D 倾斜（跟随鼠标 rotateX/Y + 移动高光）。
- 无笔记空态：3D 环形 Carousel 展示库与最近笔记（自动旋转 + 中心高亮聚焦），点击进入笔记。
- 主按钮为辉光/磁吸风格；⌘O/⌘K 浮层为液态玻璃 Raycast 风。
- `prefers-reduced-motion` 时粒子/倾斜/光环动效全部关闭；低端设备粒子降级。

### US-31：配色跟随系统主题 + 真液态玻璃（2026-08-11 增强轮 P0）

**作为** 产品用户，
**我希望** 知识库工作台的配色与系统 Tech Night 主题完全同源（主题切换时工作台跟随变化），玻璃面板有真正的液态折射质感（光纹扭曲、镜面断面、指针高光），
**从而** 工作台与全站视觉统一，玻璃质感对得起「液态玻璃」之名。

**验收标准**：
- kb 令牌全部消费全局 Tech Night 变量（`--color-accent`/`--canvas-base`/`--tech-text-primary` 等），无硬编码色值；系统主题变量调整时工作台自动跟随。
- 玻璃面板具备三层液态效果：SVG 折射光纹（feTurbulence + feDisplacementMap）、镜面断面（顶缘反光 + 底缘暗线）、指针追随高光（hover 显现）。
- 光纹/高光在面板边缘径向羽化，不出现硬边；装饰层不拦截任何交互。
- `prefers-reduced-motion` 降级契约不变（FR-SP2-10）。

### US-32：全库内容搜索（2026-08-11 增强轮 P1）

**作为** 笔记用户，
**我希望** 按 Ctrl+Shift+F 唤起全库内容搜索，输入关键词即看到命中文档与上下文片段，回车直达文档，
**从而** 我记得内容但不记得文件名时也能秒找到笔记（Obsidian/VS Code 全局搜索同款能力）。

**验收标准**：
- Ctrl+Shift+F（或顶栏搜索按钮）唤起液态玻璃搜索浮层，与 ⌘O/⌘K 互斥打开。
- 输入 300ms 防抖后检索当前 Vault 全文（复用 `SearchKnowledge` RPC），结果展示文档名、路径、相关度分数与命中上下文片段（匹配词居中截取）。
- 慢响应不覆盖新查询结果（竞态守卫）；↑↓ 导航、Enter 打开、ESC 关闭；关闭后状态清零。

### US-33：标签页管理增强（2026-08-11 增强轮 P1）

**作为** 多笔记并行编辑的用户，
**我希望** 标签页可以拖拽调整顺序、鼠标中键直接关闭，
**从而** 多开笔记时的管理效率对齐 Obsidian/VS Code/浏览器标签体验。

**验收标准**：
- 标签条支持 HTML5 原生拖拽重排（拖动中视觉反馈，落点生效），重排不改变激活标签。
- 鼠标中键点击标签直接关闭（脏标签仍走保存确认流程）。
- 纯前端状态变更，无新增依赖。

### US-34：wikilink 标题段补全与定位（2026-08-11 增强轮 P2）

**作为** 笔记作者，
**我希望** 输入 `[[文档#` 时补全该文档的标题列表，跳链后自动滚动到对应标题，
**从而** 长文档可以精确互链到小节（Obsidian heading link 语义）。

**验收标准**：
- `[[target#partial` 触发标题补全（候选来自已打开标签的大纲解析），确认插入完整 `[[target#heading]]`。
- 点击带 `#heading` 的链接芯片打开目标文档后自动滚动定位到对应标题行。
- 无标题段时行为与 SP2 初版一致（单参数跳链，不破坏既有契约）。

### US-35：命令面板效率增强（2026-08-11 增强轮 P2）

**作为** 重度键盘用户，
**我希望** 命令面板记住我最近用过的命令并置顶，且支持英文/拼音别名搜索（如输入 "baocun" 命中「保存当前笔记」），
**从而** 高频命令两次击键内可达，中文用户无需切换输入法精确匹配标题。

**验收标准**：
- 空查询时最近执行的命令（至多 3 条）置顶并显示 history 角标；MRU 持久化到 localStorage，写失败静默降级为会话内记忆。
- 每条命令注册英文/拼音别名，模糊过滤同时匹配标题与别名。
- 键入查询后由相关性排序接管（MRU 角标仅在默认列表提示）。

### US-36：未链接提及（2026-08-11 增强轮 P2）

**作为** 知识网络编织者，
**我希望** 反链面板除已建立的链接外，还能看到哪些文档在纯文本中提及了当前笔记名却没有加 `[[链接]]`，
**从而** 发现潜在的关联并决定是否补链（Obsidian "Unlinked mentions" 语义）。

**验收标准**：
- 后端提供只读 RPC：目标文档显示名（basename 去扩展名）在同库其他文档正文中的纯文本出现（`[[...]]` 内的出现不计），按来源文档聚合并计数，附首次出现上下文片段。
- 反链面板新增「未链接提及」分组，展示来源文档名 + 提及次数，点击跳转来源文档。
- 单字符文档名匹配噪声过大，直接返回空；端口未接线时降级为空，不阻断反链主流程。

### V4 功能需求清单（SP2）

| # | 需求 | 说明 |
|---|------|------|
| FR-SP2-1 | 工作台骨架 | 三栏联动布局（左导航/中笔记标签页/右五面板）取代 Tab 管理后台；图谱全屏覆盖；设置浮层 |
| FR-SP2-2 | 笔记标签页 | 多开/激活/关闭/脏标记；同文档去重激活；删除已开文档关闭对应标签 |
| FR-SP2-3 | CM6 编辑器 | CodeMirror 6 + lang-markdown；Live Preview 行级装饰；CAS 保存复用；编辑/预览切换 |
| FR-SP2-4 | wikilink 写作 | `[[` 补全（即时搜索数据源）；链接芯片渲染；点击跳链；dangling 灰显 |
| FR-SP2-5 | ⌘O 快速切换 | 文件名即时过滤 + 键盘导航 + 液态玻璃浮层 |
| FR-SP2-6 | ⌘K 命令面板 | 命令注册表（新建/保存/切模式/图谱/切库/重建索引/晋升）+ 模糊过滤 + 快捷键提示 |
| FR-SP2-7 | 右栏五面板 | 反链/出链/大纲/属性/局部图谱，全部跟随当前活动笔记联动 |
| FR-SP2-8 | 笔记生命周期 | 新建笔记/文件夹 UI 接线（RPC 已有）；重命名/删除/移动沿用既有 |
| FR-SP2-9 | 视觉特效组件 | GlassPanel（液态玻璃）/ParticleField（粒子）/TiltCard（3D 倾斜）/GlowButton（辉光磁吸）/RingCarousel（3D 环形）五个自研组件 |
| FR-SP2-10 | 降级与可达性 | prefers-reduced-motion 全局降级；粒子数按设备分级；键盘可达性（焦点环/ESC/Tab 序） |
| FR-SP2-11 | 色彩同源（P0-1） | kb 令牌全部消费全局 Tech Night 变量，跟随系统主题（US-31） |
| FR-SP2-12 | 真液态玻璃（P0-2） | SVG 折射光纹 + 镜面断面 + 指针追随高光三层效果，边缘径向羽化（US-31） |
| FR-SP2-13 | 全库内容搜索（P1-3） | Ctrl+Shift+F 搜索浮层，复用 SearchKnowledge RPC，命中片段 + 竞态守卫（US-32） |
| FR-SP2-14 | 标签页管理增强（P1-4） | 原生 HTML5 拖拽重排 + 中键关闭（US-33） |
| FR-SP2-15 | wikilink 标题段（P2-5） | `[[doc#heading]]` 标题补全 + 跳链滚动定位（US-34） |
| FR-SP2-16 | 命令面板增强（P2-6） | MRU 置顶（localStorage 持久化）+ 英文/拼音别名搜索（US-35） |
| FR-SP2-17 | 未链接提及（P2-7） | ListUnlinkedMentions 只读 RPC + 反链面板提及分组（US-36） |

### 验收标准（SP2，增补 30~38；增强轮增补 39~44）

| # | 验收 |
|---|------|
| 30 | 工作台三栏联动：当前笔记驱动右栏五面板刷新；标签页多开/关闭/脏标记正确 |
| 31 | Live Preview 编辑：非光标行渲染样式、光标行回退源码；CAS 保存与冲突提示沿用既有行为 |
| 32 | `[[` 补全出候选并插入；wikilink 芯片可点击跳链；dangling 灰显与浏览视图一致 |
| 33 | ⌘O/⌘K 浮层键盘全可操作（↑↓/Enter/ESC）；命令面板全部命令可执行且生效 |
| 34 | 新建笔记/文件夹在树与命令面板双入口可用；新建笔记自动打开进入编辑态 |
| 35 | 局部图谱以当前笔记为根 1-5 跳联动；点击节点打开笔记；可展开为全屏图谱 |
| 36 | 图谱全屏覆盖进出正常，G5 深空交互无回归；设置浮层可保存 embedder 配置 |
| 37 | 液态玻璃视觉全套落地（玻璃面板/粒子/倾斜/辉光/环形空态）；reduced-motion 下动效全关 |
| 38 | 前端 `lint + test + build` 全绿；浏览器运行时复验无控制台错误 |
| 39 | kb 令牌无硬编码色值，全部消费全局 Tech Night 变量；主题变量调整时工作台跟随（US-31） |
| 40 | 玻璃面板折射光纹/镜面断面/指针高光三层效果可见，边缘羽化无硬边，装饰层不拦截交互（US-31） |
| 41 | Ctrl+Shift+F 唤起全库搜索，命中片段正确，竞态守卫生效（旧响应不覆盖新查询）（US-32） |
| 42 | 标签页拖拽重排生效且激活态不变；中键关闭触发（脏标签走确认）（US-33） |
| 43 | `[[doc#` 标题补全出候选；带 heading 跳链后滚动定位到标题行（US-34） |
| 44 | 命令面板空查询 MRU 置顶 + history 角标；别名/拼音可命中命令；反链面板展示未链接提及分组（US-35/36） |
| FR-G5-8 | HUD 操作台 | 操作台深空 HUD 换肤（等宽字体、青色发光面板、括号开关），功能不减少；HUD 样式限定图谱 Tab 作用域，不改全局主题 |
| FR-G5-9 | 实体归一化 | NFC + case-fold + 空白折叠；归一化键唯一约束 |
| FR-G5-10 | 实体合并治理 | 合并建议列表 + 一键合并（事务重写引用 + 返回重写条数 + 流程日志） |
| FR-G5-11 | 语义合并建议（可选） | 配置 embedding 时高相似实体对进建议队列（≥0.90 候选自动合并、0.80-0.90 建议） |

### V3 非功能需求（G5 增补）

| # | 需求 | 说明 |
|---|------|------|
| NFR-G5-1 | 性能 | 2 万节点 / 5 万边：交互帧率 60fps 量级；布局收敛后静置 CPU/GPU 占用归零（lazy-render） |
| NFR-G5-2 | 确定性 | 同一图谱数据每次打开初始布局一致（确定性播种） |
| NFR-G5-3 | 可测试 | 图模型/力/八叉树/分级/调色/粒子相位/归一化/合并全部为纯函数或纯 TS 模块，单测覆盖 |
| NFR-G5-4 | 主题兼容 | 深空画布为独立沉浸区；HUD 皮肤不污染全局主题 token，亮色/暗色主题下图谱 Tab 均为深空风 |
| NFR-G5-5 | 降级 | Worker 创建失败时主线程兜底跑同一物理引擎；WebGL 不可用时给出友好占位 |

### V3 验收标准总览（G5 增补）

| # | 验收标准 |
|---|----------|
| 30 | 图谱 Tab 打开呈现深空星河视觉：发光节点簇、星云星空背景、bloom 辉光；背景不糊屏 |
| 31 | hover 节点邻居提亮 + 粒子流沿边流动；单击选中联动操作台、双击在浏览中打开 |
| 32 | 节点可拖拽钉住；滚轮朝光标缩放；标签按阈值显示 |
| 33 | 局部图谱 N 跳聚焦颜色与全局一致，可返回全局 |
| 34 | 静置与切走 Tab 时渲染暂停/物理停止；2 万节点交互流畅（性能基准记录入测试文档） |
| 35 | 归一化后 "AI"/"ai"/"ＡＩ" 聚合为同一实体，实体边不再碎裂 |
| 36 | 合并建议列表可一键合并，重写条数可见，合并后图谱边引用正确 |
| 37 | 无 embedding 时归一化与手动合并可用；操作台全部既有功能（库/边类型/范围/搜索/列表/选中卡）不回归 |

### V3 交互规格（G5 增补，用户视角）

- **图谱 Tab 布局**：左侧深空画布（沉浸区）+ 右侧 HUD 操作台；画布左下角统计（节点/边数）、右上角浮动工具条（适应视图 / 图例 / auto-rotate 开关 / 标签开关 / 返回全局）。
- **画布交互**：左键拖拽旋转、右键拖拽平移、滚轮朝光标缩放；hover 节点显示名称浮签 + 邻居高亮 + 粒子流；单击选中（金色描边 + 操作台联动）；双击「在浏览中打开」；左键按住节点拖拽 = 钉住移动（拖拽中旋转/自动旋转挂起）。
- **局部图谱**：选中节点卡或节点列表提供「聚焦邻域」入口，跳数 1-4 可选；进入后工具条显示「返回全局」；子图颜色与全局一致。
- **HUD 操作台**：保留 G4 全部控件（库选择、边类型过滤、目录范围、孤立节点开关、节点搜索定位、节点列表、选中节点卡），视觉为深空 HUD（等宽字体、青色发光边框面板、`[ ON ]` 括号式开关、边类型图例发光色块）；新增「实体治理」分区：合并建议列表（名称对 + 相似度来源）+ 一键合并 + 重写条数反馈。
- **空态/错误态**：无库或无节点显示深空占位插画文案；加载失败内联错误 + 重试；WebGL 不可用显示降级说明。

---

## 子模块：双模块级知识内核（SP1，2026-08-08 评审通过）

> **蓝图来源**：[2026-08-08 调研报告](../reports/2026-08-08-research-pkm-obsidian-blueprint.md)（学术 × 开源 × Obsidian 逆向 × SiYuan 源码四路调研）。
> **定位**：Obsidian 式笔记系统建设的第一个子项目（SP1）——把知识内核从「文档级双链」升级为「块级双链 + 双模统一索引」，为 SP2 编辑器、SP3 图谱 2.0、SP4 多模态、SP5 双模权限、SP6 AI 知识伙伴奠基。
> **核心决策**：① 一个块模型 + 两种存储后端（个人 Vault=文件真相源；团队库=PG 真相源）；② 服务端统一索引层（Obsidian MetadataCache 哲学的服务端版），个人边与团队边同图共存；③ 不做客户端团队图谱副本，走 WS 增量订阅；④ 私有→共有为「复制式晋升」（provenance 不可变）；⑤ 共有删除经边级联 + WS 增量 + dangling 语义同步各端。
> **2026-08-08 深入评审**：[评审报告](../reports/2026-08-08-review-sp1-knowledge-blueprint.md)——B-1~B-4 阻断项修订全部合入（US-24/US-27、F-SP1-7/F-SP1-11）；G1~G8 前瞻提案全部采纳，产品路线扩展为 SP1~SP7（见本节末「子项目路线图」）。

### US-24：块级双链书写与解析

**作为** 知识库用户，**我希望** 在 Markdown 中写 `[[文档]]`、`[[文档#标题]]`、`[[文档#^块锚点]]`、`![[嵌入]]` 四种语法并被正确解析为块级引用边，**以便** 像 Obsidian 一样组织细粒度知识网络。

**验收标准**：
1. 四种语法解析正确，含 `|别名` 变体；`![[` 嵌入与 `[[` 引用区分边类型
2. `[[doc#标题]]` 按标题路径定位块（标题改名后按最新解析，不存死 ID）；`[[doc#^anchor]]` 按显式锚点定位
3. 引用目标不存在时标为 dangling（悬空链），UI 灰显不报错；目标后来创建后自动复活
4. 块被引用时若未显式锚定，写路径自动向源文件回填 `^uuid` 锚点（幂等，不重复回填）
5. 跨 Collection 解析确定性：同库优先 → 当前用户可见库内最短路径唯一匹配 → 多义时确定性取首并标记 ambiguous；**不可见库不参与匹配**（不泄漏文档名）

### US-25：反链（块级）

**作为** 用户，**我希望** 查看任一文档/块的反向链接列表（谁引用了我、引用上下文），**以便** 发现知识的隐含关联（Obsidian 反链面板语义）。

**验收标准**：
1. 反链精确到块级，含引用上下文片段
2. 文档反链 = 其全部块反链的聚合
3. dangling 引用也出现在目标名的「未创建」反链语义中

### US-26：团队库（PG 权威）

**作为** 团队成员，**我希望** 有 PG 权威存储的团队知识库，**以便** 多人共享、带权限与审计地共建知识。

**验收标准**：
1. Collection 增加存储后端维度（local 文件 / team PG）；team 库文档本体存 PG
2. 两种库走同一条块解析管线，产出同构索引
3. 图谱/检索对个人边与团队边统一查询（按可见性过滤）

### US-27：私有晋升共有

**作为** 用户，**我希望** 把个人库中的块/文档晋升到团队库，**以便** 个人沉淀转化为团队资产。

**验收标准**：
1. 晋升 = 复制非移动：团队库新建块记录 `promoted_from` 谱系，源块标 `promoted_to`
2. 被晋升块引用的其他私有块：级联提示；未一并晋升的在团队侧显示为「私有外部引用」占位
3. 晋升操作有审计记录
4. 晋升后目标库立即可检索：块克隆同事务重放目标文档派生索引（chunk→embed（有语义层时）→FTS）

### US-28：共有删除的各端同步

**作为** 用户，**我希望** 团队库内容被删除或权限被收回后，我的图谱/反链视图即时一致，**以便** 不看到死链与越权内容。

**验收标准**：
1. 团队块删除 → 指向它的边级联删除 → WS 增量推送 → 各端图谱即时摘除
2. 私有块中残留的目标文本转为 dangling 灰显，不报错
3. 权限收回后图谱投影重算，被收权节点/边不再出现

### US-29：索引可全量重建

**作为** 管理员，**我希望** 一键重建全部派生索引（块/边/FTS/向量），**以便** 索引损坏或升级后恢复（SiYuan「派生索引可删除重建」哲学）。

**验收标准**：
1. `RebuildIndex` 幂等可重入，重建期间检索降级可用
2. 重建后 refs 无孤儿边（整文档删了重插语义）

### SP1 功能需求清单

| # | 需求 | 优先级 |
|---|------|--------|
| F-SP1-1 | Markdown AST 块切分（标题/段落/列表/代码块等），块级 content_hash | P0 |
| F-SP1-2 | 四语法双链解析（一趟遍历产全部索引行，纯函数） | P0 |
| F-SP1-3 | `knowledge_blocks` / `knowledge_block_refs` 物化表，整文档删了重插 | P0 |
| F-SP1-4 | 统一链接索引服务（内存图 + WS 增量推送） | P0 |
| F-SP1-5 | 块级反链 API（含 dangling 语义） | P0 |
| F-SP1-6 | Collection 存储后端维度（local/team），team 库块本体 PG 存储 | P0 |
| F-SP1-7 | 晋升（复制式 + 谱系对 + 级联提示 + 审计 + 目标库派生索引重放） | P1 |
| F-SP1-8 | 删除/收权的边级联 + WS 增量 + dangling 转换 | P0 |
| F-SP1-9 | RebuildIndex RPC（幂等、降级可用） | P1 |
| F-SP1-10 | 惰性锚点回填（被引用的未锚块自动补 `^uuid`） | P1 |
| F-SP1-11 | 跨 Collection 双链解析规则（同库优先 / 可见性过滤 / 歧义确定性取首标记 ambiguous） | P0 |

### SP1 非功能需求（增补）

| # | 需求 |
|---|------|
| NFR-SP1-1 | 解析管线为纯函数（tree→[]Row），单测可覆盖全部语法矩阵 |
| NFR-SP1-2 | refs 永无孤儿边（整文档重建语义）；索引可从源数据全量重放 |
| NFR-SP1-3 | 万级文档库的全量重建可后台流式执行，不阻塞读写 |
| NFR-SP1-4 | 统一索引内存占用与库规模线性（10 万边 < 100MB） |

### SP1 验收标准总览

| # | 验收标准 |
|---|----------|
| 38 | 四种双链语法 + 别名变体解析全部正确；嵌入与引用边类型区分 |
| 39 | 块级反链含上下文；文档反链为块反链聚合；dangling 灰显且目标创建后复活 |
| 40 | 团队库与个人库统一检索/图谱查询；跨模边在图谱中可见（按权限投影） |
| 41 | 晋升产生谱系对 + 审计；级联提示正确；私有外部引用占位显示 |
| 42 | 团队块删除后边级联清除、WS 增量到达、私有侧 dangling 化 |
| 43 | RebuildIndex 幂等；重建后 refs 无孤儿边；重建期检索降级可用 |
| 44 | 惰性锚点回填幂等；同一文件多次解析锚点稳定不漂移 |

### SP1 交互规格（用户视角）

- **书写**：编辑器外任何文本入口（含 agent knowledge_write）写入的四语法均被解析；dangling 在浏览视图灰显 + hover 提示「目标未创建」。
- **反链**：文档详情关联区新增「反向链接」分组（与既有显式/实体/语义分区并列），块级粒度。
- **晋升**：文档/块操作菜单「晋升到团队库…」→ 目标库选择 + 级联提示清单 → 确认执行 → 结果反馈（新建块数/谱系链接）。
- **团队库**：库选择器中 team 库带「团队」徽标；其余浏览/搜索/图谱交互与个人库一致。

### 子项目路线图（SP1~SP7，2026-08-08 评审扩展）

> G1~G8 前瞻提案的产品化映射（学术依据见[评审报告 §五](../reports/2026-08-08-review-sp1-knowledge-blueprint.md)）；各 SP 均须各自走 需求→设计→TDD 流程立项，本表仅为路线图锚点。

| # | 子项目 | 用户价值 | 来源提案 | 状态 |
|---|--------|---------|---------|------|
| SP1 | 知识内核（本子模块） | 块级双链/反链/双模统一索引/晋升/索引重建 | — | 📋 待启动 |
| SP2 | 编辑器与笔记体验 | Live Preview 双链书写、反链面板、大纲 | — | 待立项 |
| SP3 | 图谱 2.0 | 2D 日常 + 3D 惊艳双模式、四区控制台、局部图谱、**时间轴回放**（知识演化可视化） | G4 | 待立项 |
| SP4 | 多模态摄取管线 | 视频/音频/Office 入库可检索，时间戳回链原片 | G5 | 待立项 |
| SP5 | 个人/团队双模权限 | 片段级权限；知识成熟度光谱（草稿→共享→精炼→规范）；super note 综合晋升 | G3 | 待立项 |
| SP6 | AI 知识伙伴 | 写入即自动链接/标签建议；会话决策点主动唤回；摄入即综合（交叉引用 + 矛盾检测） | G6 | 待立项 |
| **SP7（新）** | **知识-记忆同基底 + 写回飞轮** | agent 记忆与人笔记同图共检（agent vault 投影）；会话/团队执行自动沉淀团队知识（验证门 + provenance）；「谁懂这个」专家定位；知识健康度仪表盘 | G1+G2+G7+G8 | G1/G2/G7/G8 已落地（US-37~US-44）；SP5 成熟度光谱 / SP6 JITAI 仍未做 |

---

## 子模块：知识库全面升级（V4 需求，2026-08-12）

> 设计详见 [37-knowledge.design.md §V12.9](./37-knowledge.design.md#v129-知识库全面升级m1m5--b1b22026-08-12)；任务计划见 [37-knowledge.development.md §子模块：知识库全面升级（V4）Phase 计划](./37-knowledge.development.md#子模块知识库全面升级v4phase-计划)。
> 方案档案：`docs/superpowers/plans/2026-08-12-knowledge-galaxy-liquid-glass.md`（已实施完成）。本升级为 G5「图谱深空版」的延续（前端轨道 M1~M5），并补齐两项能力缺口（B1 文档重嵌入、B2 集合语义层启用）；2 万节点/5 万边双布局性能基准已落档（验收 51，2026-08-13）。

### US-26：真折射玻璃浮层视觉（M1）

**作为** 知识库用户，
**我希望** 工作台浮层（全库搜索/命令面板/设置等）呈现真实的玻璃折射质感——背后内容透过玻璃产生细腻的扭曲折射，而不是简单模糊，
**从而** 深空工作台更具高级感与沉浸感，且在不支持该效果的浏览器上自动回退为原有模糊玻璃、不破版。

**验收标准**：
- 开启真折射的浮层对背后内容产生可见的折射扭曲；折射滤镜全应用单例管理，多浮层共存不重复定义。
- 支持真折射的浏览器启用折射；不支持时自动回退为原有模糊玻璃，视觉无破版、交互不受影响。
- 仅显式开启的浮层启用真折射，其余面板视觉不回归。

### US-27：星系盘布局与切换（M2 布局 + M3 镜头）

**作为** 知识库用户，
**我希望** 图谱可在「力导向」与「星系盘」两种布局间一键切换——星系盘布局下节点压平成盘、绕核心旋聚成银河状；切换过程节点平滑流动重组，首次进入图谱有电影感开场运镜，
**从而** 我能按浏览习惯选择结构视图，切换与开场本身具有仪式感。

**验收标准**：
- 图谱顶栏提供布局切换按钮（力导向/星系盘）；星系盘布局呈现核心凝聚 + 盘面压平 + 旋臂涡旋的银河形态，边呈曲线。
- 布局切换为连续平滑过渡（节点流动重组，不跳变、不瞬移）；选中的布局刷新后保持。
- 首次进入图谱播放开场运镜（节点依次显现）后进入待机；用户交互可随时打断运镜并接管镜头。

### US-28：聚焦模式与节点详情卡（M4）

**作为** 知识库用户，
**我希望** 单击节点可锁定聚焦其 N 跳邻域（其余节点压暗），并弹出该节点的信息卡，卡片上可直接「在浏览器打开」文档或「重新向量化」，
**从而** 在大图中专注于关心的局部，并就地完成常用操作。

**验收标准**：
- 单击节点锁定聚焦：N 跳邻域保持高亮、其余压暗；点击空白处解除聚焦。
- 聚焦时浮出节点详情卡（真折射玻璃质感）展示节点信息；卡片可拖动位置。
- 卡片提供「在浏览器打开」（跳转浏览该文档）与「重新向量化」（B1 入口②，见 US-30）按钮。
- 聚焦锁定的压暗优先级高于图例透镜与 hover（US-29），解除后恢复常态。

### US-29：图例过滤与透镜（M5）

**作为** 知识库用户，
**我希望** 图谱带一个按文档类型分组的图例（颜色点 + 组名 + 节点计数），点击图例组可隐藏/恢复整组节点，悬停图例组时画布中该组提亮、其余压暗（透镜效果），
**从而** 按类型快速聚焦或排除某类文档——图例即过滤器。

**验收标准**：
- 图例列出全部文档类型组（颜色与画布节点一致 + 组名 + 计数）；无类型的节点归入「未分类」组显示。
- 点击图例组隐藏该组节点（关联边级联隐藏），再次点击恢复；被隐藏的组在图例中置灰斜体标识。
- 全部组被隐藏时图例仍然保留（不消失），保证恢复路径；隐藏选择刷新后保持。
- 悬停图例组触发透镜：组内节点提亮、组外压暗，移出恢复；聚焦锁定（US-28）优先于透镜，透镜优先于 hover。
- 未隐藏任何组时不过滤（零开销），大图性能不回归。

### US-30：文档重嵌入（B1）

**作为** 知识库用户，
**我希望** 对向量缺失/失效的文档一键触发「重新向量化」——文件行菜单与图谱节点卡均有入口，确认后服务端重新分块并重建向量，
**从而** 因嵌入维度调整等原因失去向量的文档恢复语义检索能力，无需重新上传。

**验收标准**：
- 浏览视图文件行菜单提供「重新向量化」入口；词法库（未配置语义层）中该入口置灰并提示不可用原因。
- 触发前弹确认对话框说明影响；确认后给出受理通知（已受理 N 篇重嵌入），处理进度经既有摄取进度通道可见。
- 重嵌入基于已存正文重新分块并嵌入：正文为空或正在索引中的文档跳过并计数；Vault 文档由同步机制自愈、跳过。
- 图谱节点详情卡提供同一能力的第二入口（见 US-28）。

### US-31：集合语义层启用（B2）

**作为** 知识库用户，
**我希望** 对创建时未配置嵌入模型的词法库一键「启用语义检索」——绑定当前全局嵌入模型并为全部文档重建向量，
**从而** 词法库平滑升级为混合检索，无需重建知识库；该操作为单向启用。

**验收标准**：
- Vault 树根节点菜单对词法库显示「启用语义检索」入口；已有语义层的库不显示该入口。
- 确认对话框明确展示将绑定的嵌入模型名与维度，并说明「启用后词法检索自动升级为混合检索」。
- 确认后全部内容文档进入重嵌入队列，给出受理通知；集合列表自动刷新反映新状态。
- 已启用语义层的集合重复启用被拒绝（单向，不覆盖既有绑定）；未配置嵌入模型时给出明确错误提示。

### V4 功能需求清单（知识库全面升级）

| # | 需求 | 说明 |
|---|------|------|
| FR-V4-1 | 真折射玻璃 | 折射滤镜全应用单例集中管理；浮层按需显式开启；不支持的浏览器自动回退模糊玻璃 |
| FR-V4-2 | 双布局切换 | 力导向/星系盘双布局一键切换（星系盘 = 核心凝聚 + 盘面压平 + 旋臂涡旋 + 曲线边）；切换为连续平滑过渡；布局选择持久化 |
| FR-V4-3 | 电影感镜头 | 镜头行为由显式状态机管理（开场/飞行/环绕/巡航/待机）；开场运镜驱动节点依次显现；用户交互可打断接管 |
| FR-V4-4 | 聚焦与节点卡 | 单击锁定 N 跳聚焦压暗、空白解除；真折射节点详情卡（可拖动）含「在浏览器打开」与「重新向量化」入口 |
| FR-V4-5 | 图例过滤与透镜 | 按文档类型组图例（色点/组名/计数/未分类回退）；点击隐藏-恢复（边级联、置灰标识、全隐藏可恢复）；悬停透镜；隐藏选择持久化；空过滤零开销 |
| FR-V4-6 | 文档重嵌入 | 确认后服务端重新分块并重建向量、反馈受理/跳过篇数；文件行菜单与节点卡双入口；词法库入口置灰提示 |
| FR-V4-7 | 语义层单向启用 | 词法库一键绑定当前嵌入模型并全量重建向量（升级为混合检索）；入口仅词法库可见；确认展示模型名/维度；重复启用拒绝 |

### V4 验收标准（知识库全面升级，增补 45~51）

| # | 验收 |
|---|------|
| 45 | 浮层真折射可见且多浮层共存正常；不支持的浏览器回退模糊玻璃无破版（US-26） |
| 46 | 力导向/星系盘一键切换、过渡平滑连续，布局选择刷新后保持；开场运镜节点依次显现且可被交互打断（US-27） |
| 47 | 单击锁定聚焦 + 节点卡「在浏览器打开」/「重新向量化」可用；空白解除聚焦；聚焦优先于透镜与 hover（US-28） |
| 48 | 图例点击隐藏/恢复整组（含「未分类」组），边级联隐藏；全组隐藏时图例保留可恢复；悬停透镜提亮组内、压暗组外（US-29） |
| 49 | 「重新向量化」双入口可用：确认后受理通知篇数正确，文档向量重建完成后恢复语义检索（US-30） |
| 50 | 词法库显示「启用语义检索」、语义库不显示；确认后全部文档进入重嵌入队列并升级为混合检索（US-31） |
| 51 | 2 万节点/5 万边合成数据集双布局（力导向/星系盘）帧率基准录制并落档测试文档（✅ 已完成 2026-08-13，见 [perf-2026-08-12-graph3d-dual-layout.md](../testing/reports/perf-2026-08-12-graph3d-dual-layout.md)） |

---

## 子模块：Lazy GraphRAG + Retrieve-Then-Generate（2026-08-15）

> 设计：[37-knowledge.design.md §V12.10](./37-knowledge.design.md#v1210-lazy-graphrag--retrieve-then-generate2026-08-15)；计划：[37-knowledge.development.md](./37-knowledge.development.md#子模块lazy-graphrag--retrieve-then-generate2026-08-15)。
> 背景：3D 星河/双链/实体边只服务人看图谱，检索与 Agent 注入仍是扁平 chunk；所谓 Plan-Then-Retrieve 实际只注入库名目录。本子模块把**已有** `knowledge_links` 接到召回，并在首轮模型调用真正预检索。

### US-32：查询时图扩展（Lazy GraphRAG）

**作为** 用户/Agent，
**我希望** 语义检索在命中文档后自动带上双链/实体一跳邻居的内容，
**从而** 「A 和 B 是什么关系」这类多跳问题不必只靠偶然同 chunk。

**验收标准**：
- 混合检索种子文档沿 `knowledge_links`（explicit > entity > semantic）扩展最多 8 个邻居，每邻居取前 2 个 chunk，与种子 RRF/分数合并后截断 TopK。
- 即时定位查询（路径/扩展名/引号短语）不扩展，避免污染精确命中。
- 关联读取失败时降级为原始种子，不报错、不阻塞检索。
- 不新建实体表、不改摄取主链路（旁路纪律与 §9.6 一致）。

### US-33：对话首轮真正注入检索片段

**作为** 用户，
**我希望** 打开对话问知识库里的事实时，助手第一轮就能引用相关段落，
**从而** 不必赌模型是否记得调用 `knowledge_search`。

**验收标准**：
- BeforeModel 钩子在最新非 system 消息为 user 时，对当前问题做一次限时（2s）预检索（top 4），将段落写入 `## Retrieved Knowledge`，并保留库目录供补充搜索。
- 工具循环续跑（最新消息为 assistant/tool）跳过预检索，避免每轮重复检索。
- 检索失败或无检索器时回退为仅库目录 cue（既有行为）。

### 功能需求清单

| # | 需求 | 说明 |
|---|------|------|
| FR-LG-1 | 一跳图扩展 | AdaptiveRouter 检索后可选 GraphExpander；Search API / knowledge_search / knowledge_reflect / 联邦逐库路径自动获得 |
| FR-LG-2 | 复杂查询自动 MultiQuery | 调用方未指定 rewrite_strategy 且复杂度=complex 时自动重写；简单/中等查询不加 LLM 往返 |
| FR-LG-3 | Retrieve-Then-Generate | knowledge cue 从「库名目录」升级为「预检索段落 + 短目录」 |

---

## 子模块：编译期 Wiki 成链（2026-08-15）

> 设计：[37-knowledge.design.md §V12.11](./37-knowledge.design.md#v1211-编译期-wiki-成链2026-08-15)；计划：[37-knowledge.development.md](./37-knowledge.development.md#子模块编译期-wiki-成链2026-08-15)。
> 背景：查询期 Lazy GraphRAG 只能沿着已有 `knowledge_links` 走。Obsidian 核心把未链接提及当展示，图谱仍然稀疏；本子模块在**写入时**把同库标题的纯文本提及编译为 `[[wikilink]]`，让 explicit 边在索引期就长出来。

### US-34：写入时把未链接提及编成双链

**作为** 用户，
**我希望** 上传或保存笔记时，正文里提到的其它笔记名自动变成 `[[双链]]`，
**从而** 星河图、反链和检索一跳扩展不依赖我手工每处加括号。

**验收标准**：
- 摄取（含图片视觉提取后的 Markdown）与 Vault 编辑保存会扫描同库文档标题，将未链接提及包成 `[[Title]]`。
- 跳过 YAML frontmatter、围栏/行内代码、已有 wikilink、Markdown 链接与裸 URL。
- 英文整词匹配（最短 3 字符）；中文最短 2 字；同名歧义与指向自己的标题不链。
- 更长标题优先于短标题（「通信协议」优于「协议」）。
- 列出标题失败时保持原文，不阻断入库/保存。
- 不新增 RPC（无确认弹窗）；幂等（已是 `[[Title]]` 的不再套一层）。

### 功能需求清单

| # | 需求 | 说明 |
|---|------|------|
| FR-AL-1 | 纯函数成链 | `AutolinkWikiMentions` 可单测、无 IO |
| FR-AL-2 | 摄取编译 | `IngestDocument` 在整理后、索引前成链；vault 内 `.md` 落盘与索引正文一致 |
| FR-AL-3 | 保存编译 | `UpdateVaultDocumentContent` 写盘前成链，随后 `ApplyOne` 重建块索引 |

---

## 子模块：会话写回飞轮（2026-08-15）

> 设计：[37-knowledge.design.md §V12.12](./37-knowledge.design.md#v1212-会话写回飞轮sp7-g22026-08-15)；计划：[37-knowledge.development.md](./37-knowledge.development.md#子模块会话写回飞轮2026-08-15)。
> 背景：开源 PKM 无法把 Agent 会话变成团队知识。SP7 G2 用已有自动记忆提取，经更严验证门写入团队库日记，每条带 session/agent/fact provenance。

### US-37：过门事实自动沉淀到团队库

**作为** 团队成员，
**我希望** 对话里被记住的高置信偏好/约束/决策自动出现在团队知识库，
**从而** 不必手工把会话结论再贴进笔记，且能追溯是哪次会话写的。

**验收标准**：
- AutoMemory 成功写入 L3 后，仅 `preference/profile/goal/constraint/decision/relationship` 且置信度 ≥ 0.85、陈述 ≥ 8 字的事实进入写回。
- 追加到团队库当日日记 `inbox/writeback-YYYY-MM-DD.md`（优先「团队知识收件箱」，否则复用已有 team 库；没有则懒创建词法团队库）。
- 每条含 `fact_id` / `session_id` / `agent_id` / `confidence` / `source`；同一 `fact_id` 不重复追加。
- 写回失败不阻断自动记忆主流程。
- 不新增 RPC。

### 功能需求清单

| # | 需求 | 说明 |
|---|------|------|
| FR-WB-1 | 验证门 | 比 L3 门（0.6）更严，推测/流水账不入库 |
| FR-WB-2 | 日记 + provenance | UTC 日切分 Markdown，块级可检索 |
| FR-WB-3 | 派生索引 | 成链 + RebuildBlockIndex + chunk/FTS 重放 |

---

## 子模块：成链回填、金标与 SP7 收口（2026-08-15）

> 设计：[37-knowledge.design.md §V12.13](./37-knowledge.design.md#v1213-成链回填金标与-sp7-收口2026-08-15)；计划：[37-knowledge.development.md](./37-knowledge.development.md#子模块成链回填金标与-sp7-收口2026-08-15)。

### US-38：存量出链回填（显式命令，不挂在重建上）

**作为** 库管理员，
**我希望** 用一条单独的命令把旧笔记里未加括号的标题提及编成双链，
**从而** 存量库也能被一跳检索走到；日常「重建索引」只修 blocks/refs，不改文件。

**验收标准**：
- `POST /v1/knowledge/collections/{id}/autolink-backfill` 先 `BackfillOutgoingAutolinks` 再重建块索引；与重建共用同库互斥门。
- `RebuildKnowledgeIndex` **不**调用回填（US-29 契约：不改源文本）。
- 命令面板有确认文案，写明会改写 Markdown。
- 外部编辑器 / watcher 路径不成链。

### US-39：确认后编译本页双链

**作为** 笔记作者，
**我希望** 先看到本页会链上几处，再决定是否写入，
**从而** 歧义或误伤标题不会被静默改掉（ingest/保存仍自动成链）。

**验收标准**：
- 预览与确认走 custom HTTP（非 Proto）：`GET .../autolink-preview`、`POST .../autolink`。
- 命令面板与反链面板有入口；0 处替换时不写盘。

### US-40：检索金标证明成链提升一跳召回

**作为** 开发者，
**我希望** 有一组中英查询能在 CI 里失败当「成链+一跳」回归，
**从而** 重构 GraphExpander 时不会把邻文档事实丢回去。

**验收标准**：
- `go test ./internal/knowledge/ -run TestGoldRecall`：12 条查询在仅种子命中下不含邻文档事实，autolink 后一跳命中。
- 不声称 50 条 BM25 对真实 PG 的回归，不声称 Agentic RAG。

### US-41：Agent 记忆投影为只读笔记

**作为** 团队成员，
**我希望** 每个 Agent 的活动记忆能在团队库看到一篇 `agents/{id}.md`，
**从而** 人和检索能读到同一份投影，而不是只在记忆中心。

**验收标准**：
- AutoMemory 之后覆盖写投影（不追加）；文首标明只读。
- 独立 `AgentMemoryProjector`，不往 Knowledge `Usecase` 堆字段。
- 失败不阻断记忆提取。

### US-42：谁懂这个

**作为** 团队成员，
**我希望** 按写回 provenance 看到哪些 Agent/用户沉淀过该库事实，
**从而** 找人请教不必翻会话记录。

**验收标准**：
- `GET /v1/knowledge/collections/{id}/experts` 聚合 `inbox/writeback-*` 与 `agents/*.md` 的 agent_id/user_id。
- 命令面板展示前若干条。

### US-43：知识健康度

**作为** 库管理员，
**我希望** 一眼看到孤立率、边密度、悬空链和写回日记是否还在更新，
**从而** 知道图是不是空壳。

**验收标准**：
- `GET /v1/knowledge/collections/{id}/health` 来自 `ListCollectionGraph` + `ListDanglingLinks` + 写回路径扫描。
- 读路径不写 `meta/health.md`。

### US-44：低置信写回人工过门

**作为** 团队成员，
**我希望** 0.60–0.84 的白名单事实先进入待确认列表，我点头后再进当日日记，
**从而** 自动沉淀保持高门，猜测仍可被我捞回来。

**验收标准**：
- pending 文件 `inbox/writeback-pending.md`；确认走 `POST .../writeback-pending/apply`（确认即门，不再套 0.85）。
- 工作台按条勾选后提交 `fact_ids`；空列表不写盘。失败不阻断 AutoMemory。
- 专家 / pending 默认打到写回落点（US-46），不是当前打开的个人库。

### US-45：重建索引与成链回填解耦

**作为** 库管理员，
**我希望** 「重建索引」不再悄悄改我的笔记，
**从而** 误点重建不会把全库标题提及包成双链。

**验收标准**：
- 同 US-38 解耦后的验收；重建 RPC 与回填 POST 分路。
- 同库已有重建/回填在途时回 409。

### US-46：专家与待确认写回指向写回落点

**作为** 团队成员，
**我希望** 「谁懂这个」和「审核待写回」查的是团队收件箱，而不是我正在看的个人库，
**从而** 空列表时知道该去哪，而不是以为功能坏了。

**验收标准**：
- `GET /v1/knowledge/writeback-home` 只解析不创建：同名「团队知识收件箱」优先，否则第一个 team 库。
- 当前库不是落点时提示来源，并提供切换。
- 健康度仍统计当前打开的库；若写回在别处，附带提示。

### US-47：生产词法路径中英金标

**作为** 开发者，
**我希望** 一组中英查询打到真实的 `SearchChunksBM25`（Postgres tsvector + trigram），
**从而** 词法降级路径回归不只靠假 GraphExpander。

**验收标准**：
- `go test ./internal/data/ -run TestKnowledgeRepo_SearchChunksBM25_GoldBilingual`：12 条查询命中预期文档，负例不误中。
- 需要 `aranea_test` Postgres；**不**声称 50 条、不声称混合检索 / Agentic RAG。
- US-40 的一跳金标保持独立（不连 PG）。

### US-48：待确认写回逐条勾选

**作为** 团队成员，
**我希望** 对待确认事实逐条勾选后再写入日记，
**从而** 不必把一整批猜测一次性过门。

**验收标准**：
- `WritebackReviewDialog` 仅 props/emits；提交 `fact_ids` 由工作台调已有 apply API。
- 默认全选；零选禁用提交。

### US-49：知识检索与派生索引自动保持可用

**作为** 长期使用知识库的 Agent，
**我希望** 精确术语、自然语言问题和刚入库的文档都能快速、稳定地被召回，
**从而** 不需要人持续点击重建或等待后台偶然修好。

**验收标准**：
- 混合检索同时执行语义与词法召回，融合后再执行已配置的重排；任一路失败时另一条可独立返回。
- 路径、引号短语、错误码和短标识符优先走词法检索；两字中文正文词可被生产词法路径召回。
- 数据库上传文档完成索引后自动进入实体/关系抽取，不长期成为图谱孤立节点。
- 关系抽取只有在 evidence 可回指原文时才发布；文档关联更新关闭旧边并保留历史，不物理删除。
- 源正文已存在但 chunks/embedding 缺失时，系统在 5 分钟维护周期内自动尝试修复；词法库的空 embedding 不得被误判为损坏。
- 重复查询的 query embedding 可短时复用，命中记账不得增加检索返回延迟。

### US-50：检索质量可量化且事实演化不丢身份

**作为** 长期维护知识库的管理员，
**我希望** 每次检索改动都有固定真实问法门禁，事实更新与冲突处置保留稳定演化身份，
**从而** 系统不会在“测试通过”但召回退化、或冲突被伪标为已处理的状态下长期积累错误。

**验收标准**：
- 提供确定性的文档级 Recall@K、HitRate@K、MRR、nDCG@K 与 Abstention Accuracy 计算器；同一文档多个 chunk 不得重复计分。
- 现有 5 篇运维语料上，词法门禁用金标关键词与标识符查询：关键词 HitRate@5 ≥ 0.90，标识符 HitRate@5 ≥ 0.80。自然语言问法的词法硬门见 US-51。需要隔离的 `aranea_test` Postgres。
- `go test -race ./internal/knowledge` 全包通过，联邦广播与 Vault 后台同步测试不得用无锁夹具制造假竞态。
- LLM 判定 `supersedes` 时保留目标 `fact_id` 作为稳定演化身份，新的提取 ID 仅作为 provenance；版本链继续记录旧、新正文。
- 事实段冲突提案带稳定 `dedup_key`，pending/applied/rejected 均抑制重复骚扰。
- 事实段级 conflict 必须用 `keep_old` 或 `keep_new` 明确选择保留版本；裸 `applied` 不得静默改词条。`keep_old` 删除新段；`keep_new` 删除旧段并把新段 `fact_id` 收敛到旧 lineage。图谱文档级 conflict 仍按原契约 `applied` 关闭 `contradicts` 边。
- 30 条真实问法经 10 种前缀改写构成 ≥300 条鲁棒性查询，外加标识符切片与拒答切片；改写集不是 300 条彼此独立的新事实。

### US-51：自然语言问句可被词法路径召回

**作为** 用完整问句检索知识库的运维人员或 Agent，
**我希望** 中文问句在没有 embedding 时也能命中出处文档，
**从而** 词法库和语义降级路径对真实问法可用，而不是只对关键词有效。

**验收标准**：
- 检索前确定性去掉问句套话（请问/是什么/多久/如何等），抽取 3–8 字内容针与标识符，与原查询 RRF 融合；不调用 LLM。
- 短关键词、工单号、设备名等非问句查询不走针扩展，避免 n-gram 稀释排序。
- 现有 5 篇运维语料上，30 条自然语言问法 BM25 HitRate@5 ≥ 0.90；负例「不存在的词语xx」不得误中。
- 关键词 HitRate@5 ≥ 0.90、标识符 HitRate@5 ≥ 0.80 不回归。

### US-52：工作台可处置事实冲突与治理提案

**作为** 知识库管理员，
**我希望** 在工作台命令面板里审核待处理的治理提案，并对事实冲突明确选择保留旧陈述或采用新陈述，
**从而** 不必绕到 HTTP 或记忆管家工具才能完成二审，也不会误点「确认」静默改词条。

**验收标准**：
- 命令面板提供「审核治理提案」；列出当前库 pending 提案，当前库为空时回退写回收件箱并提示切换。
- 事实段 conflict 只提供「保留旧陈述 / 采用新陈述 / 驳回」，不提供裸 applied。
- 文档级 conflict 提供关闭矛盾关联 / 驳回；孤儿词条 applied 明确标注为删除。
- Dialog 只 props/emits，由工作台调已有 ResolveGovernanceProposal。

### US-53：问句自动走混合检索

**作为** 用自然语言向 Agent 提问的运维人员，
**我希望** 完整问句在开启语义层时同时走词法与向量，
**从而** US-51 的内容针能进入生产预检索，而不是只在纯 BM25 测试里命中。

**验收标准**：
- 疑问句（中英问号、疑问词开头）与长中文陈述的自动路由为 RRF，不因问句把复杂度抬到 Complex（不默认 LLM MultiQuery）。
- 路径、引号短语、工单号/错误码仍走 sparse。
- 短关键词（如「低压电工证」）保持原 dense 默认；无语义层时仍降级 BM25。

### US-54：治理定时实跑且入库即自关联

**作为** 长期使用知识库的管理员，
**我希望** 边权衰减和谓词提升自动发生、新入库文档自动进入图谱，未链接提及也能变成检索用的关联，
**从而** 不必等记忆管家梦境循环，也不必对每篇笔记手动编译双链或等文档变热才抽关系。

**验收标准**：
- `KnowledgeCurateWorker` 每 6 小时对全部团队库执行 `CurateAllTeamKnowledge(DryRun=false)`：decay / promote / stale / distill 实写；conflict / orphan / moc 只产 pending 提案。删除与事实冲突仍走工作台 `keep_old`/`keep_new`。无团队库时静默跳过。可用 `KNOWLEDGE_CURATE_DISABLED` 关闭。
- Vault 同步索引成功后触发 typed 关系抽取（与实体钩子同生命周期、content_hash 幂等），冷文档不再只靠热度工人。
- `RebuildBlockIndex` 把未链接提及编译成 wikilink 再物化 explicit 边；不回写源文件或 `content_text`。歧义键仍跳过。显式「编译本页 / 回填」仍是改 Markdown 的唯一写路径。

### 功能需求清单

| # | 需求 | 说明 |
|---|------|------|
| FR-SP7-1 | 历史回填 | 显式回填命令改 Markdown；索引重建不改源文本（可在内存中编译提及以建边） |
| FR-SP7-2 | 确认成链 | custom HTTP + 工作台确认 |
| FR-SP7-3 | 金标 | 一跳内嵌语料 + 生产 BM25 PG 金标（US-47） |
| FR-SP7-4 | G1 投影 | 覆盖写 `agents/{id}.md` |
| FR-SP7-5 | G7/G8 | 专家聚合 + 健康度只读；专家打写回落点 |
| FR-SP7-6 | 待确认写回 | 0.60–0.84 白名单人工过门，逐条勾选 |
| FR-SP7-7 | 写回落点 | `writeback-home` 只读解析，GET 不造库 |
| FR-SP7-8 | 核心可靠性 | RRF 后重排、并行召回、精确词分流、摄取图谱闭环、索引自动修复 |
| FR-SP7-9 | 质量与演化门禁 | 30 条真实问法 BM25 门禁（含自然语言问句）、300 条改写鲁棒性金标、标准 IR 指标、Race 全绿、事实 lineage 与 keep_old/keep_new |
| FR-SP7-10 | 问句词法规范化 | 无 LLM 的中文问句压缩 + 内容针 BM25，接入生产 `SearchChunksBM25` |
| FR-SP7-11 | 治理提案工作台 | 命令面板审核 pending 提案；事实冲突 keep_old/keep_new 按钮 |
| FR-SP7-12 | 问句混合路由 | 疑问句自动 RRF，精确词仍 sparse，不默认 MultiQuery |
| FR-SP7-13 | 自治与入库自关联 | 治理 6h 实跑；vault 冷文档抽关系；索引期成链不改源 |


