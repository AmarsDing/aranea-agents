# Memory 记忆系统 -- 实施进度

> 最后更新：2026-07-16
>
> **路径说明**：原 `internal/data/sessionmemory/` 已折叠为 `internal/data/memory_shim_*.go` + `memory_helpers.go` + `memory_chain_schema.go`。下表部分历史行仍写旧路径时，以 shim 文件名为准。

## 总览

| 层级 | 需求 | 设计 | 基础设施 | 核心功能 | 前端 | 整体进度 |
|------|------|------|----------|----------|------|----------|
| **Memory 整体** | ✅ | ✅ | ✅ | ✅ | ✅ | 90% |
| **L0 瞬时记忆** | ✅ | ✅ | ✅ | ✅ | ✅ | 100% |
| **L0 压缩优化** | ✅ | ✅ | ✅ | 阶段一 ✅ | ✅ | 阶段一 100% / 阶段二三 0% |
| **L1 工作记忆** | ✅ | ✅ | ✅ | ✅ | 🟡 | 85% |
| **L2 会话事件** | ✅ | ✅ | ✅ | ✅ | 🟡 | 90% |
| **L3 语义知识** | ✅ | ✅ | ✅ | ✅ | 🟡 | 90% |
| **L4 持久进化** | ✅ | ✅ | ✅ | ✅ | ✅ | 85% |
| **MemoryWorker** | ✅ | ✅ | ✅ | ✅ | N/A | 95% |
| **MEM-OPT 优化** | ✅ | ✅ | ✅ | ✅ | N/A | 100% |
| **Neural Memory** | ✅ | ✅ | ❌ 专用模块 | 旁路部分能力见 Phase6/编排 | ❌ | 5%（计划未启动；勿与旁路落地混淆） |

---

## L0 瞬时记忆

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| L0 组装观测 | `internal/biz/l0_assembly_snapshot.go` | ShouldWriteL0AssemblySnapshot + 警告码推导 |
| 快照持久化 | `internal/data/sessionmemory/store_l0_snapshot.go` | InsertL0AssemblySnapshot + UpdateL0SnapshotActual + GetL0SnapshotRow |
| Runner 集成 | `internal/agent/l0_snapshot_persist.go` | persistL0AssemblySnapshot + newL0SnapshotAfterModelHook |
| Prompt 分析 | `internal/agent/prompt_snapshot.go` | segment 分类、section 统计 |
| Service API | `internal/service/memory_decode.go` / `memory.go` | L0 快照列表 + 单条查询 API |
| GetL0Snapshot RPC | `api/kratos/memory/v1/memory.proto` | GET /v1/sessions/{session_id}/l0/snapshots/{id} |
| Prompt Preview UI | `web/src/features/memory/MemorySnapshotDrawer.vue` | L0 快照段落展开面板，显示 section/tokens/preview |
| Agent Prompt Preview | `web/src/features/agents/useAgentPromptPreview.ts` | Agent 级别 prompt 预览对话框 |

### 已完成 ✅（2026-07-11 验证）

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| Team 多 Agent L0 隔离 | `internal/data/memory_shim_l0.go:29` | `ListL0SnapshotRows(ctx, sessionID, agentID, limit)` 已支持 agent_id 过滤；service 层 `internal/service/memory.go:80` 传递 `req.GetAgentId()` |

---

## L0 上下文压缩优化

### 阶段一（P0 工程补强）-- ✅ 100%

| 功能 | 代码位置 | 状态 |
|------|----------|------|
| 工具结果持久化 | `internal/data/ent/schema/tool_result_blob.go`, `internal/data/tool_result_repo.go`, `internal/biz/tool_result_gate.go` | ✅ |
| L1 MicroCompact | `internal/session/micro_compact.go` | ✅ |
| L2 Memory Compact | `internal/session/memory_compact.go` | ✅ |
| L3 AutoCompact 升级 | `internal/session/compressor.go` | ✅ |
| 9 章节摘要 | `internal/session/compressor.go` | ✅ |
| LLM 压缩响应缓存 | `internal/compress/cache.go` (CompressCache + CachingCompressor) | ✅ |
| 手动压缩 API | CompactSession RPC | ✅ |
| CAS+事务 | `internal/session/compressor.go` | ✅ |
| 四种截断策略 | drop_oldest/hybrid 等 | ✅ |

### 阶段二（P2 记忆演化）-- ❌ 0%

| 功能 | 状态 |
|------|------|
| 记忆操作语义化 (ADD/UPDATE/DELETE/MERGE/NOOP) | ❌ |
| 时间维度 (static/dynamic/episodic) | ❌ |
| 动态链接 (link_ids + embedding 检索) | ❌ |

### 阶段三（P3 Agent 自主压缩）-- ❌ 0%

| 功能 | 状态 |
|------|------|
| CompactContext 工具 | ❌ |
| RecallDetail 工具 | ❌ |
| 代码骨架提取 (tree-sitter) | ❌ |

---

## L1 工作记忆

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| 数据表 | `memory_l1_tasks` / `memory_l1_fields` / `memory_l1_field_history` | SQLite 持久化 |
| Admin List API | `internal/biz/memory_admin_store.go` | ListL1TaskRows / ListL1FieldRows |
| Prompt 注入 | `internal/agent/l1_prompt.go` (L1MemoryCue) | 从 L1 task/field 读取并注入 prompt |
| 工具 Catalog 注册 | `internal/data/builtin_tools_seed.go` | 5 个 working_memory 工具种子数据 |
| 工具运行时识别 | `internal/biz/tool/tool_catalog_runtime.go` | working_memory.* 前缀识别为 RuntimeStatusAvailable |
| shared_with 传输层 | Schema + Store + Proto + Frontend | DB→Store→Service→Proto→Frontend 管道已打通 |
| working_memory 工具 | `internal/tools/working_memory/tools.go` | 5 个工具 (read/list/write/patch/delete) 完整执行链路 |
| L1 写入 Store | `internal/data/sessionmemory/store_l1.go` | StartL1Task / EndL1Task / UpsertL1Field / DeleteL1Field / PatchL1Fields / ArchiveL1Task |
| L1 Writer Biz | `internal/biz/memory_admin_store.go` | L1Writer 接口 + L1TaskInsert / L1FieldInsert DTO |
| Episode 归档 hook | `internal/biz/memory_admin_usecase.go` | EndL1Task 自动归档 + 创建 L2 Episode |
| L1 归档 Worker | `internal/cronrunner/jobs/memory_l1_archive.go` | 定时扫描空闲 L1 任务，自动归档 |
| Context 注入 | `internal/agent/working_memory_inject.go` | BeforeToolHook 注入 L1Writer/L1Reader/sessionID/agentID |

### 进行中 🟡

| 功能 | 说明 | 待办 |
|------|------|------|
| Team shared_with 业务逻辑 | 传输层管道已打通，但无写入/权限验证/跨 agent 共享逻辑 | 需实现共享逻辑 |

### 未开始 ❌

| 功能 | 说明 |
|------|------|
| field history UI | 字段版本历史前端展示，revision 字段存在但无 UI |

---

## L2 会话事件

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| Episode 写入 | `internal/data/sessionmemory/store_episodes.go` | InsertEpisodeRow（支持 pending/consolidated 状态） |
| 多策略 Recall | `internal/data/sessionmemory/store_l2_recall.go` | keyword + vector + importance + session boost 融合 |
| Embedding 索引 | `internal/data/sessionmemory/store_episodes.go` | UpsertEpisodeEmbedding |
| Decay Worker | `internal/cronrunner/jobs/memory_l2_decay.go` | 定期 importance 衰减 + retention purge |
| Prompt 注入 | `internal/agent/l2_prompt.go` (L2MemoryCue) | session 优先策略 |
| 融合召回 | `internal/agent/composite_prompt.go` (CompositeMemoryCue) | L2+L3 融合 |
| L2 Recall Usecase | `internal/biz/memory_l2_recall.go` | 业务层编排 |
| Retention 策略 | `internal/cronrunner/jobs/memory_l2_decay.go` | L2RetentionDays 配置 + PurgeEpisodesOlderThan |
| ConsolidateEpisode Worker | `internal/cronrunner/jobs/memory_l2_consolidate.go` | pending→consolidated 异步状态机 |
| Episode pending 状态 | `internal/data/sessionmemory/store_episodes.go` | ConsolidationStatus 字段，AutoMemoryWorker 显式设 consolidated |

### 已废弃 ⚠️（2026-07-11 验证）

| 功能 | 说明 | 原因 |
|------|------|------|
| ~~ListEvents 基础~~ | ~~`internal/service/event.go` + `internal/biz/event_store.go`~~ | event_store 表已在 ADR-03 迁移中 DROP（`20260901_drop_event_store_subsystem.sql`），EventService 已删除。activities 表为新的真相源，通过 ListActivities RPC 查询 |
| ~~ListEvents 跨表 UNION ALL 视图~~ | ~~设计要求跨表 UNION ALL 统一视图~~ | 设计已过时。event_store 表不存在，原"跨表 UNION ALL"需求需重新定义为"activities + episodes 统一时间线视图" |

### 未开始 ❌

| 功能 | 说明 |
|------|------|
| 异步 BM25 索引 | BM25 仅在 Knowledge 模块，Memory recall 用简单 token overlap |

---

## L3 语义知识

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| Fact CRUD | `internal/data/sessionmemory/store_facts_ops.go` | Upsert/Delete/Clear/List |
| 融合召回 | `internal/biz/memory_l3_fused_recall.go` | 跨 scope 融合 (agent+workspace+global) |
| Embedding 索引 | `internal/data/sessionmemory/store_fact_embedding.go` | UpsertFactEmbedding |
| 索引同步 (MEM-OPT-01) | `internal/data/memory_fact_index_sync.go` | stale/fresh/disabled 状态机 + reconciler |
| Legacy 回填 | `internal/data/sessionmemory/store_legacy_backfill.go` | BackfillLegacyTRPCMemoryEntities |
| Decay | `internal/data/sessionmemory/store_l3_decay.go` + `internal/cronrunner/jobs/memory_l3_decay.go` | per-agent importance 衰减 |
| Cascade 联动 | `internal/data/sessionmemory/store_cascade_facts.go` | L4 改名时自动替换 fact |
| Prompt 注入 | `internal/agent/l3_prompt.go` (L3MemoryCue) | 独立 + 融合两种模式 |
| Dead Letter | `internal/data/memory_job_deadletter.go` | 失败任务记录 |
| 批量写入 | `internal/data/sessionmemory/store_consolidate_batch.go` | 事务写入 facts + episode |
| Facts 唯一写路径 | `internal/data/sessionmemory/store_writes.go` | 统一通过 UpsertFactRow 写入，ON CONFLICT 幂等 |
| pgvector 索引同步 | `internal/data/memory_fact_index_sync.go` + `internal/cronrunner/jobs/memory_fact_index_reconciler.go` | 完整 sync + stale/fresh 状态 + 6h reconciler |
| 冲突检测 | `internal/biz/memory_admin_usecase.go` | DetectFactConflicts 启发式否定词检测 + IncrementConflictCount |
| 冲突 API | `api/kratos/memory/v1/memory.proto` + `internal/service/memory.go` | ListConflictingFacts RPC (GET /v1/memory/l3/facts/conflicts) — Service 层已实现 |
| quality_score 5维评分 | `internal/data/memory_shim_l3.go` | keyword(0.25) + vector(0.30) + importance(0.20) + recency(0.15) + quality(0.10) |
| PII 检测 (MEM-OPT-04) | `internal/biz/memory_pii.go` + `internal/service/memory.go` | 9 种 PII 检测器 + block/redact/review 策略 + ReviewPIIFact API — Service 层已实现 |
| 提取协议结构化 (MEM-OPT-05) | `internal/biz/memory_consolidator.go` + `memory_consolidator_llm.go` | function call schema + quality_score |

### 未开始 ❌

| 功能 | 说明 | 优先级 |
|------|------|--------|
| pgvector HNSW 索引 | memory_facts 使用双写：SQLite blob（主存储，暴力搜索阈值 5000）+ pgvector（可选）。HNSW 索引仅惠及启用 pgvector 的部署。knowledge_chunks 表已有 ivfflat 索引参考 | P2 |
| Conflict UI | 冲突检测前端展示 | P2 |
| rerank (P3) | Cross-encoder 重排序 | P3 |

---

## L4 持久进化

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| 知识图谱存储 | `internal/data/sessionmemory/entity.go` + `relation.go` | memory_entities + memory_relations |
| L4GraphUsecase | `internal/biz/memory_l4_usecase.go` | WriteFromUserText (英文+中文 regex) + RunDecayWithConfig |
| Cascade Saga (MEM-OPT-06) | `internal/biz/memory_l4_cascade.go` | 4 步 Saga + 补偿回滚 + Dry-Run |
| Cascade 持久化 | `internal/data/sessionmemory/store_cascade.go` + `store_cascade_saga.go` | Proposal + Saga Steps |
| Business Decay | `internal/data/sessionmemory/entity_lookup.go` | 指数衰减 + reinforcement + 归档 |
| Decay Worker | `internal/cronrunner/jobs/memory_l4_decay.go` | 定期衰减 |
| Neighborhood 查询 | `internal/data/sessionmemory/store_more.go` | 图遍历 |
| Prompt 注入 | `internal/agent/l4_prompt.go` (L4MemoryCue) | entity + neighborhood JSON |
| Evolution Metrics API | `internal/service/agent_evolution.go` | tool success rate / retrieval quality |
| Name Conflict 检测 | `internal/biz/memory_l4_usecase.go` | 同 scope person 冲突 gate |
| 中文 regex | `internal/biz/memory_l4_usecase.go` | 我叫/我的名字是/我是 + 我喜欢/我偏好/我偏爱 等 |

### 已完成 ✅（2026-07-11 验证）

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| Graph Tab | `web/src/pages/MemoryCenterPage.vue:101` + `web/src/features/memory/MemoryGraphExplorer.vue` | 前端图谱展示已集成到 Memory Center 页面"图谱与进化" Tab，无 feature flag，生产就绪 |

### 未开始 ❌

| 功能 | 说明 |
|------|------|
| LLM 实体抽取 (P2) | LLM 提取仅在 L3 consolidation 路径，L4 无 LLM 实体/关系抽取 |
| bi-temporal 边 (P2) | valid_from/valid_to 时间维度 |
| Evolution 审核 UI | 进化提议审批前端 |
| Identity/Strategy 全链路 | Agent 身份和策略完整链路 |

---

## Neural Memory 神经记忆系统

### 整体状态：❌ 未启动

所有 48 项任务（NM-01 至 NM-48）均未开始。

| Phase | 任务范围 | 状态 |
|-------|----------|------|
| Phase 1 (NM-01~15) | 时间感知 + 关联链接 | ❌ |
| Phase 2 (NM-16~33) | 联动更新 + 仿生生命周期 | ❌ |
| Phase 3 (NM-34~48) | Memory-Agent + 自主编辑 | ❌ |

---

## MemoryWorker

### 已完成 ✅

| 功能 | 代码位置 | 说明 |
|------|----------|------|
| TurnMemoryWorker | `internal/biz/memory_worker.go` | OnRunnerCompletion + OnUserFeedback |
| AutoMemoryWorker | `internal/cronrunner/jobs/auto_memory.go` | 核心后台 worker，3 次指数退避重试 |
| AutoMemoryQueue | `internal/memory/trpc/auto_memory_queue.go` | 基于 channel 的内存队列 |
| Consolidator 链 | `internal/biz/memory_consolidator.go` + `memory_consolidator_llm.go` | LLM → heuristic 降级链 |
| FeedbackConsolidator | `internal/biz/memory_consolidator.go` | 反馈偏好提取 |
| Dead Letter | `internal/data/memory_job_deadletter.go` | 失败任务记录 |
| 全局统计 | `internal/biz/memory_worker_stats.go` | job done/dead/LLM fallback 计数器 |
| EventBus 消费者 | `internal/biz/event_bus_side_consumers.go` | 触发 auto-memory 入队 |

---

## 优化项 (MEM-OPT)

| 优化项 | 状态 | 说明 |
|--------|------|------|
| MEM-OPT-01: L3 双轨读一致性 | ✅ | index_status 四状态 + reconciler |
| MEM-OPT-02: L4 实体衰减全局调度 | ✅ | L4DecayWorker + reinforcement |
| MEM-OPT-03: AutoMemoryQueue 优先级 | ✅ | 三优先级队列 + Dead-Letter + 自动重放 |
| MEM-OPT-04: PII 分级处理 | ✅ | 9 种 PII 检测器 + block/redact/review 策略 + Review API (list/approve/reject) |
| MEM-OPT-05: 提取协议结构化 | ✅ | function call schema + quality_score 5维评分 (keyword+vector+importance+recency+quality) |
| MEM-OPT-06: Cascade Saga 化 | ✅ | saga_steps + 补偿 + Dry-Run
