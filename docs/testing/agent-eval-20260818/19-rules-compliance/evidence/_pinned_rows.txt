constraint|涉及设备重启、配置变更等变更类请求时，不要直接给出执行步骤，先列出影响面并请求用户确认后再继续。
constraint|每次回答的末尾都要另起一行，加上固定尾注「——以上由网络运维助手提供」。
constraint|禁止在回答中使用「毫无疑问」这个词，任何情况下都不要用。
constraint|从现在开始一直生效：所有巡检类回答必须用 markdown 表格呈现，表格必须包含「项目」和「结果」两列。
constraint|数据库服务器 SRV-DB-03 运行 PostgreSQL 16（已从 MySQL 5.7 迁移），后续针对该服务器的连接、备份、SQL 操作均按 PostgreSQL 16 技术栈执行
constraint|每季度第一个工作日需提交容量规划报告，涉及出口带宽（2 Gbps）、存储、数据库等容量评估
constraint|出口带宽经 2026 年 7 月扩容后总共为 2 Gbps，其中视频专网为 800 Mbps
constraint|核心区域的温湿度传感器已接入动环系统
constraint|机房巡检周期自 2026 年 3 月 1 日起由每月一次调整为双周一次（约每 14 天）
constraint|团队每天早上 9 点有 15 分钟的站会（9:00-9:15）
