# 知识库问句混合路由测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-202200` |
| 执行时间 | 2026-08-17 20:22（UTC+8） |
| 执行者 | AI |
| 触发原因 | US-53：问句不再 dense-only，生产路径接上 US-51 词法针 |
| 分支/提交 | `dev`（含未提交工作区变更） |
| 循环轮次 | 第 1 轮 |

---

## 1. 总体结果

| 命令 | 结果 |
|------|------|
| `go test ./internal/knowledge -count=1` | PASS |
| `go test ./internal/biz/knowledge -run TestLexical -count=1` | PASS |

### 判定

- [x] 「什么是知识图谱」与金标问句自动模式为 RRF
- [x] 引号短语/工单号仍为 sparse
- [x] 「低压电工证」仍为 dense
- [x] 可交付用户自测

---

## 2. 行为对照

| 查询 | 路由（改前 → 改后） |
|------|---------------------|
| `"connection refused"` / `INC-2026-0817` | sparse → sparse |
| `低压电工证` | dense → dense |
| `什么是知识图谱` | dense → **rrf** |
| `核心机房的巡检周期是多久一次，周几几点开始？` | dense → **rrf** |

无语义层集合仍在 HybridRetriever 内降级纯 BM25，问句针继续生效。

---

## 3. 用户自测建议

对已启用语义检索的库，用完整问句提问（与金标相同）。命中应同时受益于向量近邻和词法针，不应再出现「关键词能搜到、问句搜不到」。
