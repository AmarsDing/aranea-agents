# 知识库核心优化全面测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-163600` |
| 执行时间 | 2026-08-17 16:01–16:24（UTC+8） |
| 执行者 | AI |
| 触发原因 | 知识库长期供粮核心可靠性优化后的全面回归 |
| 分支/提交 | `dev:b50099d2b`（含未提交工作区变更） |
| 循环轮次 | 第 3 轮 |
| 环境 | Windows 10 / Go / PostgreSQL `aranea_test` |

---

## 1. 总体结果

| 指标 | 值 |
|------|-----|
| 测试与静态门禁数 | 10 |
| PASS | 9 |
| FAIL | 1 |
| SKIP | 0 |
| ERROR | 0 |
| 门禁通过率 | 90% |
| 知识库受影响包 | 全部通过 |
| Go 覆盖率 | 本轮未采集 |
| 前端覆盖率 | 本轮未执行前端测试 |

### 判定

- [x] 知识库专项验收通过：检索、写回、图谱、索引修复及 PostgreSQL 时态链路均通过。
- [x] 本轮变更可交付用户自测。
- [ ] 全仓发布门禁通过：`go build ./...` 被仓库既有 `test/` 快照和本地原生依赖阻塞。

**结论**：知识库优化本身通过；整个仓库当前不能据此报告宣称可发布。

---

## 2. 后端测试结果

### 2.1 受影响包回归

| 范围 | 命令 | 结果 | 耗时 |
|------|------|------|------|
| 检索与抽取 | `go test ./internal/knowledge -count=1` | PASS | 0.552s |
| Knowledge Biz | `go test ./internal/biz/knowledge -count=1` | PASS | 0.600s |
| 自治 Worker | `go test ./internal/cronrunner/jobs -count=1` | PASS | 0.881s |
| Service | `go test ./internal/service -count=1` | PASS | 24.853s |
| Admin DI 编译 | `go test ./cmd/admin -count=1` | PASS | 0.310s |

最终合并命令退出码为 0，总耗时 50.468s。

### 2.2 PostgreSQL 生产路径专项

命令：

```powershell
$env:ARANEA_TEST_PG_DSN='postgres://postgres:***@127.0.0.1:5432/aranea_test?sslmode=disable'
go test ./internal/data -run "Test(...knowledge gates...)" -count=1
```

结果：PASS，包耗时 5.248s。

覆盖测试：

1. `TestKnowledgeRepo_SearchChunksBM25_GoldBilingual`
   - tsvector、trigram、短中文 substring 和词法 RRF 生产路径。
   - 两字中文“灰度”由已知缺口升级为硬断言并通过。
   - 中英文正例命中，负例不误召回。
2. `TestListDocumentsPendingReembed_LexicalChunksAreHealthy`
   - 词法库 NULL embedding 不进入修复循环。
   - 语义库 NULL embedding 正确进入修复队列。
3. `TestEnsureKnowledgeSchema_BitemporalFreshShape`
   - active-only partial unique index 与双时态字段正确。
4. `TestMigration20261220_UpgradesLegacyLinks`
   - 存量数据库迁移可重入并升级到新时态结构。
5. `TestKnowledgeRepo_ReplaceLinksPreservesHistory`
   - 显式/entity 边替换关闭旧版本，不物理删除。
6. `TestKnowledgeRepo_ListActiveLinks`
   - 图扩展仅读取 `valid_to IS NULL` 的有效边。
7. `TestKnowledgeRepo_ReplaceSemanticLinks`
   - typed semantic 边保留历史且仅维持一个 active 版本。

### 2.3 并发与热路径

| 检查 | 结果 | 验证内容 |
|------|------|----------|
| 定向 Race Test | PASS | RRF dense/sparse 并发、异步 access log、embedding cache/singleflight |
| RRF 并行启动 | PASS | dense 与 sparse 在任一路释放前均已开始 |
| 融合后 Rerank | PASS | RRF 候选进入已配置 reranker 后再裁剪 |
| Access Log 非阻塞 | PASS | 数据库记账阻塞不增加 Search 返回延迟 |
| Query Embedding 缓存 | PASS | 重复查询只请求一次；配置更新后缓存失效 |
| Relation Evidence | PASS | evidence 不在原文时禁止发布关系边 |

定向 Race Test 命令退出码为 0，包耗时 1.366s。

说明：对 `./internal/knowledge` 运行全包 Race Test 时发现既有测试夹具竞态：

- `federated_retriever_test.go` 的 `stubAllRepo` 并发写 slice。
- `vault_sync_runner_test.go` 的内存 map 在 watcher goroutine 与断言 goroutine间无锁访问。

上述竞态不位于本轮新增实现路径；本轮变更的定向 Race Test 全部通过。

### 2.4 自动修复与摄取闭环

| 能力 | 测试结果 |
|------|----------|
| `KnowledgeIndexRepairWorker` 5 分钟调度与单轮预算 | PASS |
| Worker 依赖缺失时安全禁用 | PASS |
| 修复失败不导致 Worker panic | PASS |
| 文档级重嵌入并发门 | PASS（Service 回归） |
| 上传文档完成后触发实体/关系图谱钩子 | PASS |
| 别名命中多个词条时禁止任意归并 | PASS |

---

## 3. 静态检查与构建

| 检查 | 结果 | 详情 |
|------|------|------|
| `go vet` 受影响包 | PASS | knowledge/biz/data/jobs/service/admin 无错误 |
| `git diff --check` | PASS | 无新增空白错误 |
| Wire 生成 | PASS | `wire_gen.go` 包含 `KnowledgeIndexRepairWorker` 完整装配 |
| `go build ./...` | FAIL | 失败集中于仓库既有 `test/` 快照和 KWS 原生头文件 |

### 全仓构建失败详情

1. `test/b1t3-gate/` 为历史代码快照，接口未跟随主工程演进：
   - `TRPCBuilderDeps`、Monitor 类型、Graph 类型缺失。
   - Channel、Evaluation、Skill、Team、Tool 等接口签名陈旧。
   - `FederatedRetriever.SearchAll` 参数仍为旧版本。
2. `test/kws-spike/sherpa-onnx/scripts/go` 缺少原生头文件 `c-api.h`。
3. `test/wave1b/overlay` 缺少 `TagRepo`。

这些失败在知识库受影响包和 `cmd/admin` 独立编译通过的情况下仍可复现，判定为仓库级既有门禁问题，不是本轮知识库改动引入。

---

## 4. 前端测试

本轮变更均为 Go 后端、SQL、Worker、Wire 和文档同步，没有新增前端实现。因此未重新执行：

- `pnpm test`
- `pnpm lint`
- `pnpm build`
- `pnpm check:layer`

这意味着本报告是“知识库核心后端全面回归”，不是全栈发布验收。

---

## 5. 本轮缺陷修复与回归

| 问题 | 修复 | 回归结果 |
|------|------|----------|
| RRF 串行且融合后不重排 | dense/sparse 并行，RRF 后统一 rerank | PASS |
| 精确标识符被短查询规则送入 dense | 精确意图优先 sparse | PASS |
| 两字中文词法漏召回 | 短查询 substring + 词法 RRF | PASS |
| 重复查询反复远程 embedding | 10 分钟/512 项有界缓存 + singleflight | PASS |
| access log 增加检索延迟 | `safego` 异步写入 + 2s 超时 | PASS |
| 上传文档可检索但图谱孤立 | 摄取结束触发统一图谱钩子 | PASS |
| LLM relation evidence 可能为幻觉 | evidence 必须能回指正文 | PASS |
| 图边更新删除历史 | active partial unique + `valid_to` 关闭旧边 | PASS |
| 同别名多词条任意归并 | 歧义时回退 provenance 日记 | PASS |
| chunks/embedding 故障依赖人工重建 | 5 分钟自动修复 Worker | PASS |
| 词法库 NULL embedding 被无限修复 | 修复查询区分词法库与语义库 | PASS |

---

## 6. 发布门禁

| 门禁 | 状态 |
|------|------|
| 知识库受影响包测试 | PASS |
| PostgreSQL 生产路径测试 | PASS |
| 本轮并发路径 Race Test | PASS |
| `go vet` | PASS |
| Wire 装配 | PASS |
| 全仓 `go build ./...` | FAIL |
| 全包 Race Test | FAIL（既有测试夹具竞态） |
| 前端测试/构建 | NOT RUN |
| 覆盖率 ≥ 40% | NOT MEASURED |

### 发布判定

- 知识库模块：**通过，可进入用户自测。**
- 整仓发布：**阻塞。** 需先处理测试快照编译、KWS 原生依赖，并补跑前端和覆盖率门禁。

---

## 7. 自评与后续行动

当前工程达到 L3 试运行级知识底座，本轮实现的正确性和故障恢复闭环已有测试证据，但不能称为全局最优。

后续优先级：

1. P0：将 `test/b1t3-gate` 从根模块构建范围隔离或同步接口，恢复 `go build ./...` 门禁。
2. P1：修复 `stubAllRepo` 与 vault sync 测试夹具竞态，使 `go test -race ./internal/knowledge` 全包通过。
3. P1：建立 300+ 真实查询评测集，持续记录 Recall@20、nDCG@10、MRR、Citation Precision 和 Abstention Accuracy。
4. P1：在真实 embedding/reranker 环境采集 p50/p95，确认并行检索、缓存和异步日志的线上收益。
5. P2：补齐跨措辞稳定事实键、冲突事实显式裁决和抽取置信度校准。

