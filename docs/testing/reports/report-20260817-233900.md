# 知识库图一跳 ACL / 实体词条 replay 测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-233900` |
| 执行时间 | 2026-08-17 23:39（UTC+8） |
| 执行者 | AI |
| 触发原因 | 审查优先 1：图一跳扩召回 ACL + 实体词条立刻 replay chunks |
| 分支/提交 | `dev`（含未提交工作区变更） |
| 循环轮次 | 第 1 轮 |

---

## 1. 总体结果

| 命令 | 结果 |
|------|------|
| `go test ./internal/biz/knowledge -run 'EntityWiki\|DocumentVisible\|UpdateDocumentVisibility' -count=1` | PASS |
| `GOMAXPROCS=1 go test ./internal/data -p 1 -run 'DocVisibility\|GraphExpandHidesPrivate\|ListChunksByDocuments' -count=1` | PASS（含 PG 集成） |
| `go test ./internal/data ./internal/biz/knowledge ./internal/knowledge ./internal/service -run TestDoesNotExist -count=1` | PASS（编译） |

### 判定

- [x] 非 owner 图一跳拿不到私密邻居 chunks / links / active links；owner 与 system 仍可见
- [x] 团队库实体词条创建与追加来源后都调用 `SetWriteBackReplay`
- [x] 可交付用户自测

---

## 2. 行为对照

| 场景 | 预期 |
|------|------|
| 公开笔记 `[[链]]` 到他人 private 草稿 | 他人 Agent 一跳扩召回不得带入私密段落；该边不占邻居名额 |
| 团队库抽到实体写出 `entries/<slug>.md` | 立刻 replay chunks/FTS，不必等 5 分钟索引自愈 |
| 已有词条再追加来源 wikilink | 同样 replay，正文与检索对齐 |

本轮不做：连接器、插件生态、新检索算法、chips 改成真脚注、owner-only 改可见性、`ListResolvedLinks` ACL。

---

## 3. 用户自测建议

1. 把草稿标成「仅自己可见」，从另一篇公开笔记 `[[链]]` 过去。换账号问公开笔记相关问题，回答与 chips 不应出现草稿正文。
2. 向团队库入库一篇提到明确实体的笔记，工作台 `entries/` 出现词条后立刻用该实体名检索，应能命中短页而不是空等自愈。

---

## 4. 下一步

审查优先 2：若要把 chips 做成答案脚注，再把生成侧 `[n]` 与 `chunk_id` 对齐。不默认打开 HyDE / 全量 GraphRAG / 插件市场。
