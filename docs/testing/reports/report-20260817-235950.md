# 知识库回答 [n] 脚注对齐测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-235950` |
| 执行时间 | 2026-08-17 23:59（UTC+8） |
| 执行者 | AI |
| 触发原因 | 审查优先 2：生成侧 `[n]` 与 `chunk_id` 对齐为可点脚注 |
| 分支/提交 | `dev`（含未提交工作区变更） |
| 循环轮次 | 第 1 轮 |

---

## 1. 总体结果

| 命令 | 结果 |
|------|------|
| `go test ./internal/agent -run 'FormatKnowledgeCue\|KnowledgeCueHook\|CueRendered' -count=1` | PASS |
| `go test ./internal/tools/knowledge -run 'EmitKnowledgeRecalled\|EmitNumbered' -count=1` | PASS |
| `go test ./internal/cronrunner/jobs -run KnowledgeCitationBackfill -count=1` | PASS |
| `go test ./internal/data ./internal/biz/knowledge -run TestDoesNotExist -count=1` | PASS（编译） |
| `npx vitest run` knowledgeRecall / KnowledgeRecallChips / StepBlocks / activityV2 | PASS（47 例） |

### 判定

- [x] cue `[n]`、notice `chunks[].n`、chips 编号同一套顺序；空正文/空 ID 不占号
- [x] 回答里的 `[n]` 渲染为脚注，点击有 `doc_id` 的脚注打开 `/knowledge?doc=`
- [x] cited 回采把回复中的 `[n]` 记为该 chunk；工具续搜 notice 不加 `n`
- [x] 可交付用户自测

---

## 2. 行为对照

| 场景 | 预期 |
|------|------|
| 预检索注入 2 段有效段落 | cue 为 `[1]`/`[2]`；notice `n=1,2`；chips 显示同样编号 |
| 第一段空正文 | 有效段仍从 `[1]` 起编，不出现跳号 |
| 回答写「……[1]」 | `[1]` 是可点脚注，代码块内 `[1]` 保持原文 |
| 随后 `knowledge_search` | 新命中追加 chips，不把 `[1]` 抢走 |

本轮不做：连接器、插件生态、新检索算法、owner-only 改可见性、`ListResolvedLinks` ACL。

---

## 3. 用户自测建议

1. 问一件库里有的事，回答里若出现 `[1]`，点击应打开与 chip `[1]` 相同的文档。
2. 展开知识 chips，编号应与回答脚注一致。
3. 打开「仅根据知识库作答」后，无命中仍应明确说没有。
