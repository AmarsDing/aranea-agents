# 知识库自治与入库自关联测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-214800` |
| 执行时间 | 2026-08-17 21:48（UTC+8） |
| 执行者 | AI |
| 触发原因 | US-54：治理定时实跑、vault 冷文档抽关系、索引期成链不改源 |
| 分支/提交 | `dev`（含未提交工作区变更） |
| 循环轮次 | 第 1 轮 |

---

## 1. 总体结果

| 命令 | 结果 |
|------|------|
| `go test ./internal/cronrunner/jobs ./internal/biz/knowledge ./internal/knowledge -count=1` | PASS |

### 判定

- [x] 治理工人 `DryRun=false`；无团队库静默；nil 依赖不装配
- [x] Vault 索引成功触发 relation hook
- [x] 未链接提及建成 explicit 边，源文件 / `content_text` 不变
- [x] 可交付用户自测

---

## 2. 行为对照

| 路径 | 改前 | 改后 |
|------|------|------|
| 词条治理 | 仅 `dream_cycle`，默认 dry_run | 6h cron 实跑低风险；冲突/孤儿仍 pending |
| Vault 冷文档 | 只抽实体，typed 关系等热度 | 同步后立刻 `ExtractDoc`（幂等） |
| 未链接提及 | 要人点编译/回填才有 explicit 边 | 索引期编译建边，不改 Markdown |

---

## 3. 未覆盖

- 未跑 `cmd/admin` 全量编译（低内存机易 OOM）
- 未测生产 6h 周期与 LLM 关系抽取成本
