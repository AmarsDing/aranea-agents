# 知识库自然语言词法召回测试报告

## 报告头

| 字段 | 值 |
|------|-----|
| 报告 ID | `report-20260817-195500` |
| 执行时间 | 2026-08-17 19:50–19:55（UTC+8） |
| 执行者 | AI |
| 触发原因 | US-51 中文问句词法规范化落地后的专项回归 |
| 分支/提交 | `dev:95fc49f97`（含本轮未提交工作区变更） |
| 循环轮次 | 第 1 轮 |
| 环境 | Windows 10 / Go 1.26 / PostgreSQL 18 `aranea_test`（`127.0.0.1:5432`，用户 `postgres`） |

---

## 1. 总体结果

| 指标 | 值 |
|------|-----|
| 专项门禁 | 5 |
| PASS | 5 |
| FAIL | 0 |
| SKIP | 0 |
| 知识库受影响包 | `internal/biz/knowledge`、`internal/knowledge`、`internal/data`（BM25 金标）全部通过 |
| 全仓发布门禁 | 本轮未跑 `go build ./...` / 前端 |

### 判定

- [x] 自然语言问法可由纯 BM25 召回：HitRate@5 从 0 提升到 1.00，并成为硬门（≥ 0.90）。
- [x] 关键词、标识符、短中文、双语金标未回归。
- [x] 本轮变更可交付用户自测。
- [ ] 全仓发布门禁：未执行。

**结论**：问句词法缺口已补上；不依赖 embedding。用户可用完整中文问句打知识库做自测。

---

## 2. 后端测试结果

| 层 | 命令 | 结果 | 耗时（测试本体） |
|----|------|------|------------------|
| Biz 问句针 | `go test ./internal/biz/knowledge -run TestLexical -count=1` | PASS | ~0.09s |
| Biz 全包 | `go test ./internal/biz/knowledge -count=1` | PASS | ~0.60s |
| Knowledge | `go test ./internal/knowledge -count=1` | PASS | ~0.42s |
| Data BM25 金标 | `go test ./internal/data -run TestKnowledgeRepo_SearchChunksBM25_GoldBilingual\|RetrievalBaseline\|ChineseShortQuery -count=1` | PASS | ~2.7s（含编译） |

### 2.1 PostgreSQL 词法基线（生产 `SearchChunksBM25`）

语料：`docs/testing/agent-eval-20260818/03-knowledge-rag/`（5 篇文档 / 30 条问法）。

| 切片 | HitRate@1 | HitRate@5 | MRR | 门禁 |
|------|-----------|-----------|-----|------|
| 最长 `expected_keywords` | 0.833 | **1.000** | 0.893 | Hit@5 ≥ 0.90 |
| 自然语言问法 | 0.967 | **1.000** | 0.983 | Hit@5 ≥ 0.90（本轮新增） |
| 标识符 | — | **1.000** | — | Hit@5 ≥ 0.80 |
| 负例 `不存在的词语xx` | 0 命中 | 0 命中 | — | 不得误中 |

对比上一轮：同一 30 条问法纯 BM25 HitRate@5 = **0**（问句 AND 套话）。本轮问句压缩 + 内容针后 HitRate@5 = **1.00**。

关键词 Hit@1 = 0.833：多文档共享「30 分钟」等短事实，不进硬门；Hit@5 仍为 1.00。

### 2.2 失败详情

无。

---

## 3. 前端测试结果

本轮无前端改动，未执行。

---

## 4. 变更说明

| 文件 | 作用 |
|------|------|
| `internal/biz/knowledge/lexical_query.go` | 问句判定、套话剥离、内容针抽取 |
| `internal/data/knowledge.go` | `SearchChunksBM25` 对变体并行召回并 RRF |
| `internal/data/knowledge_retrieval_baseline_test.go` | NL Hit@5 升为硬门 |

非目标（本轮明确不做）：新检索算法、LLM 改写、300 条独立新事实、`keep_old`/`keep_new` 工作台按钮。

---

## 5. 用户自测建议

1. 对词法库（未开语义）提问：「核心机房的巡检周期是多久一次，周几几点开始？」应命中巡检规范。
2. 精确词「低压电工证」「SW-Core-01」仍应首屏命中。
3. 乱问「如何申请年假和出差报销」不应从运维语料里编造答案。
