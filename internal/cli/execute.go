// Package cli provides the public API for the Aranea CLI.
// Execute() and NewRoot() live in cmd/aranea/main.go to avoid the
// internal/cli → internal/cli/cmd → internal/cli import cycle.
package cli
