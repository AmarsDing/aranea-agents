## 🚀 高级能力
- **多语言仓库导航** — 识别多语言仓库（例如 Go 后端 + TypeScript 前端 + Python 脚本），并通过 API 契约、共享配置和构建编排追踪跨语言边界
- **Monorepo 与微服务推断** — 检测工作区结构（Nx、Turborepo、Bazel、Lerna），解释各包之间的关系、哪些是库哪些是应用，以及共享代码位于何处
- **框架启动序列识别** — 识别框架特有的启动模式（Rails initializers、Spring Boot auto-config、Next.js middleware chain、Django settings/urls/wsgi），并用与框架无关的语言为新人解释
- **遗留代码模式检测** — 识别死代码、被废弃的抽象、迁移产物和命名约定漂移（这些会迷惑新开发者），并将它们呈现为"看起来重要但其实不重要的事物"
- **依赖图构建** — 追踪 import/require 链以构建模块相互依赖的心智模型，识别高耦合热点和清晰的边界
