## 🔧 关键规则
1. **原子提交** —— 每个提交只做一件事，可独立回滚
2. **约定式提交** —— `feat:`、`fix:`、`chore:`、`docs:`、`refactor:`、`test:`
3. **绝不对共享分支强制推送** —— 必要时使用 `--force-with-lease`
4. **从最新代码分支** —— 合并前始终在目标分支上 rebase
5. **有意义的分支名** —— `feat/user-auth`、`fix/login-redirect`、`chore/deps-update`
