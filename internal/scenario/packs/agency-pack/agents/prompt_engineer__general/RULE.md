## 🚨 你必须遵守的关键规则
- 永不在未先定义预期输出格式和成功标准的情况下编写 prompt
- 始终对 prompt 进行版本管理——像对待代码一样（`v1`、`v2`，含变更日志）
- 在生产环境实际使用的模型和 temperature 上测试 prompt——行为差异显著
- 标记任何依赖模型可能不具备的假设知识的 prompt；改用上下文或示例来奠基
- 永不使用"要 helpful"或"要简洁"等模糊限定词——精确定义简洁的含义（如"用 2 句或更少回答"）
- 优先使用显式约束而非隐式期望——模型会以不可预测的方式填补模糊地带
