// Code generated by ent, DO NOT EDIT.

package ent

import (
	"aranea-agents/internal/data/ent/evalcase"
	"aranea-agents/internal/data/ent/evaldataset"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// EvalCase is the model entity for the EvalCase schema.
type EvalCase struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// DatasetID holds the value of the "dataset_id" field.
	DatasetID string `json:"dataset_id,omitempty"`
	// Input holds the value of the "input" field.
	Input string `json:"input,omitempty"`
	// ExpectedOutput holds the value of the "expected_output" field.
	ExpectedOutput string `json:"expected_output,omitempty"`
	// MetadataJSON holds the value of the "metadata_json" field.
	MetadataJSON string `json:"metadata_json,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EvalCaseQuery when eager-loading is set.
	Edges        EvalCaseEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EvalCaseEdges holds the relations/edges for other nodes in the graph.
type EvalCaseEdges struct {
	// Dataset holds the value of the dataset edge.
	Dataset *EvalDataset `json:"dataset,omitempty"`
	// Results holds the value of the results edge.
	Results []*EvalCaseResult `json:"results,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// DatasetOrErr returns the Dataset value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EvalCaseEdges) DatasetOrErr() (*EvalDataset, error) {
	if e.Dataset != nil {
		return e.Dataset, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: evaldataset.Label}
	}
	return nil, &NotLoadedError{edge: "dataset"}
}

// ResultsOrErr returns the Results value or an error if the edge
// was not loaded in eager-loading.
func (e EvalCaseEdges) ResultsOrErr() ([]*EvalCaseResult, error) {
	if e.loadedTypes[1] {
		return e.Results, nil
	}
	return nil, &NotLoadedError{edge: "results"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EvalCase) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case evalcase.FieldID, evalcase.FieldDatasetID, evalcase.FieldInput, evalcase.FieldExpectedOutput, evalcase.FieldMetadataJSON:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EvalCase fields.
func (_m *EvalCase) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case evalcase.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				_m.ID = value.String
			}
		case evalcase.FieldDatasetID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field dataset_id", values[i])
			} else if value.Valid {
				_m.DatasetID = value.String
			}
		case evalcase.FieldInput:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field input", values[i])
			} else if value.Valid {
				_m.Input = value.String
			}
		case evalcase.FieldExpectedOutput:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field expected_output", values[i])
			} else if value.Valid {
				_m.ExpectedOutput = value.String
			}
		case evalcase.FieldMetadataJSON:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field metadata_json", values[i])
			} else if value.Valid {
				_m.MetadataJSON = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EvalCase.
// This includes values selected through modifiers, order, etc.
func (_m *EvalCase) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryDataset queries the "dataset" edge of the EvalCase entity.
func (_m *EvalCase) QueryDataset() *EvalDatasetQuery {
	return NewEvalCaseClient(_m.config).QueryDataset(_m)
}

// QueryResults queries the "results" edge of the EvalCase entity.
func (_m *EvalCase) QueryResults() *EvalCaseResultQuery {
	return NewEvalCaseClient(_m.config).QueryResults(_m)
}

// Update returns a builder for updating this EvalCase.
// Note that you need to call EvalCase.Unwrap() before calling this method if this EvalCase
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EvalCase) Update() *EvalCaseUpdateOne {
	return NewEvalCaseClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EvalCase entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EvalCase) Unwrap() *EvalCase {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EvalCase is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EvalCase) String() string {
	var builder strings.Builder
	builder.WriteString("EvalCase(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("dataset_id=")
	builder.WriteString(_m.DatasetID)
	builder.WriteString(", ")
	builder.WriteString("input=")
	builder.WriteString(_m.Input)
	builder.WriteString(", ")
	builder.WriteString("expected_output=")
	builder.WriteString(_m.ExpectedOutput)
	builder.WriteString(", ")
	builder.WriteString("metadata_json=")
	builder.WriteString(_m.MetadataJSON)
	builder.WriteByte(')')
	return builder.String()
}

// EvalCases is a parsable slice of EvalCase.
type EvalCases []*EvalCase
