// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	stdsql "database/sql"
	"fmt"
	"sync"

	"entgo.io/ent/dialect"
)

// Tx is a transactional client that is created by calling Client.Tx().
type Tx struct {
	config
	// Admin is the client for interacting with the Admin builders.
	Admin *AdminClient
	// Agent is the client for interacting with the Agent builders.
	Agent *AgentClient
	// AgentPerformance is the client for interacting with the AgentPerformance builders.
	AgentPerformance *AgentPerformanceClient
	// AgentPromptFile is the client for interacting with the AgentPromptFile builders.
	AgentPromptFile *AgentPromptFileClient
	// AgentRuntimeSetting is the client for interacting with the AgentRuntimeSetting builders.
	AgentRuntimeSetting *AgentRuntimeSettingClient
	// AgentTemplate is the client for interacting with the AgentTemplate builders.
	AgentTemplate *AgentTemplateClient
	// AllocationPlan is the client for interacting with the AllocationPlan builders.
	AllocationPlan *AllocationPlanClient
	// AvatarAsset is the client for interacting with the AvatarAsset builders.
	AvatarAsset *AvatarAssetClient
	// BackgroundJob is the client for interacting with the BackgroundJob builders.
	BackgroundJob *BackgroundJobClient
	// BorrowRequest is the client for interacting with the BorrowRequest builders.
	BorrowRequest *BorrowRequestClient
	// BudgetAlert is the client for interacting with the BudgetAlert builders.
	BudgetAlert *BudgetAlertClient
	// ChannelInboundReceipt is the client for interacting with the ChannelInboundReceipt builders.
	ChannelInboundReceipt *ChannelInboundReceiptClient
	// ChannelRuntimeLease is the client for interacting with the ChannelRuntimeLease builders.
	ChannelRuntimeLease *ChannelRuntimeLeaseClient
	// ChannelTurnJob is the client for interacting with the ChannelTurnJob builders.
	ChannelTurnJob *ChannelTurnJobClient
	// CircuitBreakerState is the client for interacting with the CircuitBreakerState builders.
	CircuitBreakerState *CircuitBreakerStateClient
	// CompiledTeam is the client for interacting with the CompiledTeam builders.
	CompiledTeam *CompiledTeamClient
	// CronTask is the client for interacting with the CronTask builders.
	CronTask *CronTaskClient
	// CronTaskRun is the client for interacting with the CronTaskRun builders.
	CronTaskRun *CronTaskRunClient
	// DeptLeadMessage is the client for interacting with the DeptLeadMessage builders.
	DeptLeadMessage *DeptLeadMessageClient
	// EvalCase is the client for interacting with the EvalCase builders.
	EvalCase *EvalCaseClient
	// EvalCaseResult is the client for interacting with the EvalCaseResult builders.
	EvalCaseResult *EvalCaseResultClient
	// EvalDataset is the client for interacting with the EvalDataset builders.
	EvalDataset *EvalDatasetClient
	// EvalRun is the client for interacting with the EvalRun builders.
	EvalRun *EvalRunClient
	// EventDeliveryOutbox is the client for interacting with the EventDeliveryOutbox builders.
	EventDeliveryOutbox *EventDeliveryOutboxClient
	// EvolutionSuggestion is the client for interacting with the EvolutionSuggestion builders.
	EvolutionSuggestion *EvolutionSuggestionClient
	// ExperienceReport is the client for interacting with the ExperienceReport builders.
	ExperienceReport *ExperienceReportClient
	// FailurePattern is the client for interacting with the FailurePattern builders.
	FailurePattern *FailurePatternClient
	// FlowLogEvent is the client for interacting with the FlowLogEvent builders.
	FlowLogEvent *FlowLogEventClient
	// GatewayWebhook is the client for interacting with the GatewayWebhook builders.
	GatewayWebhook *GatewayWebhookClient
	// GraphDefinition is the client for interacting with the GraphDefinition builders.
	GraphDefinition *GraphDefinitionClient
	// GraphExecution is the client for interacting with the GraphExecution builders.
	GraphExecution *GraphExecutionClient
	// GraphNodeV2 is the client for interacting with the GraphNodeV2 builders.
	GraphNodeV2 *GraphNodeV2Client
	// GraphStageV2 is the client for interacting with the GraphStageV2 builders.
	GraphStageV2 *GraphStageV2Client
	// GraphTask is the client for interacting with the GraphTask builders.
	GraphTask *GraphTaskClient
	// GraphTaskComment is the client for interacting with the GraphTaskComment builders.
	GraphTaskComment *GraphTaskCommentClient
	// GraphTaskEvent is the client for interacting with the GraphTaskEvent builders.
	GraphTaskEvent *GraphTaskEventClient
	// GraphTaskLink is the client for interacting with the GraphTaskLink builders.
	GraphTaskLink *GraphTaskLinkClient
	// GraphTaskLog is the client for interacting with the GraphTaskLog builders.
	GraphTaskLog *GraphTaskLogClient
	// GraphTaskRun is the client for interacting with the GraphTaskRun builders.
	GraphTaskRun *GraphTaskRunClient
	// HealRecord is the client for interacting with the HealRecord builders.
	HealRecord *HealRecordClient
	// LlmProviderModel is the client for interacting with the LlmProviderModel builders.
	LlmProviderModel *LlmProviderModelClient
	// MediaProvider is the client for interacting with the MediaProvider builders.
	MediaProvider *MediaProviderClient
	// MemberSessionV2 is the client for interacting with the MemberSessionV2 builders.
	MemberSessionV2 *MemberSessionV2Client
	// ModelPricingRule is the client for interacting with the ModelPricingRule builders.
	ModelPricingRule *ModelPricingRuleClient
	// ModelTokenUsageHourly is the client for interacting with the ModelTokenUsageHourly builders.
	ModelTokenUsageHourly *ModelTokenUsageHourlyClient
	// Orchestration is the client for interacting with the Orchestration builders.
	Orchestration *OrchestrationClient
	// OrchestrationStep is the client for interacting with the OrchestrationStep builders.
	OrchestrationStep *OrchestrationStepClient
	// Organization is the client for interacting with the Organization builders.
	Organization *OrganizationClient
	// PlanBoardV2 is the client for interacting with the PlanBoardV2 builders.
	PlanBoardV2 *PlanBoardV2Client
	// PlanStepV2 is the client for interacting with the PlanStepV2 builders.
	PlanStepV2 *PlanStepV2Client
	// PlatformChannel is the client for interacting with the PlatformChannel builders.
	PlatformChannel *PlatformChannelClient
	// PlatformChannelCredential is the client for interacting with the PlatformChannelCredential builders.
	PlatformChannelCredential *PlatformChannelCredentialClient
	// PlatformChannelDelivery is the client for interacting with the PlatformChannelDelivery builders.
	PlatformChannelDelivery *PlatformChannelDeliveryClient
	// PlatformChannelPeerSession is the client for interacting with the PlatformChannelPeerSession builders.
	PlatformChannelPeerSession *PlatformChannelPeerSessionClient
	// PlatformHook is the client for interacting with the PlatformHook builders.
	PlatformHook *PlatformHookClient
	// PlatformMCPServer is the client for interacting with the PlatformMCPServer builders.
	PlatformMCPServer *PlatformMCPServerClient
	// PlatformMCPUserCredential is the client for interacting with the PlatformMCPUserCredential builders.
	PlatformMCPUserCredential *PlatformMCPUserCredentialClient
	// PlatformPlugin is the client for interacting with the PlatformPlugin builders.
	PlatformPlugin *PlatformPluginClient
	// PlatformSkill is the client for interacting with the PlatformSkill builders.
	PlatformSkill *PlatformSkillClient
	// PlatformTool is the client for interacting with the PlatformTool builders.
	PlatformTool *PlatformToolClient
	// ResourceAccessAudit is the client for interacting with the ResourceAccessAudit builders.
	ResourceAccessAudit *ResourceAccessAuditClient
	// SchemaMigration is the client for interacting with the SchemaMigration builders.
	SchemaMigration *SchemaMigrationClient
	// SelfCheckReport is the client for interacting with the SelfCheckReport builders.
	SelfCheckReport *SelfCheckReportClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// SessionMetrics is the client for interacting with the SessionMetrics builders.
	SessionMetrics *SessionMetricsClient
	// SessionParticipant is the client for interacting with the SessionParticipant builders.
	SessionParticipant *SessionParticipantClient
	// SessionRun is the client for interacting with the SessionRun builders.
	SessionRun *SessionRunClient
	// SessionRunCheckpoint is the client for interacting with the SessionRunCheckpoint builders.
	SessionRunCheckpoint *SessionRunCheckpointClient
	// SessionRuntime is the client for interacting with the SessionRuntime builders.
	SessionRuntime *SessionRuntimeClient
	// SessionTurn is the client for interacting with the SessionTurn builders.
	SessionTurn *SessionTurnClient
	// SessionV2 is the client for interacting with the SessionV2 builders.
	SessionV2 *SessionV2Client
	// SkillEvolutionSuggestion is the client for interacting with the SkillEvolutionSuggestion builders.
	SkillEvolutionSuggestion *SkillEvolutionSuggestionClient
	// SkillImportJob is the client for interacting with the SkillImportJob builders.
	SkillImportJob *SkillImportJobClient
	// SkillInvocation is the client for interacting with the SkillInvocation builders.
	SkillInvocation *SkillInvocationClient
	// SkillVersion is the client for interacting with the SkillVersion builders.
	SkillVersion *SkillVersionClient
	// StepV2 is the client for interacting with the StepV2 builders.
	StepV2 *StepV2Client
	// SystemSetting is the client for interacting with the SystemSetting builders.
	SystemSetting *SystemSettingClient
	// TaskDeadLetter is the client for interacting with the TaskDeadLetter builders.
	TaskDeadLetter *TaskDeadLetterClient
	// TaskPlan is the client for interacting with the TaskPlan builders.
	TaskPlan *TaskPlanClient
	// TaskV2 is the client for interacting with the TaskV2 builders.
	TaskV2 *TaskV2Client
	// Team is the client for interacting with the Team builders.
	Team *TeamClient
	// TeamRun is the client for interacting with the TeamRun builders.
	TeamRun *TeamRunClient
	// TeamRunStep is the client for interacting with the TeamRunStep builders.
	TeamRunStep *TeamRunStepClient
	// TeamRunV2 is the client for interacting with the TeamRunV2 builders.
	TeamRunV2 *TeamRunV2Client
	// TeamStageV2 is the client for interacting with the TeamStageV2 builders.
	TeamStageV2 *TeamStageV2Client
	// ToolAgentOverride is the client for interacting with the ToolAgentOverride builders.
	ToolAgentOverride *ToolAgentOverrideClient
	// ToolGrant is the client for interacting with the ToolGrant builders.
	ToolGrant *ToolGrantClient
	// ToolInvocation is the client for interacting with the ToolInvocation builders.
	ToolInvocation *ToolInvocationClient
	// ToolInvocationAudit is the client for interacting with the ToolInvocationAudit builders.
	ToolInvocationAudit *ToolInvocationAuditClient
	// ToolInvocationParam is the client for interacting with the ToolInvocationParam builders.
	ToolInvocationParam *ToolInvocationParamClient
	// ToolResultBlob is the client for interacting with the ToolResultBlob builders.
	ToolResultBlob *ToolResultBlobClient
	// ToolResultReplacement is the client for interacting with the ToolResultReplacement builders.
	ToolResultReplacement *ToolResultReplacementClient
	// TurnV2 is the client for interacting with the TurnV2 builders.
	TurnV2 *TurnV2Client
	// UsageQuota is the client for interacting with the UsageQuota builders.
	UsageQuota *UsageQuotaClient
	// UserEmbeddingSetting is the client for interacting with the UserEmbeddingSetting builders.
	UserEmbeddingSetting *UserEmbeddingSettingClient

	// lazily loaded.
	client     *Client
	clientOnce sync.Once
	// ctx lives for the life of the transaction. It is
	// the same context used by the underlying connection.
	ctx context.Context
}

type (
	// Committer is the interface that wraps the Commit method.
	Committer interface {
		Commit(context.Context, *Tx) error
	}

	// The CommitFunc type is an adapter to allow the use of ordinary
	// function as a Committer. If f is a function with the appropriate
	// signature, CommitFunc(f) is a Committer that calls f.
	CommitFunc func(context.Context, *Tx) error

	// CommitHook defines the "commit middleware". A function that gets a Committer
	// and returns a Committer. For example:
	//
	//	hook := func(next ent.Committer) ent.Committer {
	//		return ent.CommitFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Commit(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	CommitHook func(Committer) Committer
)

// Commit calls f(ctx, m).
func (f CommitFunc) Commit(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Commit commits the transaction.
func (tx *Tx) Commit() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Committer = CommitFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Commit()
	})
	txDriver.mu.Lock()
	hooks := append([]CommitHook(nil), txDriver.onCommit...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Commit(tx.ctx, tx)
}

// OnCommit adds a hook to call on commit.
func (tx *Tx) OnCommit(f CommitHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onCommit = append(txDriver.onCommit, f)
	txDriver.mu.Unlock()
}

type (
	// Rollbacker is the interface that wraps the Rollback method.
	Rollbacker interface {
		Rollback(context.Context, *Tx) error
	}

	// The RollbackFunc type is an adapter to allow the use of ordinary
	// function as a Rollbacker. If f is a function with the appropriate
	// signature, RollbackFunc(f) is a Rollbacker that calls f.
	RollbackFunc func(context.Context, *Tx) error

	// RollbackHook defines the "rollback middleware". A function that gets a Rollbacker
	// and returns a Rollbacker. For example:
	//
	//	hook := func(next ent.Rollbacker) ent.Rollbacker {
	//		return ent.RollbackFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Rollback(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	RollbackHook func(Rollbacker) Rollbacker
)

// Rollback calls f(ctx, m).
func (f RollbackFunc) Rollback(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Rollback rollbacks the transaction.
func (tx *Tx) Rollback() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Rollbacker = RollbackFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Rollback()
	})
	txDriver.mu.Lock()
	hooks := append([]RollbackHook(nil), txDriver.onRollback...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Rollback(tx.ctx, tx)
}

// OnRollback adds a hook to call on rollback.
func (tx *Tx) OnRollback(f RollbackHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onRollback = append(txDriver.onRollback, f)
	txDriver.mu.Unlock()
}

// Client returns a Client that binds to current transaction.
func (tx *Tx) Client() *Client {
	tx.clientOnce.Do(func() {
		tx.client = &Client{config: tx.config}
		tx.client.init()
	})
	return tx.client
}

func (tx *Tx) init() {
	tx.Admin = NewAdminClient(tx.config)
	tx.Agent = NewAgentClient(tx.config)
	tx.AgentPerformance = NewAgentPerformanceClient(tx.config)
	tx.AgentPromptFile = NewAgentPromptFileClient(tx.config)
	tx.AgentRuntimeSetting = NewAgentRuntimeSettingClient(tx.config)
	tx.AgentTemplate = NewAgentTemplateClient(tx.config)
	tx.AllocationPlan = NewAllocationPlanClient(tx.config)
	tx.AvatarAsset = NewAvatarAssetClient(tx.config)
	tx.BackgroundJob = NewBackgroundJobClient(tx.config)
	tx.BorrowRequest = NewBorrowRequestClient(tx.config)
	tx.BudgetAlert = NewBudgetAlertClient(tx.config)
	tx.ChannelInboundReceipt = NewChannelInboundReceiptClient(tx.config)
	tx.ChannelRuntimeLease = NewChannelRuntimeLeaseClient(tx.config)
	tx.ChannelTurnJob = NewChannelTurnJobClient(tx.config)
	tx.CircuitBreakerState = NewCircuitBreakerStateClient(tx.config)
	tx.CompiledTeam = NewCompiledTeamClient(tx.config)
	tx.CronTask = NewCronTaskClient(tx.config)
	tx.CronTaskRun = NewCronTaskRunClient(tx.config)
	tx.DeptLeadMessage = NewDeptLeadMessageClient(tx.config)
	tx.EvalCase = NewEvalCaseClient(tx.config)
	tx.EvalCaseResult = NewEvalCaseResultClient(tx.config)
	tx.EvalDataset = NewEvalDatasetClient(tx.config)
	tx.EvalRun = NewEvalRunClient(tx.config)
	tx.EventDeliveryOutbox = NewEventDeliveryOutboxClient(tx.config)
	tx.EvolutionSuggestion = NewEvolutionSuggestionClient(tx.config)
	tx.ExperienceReport = NewExperienceReportClient(tx.config)
	tx.FailurePattern = NewFailurePatternClient(tx.config)
	tx.FlowLogEvent = NewFlowLogEventClient(tx.config)
	tx.GatewayWebhook = NewGatewayWebhookClient(tx.config)
	tx.GraphDefinition = NewGraphDefinitionClient(tx.config)
	tx.GraphExecution = NewGraphExecutionClient(tx.config)
	tx.GraphNodeV2 = NewGraphNodeV2Client(tx.config)
	tx.GraphStageV2 = NewGraphStageV2Client(tx.config)
	tx.GraphTask = NewGraphTaskClient(tx.config)
	tx.GraphTaskComment = NewGraphTaskCommentClient(tx.config)
	tx.GraphTaskEvent = NewGraphTaskEventClient(tx.config)
	tx.GraphTaskLink = NewGraphTaskLinkClient(tx.config)
	tx.GraphTaskLog = NewGraphTaskLogClient(tx.config)
	tx.GraphTaskRun = NewGraphTaskRunClient(tx.config)
	tx.HealRecord = NewHealRecordClient(tx.config)
	tx.LlmProviderModel = NewLlmProviderModelClient(tx.config)
	tx.MediaProvider = NewMediaProviderClient(tx.config)
	tx.MemberSessionV2 = NewMemberSessionV2Client(tx.config)
	tx.ModelPricingRule = NewModelPricingRuleClient(tx.config)
	tx.ModelTokenUsageHourly = NewModelTokenUsageHourlyClient(tx.config)
	tx.Orchestration = NewOrchestrationClient(tx.config)
	tx.OrchestrationStep = NewOrchestrationStepClient(tx.config)
	tx.Organization = NewOrganizationClient(tx.config)
	tx.PlanBoardV2 = NewPlanBoardV2Client(tx.config)
	tx.PlanStepV2 = NewPlanStepV2Client(tx.config)
	tx.PlatformChannel = NewPlatformChannelClient(tx.config)
	tx.PlatformChannelCredential = NewPlatformChannelCredentialClient(tx.config)
	tx.PlatformChannelDelivery = NewPlatformChannelDeliveryClient(tx.config)
	tx.PlatformChannelPeerSession = NewPlatformChannelPeerSessionClient(tx.config)
	tx.PlatformHook = NewPlatformHookClient(tx.config)
	tx.PlatformMCPServer = NewPlatformMCPServerClient(tx.config)
	tx.PlatformMCPUserCredential = NewPlatformMCPUserCredentialClient(tx.config)
	tx.PlatformPlugin = NewPlatformPluginClient(tx.config)
	tx.PlatformSkill = NewPlatformSkillClient(tx.config)
	tx.PlatformTool = NewPlatformToolClient(tx.config)
	tx.ResourceAccessAudit = NewResourceAccessAuditClient(tx.config)
	tx.SchemaMigration = NewSchemaMigrationClient(tx.config)
	tx.SelfCheckReport = NewSelfCheckReportClient(tx.config)
	tx.Session = NewSessionClient(tx.config)
	tx.SessionMetrics = NewSessionMetricsClient(tx.config)
	tx.SessionParticipant = NewSessionParticipantClient(tx.config)
	tx.SessionRun = NewSessionRunClient(tx.config)
	tx.SessionRunCheckpoint = NewSessionRunCheckpointClient(tx.config)
	tx.SessionRuntime = NewSessionRuntimeClient(tx.config)
	tx.SessionTurn = NewSessionTurnClient(tx.config)
	tx.SessionV2 = NewSessionV2Client(tx.config)
	tx.SkillEvolutionSuggestion = NewSkillEvolutionSuggestionClient(tx.config)
	tx.SkillImportJob = NewSkillImportJobClient(tx.config)
	tx.SkillInvocation = NewSkillInvocationClient(tx.config)
	tx.SkillVersion = NewSkillVersionClient(tx.config)
	tx.StepV2 = NewStepV2Client(tx.config)
	tx.SystemSetting = NewSystemSettingClient(tx.config)
	tx.TaskDeadLetter = NewTaskDeadLetterClient(tx.config)
	tx.TaskPlan = NewTaskPlanClient(tx.config)
	tx.TaskV2 = NewTaskV2Client(tx.config)
	tx.Team = NewTeamClient(tx.config)
	tx.TeamRun = NewTeamRunClient(tx.config)
	tx.TeamRunStep = NewTeamRunStepClient(tx.config)
	tx.TeamRunV2 = NewTeamRunV2Client(tx.config)
	tx.TeamStageV2 = NewTeamStageV2Client(tx.config)
	tx.ToolAgentOverride = NewToolAgentOverrideClient(tx.config)
	tx.ToolGrant = NewToolGrantClient(tx.config)
	tx.ToolInvocation = NewToolInvocationClient(tx.config)
	tx.ToolInvocationAudit = NewToolInvocationAuditClient(tx.config)
	tx.ToolInvocationParam = NewToolInvocationParamClient(tx.config)
	tx.ToolResultBlob = NewToolResultBlobClient(tx.config)
	tx.ToolResultReplacement = NewToolResultReplacementClient(tx.config)
	tx.TurnV2 = NewTurnV2Client(tx.config)
	tx.UsageQuota = NewUsageQuotaClient(tx.config)
	tx.UserEmbeddingSetting = NewUserEmbeddingSettingClient(tx.config)
}

// txDriver wraps the given dialect.Tx with a nop dialect.Driver implementation.
// The idea is to support transactions without adding any extra code to the builders.
// When a builder calls to driver.Tx(), it gets the same dialect.Tx instance.
// Commit and Rollback are nop for the internal builders and the user must call one
// of them in order to commit or rollback the transaction.
//
// If a closed transaction is embedded in one of the generated entities, and the entity
// applies a query, for example: Admin.QueryXXX(), the query will be executed
// through the driver which created this transaction.
//
// Note that txDriver is not goroutine safe.
type txDriver struct {
	// the driver we started the transaction from.
	drv dialect.Driver
	// tx is the underlying transaction.
	tx dialect.Tx
	// completion hooks.
	mu         sync.Mutex
	onCommit   []CommitHook
	onRollback []RollbackHook
}

// newTx creates a new transactional driver.
func newTx(ctx context.Context, drv dialect.Driver) (*txDriver, error) {
	tx, err := drv.Tx(ctx)
	if err != nil {
		return nil, err
	}
	return &txDriver{tx: tx, drv: drv}, nil
}

// Tx returns the transaction wrapper (txDriver) to avoid Commit or Rollback calls
// from the internal builders. Should be called only by the internal builders.
func (tx *txDriver) Tx(context.Context) (dialect.Tx, error) { return tx, nil }

// Dialect returns the dialect of the driver we started the transaction from.
func (tx *txDriver) Dialect() string { return tx.drv.Dialect() }

// Close is a nop close.
func (*txDriver) Close() error { return nil }

// Commit is a nop commit for the internal builders.
// User must call `Tx.Commit` in order to commit the transaction.
func (*txDriver) Commit() error { return nil }

// Rollback is a nop rollback for the internal builders.
// User must call `Tx.Rollback` in order to rollback the transaction.
func (*txDriver) Rollback() error { return nil }

// Exec calls tx.Exec.
func (tx *txDriver) Exec(ctx context.Context, query string, args, v any) error {
	return tx.tx.Exec(ctx, query, args, v)
}

// Query calls tx.Query.
func (tx *txDriver) Query(ctx context.Context, query string, args, v any) error {
	return tx.tx.Query(ctx, query, args, v)
}

var _ dialect.Driver = (*txDriver)(nil)

// ExecContext allows calling the underlying ExecContext method of the transaction if it is supported by it.
// See, database/sql#Tx.ExecContext for more information.
func (tx *txDriver) ExecContext(ctx context.Context, query string, args ...any) (stdsql.Result, error) {
	ex, ok := tx.tx.(interface {
		ExecContext(context.Context, string, ...any) (stdsql.Result, error)
	})
	if !ok {
		return nil, fmt.Errorf("Tx.ExecContext is not supported")
	}
	return ex.ExecContext(ctx, query, args...)
}

// QueryContext allows calling the underlying QueryContext method of the transaction if it is supported by it.
// See, database/sql#Tx.QueryContext for more information.
func (tx *txDriver) QueryContext(ctx context.Context, query string, args ...any) (*stdsql.Rows, error) {
	q, ok := tx.tx.(interface {
		QueryContext(context.Context, string, ...any) (*stdsql.Rows, error)
	})
	if !ok {
		return nil, fmt.Errorf("Tx.QueryContext is not supported")
	}
	return q.QueryContext(ctx, query, args...)
}
