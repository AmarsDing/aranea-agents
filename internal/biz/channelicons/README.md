# Channel platform icons

PNG assets (256×256) embedded for `avatar_assets` seed (`channel_*` keys).

- Source: [Simple Icons](https://simpleicons.org/) (CC0 1.0) via Iconify API.
- Regenerate: `go run ./cmd/fetch-channel-icons` (requires network).

Icons are rendered on brand-tinted squares for consistent picker appearance.
