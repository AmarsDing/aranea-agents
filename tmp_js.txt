(function(){const agents=document.querySelectorAll(".agent-card");const result=[];agents.forEach((a,i)=>{const name=a.querySelector(".agent-card__name, .agent-card__title, h4, h3")?.textContent?.trim()||"";const status=a.querySelector(".agent-card__status")?.textContent?.trim()||"";const thinking=a.querySelectorAll(".thinking-block, [class*=thinking]").length;const actions=a.querySelectorAll(".action-block, [class*=action]").length;const reply=a.querySelectorAll(".reply-block, [class*=reply]").length;const plan=a.querySelectorAll(".plan-block").length;const buttons=a.querySelectorAll("button").length;const isExpanded=a.classList.contains("agent-card--expanded");result.push({index:i,name:name.slice(0,40),status:status.slice(0,30),thinking,actions,reply,plan,buttons,isExpanded});});return JSON.stringify(result,null,2);})();