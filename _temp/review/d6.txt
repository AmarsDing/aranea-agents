diff --git a/web/src/stores/artifact/index.ts b/web/src/stores/artifact/index.ts
index 146a23272..3e2b66c38 100644
--- a/web/src/stores/artifact/index.ts
+++ b/web/src/stores/artifact/index.ts
@@ -10,6 +10,8 @@ import {
   signDownloadUrl,
   artifactDownloadHref,
   previewArtifact,
+  revealArtifact,
+  fetchLocalRevealEnabled,
 } from '../../features/artifact/api';
 import type {
   ArtifactData,
@@ -72,6 +74,16 @@ export const useArtifactStore = defineStore('artifact', () => {
     return previewArtifact(id, version);
   }
 
+  /** 本地打开文件夹（M27 Phase 5，默认关闭，仅本地单机部署启用）。 */
+  async function revealLocal(id: string): Promise<{ path: string }> {
+    return revealArtifact(id);
+  }
+
+  /** 探测本地 reveal 功能开关（探知失败按未启用处理）。 */
+  async function loadLocalRevealEnabled(): Promise<boolean> {
+    return fetchLocalRevealEnabled();
+  }
+
   return {
     artifacts,
     total,
@@ -84,6 +96,8 @@ export const useArtifactStore = defineStore('artifact', () => {
     listVersions,
     signDownload,
     loadPreview,
+    revealLocal,
+    loadLocalRevealEnabled,
     artifactDownloadHref,
   };
 });
