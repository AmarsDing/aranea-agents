// Package application hosts Conversation 用例：会话（session_service）、单智能体与团队回合
//（run_turn_handler、team_runtime_service、团队运行事件）。internal/service 对 ChatService
// 嵌入本包、对 SessionService 作类型别名以兼容原 import 路径。
package application
