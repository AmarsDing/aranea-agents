// Package clock abstracts wall-clock access to enable deterministic tests.
// Skeleton (P0): Clock interface and the production / fake implementations
// will land via row-level migrations.
package clock
