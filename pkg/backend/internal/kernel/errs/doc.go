// Package errs defines stable error Code string constants (codes.go),
// shared sentinel errors (error.go), and future Wrap/Is helpers for domain
// errors. Kernel must not import any internal/<context>/ package.
package errs
