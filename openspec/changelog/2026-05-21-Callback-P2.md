# Callback P2 — 审计查询 + modify 深度合并

**日期**：2026-05-21 · **模块**：Callback (28)

## 摘要

- **CB-01**：Hook `blocked`/`error` 写入 `plugin_runs`（`hook:<key>`）；运行记录页按 lifecycle/agent/status/时间筛选。
- **CB-02**：`merge_arguments` 深度合并；`arguments` 整包替换优先。
- **CB-03**：`features/callback/constants.ts` 统一筛选常量；Hooks 页深链至 Runs。

## 代码

| 文件 | 变更 |
|------|------|
| `internal/plugin/trpc/tool_argument_merge.go` | 深度合并策略 |
| `internal/plugin/trpc/hook_audit.go` | Hook 审计落库 |
| `internal/plugin/trpc/hook_callbacks.go` | 注入 StatsRecorder |
| `internal/data/plugin_run.go` | `plugin_key` 前缀 `hook:` LIKE |
| `web/src/features/callback/constants.ts` | 共享筛选常量 |
| `web/src/pages/PluginRunsPage.vue` | 筛选 + 详情 + 路由 query |
| `web/src/pages/HooksPage.vue` | 回调点筛选 + 运行记录链接 |

## 验证

```bash
go test ./internal/plugin/trpc/... -count=1
cd web && pnpm exec vue-tsc --noEmit
```
