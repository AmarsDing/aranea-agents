# Callback P3 — 编排策略 + Hook 投递队列

**日期**：2026-05-21 · **模块**：Callback (28)

## 摘要

- **CB-ARCH-01**：`callback_orchestration: "chain"` 可选将 Plugin 生命周期镜像进 LLMAgent Chain；内置插件与 `on_event` 强制 Runner，避免双触发。
- **CB-ARCH-02**：`hook_deliveries` 表 + `HookNotifier` 持久化 notify 并重试；Hook `notify_max_retries` / `notify_timeout_sec`。

## 代码

| 文件 | 变更 |
|------|------|
| `internal/plugin/trpc/orchestration_policy.go` | Runner/Chain 路径解析 |
| `internal/plugin/trpc/plugin_chain_mirror.go` | Plugin → Chain 适配 |
| `internal/plugin/trpc/hook_notify.go` | 投递队列与重试 |
| `internal/data/hook_delivery*.go` | 落库 |
| `internal/agent/callbacks/adapter.go` | `AfterToolHookFunc` |
| `docs/sql/28_callback_delivery.sql` | DDL |

## 验证

```bash
go test ./internal/plugin/trpc/... ./internal/agent/callbacks/... -count=1
```
