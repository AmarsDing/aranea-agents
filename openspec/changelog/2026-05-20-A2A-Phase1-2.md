# Changelog — A2A Phase 1–2（call_agent + Agent Kind）

**日期**：2026-05-20  
**模块**：A2A（26）、Agent 创建（2）、Agent 设置（5）

## 摘要

完成 A2A **Phase 1（P0）** 与 **Phase 2（P1）** 主体实现：`call_agent` 运行时打通、HTTP Invoke 派发、`agent_kind=a2a_proxy` 创建与 Chat 远程代理、LLM Agent A2A Endpoint Tab、列表徽章与 `/a2a` 运维页增强。

## 后端

- `internal/a2a/invoker.go`：`NewInvoker` + 工作区校验 + AgentCard capability 校验
- `internal/service/chat_native.go`：`RunAgentTurn`、`injectA2AContext`
- `internal/service/trpc_turn.go`：A2A 上下文注入；`a2a_proxy` 跳过 intent pass
- `internal/service/a2a.go`：Invoke 经 `AgentTurnRunner` 实际派发
- `internal/biz/agent_kind.go`：`agent_kind` / `a2a_proxy` config_json 读写
- `internal/agent/trpc_build_router.go`：`BuildTRPCAgent` / `BuildTRPCAgentCached`
- `internal/a2a/trpc/agent.go`：`BuildTRPCA2AAgent`（含 client timeout）
- `api/kratos/agent/v1/agent.proto`：`agent_kind`、`A2AProxyConfig`
- `internal/data/builtin_tools_seed.go`：种子 `call_agent`、`knowledge_search` 等
- `internal/biz/agent_effective_tools.go`：`group:integration` 纳入 `full` profile

## 前端

- `AgentCreateDialog.vue` / `useAgentsPage.ts`：LLM / A2A Proxy 创建
- `AgentSettingsA2ATab.vue` / `AgentSettingsA2AEndpointTab.vue`：设置页 A2A Tab
- `AgentCard.vue`：A2A ↗ / A2A ↙ 徽章
- `A2AInvokePanel.vue`：Discover 列表选 Agent/Capability
- Store 合规：A2A Tab 经 `useAgentDetailStore` / `useA2AStore`

## 未交付（Phase 3+）

> Phase 3 已交付，见 [2026-05-20-A2A-Phase3.md](./2026-05-20-A2A-Phase3.md)。

- 流式 Proxy / Endpoint SSE
- Graph resume via A2A
- 远程 registry 条目直接 Invoke
- A2A 网关注册中心

## 验证

- `go test ./internal/a2a/... ./internal/biz/... ./internal/service/...`
- `make runtime-boundary`
- `pnpm test && pnpm build`

## 优化（Code Review P1/P2）

- `CreateAgentRequest.provider/model` 不再 proto REQUIRED；`a2a_proxy` 可省略（服务端默认 `a2a/proxy`）
- `Agent.a2a_endpoint_enabled`：List/Get 批量 enrichment，列表页不再额外 Discover
- `agent_kind` 更新不可变（`agent_kind is immutable`）
- `invoker` / `RunAgentTurn` 校验错误统一为 `kerrors`
- `AgentService` 注入 `A2AUsecase`（Wire 已更新）

## 优化（第二轮）

- `internal/a2a/card_validate.go`：`CheckCalleeCard` 统一 Invoke / call_agent 校验
- `internal/a2a/trpc/auth.go`：`auth_type`（api_key / bearer）接线至 A2A client
- `A2AService.Invoke` 复用 `NewInvoker`，与 call_agent 结果格式一致
- Create / Update / ToggleFavorite / Get 均返回 `a2a_endpoint_enabled`
- A2A Proxy 设置 Tab 支持鉴权类型与密钥配置
