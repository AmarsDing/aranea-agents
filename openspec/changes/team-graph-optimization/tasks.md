## 1. M1: Fix P0 Business Bugs

- [x] 1.1 BUG-01: Add negation detection to `criticLoopCondFunc` in `internal/graph/adapter/critic_loop_cond.go` — add `containsNegationBeforeWord()` helper, update condition to reject "not approved" / "denied" / "rejected". DoD: `"not approved"` returns false; `"approved"` returns true; unit test passes
- [x] 1.2 BUG-02: Fix `finishRunErr` double event publish in `internal/team/runner_helpers.go:110-121` — remove `TeamRunFinished` publish on error path, keep only `TeamRunFailed`. DoD: failure path publishes exactly 1 event (`TeamRunFailed`)
- [x] 1.3 BUG-03: Fix non-deterministic entry point in `internal/team/embedded_graph.go:193-198` — sort `executableIDs` keys before selecting entry point. DoD: same definition produces same entry point across multiple runs
- [x] 1.4 BUG-04: Fix Fallback Agent bypass in `internal/graph/trpc/failure_recovery.go:33-35` — when `resolvedFallback` is nil, skip fallback attempt instead of using `NewAgentNodeFunc(fallback)`. DoD: fallback agent only invoked when properly resolved through resolver pipeline
- [x] 1.5 BUG-05: Fix hardcoded "key-" prefix in `internal/team/team_graph_run_finisher.go:128` — production code uses `memberNodeID()` correctly; "key-" prefix only in test helpers. DoD: verified production code correct
- [x] 1.6 BUG-06: Fix nil logger in `internal/graph/adapter/runtime_adapter.go:212` — pass `lg` parameter instead of `nil` to `NewEventBridge`. DoD: JSON deserialization failure logs error instead of panic
- [x] 1.7 BUG-07: Fix EventBridge per-event reconstruction in `internal/graph/adapter/runtime_adapter.go:210-213` — hold single `EventBridge` instance in `trpcGraphRuntime`, reuse across events. DoD: `execution_summary` contains all node records
- [x] 1.8 M1 verification: `make api && make wire && make build && make test && make lint`

## 2. M2: Fix P1 Race Conditions and State Safety

- [x] 2.1 ARCH-07: Fix TOCTOU race in `internal/biz/graph_execution.go:304-315` — copy data inside lock, write DB outside lock. DoD: no data race detected by `go test -race` — **已实现**：`execMu sync.RWMutex` 保护 Status/CurrentNode/Steps 等字段，`SnapshotForPersist()` 在锁内拷贝、锁外写 DB
- [x] 2.2 ARCH-08: Cancel runtime before GC eviction in `internal/biz/graph.go:291-296` — call `exec.runtime.Cancel()` before evicting from cache. DoD: no zombie goroutines after GC eviction — **已实现**：`gc()` 和 `evictIfNeeded()` 均在 eviction 前调用 `exec.runtime.Cancel()`
- [x] 2.3 ARCH-10: Add per-execution mutex in `internal/biz/graph_team_execution.go:68-75` — replace shared pointer with `sync.RWMutex` per `GraphExecution`. DoD: `MarkTeamGraphInterrupt` concurrent access is safe — **已实现**：`interruptMu sync.RWMutex` 保护 interrupted/InterruptNode，`execMu sync.RWMutex` 保护 Status/CurrentNode 等
- [x] 2.4 ARCH-06: Bind Circuit Breaker to GraphAgent instance in `internal/graph/trpc/circuit_breaker.go` + `builder.go` — replace package-level global map with instance-level map. DoD: no cross-session pollution; bounded growth — **已实现**：`CircuitBreakerState` 为实例级结构体，`BuildStateGraphWithAgents` 每次构建创建新实例，`GraphAgent.cbState` 持有实例引用
- [x] 2.5 ARCH-09: Add recovery path for evicted team build configs in `internal/biz/graph_team_execution.go` — as interim fix, reload from DB or recompile. DoD: resume succeeds after GC eviction (will be fully replaced by M3.8) — **已实现**：`buildConfigForExecution()` 先查内存缓存，miss 后从 `compiledTeamRepo.LoadForSession()` 加载
- [x] 2.6 M2 verification: `make api && make wire && make build && make test && make lint` — **已实现**：M2 所有子任务（2.1~2.5）均已在代码中实现，竞态修复和状态安全增强已落地

## 3. M3: Introduce CompiledTeam — Break Coupling Root

- [x] 3.1 Define `CompiledTeam`, `RoleInfo`, `NodeTaskMeta` structs in `internal/biz/compiled_team.go` (new file). DoD: structs compile; `CompiledTeam` embeds `GraphBuildConfig`; `NodeTaskMeta` has 8 fields; `RoleInfo` has 5 fields + `Capabilities []string` — **已实现**：`CompiledTeam` 值嵌入 `GraphBuildConfig`，`NodeTaskMeta` 8 字段，`RoleInfo` 5 字段 + `Capabilities []string`。**偏差**：`TaskMeta` 放在 `GraphBuildConfig` 上（第 12 字段）而非 `CompiledTeam` 独立字段，导致 `GraphBuildConfig` 为 12 字段而非预期的 11 字段
- [ ] 3.2 FailurePolicy compile-time expansion: in `internal/biz/failure_policy.go`, ensure `ApplyFailurePolicy` writes `NodeDef.FailureAction/FallbackAgent/RetryMaxAttempts`; in `internal/biz/graph.go`, remove `FailurePolicy *TeamFailurePolicy` field from `GraphBuildConfig`; update `internal/graph/trpc/builder.go` and `node_wiring.go` to consume expanded fields only. DoD: `GraphBuildConfig` has 12 fields (down from 13); `graph/trpc` no longer imports `biz.TeamFailurePolicy` — **部分实现**：`ApplyFailurePolicy` 已实现完整的展开逻辑（Retry→RetryMaxAttempts、Failover→FailureAction+FallbackAgent、Skip→FailureAction），`GraphBuildConfig` 从未包含 `FailurePolicy` 字段（DEV-02 确认为错误前提）；但 `graph/trpc/node_wiring.go` 的 `nodeOptions` 仍接收 `*biz.TeamFailurePolicy` 参数（用于 CircuitBreaker 回调注册），DoD 第二条未满足
- [ ] 3.3 ParallelBranchIDs compile-time expansion: in `internal/biz/failure_policy.go`, expand parallel branch nodes to `FailureAction="skip_on_failure"`; remove `ParallelBranchIDs []string` from `GraphBuildConfig`. DoD: `GraphBuildConfig` has 11 fields; parallel branch failure behavior preserved — **部分实现**：`ApplyParallelFailContinue` 已实现完整的展开逻辑（自动检测并行分支或使用显式 `parallelBranchIDs`，标记 `FailureAction="skip_on_failure"`），`ParallelBranchIDs` 从未在 `GraphBuildConfig` 上定义（DEV-02）；但 `ParallelBranchIDs` 仍作为参数传递给 `FinalizeGraphFailurePolicy`（从编译管线传入），且 `GraphBuildConfig` 因 TaskMeta 偏差为 12 字段而非 11
- [ ] 3.4 CircuitBreaker compile-time expansion: in `internal/graph/trpc/node_wiring.go`, write `RetryMaxAttempts` from breaker config; `nodeOptions` no longer receives `*biz.TeamFailurePolicy`. DoD: circuit breaker effect preserved via `RetryMaxAttempts`; `nodeOptions` has no Team policy dependency — **未实现**：`nodeOptions` 仍接收 `*biz.TeamFailurePolicy` 并通过 `policy.CircuitBreaker` 调用 `circuitBreakerOptions`
- [x] 3.5 Task field separation: move 8 Task fields from `NodeDef` to `NodeTaskMeta` in `internal/biz/graph.go`; update `internal/biz/graph_task_input.go`, `internal/team/embedded_graph.go`, `internal/graph/trpc/builder.go`. DoD: `NodeDef` has 20 fields; Task coordination logic reads from `CompiledTeam.TaskMeta` — **已实现**：`NodeDef` 当前 20 字段，8 个 Task 字段已移至 `NodeTaskMeta`，`GraphTaskInputFromNode` 从 `NodeTaskMeta` 读取
- [x] 3.6 Replace `CompileToGraphRuntimeConfig` with `CompileToCompiledTeam` in `internal/team/graph_runtime_config.go` + `internal/team/graph_compile.go` (new file). DoD: compilation produces `CompiledTeam`; all callers updated — **已实现**：`CompileToCompiledTeam` 在 `graph_compile.go` 中定义，`CompileToGraphRuntimeConfig` 委托给 `CompileToCompiledTeam`，`CompileToGraphRuntimeConfigFromJSON` 也委托
- [x] 3.7 Generate `RoleManifest` during compilation in `internal/team/graph_compile.go` — collect AgentKey/DisplayName/Role from catalog for each node. DoD: `CompiledTeam.RoleManifest` populated; `RoleInfo.AgentKey` matches runtime `ag.AgentKey` — **已实现**：`buildRoleManifest()` 在编译时为每个有 AgentName 的节点生成 `RoleInfo`，`Capabilities` 初始化为空切片
- [x] 3.8 Persist `CompiledTeam` to DB: add Ent schema for `compiled_team` table in `internal/data/ent/schema/`; implement `CompiledTeamRepo` in `internal/data/compiled_team_repo.go`; replace `teamBuildConfigs` in-memory cache in `internal/biz/graph_team_execution.go`. DoD: Team Graph survives process restart; GC eviction no longer breaks resume — **已实现**：Ent Schema（`ent/schema/compiled_team.go`）+ 手写 SQL DDL（`compiled_team_schema.go`）双定义，`CompiledTeamRepo` 接口含 Save/Load/LoadForSession/Delete（实际 CRUD 使用手写 SQL），`RegisterTeamGraphExecution` 同时写入内存和 DB，`buildConfigForExecution` 先查内存再查 DB。DDL 迁移通过 `ddl_migration_registry.go` 注册（版本 20260705 初始建表 + 版本 20260714 新增 `session_id` 字段）
- [ ] 3.9 M3 verification: `make api && make wire && make build && make test && make lint`; verify `graph/trpc` only imports `biz` constants (not `TeamFailurePolicy`)

## 4. M4: Graph Independence + biz/trpc Type Unification

- [ ] 4.1 biz/trpc type unification: in `internal/graph/trpc/builder.go`, replace dual-defined types with embedding + aliases — `type EdgeDef = biz.EdgeDef`, `type StateFieldDef = biz.StateFieldDef`, `type GraphBuildConfig = biz.GraphBuildConfig`; for `NodeDef` and `ConditionalEdgeDef`, embed biz type + add trpc-specific fields. DoD: 6 dual-source types eliminated — **部分实现**：`EdgeDef` 和 `StateFieldDef` 已为类型别名，`NodeDef` 和 `ConditionalEdgeDef` 已使用嵌入（`biz.NodeDef` + `Func trpcgraph.NodeFunc`、`biz.ConditionalEdgeDef` + `CondFunc any`）；但 `GraphBuildConfig` 和 `SubgraphDef` 仍为双源定义（trpc `GraphBuildConfig` 11 字段无 `TaskMeta`，biz `GraphBuildConfig` 12 字段含 `TaskMeta`；trpc `SubgraphDef` 含 `InputMapper`/`OutputMapper` 函数字段），DoD 未完全满足（6 组中 4 组已统一，2 组待完成）
- [ ] 4.2 Remove `bizCfgToTrpc`/`trpcCfgToBiz` functions from `internal/graph/adapter/runtime_adapter.go` — update callers to use unified types directly. DoD: ~93 lines of manual mapping code removed — **未实现**：`bizCfgToTrpc`/`trpcCfgToBiz` 仍存在于 `runtime_adapter.go`（约 46 行），因 `GraphBuildConfig` 和 `SubgraphDef` 仍为双源定义（trpc 层无 `TaskMeta`、有 `Func`/`CondFunc`/`InputMapper`/`OutputMapper` 函数字段），映射代码无法消除直到 4.1 完成
- [x] 4.3 Split `GraphBuilderFactory` into 5 narrow interfaces in `internal/biz/graph_runtime.go` + `internal/graph/adapter/`: `GraphRunnerFactory` (3 methods), `GraphVisualizer` (1 method), `GraphValidator` (1 method), `GraphTemplateProvider` (3 methods), `GraphNodeInfoProvider` (2 methods). DoD: no interface exceeds 5 methods — **已实现**：5 个窄接口（`GraphRunnerFactory` 3 方法、`GraphVisualizer` 1 方法、`GraphValidator` 1 方法、`GraphTemplateProvider` 3 方法、`GraphNodeInfoProvider` 2 方法）+ 复合接口 `GraphBuilderFactory`，均不超过 5 方法
- [ ] 4.4 Split `GraphRepo` into `GraphReader` + `GraphWriter` in `internal/biz/graph.go` + `internal/data/`. DoD: read and write paths separated
- [ ] 4.5 Split `GraphRuntime` into execution control + checkpoint in `internal/biz/graph_runtime.go`. DoD: execution methods and checkpoint methods on separate interfaces
- [ ] 4.6 M4 verification: `make api && make wire && make build && make test && make lint`

## 5. M5: Team Lifecycle Unification + Runner Refactoring

- [x] 5.1 Define `RunnerConfig` in `internal/team/runner_config.go` — consolidate 10 non-circular dependency fields into single struct. DoD: Runner constructor accepts `RunnerConfig` — **已实现**：`RunnerConfig` 包含 GraphLoader/TeamGraphTasks/AwaitHookProvider/Knowledge/StreamOptsFactory/AgentHelper/Runs/GraphRoot/PluginRT/PluginManager，`NewRunner` 接受 `RunnerConfig` 参数
- [ ] 5.2 Implement `TeamRunMediator` in `internal/team/runner_mediator.go` — mediate Runner ↔ Coordinator bidirectional dependency. DoD: only 2 Setter methods remain on Runner — **部分实现**：`TeamRunMediator` 结构体已定义（`runner_mediator.go`），实现 `TeamGraphCoordAccess`（4 方法：RegisterTeamGraphExecution/MarkTeamGraphInterrupt/DeferTeamRunSuccessIfHITL/StartGraphStepWatch）和 `TeamGraphRunFinisher`（2 方法：PersistGraphRunStep/FinalizeGraphTeamRun）双接口，含 `SetCoordinator`/`SetFinisher` 后置接线；但 Runner 仍直接持有 `teamGraphCoord *TeamGraphRunCoordinator`，仍有 5 个 Setter（SetTeamGraphRunCoordinator/SetAwaitHookProvider/SetRuns/SetStreamOptsFactory/SetAgentHelper），Mediator 尚未集成到 Runner 调用链中，DoD 未满足
- [x] 5.3 Encapsulate Knowledge 4 fields into `KnowledgeFacade` in `internal/team/runner.go` + `internal/knowledge/`. DoD: Runner holds single `knowledgeFacade` field — **已实现**：`KnowledgeFacade` 在 `runner_config.go` 中定义（Retriever/Router/FederatedRetriever/Evaluator），`RunnerConfig.Knowledge *KnowledgeFacade` 替代了 4 个独立字段
- [ ] 5.4 Split `GraphUsecase` into `GraphDefinitionUsecase` + `GraphExecutionUsecase` + `GraphCacheManager` in `internal/biz/graph.go` + `internal/biz/graph_*.go`. DoD: each usecase has ≤3 responsibilities — **部分实现**：`GraphDefinitionUsecase` 已从 `GraphUsecase` 中分离（`graph_definition_usecase.go`，含 CreateGraph/GetGraph/ListGraphs/UpdateGraph/DeleteGraph/ReorderGraphs 等 CRUD 方法），`GraphUsecase` 通过 `defUC *GraphDefinitionUsecase` 字段持有并委托定义操作（`DefUC()` 返回定义子用例）；但 `GraphExecutionUsecase` 和 `GraphCacheManager` 尚未独立拆分，执行逻辑（ExecuteGraph/ResumeGraph/CancelGraph）和缓存管理（teamBuildConfigs/evictIfNeeded/buildConfigForExecution）仍在 `GraphUsecase` 中
- [ ] 5.5 Eliminate `any` return types in `internal/biz/graph_runtime.go` — define biz-layer value types. DoD: no `any` returns in GraphRuntime interfaces — **未实现**：`GraphRuntime` 接口仍有 4 个 `any` 返回（`TimeTravelGetState`/`TimeTravelHistory`/`TimeTravelEditState`/`ListCheckpoints`），`GraphVisualizer.Visualize` 返回 `any`，`GraphTemplateProvider.ListTemplates`/`GetTemplate`/`TemplateToDef` 使用 `any` 参数或返回，`GraphRuntimeEvent.RawEvent` 为 `any` 类型
- [ ] 5.6 Optimize Team port interfaces (DES-03/04/05) in `internal/biz/team_ports.go` + `internal/biz/team_agent_ports.go`. DoD: `TeamAgentHelper` split into narrow interfaces; `TeamTurnRuntime`/`TeamBuildRunner` merged or clarified; `TeamTurnResult` decoupled from `ChatMessage` — **未实现**：`TeamAgentHelper` 仍为单一接口（8 方法：RFC3339Now/UserIDFromCtx/UserOptionsJSON/AssistantOptionsJSON/MergeReasoningIntoAssistantOptionsJSON/DisplayMarkdownFromStream/EstimateTokensIfMissing/ResolveRalphLoopTurn），`TeamTurnRuntime` 和 `TeamBuildRunner` 仍为独立接口，`TeamTurnResult` 仍包含 `ChatMessage` 类型字段
- [ ] 5.7 Integrate or clean up `FunctionResolver` (DES-08/09) in `internal/graph/trpc/node_wiring.go` + `internal/graph/trpc/build_deps.go`. DoD: either wired into DI or removed — **部分实现**：`FunctionResolver` 接口已定义在 `build_deps.go`（`ResolveFunction(ctx, funcRef) -> CallableTool`），已纳入 `GraphNodeResolverSet.Functions` 字段，`GraphNodeResolverSet` 已注入到 `trpcGraphBuilderFactory`，`Functions` 字段已在 DI 中接线（`wire.go` 中 `CatalogFunctionResolver` 赋值）；但 `wireNode` 中未消费 `deps.Functions`（函数节点仍通过 `FuncRef` 解析，未走 `FunctionResolver` 管线），`Subgraphs` 字段已定义但无实现，`BuildDeps` 已标记为 Deprecated 但仍通过 `ToBuildDepsPtr()` 使用
- [ ] 5.8 Batch fix P3 code quality issues (Q-01~Q-11) across multiple files. DoD: all P3 items resolved
- [ ] 5.9 M5 verification: `make api && make wire && make build && make test && make lint`

## 6. Documentation Sync + Review

- [ ] 6.1 Update `docs/team-graph问题与方案.md` — mark completed items, update field counts, add implementation notes
- [ ] 6.2 Update `docs/architecture-blueprint.md` — reflect CompiledTeam, new interfaces, type unification
- [ ] 6.3 Update `docs/module-cross-reference.md` — update Team/Graph module cards with new dependencies
- [ ] 6.4 Run `aranea-review` skill on all modified files
