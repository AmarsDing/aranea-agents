---
description: 页面实现时把可复用块抽到 src/components
globs: aranea/frontend/src/pages/**/*.vue
alwaysApply: false
---

# Aranea 前端：页面与组件分层

- **原则**：在 `src/pages`（及必要时 `src/layouts`）里**只保留页面级编排**（路由、数据拉取、组合子组件、少量布局类样式）。凡是**可在其它页面复用**、或**单块 UI/逻辑已复杂到难以扫读**的，应抽成**独立组件**，放在 **`src/components/`** 下，而不是堆在页面文件里。

- **命名与组织**：按域或功能分子目录也可以（例如 `components/chat/`），组件名用 **PascalCase**（如 `AgentListCard.vue`），与 Quasar 组合时保持与现有 `src` 风格一致。

- **何时抽组件**：列表项/卡片、表单区、带内部状态的独立面板、重复出现的工具栏/空状态/加载态、与路由无关的纯展示块等。

- **不抽的情况**：仅本页用一次、且行数很少、无测试或复用价值的内联，可暂留页面内；但新增功能时**优先**评估是否应成组件，避免页面文件无限膨胀。

完成新页面或大面积编辑后，若 `pages` 下单文件明显过长，应主动重构为 `components` 中的组合。
