// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type SystemSettings = {
  // Absolute or workspace-relative path used as the platform working directory.
  workDirectory: string | undefined;
  updateTime: wellKnownTimestamp | undefined;
  // Platform root path (install / deployment root for data and resources).
  rootDirectory: string | undefined;
  // Platform-wide monthly spend cap (micro-USD). 0 = disabled. Synced to usage_quotas scope global/global.
  globalMonthlyMicroUsd: number | undefined;
  // Public A2A endpoint URL prefix (no trailing slash). Empty uses env/config/derived chain.
  a2aPublicBaseUrl: string | undefined;
  // True when platform credential_encryption_key is stored (auto-generated on first use). Key value is not exposed.
  credentialEncryptionKeyConfigured: boolean | undefined;
  // Knowledge RAG embedder defaults (persisted; env KRATOS_KNOWLEDGE_EMBED_* overrides at startup).
  knowledgeEmbed: KnowledgeEmbedSettings | undefined;
  // Allow MCP Broker mcp_call against ad-hoc HTTP endpoints (production off by default).
  mcpAllowAdhocHttp: boolean | undefined;
  // Evaluation UserSim / LLM-as-Judge defaults (env KRATOS_EVAL_* overrides at runtime).
  evalLlm: EvalLLMSettings | undefined;
  // Web research (Tavily / SerpAPI) defaults for web_research tool. Env TAVILY_API_KEY / SERPAPI_API_KEY overrides api_key when DB empty.
  webResearch: WebResearchSettings | undefined;
  // Voice companion (M74) ASR/TTS provider config. Empty fields fall back to
  // SPEECH_* env vars at read time (field-level merge); credentials never returned.
  speech: SpeechSettings | undefined;
  // Platform default Refine LLM (PGO-3): shared by PromptRefiner / self-improvement
  // Analyst-Patcher-Critic / knowledge organizer / skill auto-creator / evolution
  // drafter. Credentials resolve via model catalog first; stored api_key is the
  // fallback and is never returned.
  defaultRefine: RefineLLMSettings | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type KnowledgeEmbedSettings = {
  provider: string | undefined;
  baseUrl: string | undefined;
  model: string | undefined;
  dim: number | undefined;
  configured: boolean | undefined;
  hasApiKey: boolean | undefined;
};

export type EvalLLMSettings = {
  simProvider: string | undefined;
  simModel: string | undefined;
  judgeProvider: string | undefined;
  judgeModel: string | undefined;
  configured: boolean | undefined;
};

export type WebResearchSettings = {
  provider: string | undefined;
  maxResults: number | undefined;
  fetchTop: number | undefined;
  searchDepth: string | undefined;
  timeoutSec: number | undefined;
  httpProxy: string | undefined;
  configured: boolean | undefined;
  hasApiKey: boolean | undefined;
};

export type SpeechSettings = {
  asr: SpeechASRSettings | undefined;
  tts: SpeechTTSSettings | undefined;
  // Stored voice-archive toggle. False may mean "explicitly off" or "unset →
  // env SPEECH_ARCHIVE_USER_AUDIO fallback" (tri-state collapsed for display).
  archiveUserAudio: boolean | undefined;
};

export type SpeechASRSettings = {
  driver: string | undefined;
  endpoint: string | undefined;
  resourceId: string | undefined;
  language: string | undefined;
  configured: boolean | undefined;
  hasApiKey: boolean | undefined;
};

export type SpeechTTSSettings = {
  driver: string | undefined;
  endpoint: string | undefined;
  resourceId: string | undefined;
  voice: string | undefined;
  speedRatio: number | undefined;
  configured: boolean | undefined;
  hasApiKey: boolean | undefined;
};

export type RefineLLMSettings = {
  provider: string | undefined;
  model: string | undefined;
  baseUrl: string | undefined;
  configured: boolean | undefined;
  hasApiKey: boolean | undefined;
};

export type UpdateSystemSettingsRequest = {
  workDirectory: string | undefined;
  rootDirectory: string | undefined;
  globalMonthlyMicroUsd: number | undefined;
  a2aPublicBaseUrl: string | undefined;
  knowledgeEmbedProvider: string | undefined;
  knowledgeEmbedBaseUrl: string | undefined;
  // Empty = do not change stored key; non-empty replaces.
  knowledgeEmbedApiKey: string | undefined;
  knowledgeEmbedModel: string | undefined;
  knowledgeEmbedDim: number | undefined;
  mcpAllowAdhocHttp: boolean | undefined;
  evalSimProvider: string | undefined;
  evalSimModel: string | undefined;
  evalJudgeProvider: string | undefined;
  evalJudgeModel: string | undefined;
  webResearchProvider: string | undefined;
  // Empty = do not change stored key; non-empty replaces.
  webResearchApiKey: string | undefined;
  webResearchMaxResults: number | undefined;
  webResearchFetchTop: number | undefined;
  webResearchSearchDepth: string | undefined;
  webResearchTimeoutSec: number | undefined;
  webResearchHttpProxy: string | undefined;
  // Voice companion speech settings (M74 V2-T7). Empty string / 0 = keep stored
  // value; credential fields: empty = do not change stored key, non-empty replaces.
  speechAsrDriver: string | undefined;
  speechAsrEndpoint: string | undefined;
  speechAsrAppKey: string | undefined;
  speechAsrAccessKey: string | undefined;
  speechAsrResourceId: string | undefined;
  speechAsrLanguage: string | undefined;
  speechTtsDriver: string | undefined;
  speechTtsEndpoint: string | undefined;
  speechTtsAppKey: string | undefined;
  speechTtsAccessKey: string | undefined;
  speechTtsResourceId: string | undefined;
  speechTtsVoice: string | undefined;
  speechTtsSpeedRatio: number | undefined;
  // Tri-state via proto3 optional: unset = keep stored value; explicit true/false
  // replaces (explicit value ends env fallback for the toggle).
  speechArchiveUserAudio?: boolean;
  // X-Api-Key 鉴权模式（火山控制台新 API Key，M74 真机校准）：write-only，
  // 非空即替换并存值；与 app_key/access_key legacy 对并存，api_key 非空优先。
  speechAsrApiKey: string | undefined;
  speechTtsApiKey: string | undefined;
  // Platform default Refine LLM (PGO-3-PROTO-02). Section applied when any of
  // provider/model/base_url is non-empty (same convention as knowledge_embed);
  // api_key: empty = do not change stored key, non-empty replaces.
  refineLlmProvider: string | undefined;
  refineLlmModel: string | undefined;
  refineLlmBaseUrl: string | undefined;
  refineLlmApiKey: string | undefined;
};

export type TestWebResearchRequest = {
  provider: string | undefined;
  // Empty = use stored system_settings key or env fallback.
  apiKey: string | undefined;
  maxResults: number | undefined;
  fetchTop: number | undefined;
  searchDepth: string | undefined;
  timeoutSec: number | undefined;
  httpProxy: string | undefined;
};

export type TestWebResearchResponse = {
  ok: boolean | undefined;
  message: string | undefined;
  provider: string | undefined;
  resultCount: number | undefined;
  latencyMs: number | undefined;
  responseTimeSec: number | undefined;
};

export interface SystemSettingService {
  GetSystemSettings(request: wellKnownEmpty): Promise<SystemSettings>;
  UpdateSystemSettings(request: UpdateSystemSettingsRequest): Promise<SystemSettings>;
  TestWebResearch(request: TestWebResearchRequest): Promise<TestWebResearchResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createSystemSettingServiceClient(
  handler: RequestHandler
): SystemSettingService {
  return {
    GetSystemSettings(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/system-settings`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SystemSettingService",
        method: "GetSystemSettings",
      }) as Promise<SystemSettings>;
    },
    UpdateSystemSettings(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/system-settings`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "SystemSettingService",
        method: "UpdateSystemSettings",
      }) as Promise<SystemSettings>;
    },
    TestWebResearch(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/system-settings/web-research/test`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SystemSettingService",
        method: "TestWebResearch",
      }) as Promise<TestWebResearchResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
