// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// ComputerUseStep 一步桌面动作的审计记录（监控页步骤流）。
export type ComputerUseStep = {
  id: number | undefined;
  sessionId: string | undefined;
  agentKey: string | undefined;
  stepIndex: number | undefined;
  target: string | undefined;
  path: string | undefined;
  action: string | undefined;
  paramsJson: string | undefined;
  result: string | undefined;
  error: string | undefined;
  durationMs: number | undefined;
  confirmedBy: string | undefined;
  danger: boolean | undefined;
  createdAt: string | undefined;
  screenshotRef: string | undefined;
  degraded: boolean | undefined;
};

export type KillComputerUseSessionRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type KillComputerUseSessionResponse = {
  sessionId: string | undefined;
  status: string | undefined;
};

export type ListComputerUseStepsRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ListComputerUseStepsResponse = {
  items: ComputerUseStep[] | undefined;
};

// GetComputerUseStatusResponse sidecar / 视觉组件健康状态。
export type GetComputerUseStatusResponse = {
  sidecar: string | undefined;
  platform: string | undefined;
  scaleFactor: number | undefined;
  vision: boolean | undefined;
};

export interface ComputerUseService {
  // 急停：取消进行中动作并将会话置为 cancelled。
  KillComputerUseSession(request: KillComputerUseSessionRequest): Promise<KillComputerUseSessionResponse>;
  // 审计步骤查询（监控页）。
  ListComputerUseSteps(request: ListComputerUseStepsRequest): Promise<ListComputerUseStepsResponse>;
  // sidecar / OmniParser 健康状态。
  GetComputerUseStatus(request: wellKnownEmpty): Promise<GetComputerUseStatusResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createComputerUseServiceClient(
  handler: RequestHandler
): ComputerUseService {
  return {
    KillComputerUseSession(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/computeruse/sessions/${request.id}/kill`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ComputerUseService",
        method: "KillComputerUseSession",
      }) as Promise<KillComputerUseSessionResponse>;
    },
    ListComputerUseSteps(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/computeruse/sessions/${request.id}/steps`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ComputerUseService",
        method: "ListComputerUseSteps",
      }) as Promise<ListComputerUseStepsResponse>;
    },
    GetComputerUseStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/computeruse/status`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ComputerUseService",
        method: "GetComputerUseStatus",
      }) as Promise<GetComputerUseStatusResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
