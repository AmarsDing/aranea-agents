// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ToolPermissions = {
  canManage: boolean | undefined;
};

export type Tool = {
  id: string | undefined;
  key: string | undefined;
  displayName: string | undefined;
  description: string | undefined;
  category: string | undefined;
  source: string | undefined;
  riskLevel: string | undefined;
  enabled: boolean | undefined;
  readonly: boolean | undefined;
  requiresConfirmation: boolean | undefined;
  supportsStreaming: boolean | undefined;
  supportsConcurrency: boolean | undefined;
  parametersSchemaJson: string | undefined;
  resultSchemaJson: string | undefined;
  configSchemaJson: string | undefined;
  configJson: string | undefined;
  defaultConfigJson: string | undefined;
  metadataJson: string | undefined;
  runtimeStatus: string | undefined;
  runtimeKind: string | undefined;
  invokeCount: number | undefined;
  invokeCount24h: number | undefined;
  successCount: number | undefined;
  failureCount: number | undefined;
  blockedCount: number | undefined;
  agentOverrideCount: number | undefined;
  // repaired_count/invalid_count：修复守卫标记聚合（90 天统计窗口）。
  // 参数一次合法率 = 1 - (repaired_count + invalid_count) / invoke_count。
  repairedCount: number | undefined;
  invalidCount: number | undefined;
  avgDurationMs?: number;
  p95DurationMs: number | undefined;
  lastInvokedAt: string | undefined;
  lastStatus: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  permissions: ToolPermissions | undefined;
};

export type ToolSummary = {
  totalTools: number | undefined;
  enabledTools: number | undefined;
  highRiskEnabled: number | undefined;
  calls24h: number | undefined;
  failureRate24h: number | undefined;
};

export type ListToolsRequest = {
  search: string | undefined;
  category: string | undefined;
  source: string | undefined;
  riskLevel: string | undefined;
  enabled: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
  sort: string | undefined;
  // abnormal=true：仅返回最近一次调用以 error/blocked 收尾的工具（「仅看异常」）。
  abnormal: boolean | undefined;
};

export type ListToolsResponse = {
  items: Tool[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
  summary: ToolSummary | undefined;
};

export type GetToolRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CreateToolRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  displayName: string | undefined;
  description: string | undefined;
  category: string | undefined;
  source: string | undefined;
  riskLevel: string | undefined;
  enabled: boolean | undefined;
  readonly: boolean | undefined;
  requiresConfirmation: boolean | undefined;
  supportsStreaming: boolean | undefined;
  supportsConcurrency: boolean | undefined;
  parametersSchemaJson: string | undefined;
  resultSchemaJson: string | undefined;
  configSchemaJson: string | undefined;
  configJson: string | undefined;
  defaultConfigJson: string | undefined;
  metadataJson: string | undefined;
};

export type UpdateToolRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  displayName: string | undefined;
  description: string | undefined;
  category: string | undefined;
  source: string | undefined;
  riskLevel: string | undefined;
  enabled: boolean | undefined;
  readonly: boolean | undefined;
  requiresConfirmation: boolean | undefined;
  supportsStreaming: boolean | undefined;
  supportsConcurrency: boolean | undefined;
  parametersSchemaJson: string | undefined;
  resultSchemaJson: string | undefined;
  configSchemaJson: string | undefined;
  configJson: string | undefined;
  defaultConfigJson: string | undefined;
  metadataJson: string | undefined;
};

export type DeleteToolRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ToggleToolEnabledRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  enabled: boolean | undefined;
  // confirm_intent must be "I_UNDERSTAND_RISK" when enabling high/critical
  // risk tools. Replaces the old confirm_key which matched the tool key
  // (a guessable value that provided no real security).
  confirmIntent: string | undefined;
};

export type ToolInvocation = {
  id: string | undefined;
  requestId: string | undefined;
  invocationId: string | undefined;
  toolId: string | undefined;
  toolKey: string | undefined;
  toolDisplayName: string | undefined;
  agentId: string | undefined;
  agentKey: string | undefined;
  agentDisplayName: string | undefined;
  sessionId: string | undefined;
  messageId: string | undefined;
  userId: string | undefined;
  source: string | undefined;
  status: string | undefined;
  startedAt: string | undefined;
  endedAt: string | undefined;
  durationMs: number | undefined;
  inputPreview: string | undefined;
  inputHash: string | undefined;
  outputPreview: string | undefined;
  outputHash: string | undefined;
  errorCode: string | undefined;
  errorMessage: string | undefined;
  redactionApplied: boolean | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  streaming: boolean | undefined;
  chunkCount: number | undefined;
};

export type ListToolRunsRequest = {
  toolKey: string | undefined;
  agentId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
  hasError?: boolean;
};

export type ListToolRunsResponse = {
  items: ToolInvocation[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListToolRunsForToolRequest = {
  //
  // Behaviors: REQUIRED
  toolId: string | undefined;
  agentId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ToolAgentOverride = {
  id: string | undefined;
  toolId: string | undefined;
  toolKey: string | undefined;
  agentId: string | undefined;
  // enabled is legacy: runtime enablement is decided by mode alone
  // (inherit/allow/deny); this field is always stored as true.
  enabled: boolean | undefined;
  mode: string | undefined;
  configOverrideJson: string | undefined;
  requiresConfirmation: boolean | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListToolAgentOverridesRequest = {
  //
  // Behaviors: REQUIRED
  toolId: string | undefined;
};

export type ListToolAgentOverridesResponse = {
  items: ToolAgentOverride[] | undefined;
};

export type ListToolAgentOverridesByAgentRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type ListToolAgentOverridesByAgentResponse = {
  items: ToolAgentOverride[] | undefined;
};

// ToolAgentBinding is one agent's effective state for a specific tool,
// computed server-side in bulk (replaces the N+1 per-agent effective scan).
export type ToolAgentBinding = {
  agentId: string | undefined;
  agentKey: string | undefined;
  agentName: string | undefined;
  // agent_status is the agent lifecycle status (enabled / disabled / ...).
  agentStatus: string | undefined;
  // tools_enabled is the agent-level master switch for tool calling.
  toolsEnabled: boolean | undefined;
  profile: string | undefined;
  // effective_state: allowed | denied (mirrors EffectiveAgentTool.effective_state).
  effectiveState: string | undefined;
  reason: string | undefined;
  // override_mode: allow | deny | inherit when a per-agent override row exists, "" otherwise.
  overrideMode: string | undefined;
};

export type GetToolAgentBindingsRequest = {
  //
  // Behaviors: REQUIRED
  toolId: string | undefined;
};

export type ToolAgentBindingsView = {
  items: ToolAgentBinding[] | undefined;
};

// ToolGrant is a persisted "always allow" grant recorded when a user
// approves a tool confirmation with the always scope.
export type ToolGrant = {
  id: string | undefined;
  agentId: string | undefined;
  toolKey: string | undefined;
  grantedBy: string | undefined;
  createdAt: string | undefined;
};

export type ListToolGrantsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type ListToolGrantsResponse = {
  items: ToolGrant[] | undefined;
};

export type DeleteToolGrantRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  toolKey: string | undefined;
};

export type UpsertToolAgentOverrideRequest = {
  //
  // Behaviors: REQUIRED
  toolId: string | undefined;
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  // enabled is legacy (see ToolAgentOverride): callers should send true.
  enabled: boolean | undefined;
  mode: string | undefined;
  configOverrideJson: string | undefined;
  requiresConfirmation: boolean | undefined;
};

export type DeleteToolAgentOverrideRequest = {
  //
  // Behaviors: REQUIRED
  toolId: string | undefined;
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type ToolInvocationParam = {
  id: string | undefined;
  invocationId: string | undefined;
  toolKey: string | undefined;
  paramsJson: string | undefined;
  redactionApplied: boolean | undefined;
  createdAt: string | undefined;
};

export type GetToolInvocationParamsRequest = {
  //
  // Behaviors: REQUIRED
  invocationId: string | undefined;
};

export type UpdateToolConfigRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  configJson: string | undefined;
};

export type TestToolRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  argumentsJson: string | undefined;
  timeoutSec: number | undefined;
};

export type TestToolResponse = {
  status: string | undefined;
  resultPreview: string | undefined;
  errorMessage: string | undefined;
  durationMs: number | undefined;
};

export type ToolInvocationAudit = {
  id: string | undefined;
  invocationId: string | undefined;
  toolKey: string | undefined;
  agentId: string | undefined;
  userId: string | undefined;
  sessionId: string | undefined;
  action: string | undefined;
  resultSummary: string | undefined;
  status: string | undefined;
  source: string | undefined;
  createdAt: string | undefined;
};

export type ListToolInvocationAuditsRequest = {
  toolKey: string | undefined;
  agentId: string | undefined;
  userId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListToolInvocationAuditsResponse = {
  items: ToolInvocationAudit[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export interface ToolService {
  ListTools(request: ListToolsRequest): Promise<ListToolsResponse>;
  // Static GET paths MUST be registered before /v1/tools/{id} to avoid
  // gorilla/mux matching the variable segment first (e.g. "runs" as {id}).
  ListToolRuns(request: ListToolRunsRequest): Promise<ListToolRunsResponse>;
  ListToolInvocationAudits(request: ListToolInvocationAuditsRequest): Promise<ListToolInvocationAuditsResponse>;
  GetToolInvocationParams(request: GetToolInvocationParamsRequest): Promise<ToolInvocationParam>;
  GetTool(request: GetToolRequest): Promise<Tool>;
  CreateTool(request: CreateToolRequest): Promise<Tool>;
  UpdateTool(request: UpdateToolRequest): Promise<Tool>;
  DeleteTool(request: DeleteToolRequest): Promise<wellKnownEmpty>;
  ToggleToolEnabled(request: ToggleToolEnabledRequest): Promise<Tool>;
  ListToolRunsForTool(request: ListToolRunsForToolRequest): Promise<ListToolRunsResponse>;
  ListToolAgentOverrides(request: ListToolAgentOverridesRequest): Promise<ListToolAgentOverridesResponse>;
  GetToolAgentBindings(request: GetToolAgentBindingsRequest): Promise<ToolAgentBindingsView>;
  ListToolAgentOverridesByAgent(request: ListToolAgentOverridesByAgentRequest): Promise<ListToolAgentOverridesByAgentResponse>;
  ListToolGrants(request: ListToolGrantsRequest): Promise<ListToolGrantsResponse>;
  DeleteToolGrant(request: DeleteToolGrantRequest): Promise<wellKnownEmpty>;
  UpsertToolAgentOverride(request: UpsertToolAgentOverrideRequest): Promise<ToolAgentOverride>;
  DeleteToolAgentOverride(request: DeleteToolAgentOverrideRequest): Promise<wellKnownEmpty>;
  UpdateToolConfig(request: UpdateToolConfigRequest): Promise<Tool>;
  TestTool(request: TestToolRequest): Promise<TestToolResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createToolServiceClient(
  handler: RequestHandler
): ToolService {
  return {
    ListTools(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tools`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.search) {
        queryParams.push(`search=${encodeURIComponent(request.search.toString())}`)
      }
      if (request.category) {
        queryParams.push(`category=${encodeURIComponent(request.category.toString())}`)
      }
      if (request.source) {
        queryParams.push(`source=${encodeURIComponent(request.source.toString())}`)
      }
      if (request.riskLevel) {
        queryParams.push(`riskLevel=${encodeURIComponent(request.riskLevel.toString())}`)
      }
      if (request.enabled) {
        queryParams.push(`enabled=${encodeURIComponent(request.enabled.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.sort) {
        queryParams.push(`sort=${encodeURIComponent(request.sort.toString())}`)
      }
      if (request.abnormal) {
        queryParams.push(`abnormal=${encodeURIComponent(request.abnormal.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListTools",
      }) as Promise<ListToolsResponse>;
    },
    ListToolRuns(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tools/runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.toolKey) {
        queryParams.push(`toolKey=${encodeURIComponent(request.toolKey.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`)
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.hasError) {
        queryParams.push(`hasError=${encodeURIComponent(request.hasError.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolRuns",
      }) as Promise<ListToolRunsResponse>;
    },
    ListToolInvocationAudits(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tools/audits`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.toolKey) {
        queryParams.push(`toolKey=${encodeURIComponent(request.toolKey.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`)
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolInvocationAudits",
      }) as Promise<ListToolInvocationAuditsResponse>;
    },
    GetToolInvocationParams(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.invocationId) {
        throw new Error("missing required field request.invocation_id");
      }
      const path = `v1/tools/runs/${request.invocationId}/params`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "GetToolInvocationParams",
      }) as Promise<ToolInvocationParam>;
    },
    GetTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "GetTool",
      }) as Promise<Tool>;
    },
    CreateTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/tools`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ToolService",
        method: "CreateTool",
      }) as Promise<Tool>;
    },
    UpdateTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "ToolService",
        method: "UpdateTool",
      }) as Promise<Tool>;
    },
    DeleteTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "ToolService",
        method: "DeleteTool",
      }) as Promise<wellKnownEmpty>;
    },
    ToggleToolEnabled(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}/enabled`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "ToolService",
        method: "ToggleToolEnabled",
      }) as Promise<Tool>;
    },
    ListToolRunsForTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.toolId) {
        throw new Error("missing required field request.tool_id");
      }
      const path = `v1/tools/${request.toolId}/runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`)
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolRunsForTool",
      }) as Promise<ListToolRunsResponse>;
    },
    ListToolAgentOverrides(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.toolId) {
        throw new Error("missing required field request.tool_id");
      }
      const path = `v1/tools/${request.toolId}/agent-overrides`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolAgentOverrides",
      }) as Promise<ListToolAgentOverridesResponse>;
    },
    GetToolAgentBindings(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.toolId) {
        throw new Error("missing required field request.tool_id");
      }
      const path = `v1/tools/${request.toolId}/agent-bindings`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "GetToolAgentBindings",
      }) as Promise<ToolAgentBindingsView>;
    },
    ListToolAgentOverridesByAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/tool-overrides`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolAgentOverridesByAgent",
      }) as Promise<ListToolAgentOverridesByAgentResponse>;
    },
    ListToolGrants(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/tool-grants`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ToolService",
        method: "ListToolGrants",
      }) as Promise<ListToolGrantsResponse>;
    },
    DeleteToolGrant(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.toolKey) {
        throw new Error("missing required field request.tool_key");
      }
      const path = `v1/agents/${request.agentId}/tool-grants/${request.toolKey}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "ToolService",
        method: "DeleteToolGrant",
      }) as Promise<wellKnownEmpty>;
    },
    UpsertToolAgentOverride(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.toolId) {
        throw new Error("missing required field request.tool_id");
      }
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/tools/${request.toolId}/agent-overrides/${request.agentId}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "ToolService",
        method: "UpsertToolAgentOverride",
      }) as Promise<ToolAgentOverride>;
    },
    DeleteToolAgentOverride(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.toolId) {
        throw new Error("missing required field request.tool_id");
      }
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/tools/${request.toolId}/agent-overrides/${request.agentId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "ToolService",
        method: "DeleteToolAgentOverride",
      }) as Promise<wellKnownEmpty>;
    },
    UpdateToolConfig(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}/config`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "ToolService",
        method: "UpdateToolConfig",
      }) as Promise<Tool>;
    },
    TestTool(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/tools/${request.id}/test`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ToolService",
        method: "TestTool",
      }) as Promise<TestToolResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
