// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type GetEvolutionDiversityOverviewRequest = {
  // 观测窗口起点（含）；缺省时后端给默认值（最近 24h）。
  since: wellKnownTimestamp | undefined;
  // 每桶 dims.tools 频次 TopN 截断；<=0 由后端给默认值。
  topTools: number | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type EvolutionDiversityBucket = {
  triggerSource: string | undefined;
  // 窗口内建议数。
  count: number | undefined;
  // 窗口内最近建议时间；长期停滞即塌缩信号。
  latestAt: wellKnownTimestamp | undefined;
  // dims.tools 频次 TopN（同频次按工具名字典序，保证稳定）。
  topTools: string[] | undefined;
};

export type GetEvolutionDiversityOverviewResponse = {
  // 按 count 降序、trigger_source 字典序 tie-break。
  buckets: EvolutionDiversityBucket[] | undefined;
};

// EvolutionService 是统一进化建议（unified_evolution_suggestions）的平台级
// 观测入口：跨 target（agent/skill/persona/...）的聚合视图，区别于
// AgentService / SkillEvolutionSuggestionService 的单 target 视角。
export interface EvolutionService {
  // GetEvolutionDiversityOverview 平台级进化多样性聚合观测（P3 M5）：
  // 按 trigger_source 分桶，统计窗口内建议数、最近建议时间、dims.tools
  // 频次 TopN。某桶长期停滞即搜索塌缩信号。
  GetEvolutionDiversityOverview(request: GetEvolutionDiversityOverviewRequest): Promise<GetEvolutionDiversityOverviewResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createEvolutionServiceClient(
  handler: RequestHandler
): EvolutionService {
  return {
    GetEvolutionDiversityOverview(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/evolution/diversity-overview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.since) {
        queryParams.push(`since=${encodeURIComponent(request.since.toString())}`)
      }
      if (request.topTools) {
        queryParams.push(`topTools=${encodeURIComponent(request.topTools.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "EvolutionService",
        method: "GetEvolutionDiversityOverview",
      }) as Promise<GetEvolutionDiversityOverviewResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
