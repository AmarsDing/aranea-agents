// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// Mirrors legacy query `/model-usage/*` GET params.
export type UsageQuery = {
  range: string | undefined;
  startDate: string | undefined;
  endDate: string | undefined;
  providerCode: string | undefined;
  modelApiId: string | undefined;
  agentId: string | undefined;
  status: string | undefined;
  limit: number | undefined;
  granularity: string | undefined;
  teamId: string | undefined;
  usageKind: string | undefined;
};

export type UsageSummary = {
  callCount: number | undefined;
  requestCount: number | undefined;
  successCount: number | undefined;
  failedCount: number | undefined;
  cancelledCount: number | undefined;
  inputTokens: number | undefined;
  outputTokens: number | undefined;
  totalTokens: number | undefined;
  totalCostMicroUsd: number | undefined;
  avgLatencyMs: number | undefined;
  avgTokensPerSecond: number | undefined;
  successRate: number | undefined;
};

export type UsageTrendPoint = {
  dateKey: string | undefined;
  callCount: number | undefined;
  inputTokens: number | undefined;
  outputTokens: number | undefined;
  totalTokens: number | undefined;
  totalCostMicroUsd: number | undefined;
  successCount: number | undefined;
  failedCount: number | undefined;
  cancelledCount: number | undefined;
  avgLatencyMs: number | undefined;
  avgTokensPerSecond: number | undefined;
};

export type UsageBreakdownRow = {
  providerCode: string | undefined;
  modelApiId: string | undefined;
  modelDisplayName: string | undefined;
  agentId: string | undefined;
  agentKey: string | undefined;
  callCount: number | undefined;
  inputTokens: number | undefined;
  outputTokens: number | undefined;
  totalTokens: number | undefined;
  totalCostMicroUsd: number | undefined;
  avgLatencyMs: number | undefined;
  avgTokensPerSecond: number | undefined;
  successRate: number | undefined;
};

// UsageModelInsight marks models with high cost and poor efficiency (low TPS / failures).
export type UsageModelInsight = {
  providerCode: string | undefined;
  modelApiId: string | undefined;
  modelDisplayName: string | undefined;
  callCount: number | undefined;
  totalTokens: number | undefined;
  totalCostMicroUsd: number | undefined;
  avgLatencyMs: number | undefined;
  avgTokensPerSecond: number | undefined;
  successRate: number | undefined;
  flags: string[] | undefined;
};

export type TokenUsageEvent = {
  id: string | undefined;
  occurredAt: string | undefined;
  dateKey: string | undefined;
  hourKey: string | undefined;
  workspaceId: string | undefined;
  userId: string | undefined;
  teamId: string | undefined;
  agentId: string | undefined;
  agentKey: string | undefined;
  sessionId: string | undefined;
  messageId: string | undefined;
  requestId: string | undefined;
  providerCode: string | undefined;
  providerType: string | undefined;
  providerDisplayName: string | undefined;
  modelApiId: string | undefined;
  modelDisplayName: string | undefined;
  modelCategoryJson: string | undefined;
  usageKind: string | undefined;
  callCount: number | undefined;
  inputTokens: number | undefined;
  outputTokens: number | undefined;
  cachedInputTokens: number | undefined;
  reasoningTokens: number | undefined;
  embeddingTokens: number | undefined;
  totalTokens: number | undefined;
  inputPriceMicroUsdPer1k: number | undefined;
  outputPriceMicroUsdPer1k: number | undefined;
  cachedInputPriceMicroUsdPer1k: number | undefined;
  reasoningPriceMicroUsdPer1k: number | undefined;
  embeddingPriceMicroUsdPer1k: number | undefined;
  inputCostMicroUsd: number | undefined;
  outputCostMicroUsd: number | undefined;
  cachedInputCostMicroUsd: number | undefined;
  reasoningCostMicroUsd: number | undefined;
  embeddingCostMicroUsd: number | undefined;
  totalCostMicroUsd: number | undefined;
  latencyMs: number | undefined;
  timeToFirstTokenMs: number | undefined;
  tokensPerSecond: number | undefined;
  status: string | undefined;
  errorCode: string | undefined;
  errorMessage: string | undefined;
  retryCount: number | undefined;
  promptMode: string | undefined;
  maxOutputTokens: number | undefined;
  contextWindowK: number | undefined;
  streamEnabled: boolean | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
};

export type UsageQuotaDashboard = {
  configuredCount: number | undefined;
  totalCapMicroUsd: number | undefined;
  totalSpentMicroUsd: number | undefined;
  maxUtilizationRatio: number | undefined;
};

export type UsageOverview = {
  today: UsageSummary | undefined;
  yesterday: UsageSummary | undefined;
  month: UsageSummary | undefined;
  rangeSummary: UsageSummary | undefined;
  trends: UsageTrendPoint[] | undefined;
  topModels: UsageBreakdownRow[] | undefined;
  topAgents: UsageBreakdownRow[] | undefined;
  anomalies: TokenUsageEvent[] | undefined;
  quotaDashboard: UsageQuotaDashboard | undefined;
  inefficientModels: UsageModelInsight[] | undefined;
};

export type BudgetAlert = {
  id: string | undefined;
  scopeType: string | undefined;
  scopeId: string | undefined;
  alertRatio: number | undefined;
  enabled: boolean | undefined;
  lastFiredAt: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListBudgetAlertsRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
};

export type ListBudgetAlertsResponse = {
  items: BudgetAlert[] | undefined;
};

export type SetBudgetAlertRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  alertRatio: number | undefined;
  enabled: boolean | undefined;
};

export type ExportUsageEventsResponse = {
  csv: string | undefined;
};

export type ListUsageTrendsResponse = {
  items: UsageTrendPoint[] | undefined;
};

export type ListBreakdownResponse = {
  items: UsageBreakdownRow[] | undefined;
};

export type ListUsageEventsResponse = {
  items: TokenUsageEvent[] | undefined;
};

export type UsageQuota = {
  id: string | undefined;
  scopeType: string | undefined;
  scopeId: string | undefined;
  monthlyMicroUsd: number | undefined;
  periodStart: string | undefined;
  periodEnd: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type GetUsageQuotaRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
};

export type SetUsageQuotaRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  monthlyMicroUsd: number | undefined;
  periodStart: string | undefined;
  periodEnd: string | undefined;
};

export type CheckUsageQuotaRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
};

export type CheckUsageQuotaResponse = {
  allowed: boolean | undefined;
  quota: UsageQuota | undefined;
  spentMicroUsd: number | undefined;
  remainingMicroUsd: number | undefined;
  reason: string | undefined;
};

export type PurgeUsageEventsRequest = {
  retainDays: number | undefined;
};

export type PurgeUsageEventsResponse = {
  deletedCount: number | undefined;
};

// ListAllModelsBreakdownRequest mirrors api/kratos/usage/v1/usage.proto ListAllModelsBreakdownRequest.
// Unlike UsageQuery (top-N capped), this supports server-side pagination + LIKE search + dynamic sort.
export type ListAllModelsBreakdownRequest = {
  range?: string; // today | 7d | 30d | month
  startDate?: string; // YYYY-MM-DD explicit override
  endDate?: string; // YYYY-MM-DD explicit override
  providerCode?: string; // exact provider filter
  search?: string; // LIKE on provider_code + model_api_id
  sortField?: string; // call_count | total_tokens | total_cost_micro_usd | success_rate | avg_latency_ms
  sortDir?: string; // asc | desc (default desc)
  page?: number; // 1-based
  pageSize?: number; // default 20, max 100
};

export type ListAllModelsBreakdownResponse = {
  items: UsageBreakdownRow[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export interface UsageService {
  GetUsageOverview(request: UsageQuery): Promise<UsageOverview>;
  ListUsageTrends(request: UsageQuery): Promise<ListUsageTrendsResponse>;
  ListTopModels(request: UsageQuery): Promise<ListBreakdownResponse>;
  ListTopAgents(request: UsageQuery): Promise<ListBreakdownResponse>;
  ListUsageEvents(request: UsageQuery): Promise<ListUsageEventsResponse>;
  // Inserts model_token_usage_events, rolls up sessions counters, upserts model_token_usage_daily (parity with pkg/backend AddModelTokenUsageEvent + UpsertModelTokenUsageDaily).
  RecordTokenUsageEvent(request: TokenUsageEvent): Promise<TokenUsageEvent>;
  GetUsageQuota(request: GetUsageQuotaRequest): Promise<UsageQuota>;
  SetUsageQuota(request: SetUsageQuotaRequest): Promise<UsageQuota>;
  CheckUsageQuota(request: CheckUsageQuotaRequest): Promise<CheckUsageQuotaResponse>;
  ListBudgetAlerts(request: ListBudgetAlertsRequest): Promise<ListBudgetAlertsResponse>;
  SetBudgetAlert(request: SetBudgetAlertRequest): Promise<BudgetAlert>;
  ExportUsageEvents(request: UsageQuery): Promise<ExportUsageEventsResponse>;
  PurgeUsageEvents(request: PurgeUsageEventsRequest): Promise<PurgeUsageEventsResponse>;
  // ListAllModelsBreakdown returns a paginated, searchable, sortable breakdown of all models
  // for the full-model consumption overview table. Server-side pagination/sort/search.
  ListAllModelsBreakdown(request: ListAllModelsBreakdownRequest): Promise<ListAllModelsBreakdownResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createUsageServiceClient(handler: RequestHandler): UsageService {
  return {
    GetUsageOverview(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/overview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'GetUsageOverview',
        },
      ) as Promise<UsageOverview>;
    },
    ListUsageTrends(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/trends`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListUsageTrends',
        },
      ) as Promise<ListUsageTrendsResponse>;
    },
    ListTopModels(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/top-models`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListTopModels',
        },
      ) as Promise<ListBreakdownResponse>;
    },
    ListTopAgents(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/top-agents`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListTopAgents',
        },
      ) as Promise<ListBreakdownResponse>;
    },
    ListUsageEvents(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/events`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListUsageEvents',
        },
      ) as Promise<ListUsageEventsResponse>;
    },
    RecordTokenUsageEvent(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/token-events`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'UsageService',
          method: 'RecordTokenUsageEvent',
        },
      ) as Promise<TokenUsageEvent>;
    },
    GetUsageQuota(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.scopeType) {
        throw new Error('missing required field request.scope_type');
      }
      if (!request.scopeId) {
        throw new Error('missing required field request.scope_id');
      }
      const path = `v1/usage/quotas/${request.scopeType}/${request.scopeId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'GetUsageQuota',
        },
      ) as Promise<UsageQuota>;
    },
    SetUsageQuota(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.scopeType) {
        throw new Error('missing required field request.scope_type');
      }
      if (!request.scopeId) {
        throw new Error('missing required field request.scope_id');
      }
      const path = `v1/usage/quotas/${request.scopeType}/${request.scopeId}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'UsageService',
          method: 'SetUsageQuota',
        },
      ) as Promise<UsageQuota>;
    },
    CheckUsageQuota(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.scopeType) {
        throw new Error('missing required field request.scope_type');
      }
      if (!request.scopeId) {
        throw new Error('missing required field request.scope_id');
      }
      const path = `v1/usage/quotas/${request.scopeType}/${request.scopeId}/check`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'CheckUsageQuota',
        },
      ) as Promise<CheckUsageQuotaResponse>;
    },
    ListBudgetAlerts(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/budget-alerts`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.scopeType) {
        queryParams.push(`scopeType=${encodeURIComponent(request.scopeType.toString())}`);
      }
      if (request.scopeId) {
        queryParams.push(`scopeId=${encodeURIComponent(request.scopeId.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListBudgetAlerts',
        },
      ) as Promise<ListBudgetAlertsResponse>;
    },
    SetBudgetAlert(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/budget-alerts`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'UsageService',
          method: 'SetBudgetAlert',
        },
      ) as Promise<BudgetAlert>;
    },
    ExportUsageEvents(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/events/export`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.modelApiId) {
        queryParams.push(`modelApiId=${encodeURIComponent(request.modelApiId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.granularity) {
        queryParams.push(`granularity=${encodeURIComponent(request.granularity.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.usageKind) {
        queryParams.push(`usageKind=${encodeURIComponent(request.usageKind.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ExportUsageEvents',
        },
      ) as Promise<ExportUsageEventsResponse>;
    },
    PurgeUsageEvents(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/events/purge`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'UsageService',
          method: 'PurgeUsageEvents',
        },
      ) as Promise<PurgeUsageEventsResponse>;
    },
    ListAllModelsBreakdown(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/usage/all-models-breakdown`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`);
      }
      if (request.startDate) {
        queryParams.push(`startDate=${encodeURIComponent(request.startDate.toString())}`);
      }
      if (request.endDate) {
        queryParams.push(`endDate=${encodeURIComponent(request.endDate.toString())}`);
      }
      if (request.providerCode) {
        queryParams.push(`providerCode=${encodeURIComponent(request.providerCode.toString())}`);
      }
      if (request.search) {
        queryParams.push(`search=${encodeURIComponent(request.search.toString())}`);
      }
      if (request.sortField) {
        queryParams.push(`sortField=${encodeURIComponent(request.sortField.toString())}`);
      }
      if (request.sortDir) {
        queryParams.push(`sortDir=${encodeURIComponent(request.sortDir.toString())}`);
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'UsageService',
          method: 'ListAllModelsBreakdown',
        },
      ) as Promise<ListAllModelsBreakdownResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
