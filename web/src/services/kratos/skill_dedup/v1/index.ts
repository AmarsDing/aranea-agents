// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type DimensionScore = {
  dimension: string | undefined;
  score: number | undefined;
  method: string | undefined;
};

export type SkillSummary = {
  id: string | undefined;
  name: string | undefined;
  slug: string | undefined;
  description: string | undefined;
  bodyPreview: string | undefined;
  tags: string[] | undefined;
};

export type SkillDuplicateGroup = {
  groupId: string | undefined;
  skills: SkillSummary[] | undefined;
  overlapType: string | undefined;
  overlapScore: number | undefined;
  dimensions: DimensionScore[] | undefined;
  conflictRisk: string | undefined;
  recommendation: string | undefined;
};

export type ListSkillDuplicateGroupsRequest = {
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListSkillDuplicateGroupsResponse = {
  groups: SkillDuplicateGroup[] | undefined;
  total: number | undefined;
};

export type MergeSkillsRequest = {
  //
  // Behaviors: REQUIRED
  sourceSkillId: string | undefined;
  //
  // Behaviors: REQUIRED
  targetSkillId: string | undefined;
  strategy: string | undefined;
  manualBody: string | undefined;
};

export type MergeSkillsResponse = {
  message: string | undefined;
};

export interface SkillDedupService {
  ListSkillDuplicateGroups(request: ListSkillDuplicateGroupsRequest): Promise<ListSkillDuplicateGroupsResponse>;
  MergeSkills(request: MergeSkillsRequest): Promise<MergeSkillsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createSkillDedupServiceClient(handler: RequestHandler): SkillDedupService {
  return {
    ListSkillDuplicateGroups(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-dedup/duplicate-groups`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'SkillDedupService',
          method: 'ListSkillDuplicateGroups',
        },
      ) as Promise<ListSkillDuplicateGroupsResponse>;
    },
    MergeSkills(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-dedup/merge`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'SkillDedupService',
          method: 'MergeSkills',
        },
      ) as Promise<MergeSkillsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
