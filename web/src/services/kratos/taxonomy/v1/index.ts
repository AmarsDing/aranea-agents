// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type TaxonomyNode = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  parentId: string | undefined;
  level: string | undefined;
  workspaceId: string | undefined;
  ownerUserId: string | undefined;
  isSystem: boolean | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
};

export type ListTaxonomyResponse = {
  items: TaxonomyNode[] | undefined;
};

export type ListTaxonomyTreeResponse = {
  items: TaxonomyTreeNode[] | undefined;
};

export type TaxonomyTreeNode = {
  node: TaxonomyNode | undefined;
  children: TaxonomyTreeNode[] | undefined;
};

export type CreateTaxonomyRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled?: boolean;
  sortOrder: number | undefined;
  parentId: string | undefined;
  level: string | undefined;
  workspaceId: string | undefined;
  ownerUserId: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
};

export type GetTaxonomyRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateTaxonomyRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  node: TaxonomyNode | undefined;
};

export type DeleteTaxonomyRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ReorderTaxonomyRequest = {
  ids: string[] | undefined;
};

export type ReorderTaxonomyResponse = {};

export interface TaxonomyService {
  ListTaxonomy(request: wellKnownEmpty): Promise<ListTaxonomyResponse>;
  ListTaxonomyTree(request: wellKnownEmpty): Promise<ListTaxonomyTreeResponse>;
  CreateTaxonomy(request: CreateTaxonomyRequest): Promise<TaxonomyNode>;
  GetTaxonomy(request: GetTaxonomyRequest): Promise<TaxonomyNode>;
  UpdateTaxonomy(request: UpdateTaxonomyRequest): Promise<TaxonomyNode>;
  DeleteTaxonomy(request: DeleteTaxonomyRequest): Promise<wellKnownEmpty>;
  ReorderTaxonomy(request: ReorderTaxonomyRequest): Promise<ReorderTaxonomyResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createTaxonomyServiceClient(handler: RequestHandler): TaxonomyService {
  return {
    ListTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/taxonomy`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'ListTaxonomy',
        },
      ) as Promise<ListTaxonomyResponse>;
    },
    ListTaxonomyTree(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/taxonomy/tree`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'ListTaxonomyTree',
        },
      ) as Promise<ListTaxonomyTreeResponse>;
    },
    CreateTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/taxonomy`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'CreateTaxonomy',
        },
      ) as Promise<TaxonomyNode>;
    },
    GetTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/taxonomy/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'GetTaxonomy',
        },
      ) as Promise<TaxonomyNode>;
    },
    UpdateTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/taxonomy/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.node ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'UpdateTaxonomy',
        },
      ) as Promise<TaxonomyNode>;
    },
    DeleteTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/taxonomy/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'DeleteTaxonomy',
        },
      ) as Promise<wellKnownEmpty>;
    },
    ReorderTaxonomy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/taxonomy/reorder`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'TaxonomyService',
          method: 'ReorderTaxonomy',
        },
      ) as Promise<ReorderTaxonomyResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
