// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ExperienceReport = {
  id: string | undefined;
  tenantId: string | undefined;
  sessionId: string | undefined;
  invocationId: string | undefined;
  skillId: string | undefined;
  isSuccess: boolean | undefined;
  score: number | undefined;
  failureTags: string[] | undefined;
  flowSummary: string | undefined;
  optimizationAdvice: string | undefined;
  selectionSnapshot: wellKnownStruct | undefined;
  generatedSuggestionId: string | undefined;
  createdAt: wellKnownTimestamp | undefined;
  rootCauseAnalysis: string | undefined;
  suggestedFix: string | undefined;
  skillName: string | undefined;
};

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type ListExperienceReportsRequest = {
  skillId: string | undefined;
  startTime: wellKnownTimestamp | undefined;
  endTime: wellKnownTimestamp | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type FailureTagCount = {
  tag: string | undefined;
  count: number | undefined;
};

export type ListExperienceReportsResponse = {
  items: ExperienceReport[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
  failureTagCounts: FailureTagCount[] | undefined;
  rootCauseReports: ExperienceReport[] | undefined;
};

export type GetExperienceReportRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetExperienceReportResponse = {
  report: ExperienceReport | undefined;
};

export interface SkillIntelligenceService {
  ListExperienceReports(request: ListExperienceReportsRequest): Promise<ListExperienceReportsResponse>;
  GetExperienceReport(request: GetExperienceReportRequest): Promise<GetExperienceReportResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createSkillIntelligenceServiceClient(handler: RequestHandler): SkillIntelligenceService {
  return {
    ListExperienceReports(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-intelligence/experience-reports`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.skillId) {
        queryParams.push(`skillId=${encodeURIComponent(request.skillId.toString())}`);
      }
      if (request.startTime) {
        queryParams.push(`startTime=${encodeURIComponent(request.startTime.toString())}`);
      }
      if (request.endTime) {
        queryParams.push(`endTime=${encodeURIComponent(request.endTime.toString())}`);
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'SkillIntelligenceService',
          method: 'ListExperienceReports',
        },
      ) as Promise<ListExperienceReportsResponse>;
    },
    GetExperienceReport(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/skill-intelligence/experience-reports/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'SkillIntelligenceService',
          method: 'GetExperienceReport',
        },
      ) as Promise<GetExperienceReportResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
