// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// ExportPackRequest specifies what to export.
export type ExportPackRequest = {
  // Export granularity: "agent", "team", or "industry".
  //
  // Behaviors: REQUIRED
  kind: string | undefined;
  // Entity ID (agent_id, team_id) or key (industry_key) depending on kind.
  //
  // Behaviors: REQUIRED
  ref: string | undefined;
};

// ExportPackResponse returns the .arpack tar.gz data.
export type ExportPackResponse = {
  // .arpack tar.gz content (base64-encoded for JSON transport).
  data: string | undefined;
  // Manifest name from the pack.
  name: string | undefined;
  // Manifest kind.
  kind: string | undefined;
};

// ImportPackRequest contains the .arpack data and import options.
export type ImportPackRequest = {
  // .arpack tar.gz content (base64-encoded for JSON transport).
  //
  // Behaviors: REQUIRED
  data: string | undefined;
  // Conflict strategy: "skip", "overwrite", or "duplicate".
  conflictStrategy: string | undefined;
};

// ImportPackResponse returns the import result.
export type ImportPackResponse = {
  // Number of organization nodes created.
  orgNodes: number | undefined;
  // Number of agents created.
  agentsCreated: number | undefined;
  // Number of agents updated.
  agentsUpdated: number | undefined;
  // Number of agents skipped (conflict with skip strategy).
  agentsSkipped: number | undefined;
  // Number of graphs created.
  graphsCreated: number | undefined;
  // Number of teams created.
  teamsCreated: number | undefined;
  // Number of teams updated.
  teamsUpdated: number | undefined;
  // Number of teams skipped.
  teamsSkipped: number | undefined;
  // Conflict strategy used: "skip", "overwrite", or "duplicate".
  conflictStrategy: string | undefined;
  // Failures encountered during import.
  failures: ImportFailure[] | undefined;
};

// ImportFailure records a single entity import failure.
export type ImportFailure = {
  entityType: string | undefined;
  key: string | undefined;
  reason: string | undefined;
};

// ValidatePackRequest contains the .arpack data to validate.
export type ValidatePackRequest = {
  // .arpack tar.gz content (base64-encoded for JSON transport).
  //
  // Behaviors: REQUIRED
  data: string | undefined;
};

// ValidatePackResponse returns the validation result.
export type ValidatePackResponse = {
  // Whether the pack is valid for import.
  valid: boolean | undefined;
  // Validation errors (blocking).
  errors: string[] | undefined;
  // Validation warnings (non-blocking).
  warnings: string[] | undefined;
  // Missing skills that need to be installed before import.
  missingSkills: string[] | undefined;
  // Missing func_refs that need to be registered before import.
  missingFuncRefs: string[] | undefined;
  // Conflicts with existing entities.
  conflicts: ConflictItem[] | undefined;
};

// ConflictItem describes a conflict between the pack and existing entities.
export type ConflictItem = {
  entityType: string | undefined;
  key: string | undefined;
};

// PackService manages Aranea Pack (.arpack) export, import, and validation.
// File upload/download is handled via custom HTTP routes (multipart/form-data).
export interface PackService {
  // ExportPack exports entities and returns the .arpack as base64-encoded bytes.
  ExportPack(request: ExportPackRequest): Promise<ExportPackResponse>;
  // ImportPack imports a .arpack from base64-encoded bytes.
  ImportPack(request: ImportPackRequest): Promise<ImportPackResponse>;
  // ValidatePack dry-run validates a .arpack without importing.
  ValidatePack(request: ValidatePackRequest): Promise<ValidatePackResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createPackServiceClient(
  handler: RequestHandler
): PackService {
  return {
    ExportPack(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/pack/export`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "PackService",
        method: "ExportPack",
      }) as Promise<ExportPackResponse>;
    },
    ImportPack(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/pack/import`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "PackService",
        method: "ImportPack",
      }) as Promise<ImportPackResponse>;
    },
    ValidatePack(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/pack/validate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "PackService",
        method: "ValidatePack",
      }) as Promise<ValidatePackResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
