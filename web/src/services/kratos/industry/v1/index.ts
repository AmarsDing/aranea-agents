// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Industry = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  icon: string | undefined;
  description: string | undefined;
  scenarioKey: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
};

export type Department = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  industryKey: string | undefined;
  description: string | undefined;
  responsibilitiesJson: string | undefined;
  sortOrder: number | undefined;
};

export type Position = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  departmentKey: string | undefined;
  description: string | undefined;
  responsibilitiesJson: string | undefined;
  skillsRequiredJson: string | undefined;
  seniorityLevel: string | undefined;
  sortOrder: number | undefined;
};

export type ListIndustriesRequest = {};

export type ListIndustriesResponse = {
  items: Industry[] | undefined;
  total: number | undefined;
};

export type GetIndustryRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
};

export type ListDepartmentsRequest = {
  //
  // Behaviors: REQUIRED
  industryKey: string | undefined;
};

export type ListDepartmentsResponse = {
  items: Department[] | undefined;
  total: number | undefined;
};

export type ListPositionsRequest = {
  industryKey: string | undefined;
  departmentKey: string | undefined;
};

export type ListPositionsResponse = {
  items: Position[] | undefined;
  total: number | undefined;
};

export type GetPositionPromptRequest = {
  industryKey: string | undefined;
  positionKey: string | undefined;
  variant: string | undefined;
};

export type GetPositionPromptResponse = {
  promptContent: string | undefined;
  variant: string | undefined;
  positionName: string | undefined;
  departmentName: string | undefined;
  industryName: string | undefined;
  industryDescription: string | undefined;
  departmentDescription: string | undefined;
  positionDescription: string | undefined;
  responsibilitiesJson: string | undefined;
  variantDescription: string | undefined;
};

export type ListPositionVariantsRequest = {
  //
  // Behaviors: REQUIRED
  industryKey: string | undefined;
  //
  // Behaviors: REQUIRED
  positionKey: string | undefined;
};

export type VariantInfo = {
  key: string | undefined;
  label: string | undefined;
};

export type ListPositionVariantsResponse = {
  variants: VariantInfo[] | undefined;
};

export interface IndustryService {
  ListIndustries(request: ListIndustriesRequest): Promise<ListIndustriesResponse>;
  GetIndustry(request: GetIndustryRequest): Promise<Industry>;
  ListDepartments(request: ListDepartmentsRequest): Promise<ListDepartmentsResponse>;
  ListPositions(request: ListPositionsRequest): Promise<ListPositionsResponse>;
  GetPositionPrompt(request: GetPositionPromptRequest): Promise<GetPositionPromptResponse>;
  ListPositionVariants(request: ListPositionVariantsRequest): Promise<ListPositionVariantsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createIndustryServiceClient(handler: RequestHandler): IndustryService {
  return {
    ListIndustries(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/industries`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'ListIndustries',
        },
      ) as Promise<ListIndustriesResponse>;
    },
    GetIndustry(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.key) {
        throw new Error('missing required field request.key');
      }
      const path = `v1/industries/${request.key}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'GetIndustry',
        },
      ) as Promise<Industry>;
    },
    ListDepartments(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.industryKey) {
        throw new Error('missing required field request.industry_key');
      }
      const path = `v1/industries/${request.industryKey}/departments`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'ListDepartments',
        },
      ) as Promise<ListDepartmentsResponse>;
    },
    ListPositions(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.industryKey) {
        throw new Error('missing required field request.industry_key');
      }
      const path = `v1/industries/${request.industryKey}/positions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.departmentKey) {
        queryParams.push(`departmentKey=${encodeURIComponent(request.departmentKey.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'ListPositions',
        },
      ) as Promise<ListPositionsResponse>;
    },
    GetPositionPrompt(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.industryKey) {
        throw new Error('missing required field request.industry_key');
      }
      if (!request.positionKey) {
        throw new Error('missing required field request.position_key');
      }
      const path = `v1/industries/${request.industryKey}/positions/${request.positionKey}/prompt`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.variant) {
        queryParams.push(`variant=${encodeURIComponent(request.variant.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'GetPositionPrompt',
        },
      ) as Promise<GetPositionPromptResponse>;
    },
    ListPositionVariants(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.industryKey) {
        throw new Error('missing required field request.industry_key');
      }
      if (!request.positionKey) {
        throw new Error('missing required field request.position_key');
      }
      const path = `v1/industries/${request.industryKey}/positions/${request.positionKey}/variants`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'IndustryService',
          method: 'ListPositionVariants',
        },
      ) as Promise<ListPositionVariantsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
