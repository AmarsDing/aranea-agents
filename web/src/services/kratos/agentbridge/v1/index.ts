// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// CodingAgent 是已注册的外部编程 CLI（ACP stdio 子进程配置）。
export type CodingAgent = {
  id: string | undefined;
  agentKey: string | undefined;
  displayName: string | undefined;
  command: string | undefined;
  args: string[] | undefined;
  env: { [key: string]: string } | undefined;
  enabled: boolean | undefined;
  lastProbeOk: boolean | undefined;
  lastProbeError: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type UpsertAgentRequest = {
  //
  // Behaviors: REQUIRED
  agentKey: string | undefined;
  displayName: string | undefined;
  //
  // Behaviors: REQUIRED
  command: string | undefined;
  args: string[] | undefined;
  env: { [key: string]: string } | undefined;
  enabled: boolean | undefined;
};

export type ListAgentsResponse = {
  items: CodingAgent[] | undefined;
};

export type ProbeAgentRequest = {
  //
  // Behaviors: REQUIRED
  agentKey: string | undefined;
};

export type ProbeAgentResponse = {
  ok: boolean | undefined;
  error: string | undefined;
};

// CodingProject 是可派发任务的本机项目目录（cwd 白名单）。
export type CodingProject = {
  id: string | undefined;
  name: string | undefined;
  path: string | undefined;
  description: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type UpsertProjectRequest = {
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  //
  // Behaviors: REQUIRED
  path: string | undefined;
  description: string | undefined;
};

export type ListProjectsResponse = {
  items: CodingProject[] | undefined;
};

export type DeleteProjectRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// CodingTask 是一次外部编程 agent 任务执行记录。
export type CodingTask = {
  id: string | undefined;
  sessionId: string | undefined;
  agentKey: string | undefined;
  projectName: string | undefined;
  prompt: string | undefined;
  status: string | undefined;
  summary: string | undefined;
  error: string | undefined;
  progressCount: number | undefined;
  createdAt: string | undefined;
  completedAt: string | undefined;
};

export type ListTasksRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  limit: number | undefined;
};

export type ListTasksResponse = {
  items: CodingTask[] | undefined;
};

export type GetTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CancelTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export interface AgentBridgeService {
  // 工具注册管理（删除经 enabled=false，M1 不提供物理删除）
  UpsertAgent(request: UpsertAgentRequest): Promise<CodingAgent>;
  ListAgents(request: wellKnownEmpty): Promise<ListAgentsResponse>;
  ProbeAgent(request: ProbeAgentRequest): Promise<ProbeAgentResponse>;
  // 项目目录注册管理
  UpsertProject(request: UpsertProjectRequest): Promise<CodingProject>;
  ListProjects(request: wellKnownEmpty): Promise<ListProjectsResponse>;
  DeleteProject(request: DeleteProjectRequest): Promise<wellKnownEmpty>;
  // 任务查询/取消
  ListTasks(request: ListTasksRequest): Promise<ListTasksResponse>;
  GetTask(request: GetTaskRequest): Promise<CodingTask>;
  CancelTask(request: CancelTaskRequest): Promise<CodingTask>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createAgentBridgeServiceClient(
  handler: RequestHandler
): AgentBridgeService {
  return {
    UpsertAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/agents`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentBridgeService",
        method: "UpsertAgent",
      }) as Promise<CodingAgent>;
    },
    ListAgents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/agents`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentBridgeService",
        method: "ListAgents",
      }) as Promise<ListAgentsResponse>;
    },
    ProbeAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/agents/probe`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentBridgeService",
        method: "ProbeAgent",
      }) as Promise<ProbeAgentResponse>;
    },
    UpsertProject(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/projects`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentBridgeService",
        method: "UpsertProject",
      }) as Promise<CodingProject>;
    },
    ListProjects(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/projects`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentBridgeService",
        method: "ListProjects",
      }) as Promise<ListProjectsResponse>;
    },
    DeleteProject(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agentbridge/projects/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "AgentBridgeService",
        method: "DeleteProject",
      }) as Promise<wellKnownEmpty>;
    },
    ListTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agentbridge/tasks`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentBridgeService",
        method: "ListTasks",
      }) as Promise<ListTasksResponse>;
    },
    GetTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agentbridge/tasks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentBridgeService",
        method: "GetTask",
      }) as Promise<CodingTask>;
    },
    CancelTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agentbridge/tasks/${request.id}/cancel`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentBridgeService",
        method: "CancelTask",
      }) as Promise<CodingTask>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
