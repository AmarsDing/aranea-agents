// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// Send / options mirror legacy JSON (snake_case on wire, same as web `features/chat/api.ts`).
// Streaming events are delivered via WebSocket + EventBus (no SSE route).
export type SendChatMessageRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  agentKey?: string;
  teamId?: string;
  //
  // Behaviors: REQUIRED
  content: string | undefined;
  options: SendMessageOptions | undefined;
  // request_id 是客户端生成的幂等键（与 WS user_message 的 request_id 同一
  // 约定，如 pending-user-<uuid>）。重试复用同一键，服务端按
  // session_id+request_id 去重，重复提交不产生第二条用户消息/第二轮 turn。
  // 空 = 不去重（channel/cron 等无客户端键的入口）。
  requestId: string | undefined;
};

export type SendMessageOptions = {
  dialogMode?: string;
  provider?: string;
  model?: string;
  attachments: AttachmentRef[] | undefined;
  // knowledge_bases limits knowledge_search to these collection IDs for this turn.
  knowledgeBases: string[] | undefined;
};

export type AttachmentRef = {
  id: string | undefined;
};

// Opaque message objects match legacy `domain.Message` JSON without duplicating every field in proto.
export type SendChatMessageResponse = {
  userMessage: wellKnownStruct | undefined;
  agentMessage: wellKnownStruct | undefined;
};

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

// SubmitChatMessageResponse is the ACK-only response for the async command
// channel (B2). Full message/state/streaming data arrives via the WS data
// channel — this response intentionally carries no message content.
export type SubmitChatMessageResponse = {
  // accepted = true means the message was accepted and turn processing started.
  accepted: boolean | undefined;
  // status: "accepted" = turn started; "queued" = enqueued for later (active run).
  status: string | undefined;
  // message_id is empty on accept (assigned when the user message is persisted,
  // delivered via WS `message.persisted` event). For queued messages, this is
  // the pending queue entry ID.
  messageId: string | undefined;
  // turn_id is empty on accept (assigned when the turn starts, delivered via
  // WS `run_status=running` event).
  turnId: string | undefined;
};

export type GetChatOptionsRequest = {
  // Query: ?type=
  type: string | undefined;
};

export type ChatOption = {
  type: string | undefined;
  key: string | undefined;
  label: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  metadataJson: string | undefined;
};

export type GetChatOptionsResponse = {
  items: ChatOption[] | undefined;
};

export type StopGenerationRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

export type StopGenerationResponse = {
  stopped: boolean | undefined;
};

export type RetrySessionRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

export type RetrySessionResponse = {
  retried: boolean | undefined;
};

// PauseSessionRequest pauses a running chat session (running → paused).
// User-initiated pause for an agent card; the current step completes before pausing.
export type PauseSessionRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

export type PauseSessionResponse = {
  paused: boolean | undefined;
};

// ResumeSessionRequest resumes a paused chat session (paused → running).
// User-initiated resume; used by AgentCard resume button.
export type ResumeSessionRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

export type ResumeSessionResponse = {
  resumed: boolean | undefined;
};

export type GetPendingMessagesRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

export type PendingMessage = {
  id: string | undefined;
  content: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
};

export type GetPendingMessagesResponse = {
  items: PendingMessage[] | undefined;
};

export type CancelPendingMessageRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  pendingId: string | undefined;
};

export type CancelPendingMessageResponse = {
  cancelled: boolean | undefined;
};

export type UpdatePendingMessageRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  pendingId: string | undefined;
  //
  // Behaviors: REQUIRED
  content: string | undefined;
};

export type UpdatePendingMessageResponse = {
  updated: boolean | undefined;
};

export type InterruptAndSendMessageRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  pendingId: string | undefined;
};

export type InterruptAndSendMessageResponse = {
  sent: boolean | undefined;
};

// RunStatus represents the lifecycle state of an agent run within a session.
export type RunStatus = {
  // run_id is the unique identifier of the current or most-recent run.
  runId: string | undefined;
  // status is one of: idle | pending | running | awaiting_user | completed | failed | cancelled | sync.
  status: string | undefined;
  // error_message is only set when status == failed.
  errorMessage: string | undefined;
  // updated_at is an RFC3339 timestamp of the last status transition.
  updatedAt: string | undefined;
  // invocation_id is the framework invocation id when a run is active (ManagedRunner.RunStatus).
  invocationId: string | undefined;
  // agent_name is the root agent name for the active invocation.
  agentName: string | undefined;
  // started_at is RFC3339 when the framework run started.
  startedAt: string | undefined;
  // last_event_at is RFC3339 of the last runner event for the invocation.
  lastEventAt: string | undefined;
  // event_count is the number of events observed for the invocation.
  eventCount: number | undefined;
  // await_kind distinguishes awaiting_user reasons: "" | "reply" | "tool_confirm".
  awaitKind: string | undefined;
  // await_tool_key is set when await_kind == "tool_confirm".
  awaitToolKey: string | undefined;
  // await_tool_call_id is set when await_kind == "tool_confirm".
  awaitToolCallId: string | undefined;
};

export type GetRunStatusRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

// AwaitUserReplyRequest submits the human reply for a paused run that is in awaiting_user state.
export type AwaitUserReplyRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  // run_id identifies which run to resume. If omitted, resumes the latest awaiting_user run.
  runId?: string;
  //
  // Behaviors: REQUIRED
  reply: string | undefined;
};

export type AwaitUserReplyResponse = {
  accepted: boolean | undefined;
};

export type EnqueueUserMessageRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  content: string | undefined;
  // kind 是注入级别（P2-3 三级注入语义）：
  // "" / "steer" = 用户插话，优先经框架 steer 队列在下一 step 边界消费；
  // "followup"   = 显式追问，跳过 steer 直接入 pending 队列；
  // "inject"     = 系统上下文静默排队，不单独唤醒 turn。
  kind: string | undefined;
};

export type EnqueueUserMessageResponse = {
  // accepted is true when the message was accepted by either runner steering or the pending queue.
  accepted: boolean | undefined;
  // queued is true when the message fell back to the service pending queue.
  queued: boolean | undefined;
  pendingId: string | undefined;
};

export type ListChatBackgroundJobsRequest = {
  sessionId?: string;
  agentId?: string;
  status?: string;
  limit?: number;
};

export type ChatBackgroundJob = {
  id: string | undefined;
  source: string | undefined;
  sessionId: string | undefined;
  agentId: string | undefined;
  status: string | undefined;
  targetType: string | undefined;
  targetId: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  summary?: string;
  errorMessage?: string;
  channelId: string | undefined;
  graphId?: string;
  turnId?: string;
  sessionRunId?: string;
  phase?: string;
};

export type ListChatBackgroundJobsResponse = {
  items: ChatBackgroundJob[] | undefined;
};

export type CancelChatBackgroundJobRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  source: string | undefined;
};

export type CancelChatBackgroundJobResponse = {
  cancelled: boolean | undefined;
};

export type SubmitMessageFeedbackRequest = {
  //
  // Behaviors: REQUIRED
  messageId: string | undefined;
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  // rating: "positive" | "negative"
  //
  // Behaviors: REQUIRED
  rating: string | undefined;
  comment?: string;
  // context_json: optional JSON snapshot making the review list self-contained,
  // e.g. {"task_id":"...","input":"...","output":"..."}. Leniently parsed.
  contextJson?: string;
};

export type SubmitMessageFeedbackResponse = {
  accepted: boolean | undefined;
};

// ConfirmActivityRequest is the request for confirming a tool-blocked activity.
export type ConfirmActivityRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  activityId: string | undefined;
  // approved = true → resume tool execution; approved = false → cancel tool
  //
  // Behaviors: REQUIRED
  approved: boolean | undefined;
  // reply is an optional structured confirm token (see
  // internal/tools/serviceawaitreply: __aranea:tool_confirm:approve |
  // deny | approve_session | approve_always). When set, it takes
  // precedence over the approved flag and carries the grant scope.
  reply: string | undefined;
};

export type ConfirmActivityResponse = {
  accepted: boolean | undefined;
  status: string | undefined;
};

// SubmitClarificationRequest is the request for submitting clarification answers.
export type SubmitClarificationRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  stepId: string | undefined;
  // answers is the user's answers to each clarification question.
  // The array index corresponds to the question index in the step content.
  // Empty selected array means the user skipped that question (use recommended).
  //
  // Behaviors: REQUIRED
  answers: ClarificationAnswer[] | undefined;
};

// ClarificationAnswer represents the user's answer to a single clarification question.
export type ClarificationAnswer = {
  // selected contains the selected option values (empty = use recommended).
  selected: string[] | undefined;
  // other is an optional free-form text input for additional context.
  other: string | undefined;
};

export type SubmitClarificationResponse = {
  accepted: boolean | undefined;
  status: string | undefined;
  // clarified_context is the rendered context text injected into the resumed turn.
  clarifiedContext: string | undefined;
};

// ConfirmPlanRequest is the request for confirming or rejecting a draft TaskPlan
// produced by the pre-planning hard gate (P1-2).
export type ConfirmPlanRequest = {
  //
  // Behaviors: REQUIRED
  planId: string | undefined;
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  // approved = true → confirm plan (draft → confirmed); approved = false → reject plan
  //
  // Behaviors: REQUIRED
  approved: boolean | undefined;
  // strategy_override optionally overrides the suggested orchestration strategy.
  // Valid values: direct | single_agent | parallel | dag | coordinator
  strategyOverride?: string;
  // reason is an optional user-supplied justification for the decision.
  reason?: string;
};

export type ConfirmPlanResponse = {
  accepted: boolean | undefined;
  status: string | undefined;
  planId: string | undefined;
  // strategy is the final strategy after applying any override.
  strategy: string | undefined;
  // subtask_count is the number of subtasks in the confirmed plan.
  subtaskCount: number | undefined;
};

// ListPlansRequest is the request for listing TaskPlans of a spirit session.
export type ListPlansRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

// ListPlansResponse contains the list of TaskPlans, newest first.
export type ListPlansResponse = {
  plans: TaskPlanSummary[] | undefined;
};

// TaskPlanSummary is a compact view of a TaskPlan for list display.
export type TaskPlanSummary = {
  planId: string | undefined;
  sessionId: string | undefined;
  traceId: string | undefined;
  userMessage: string | undefined;
  complexityLevel: string | undefined;
  complexityScore: number | undefined;
  strategy: string | undefined;
  status: string | undefined;
  subtaskCount: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

// GetPlanRequest is the request for fetching a single TaskPlan by ID.
export type GetPlanRequest = {
  //
  // Behaviors: REQUIRED
  planId: string | undefined;
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

// GetPlanResponse contains the full TaskPlan details.
export type GetPlanResponse = {
  plan: TaskPlanDetail | undefined;
};

// TaskPlanDetail is the full view of a TaskPlan for detail display.
export type TaskPlanDetail = {
  planId: string | undefined;
  sessionId: string | undefined;
  traceId: string | undefined;
  userMessage: string | undefined;
  intentArtifactJson: string | undefined;
  complexityLevel: string | undefined;
  complexityScore: number | undefined;
  dimensions: DimensionScores | undefined;
  subTasks: SubTask[] | undefined;
  taskDag: PlanTaskDAG | undefined;
  decomposeReason: string | undefined;
  strategy: string | undefined;
  strategyReason: string | undefined;
  topologyHint: string | undefined;
  memoryHit: MemoryHit | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

// DimensionScores holds the six-dimension complexity assessment.
export type DimensionScores = {
  semantic: number | undefined;
  structural: number | undefined;
  domain: number | undefined;
  tool: number | undefined;
  context: number | undefined;
  historical: number | undefined;
};

// SubTask represents a decomposed sub-task within a TaskPlan.
export type SubTask = {
  id: string | undefined;
  name: string | undefined;
  description: string | undefined;
  dependsOn: string[] | undefined;
  requiredCapabilities: string[] | undefined;
  priority: number | undefined;
  estimatedComplexity: number | undefined;
};

// PlanTaskDAG represents the dependency graph of subtasks.
export type PlanTaskDAG = {
  nodes: SubTask[] | undefined;
  rootIds: string[] | undefined;
  leafIds: string[] | undefined;
};

// MemoryHit represents a memory cache hit from OrchestrationCache.
export type MemoryHit = {
  cacheId: string | undefined;
  dqScore: number | undefined;
  topologyUsed: string | undefined;
  agentKeysUsed: string[] | undefined;
};

export interface ChatService {
  SendChatMessage(request: SendChatMessageRequest): Promise<SendChatMessageResponse>;
  // SubmitChatMessage submits a user message asynchronously and returns an ACK
  // only (B2 channel separation). Turn execution runs in the background; all
  // message/state/streaming data arrives via the WS data channel.
  // Use this instead of SendChatMessage when the client is WS-connected and
  // only needs a fire-and-ack command channel.
  SubmitChatMessage(request: SendChatMessageRequest): Promise<SubmitChatMessageResponse>;
  GetChatOptions(request: GetChatOptionsRequest): Promise<GetChatOptionsResponse>;
  StopGeneration(request: StopGenerationRequest): Promise<StopGenerationResponse>;
  // RetrySession re-triggers the last failed/interrupted turn for a session.
  // Used by AgentCard retry button to recover a stuck/failed sub-agent run.
  // Finds the last user message in the session and re-enqueues it.
  RetrySession(request: RetrySessionRequest): Promise<RetrySessionResponse>;
  // PauseSession pauses a running chat session (running → paused).
  // Used by AgentCard pause button; the current step completes before pausing.
  PauseSession(request: PauseSessionRequest): Promise<PauseSessionResponse>;
  // ResumeSession resumes a paused chat session (paused → running).
  // Used by AgentCard resume button.
  ResumeSession(request: ResumeSessionRequest): Promise<ResumeSessionResponse>;
  GetPendingMessages(request: GetPendingMessagesRequest): Promise<GetPendingMessagesResponse>;
  CancelPendingMessage(request: CancelPendingMessageRequest): Promise<CancelPendingMessageResponse>;
  UpdatePendingMessage(request: UpdatePendingMessageRequest): Promise<UpdatePendingMessageResponse>;
  InterruptAndSendMessage(request: InterruptAndSendMessageRequest): Promise<InterruptAndSendMessageResponse>;
  GetRunStatus(request: GetRunStatusRequest): Promise<RunStatus>;
  AwaitUserReply(request: AwaitUserReplyRequest): Promise<AwaitUserReplyResponse>;
  EnqueueUserMessage(request: EnqueueUserMessageRequest): Promise<EnqueueUserMessageResponse>;
  ListChatBackgroundJobs(request: ListChatBackgroundJobsRequest): Promise<ListChatBackgroundJobsResponse>;
  CancelChatBackgroundJob(request: CancelChatBackgroundJobRequest): Promise<CancelChatBackgroundJobResponse>;
  SubmitMessageFeedback(request: SubmitMessageFeedbackRequest): Promise<SubmitMessageFeedbackResponse>;
  ConfirmActivity(request: ConfirmActivityRequest): Promise<ConfirmActivityResponse>;
  // ConfirmPlan allows the user to confirm or reject a draft TaskPlan created
  // by the pre-planning hard gate. When approved=true, the plan transitions
  // from draft → confirmed and execution may proceed; when approved=false,
  // the plan is abandoned and the turn aborts with a "plan rejected" status.
  ConfirmPlan(request: ConfirmPlanRequest): Promise<ConfirmPlanResponse>;
  // ListPlans returns all TaskPlans for a spirit session, newest first (T3.2).
  // Used by the Observability Dashboard to display task planning history.
  ListPlans(request: ListPlansRequest): Promise<ListPlansResponse>;
  // GetPlan returns a single TaskPlan by ID with full details (T3.2).
  // Used by the Observability Dashboard to display plan details.
  GetPlan(request: GetPlanRequest): Promise<GetPlanResponse>;
  // SubmitClarification submits user answers to a clarification step and
  // resumes the turn with the clarified context.
  SubmitClarification(request: SubmitClarificationRequest): Promise<SubmitClarificationResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createChatServiceClient(
  handler: RequestHandler
): ChatService {
  return {
    SendChatMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/messages`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "SendChatMessage",
      }) as Promise<SendChatMessageResponse>;
    },
    SubmitChatMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/messages/submit`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "SubmitChatMessage",
      }) as Promise<SubmitChatMessageResponse>;
    },
    GetChatOptions(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/options`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.type) {
        queryParams.push(`type=${encodeURIComponent(request.type.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "GetChatOptions",
      }) as Promise<GetChatOptionsResponse>;
    },
    StopGeneration(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/stop`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "StopGeneration",
      }) as Promise<StopGenerationResponse>;
    },
    RetrySession(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/chat/sessions/${request.sessionId}/retry`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "RetrySession",
      }) as Promise<RetrySessionResponse>;
    },
    PauseSession(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/chat/sessions/${request.sessionId}/pause`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "PauseSession",
      }) as Promise<PauseSessionResponse>;
    },
    ResumeSession(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/chat/sessions/${request.sessionId}/resume`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "ResumeSession",
      }) as Promise<ResumeSessionResponse>;
    },
    GetPendingMessages(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/pending`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "GetPendingMessages",
      }) as Promise<GetPendingMessagesResponse>;
    },
    CancelPendingMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/pending/cancel`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "CancelPendingMessage",
      }) as Promise<CancelPendingMessageResponse>;
    },
    UpdatePendingMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/pending/update`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "UpdatePendingMessage",
      }) as Promise<UpdatePendingMessageResponse>;
    },
    InterruptAndSendMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/pending/interrupt-and-send`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "InterruptAndSendMessage",
      }) as Promise<InterruptAndSendMessageResponse>;
    },
    GetRunStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/run-status`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "GetRunStatus",
      }) as Promise<RunStatus>;
    },
    AwaitUserReply(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/await-reply`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "AwaitUserReply",
      }) as Promise<AwaitUserReplyResponse>;
    },
    EnqueueUserMessage(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/enqueue`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "EnqueueUserMessage",
      }) as Promise<EnqueueUserMessageResponse>;
    },
    ListChatBackgroundJobs(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/jobs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "ListChatBackgroundJobs",
      }) as Promise<ListChatBackgroundJobsResponse>;
    },
    CancelChatBackgroundJob(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/chat/jobs/${request.id}/cancel`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "CancelChatBackgroundJob",
      }) as Promise<CancelChatBackgroundJobResponse>;
    },
    SubmitMessageFeedback(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.messageId) {
        throw new Error("missing required field request.message_id");
      }
      const path = `v1/chat/messages/${request.messageId}/feedback`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "SubmitMessageFeedback",
      }) as Promise<SubmitMessageFeedbackResponse>;
    },
    ConfirmActivity(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.activityId) {
        throw new Error("missing required field request.activity_id");
      }
      const path = `v1/chat/activities/${request.activityId}/confirm`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "ConfirmActivity",
      }) as Promise<ConfirmActivityResponse>;
    },
    ConfirmPlan(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.planId) {
        throw new Error("missing required field request.plan_id");
      }
      const path = `v1/chat/plans/${request.planId}/confirm`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "ConfirmPlan",
      }) as Promise<ConfirmPlanResponse>;
    },
    ListPlans(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/chat/plans`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "ListPlans",
      }) as Promise<ListPlansResponse>;
    },
    GetPlan(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.planId) {
        throw new Error("missing required field request.plan_id");
      }
      const path = `v1/chat/plans/${request.planId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "ChatService",
        method: "GetPlan",
      }) as Promise<GetPlanResponse>;
    },
    SubmitClarification(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.stepId) {
        throw new Error("missing required field request.step_id");
      }
      const path = `v1/chat/clarifications/${request.stepId}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "ChatService",
        method: "SubmitClarification",
      }) as Promise<SubmitClarificationResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
