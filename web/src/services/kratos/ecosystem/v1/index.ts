// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Product = {
  id: string | undefined;
  name: string | undefined;
  displayName: string | undefined;
  description: string | undefined;
  type: string | undefined;
  authorId: string | undefined;
  version: string | undefined;
  priceModel: string | undefined;
  priceCents: number | undefined;
  rating: number | undefined;
  installCount: number | undefined;
  configJson: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  installed: boolean | undefined;
};

export type ListProductsRequest = {
  type: string | undefined;
  search: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListProductsResponse = {
  items: Product[] | undefined;
  total: number | undefined;
};

export type GetProductRequest = {
  id: string | undefined;
};

export type PublishProductRequest = {
  name: string | undefined;
  displayName: string | undefined;
  description: string | undefined;
  type: string | undefined;
  version: string | undefined;
  priceModel: string | undefined;
  priceCents: number | undefined;
  configJson: string | undefined;
};

export type InstallProductRequest = {
  id: string | undefined;
};

export type InstallResult = {
  productId: string | undefined;
  installedIds: string[] | undefined;
  message: string | undefined;
};

export type UninstallProductRequest = {
  id: string | undefined;
};

export interface EcosystemService {
  ListProducts(request: ListProductsRequest): Promise<ListProductsResponse>;
  GetProduct(request: GetProductRequest): Promise<Product>;
  PublishProduct(request: PublishProductRequest): Promise<Product>;
  InstallProduct(request: InstallProductRequest): Promise<InstallResult>;
  UninstallProduct(request: UninstallProductRequest): Promise<wellKnownEmpty>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createEcosystemServiceClient(
  handler: RequestHandler
): EcosystemService {
  return {
    ListProducts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/ecosystem/products`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.type) {
        queryParams.push(`type=${encodeURIComponent(request.type.toString())}`)
      }
      if (request.search) {
        queryParams.push(`search=${encodeURIComponent(request.search.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "EcosystemService",
        method: "ListProducts",
      }) as Promise<ListProductsResponse>;
    },
    GetProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/ecosystem/products/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "EcosystemService",
        method: "GetProduct",
      }) as Promise<Product>;
    },
    PublishProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/ecosystem/products`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "EcosystemService",
        method: "PublishProduct",
      }) as Promise<Product>;
    },
    InstallProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/ecosystem/products/${request.id}/install`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "EcosystemService",
        method: "InstallProduct",
      }) as Promise<InstallResult>;
    },
    UninstallProduct(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/ecosystem/products/${request.id}/install`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "EcosystemService",
        method: "UninstallProduct",
      }) as Promise<wellKnownEmpty>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
