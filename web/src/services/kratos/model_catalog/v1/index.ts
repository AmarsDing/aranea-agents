// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ModelCatalogPolicy = {
  sourceUrl: string | undefined;
  // off | scheduled
  syncPolicy: string | undefined;
  syncIntervalHours: number | undefined;
  // none | metadata_and_pricing | full_spec | full_spec_and_runtime_overlay
  autoApply: string | undefined;
};

export type UpdateModelCatalogPolicyRequest = {
  sourceUrl: string | undefined;
  syncPolicy: string | undefined;
  syncIntervalHours: number | undefined;
  autoApply: string | undefined;
};

export type ModelCatalogStatus = {
  policy: ModelCatalogPolicy | undefined;
  lastSyncAt: wellKnownTimestamp | undefined;
  lastSyncStatus: string | undefined;
  lastSyncSummary: string | undefined;
  etag: string | undefined;
  providerCount: number | undefined;
  modelCount: number | undefined;
  catalogBytes: number | undefined;
  catalogLoaded: boolean | undefined;
  localPath: string | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type CatalogProviderSummary = {
  id: string | undefined;
  name: string | undefined;
  doc: string | undefined;
  npm: string | undefined;
  api: string | undefined;
  modelCount: number | undefined;
  logoUrl: string | undefined;
  logoCached: boolean | undefined;
  env: string[] | undefined;
};

export type ListCatalogProvidersRequest = {
  q: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListCatalogProvidersResponse = {
  items: CatalogProviderSummary[] | undefined;
  total: number | undefined;
};

export type CatalogModelSummary = {
  id: string | undefined;
  name: string | undefined;
  status: string | undefined;
  reasoning: boolean | undefined;
  toolCall: boolean | undefined;
  attachment: boolean | undefined;
  inputUsdPer1m: number | undefined;
  outputUsdPer1m: number | undefined;
  contextTokens: number | undefined;
  outputTokens: number | undefined;
  cacheReadUsdPer1m: number | undefined;
  cacheWriteUsdPer1m: number | undefined;
  reasoningUsdPer1m: number | undefined;
  structuredOutput: boolean | undefined;
  openWeights: boolean | undefined;
  temperature: boolean | undefined;
  modalityInput: string[] | undefined;
  modalityOutput: string[] | undefined;
  family: string | undefined;
  knowledge: string | undefined;
  releaseDate: string | undefined;
  lastUpdated: string | undefined;
  interleavedJson: string | undefined;
};

export type ListCatalogModelsRequest = {
  providerId: string | undefined;
  q: string | undefined;
  includeDeprecated: boolean | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListCatalogModelsResponse = {
  items: CatalogModelSummary[] | undefined;
  total: number | undefined;
};

export type ModelCatalogRawResponse = {
  jsonPretty: string | undefined;
  bytes: number | undefined;
};

export type SearchCatalogRawRequest = {
  q: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type SearchCatalogRawResponse = {
  // Pretty-printed JSON blocks (each item is a complete provider or provider+model object).
  lines: string[] | undefined;
  total: number | undefined;
  offset: number | undefined;
  truncated: boolean | undefined;
};

export type ModelCatalogSyncLogEntry = {
  id: string | undefined;
  startedAt: wellKnownTimestamp | undefined;
  finishedAt: wellKnownTimestamp | undefined;
  status: string | undefined;
  message: string | undefined;
  detailsJson: string | undefined;
};

export type ListModelCatalogSyncLogsRequest = {
  limit: number | undefined;
};

export type ListModelCatalogSyncLogsResponse = {
  items: ModelCatalogSyncLogEntry[] | undefined;
};

export type SyncModelCatalogRequest = {
  dryRun: boolean | undefined;
};

export type SyncModelCatalogResponse = {
  ok: boolean | undefined;
  message: string | undefined;
  logId: string | undefined;
  status: ModelCatalogStatus | undefined;
  applyErrors: string[] | undefined;
  applyFailed: boolean | undefined;
};

export type MigrationPreviewItem = {
  legacyProvider: string | undefined;
  catalogProvider: string | undefined;
  llmRows: number | undefined;
  agents: number | undefined;
  sessions: number | undefined;
  evalFields: number | undefined;
  runtimeSettings: number | undefined;
  skills: number | undefined;
  knowledgeEmbed: number | undefined;
  webResearch: number | undefined;
};

export type PreviewMigrationResponse = {
  items: MigrationPreviewItem[] | undefined;
};

export type GetCatalogProviderLogoRequest = {
  providerId: string | undefined;
};

export type GetCatalogProviderLogoResponse = {
  svg: string | undefined;
  cached: boolean | undefined;
};

export type ProviderMigrationRule = {
  legacy: string | undefined;
  catalog: string | undefined;
};

export type ProviderMigrationRulesResponse = {
  rules: ProviderMigrationRule[] | undefined;
  version: string | undefined;
  lastAppliedAt: string | undefined;
};

export type ApplyProviderMigrationResponse = {
  ok: boolean | undefined;
  message: string | undefined;
  totals: MigrationPreviewItem | undefined;
  errors: string[] | undefined;
};

// ModelCatalogService manages the local models.dev catalog cache and sync policy.
export interface ModelCatalogService {
  GetModelCatalogStatus(request: wellKnownEmpty): Promise<ModelCatalogStatus>;
  GetModelCatalogPolicy(request: wellKnownEmpty): Promise<ModelCatalogPolicy>;
  UpdateModelCatalogPolicy(request: UpdateModelCatalogPolicyRequest): Promise<ModelCatalogPolicy>;
  ListCatalogProviders(request: ListCatalogProvidersRequest): Promise<ListCatalogProvidersResponse>;
  ListCatalogModels(request: ListCatalogModelsRequest): Promise<ListCatalogModelsResponse>;
  GetModelCatalogRaw(request: wellKnownEmpty): Promise<ModelCatalogRawResponse>;
  SearchCatalogRaw(request: SearchCatalogRawRequest): Promise<SearchCatalogRawResponse>;
  ListModelCatalogSyncLogs(request: ListModelCatalogSyncLogsRequest): Promise<ListModelCatalogSyncLogsResponse>;
  SyncModelCatalog(request: SyncModelCatalogRequest): Promise<SyncModelCatalogResponse>;
  PreviewMigration(request: wellKnownEmpty): Promise<PreviewMigrationResponse>;
  GetCatalogProviderLogo(request: GetCatalogProviderLogoRequest): Promise<GetCatalogProviderLogoResponse>;
  ApplyProviderMigration(request: wellKnownEmpty): Promise<ApplyProviderMigrationResponse>;
  GetProviderMigrationRules(request: wellKnownEmpty): Promise<ProviderMigrationRulesResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createModelCatalogServiceClient(handler: RequestHandler): ModelCatalogService {
  return {
    GetModelCatalogStatus(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/status`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'GetModelCatalogStatus',
        },
      ) as Promise<ModelCatalogStatus>;
    },
    GetModelCatalogPolicy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/policy`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'GetModelCatalogPolicy',
        },
      ) as Promise<ModelCatalogPolicy>;
    },
    UpdateModelCatalogPolicy(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/policy`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'UpdateModelCatalogPolicy',
        },
      ) as Promise<ModelCatalogPolicy>;
    },
    ListCatalogProviders(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/providers`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.q) {
        queryParams.push(`q=${encodeURIComponent(request.q.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'ListCatalogProviders',
        },
      ) as Promise<ListCatalogProvidersResponse>;
    },
    ListCatalogModels(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.providerId) {
        throw new Error('missing required field request.provider_id');
      }
      const path = `v1/model-catalog/providers/${request.providerId}/models`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.q) {
        queryParams.push(`q=${encodeURIComponent(request.q.toString())}`);
      }
      if (request.includeDeprecated) {
        queryParams.push(`includeDeprecated=${encodeURIComponent(request.includeDeprecated.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'ListCatalogModels',
        },
      ) as Promise<ListCatalogModelsResponse>;
    },
    GetModelCatalogRaw(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/raw`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'GetModelCatalogRaw',
        },
      ) as Promise<ModelCatalogRawResponse>;
    },
    SearchCatalogRaw(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/raw/search`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.q) {
        queryParams.push(`q=${encodeURIComponent(request.q.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'SearchCatalogRaw',
        },
      ) as Promise<SearchCatalogRawResponse>;
    },
    ListModelCatalogSyncLogs(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/sync-logs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'ListModelCatalogSyncLogs',
        },
      ) as Promise<ListModelCatalogSyncLogsResponse>;
    },
    SyncModelCatalog(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/sync`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'SyncModelCatalog',
        },
      ) as Promise<SyncModelCatalogResponse>;
    },
    PreviewMigration(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/preview-migration`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'PreviewMigration',
        },
      ) as Promise<PreviewMigrationResponse>;
    },
    GetCatalogProviderLogo(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.providerId) {
        throw new Error('missing required field request.provider_id');
      }
      const path = `v1/model-catalog/logos/${request.providerId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'GetCatalogProviderLogo',
        },
      ) as Promise<GetCatalogProviderLogoResponse>;
    },
    ApplyProviderMigration(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/apply-migration`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'ApplyProviderMigration',
        },
      ) as Promise<ApplyProviderMigrationResponse>;
    },
    GetProviderMigrationRules(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/model-catalog/provider-migration`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ModelCatalogService',
          method: 'GetProviderMigrationRules',
        },
      ) as Promise<ProviderMigrationRulesResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
