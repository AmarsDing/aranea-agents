// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type PluginPermissions = {
  canView: boolean | undefined;
  canToggle: boolean | undefined;
  canEditConfig: boolean | undefined;
  canViewLogs: boolean | undefined;
};

export type Plugin = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  category: string | undefined;
  riskLevel: string | undefined;
  enabled: boolean | undefined;
  scope: string | undefined;
  callbackPoints: string[] | undefined;
  sortOrder: number | undefined;
  configSchemaJson: string | undefined;
  configJson: string | undefined;
  defaultConfigJson: string | undefined;
  invokeCount: number | undefined;
  blockCount: number | undefined;
  errorCount: number | undefined;
  lastInvokedAt: string | undefined;
  lastStatus: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  permissions: PluginPermissions | undefined;
};

export type ListPluginsRequest = {
  search: string | undefined;
  category: string | undefined;
  enabled: string | undefined;
  callbackPoint: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListPluginsResponse = {
  items: Plugin[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type TogglePluginEnabledRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  enabled: boolean | undefined;
};

export type UpdatePluginConfigRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  configJson: string | undefined;
};

export type UpdatePluginSortOrderRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  sortOrder: number | undefined;
};

export type UpdatePluginScopeRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  scope: string | undefined;
};

export type PluginRun = {
  id: string | undefined;
  pluginKey: string | undefined;
  pluginId: string | undefined;
  sessionId: string | undefined;
  agentId: string | undefined;
  callbackPoint: string | undefined;
  status: string | undefined;
  durationMs: number | undefined;
  detailJson: string | undefined;
  createdAt: string | undefined;
};

export type ListPluginRunsRequest = {
  pluginKey: string | undefined;
  pluginId: string | undefined;
  sessionId: string | undefined;
  agentId: string | undefined;
  callbackPoint: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListPluginRunsResponse = {
  items: PluginRun[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type DeleteAllPluginRunsRequest = {};

export type DeleteAllPluginRunsResponse = {
  deletedCount: number | undefined;
};

export interface PluginService {
  ListPlugins(request: ListPluginsRequest): Promise<ListPluginsResponse>;
  TogglePluginEnabled(request: TogglePluginEnabledRequest): Promise<Plugin>;
  UpdatePluginConfig(request: UpdatePluginConfigRequest): Promise<Plugin>;
  UpdatePluginSortOrder(request: UpdatePluginSortOrderRequest): Promise<Plugin>;
  UpdatePluginScope(request: UpdatePluginScopeRequest): Promise<Plugin>;
  ListPluginRuns(request: ListPluginRunsRequest): Promise<ListPluginRunsResponse>;
  DeleteAllPluginRuns(request: DeleteAllPluginRunsRequest): Promise<DeleteAllPluginRunsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createPluginServiceClient(handler: RequestHandler): PluginService {
  return {
    ListPlugins(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/plugins`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.search) {
        queryParams.push(`search=${encodeURIComponent(request.search.toString())}`);
      }
      if (request.category) {
        queryParams.push(`category=${encodeURIComponent(request.category.toString())}`);
      }
      if (request.enabled) {
        queryParams.push(`enabled=${encodeURIComponent(request.enabled.toString())}`);
      }
      if (request.callbackPoint) {
        queryParams.push(`callbackPoint=${encodeURIComponent(request.callbackPoint.toString())}`);
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'PluginService',
          method: 'ListPlugins',
        },
      ) as Promise<ListPluginsResponse>;
    },
    TogglePluginEnabled(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/plugins/${request.id}/enabled`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'PluginService',
          method: 'TogglePluginEnabled',
        },
      ) as Promise<Plugin>;
    },
    UpdatePluginConfig(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/plugins/${request.id}/config`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'PluginService',
          method: 'UpdatePluginConfig',
        },
      ) as Promise<Plugin>;
    },
    UpdatePluginSortOrder(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/plugins/${request.id}/sort-order`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'PluginService',
          method: 'UpdatePluginSortOrder',
        },
      ) as Promise<Plugin>;
    },
    UpdatePluginScope(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/plugins/${request.id}/scope`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'PluginService',
          method: 'UpdatePluginScope',
        },
      ) as Promise<Plugin>;
    },
    ListPluginRuns(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/plugins/runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.pluginKey) {
        queryParams.push(`pluginKey=${encodeURIComponent(request.pluginKey.toString())}`);
      }
      if (request.pluginId) {
        queryParams.push(`pluginId=${encodeURIComponent(request.pluginId.toString())}`);
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.callbackPoint) {
        queryParams.push(`callbackPoint=${encodeURIComponent(request.callbackPoint.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`);
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`);
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'PluginService',
          method: 'ListPluginRuns',
        },
      ) as Promise<ListPluginRunsResponse>;
    },
    DeleteAllPluginRuns(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/plugins/runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'PluginService',
          method: 'DeleteAllPluginRuns',
        },
      ) as Promise<DeleteAllPluginRunsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
