// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type PromptConfig = {
  instruction: string | undefined;
  systemPrompt: string | undefined;
};

export type ToolPolicy = {
  include: string[] | undefined;
  exclude: string[] | undefined;
  executionInclude: string[] | undefined;
  executionExclude: string[] | undefined;
  toolsets: string[] | undefined;
  credentialRefs: { [key: string]: string } | undefined;
};

export type SkillPolicy = {
  include: string[] | undefined;
  exclude: string[] | undefined;
  roots: string[] | undefined;
};

export type KnowledgePolicy = {
  indexes: string[] | undefined;
  filter: wellKnownStruct | undefined;
};

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

export type WorkspacePolicy = {
  workdir: string | undefined;
  allowedRoots: string[] | undefined;
};

export type CredentialPolicy = {
  allowedRefs: string[] | undefined;
};

export type IsolationPolicy = {
  mode: string | undefined;
  agentCache: boolean | undefined;
  toolsetCache: boolean | undefined;
  serviceMode: string | undefined;
};

export type RuntimeProfile = {
  id: string | undefined;
  agentId: string | undefined;
  name: string | undefined;
  description: string | undefined;
  version: string | undefined;
  isActive: boolean | undefined;
  priority: number | undefined;
  promptConfig: PromptConfig | undefined;
  toolPolicy: ToolPolicy | undefined;
  skillPolicy: SkillPolicy | undefined;
  knowledgePolicy: KnowledgePolicy | undefined;
  workspacePolicy: WorkspacePolicy | undefined;
  credentialPolicy: CredentialPolicy | undefined;
  isolationPolicy: IsolationPolicy | undefined;
  extraModelConfig: wellKnownStruct | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListRuntimeProfilesRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  activeOnly: boolean | undefined;
};

export type ListRuntimeProfilesResponse = {
  items: RuntimeProfile[] | undefined;
};

export type GetRuntimeProfileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CreateRuntimeProfileRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  version: string | undefined;
  isActive: boolean | undefined;
  priority: number | undefined;
  promptConfig: PromptConfig | undefined;
  toolPolicy: ToolPolicy | undefined;
  skillPolicy: SkillPolicy | undefined;
  knowledgePolicy: KnowledgePolicy | undefined;
  workspacePolicy: WorkspacePolicy | undefined;
  credentialPolicy: CredentialPolicy | undefined;
  isolationPolicy: IsolationPolicy | undefined;
  extraModelConfig: wellKnownStruct | undefined;
};

export type UpdateRuntimeProfileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  version: string | undefined;
  isActive: boolean | undefined;
  priority: number | undefined;
  promptConfig: PromptConfig | undefined;
  toolPolicy: ToolPolicy | undefined;
  skillPolicy: SkillPolicy | undefined;
  knowledgePolicy: KnowledgePolicy | undefined;
  workspacePolicy: WorkspacePolicy | undefined;
  credentialPolicy: CredentialPolicy | undefined;
  isolationPolicy: IsolationPolicy | undefined;
  extraModelConfig: wellKnownStruct | undefined;
};

export type DeleteRuntimeProfileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type SetActiveRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  active: boolean | undefined;
};

export interface RuntimeProfileService {
  ListRuntimeProfiles(request: ListRuntimeProfilesRequest): Promise<ListRuntimeProfilesResponse>;
  GetRuntimeProfile(request: GetRuntimeProfileRequest): Promise<RuntimeProfile>;
  CreateRuntimeProfile(request: CreateRuntimeProfileRequest): Promise<RuntimeProfile>;
  UpdateRuntimeProfile(request: UpdateRuntimeProfileRequest): Promise<RuntimeProfile>;
  DeleteRuntimeProfile(request: DeleteRuntimeProfileRequest): Promise<wellKnownEmpty>;
  SetActive(request: SetActiveRequest): Promise<RuntimeProfile>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createRuntimeProfileServiceClient(
  handler: RequestHandler
): RuntimeProfileService {
  return {
    ListRuntimeProfiles(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/runtime-profiles`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.activeOnly) {
        queryParams.push(`activeOnly=${encodeURIComponent(request.activeOnly.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "ListRuntimeProfiles",
      }) as Promise<ListRuntimeProfilesResponse>;
    },
    GetRuntimeProfile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/runtime-profiles/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "GetRuntimeProfile",
      }) as Promise<RuntimeProfile>;
    },
    CreateRuntimeProfile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/runtime-profiles`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "CreateRuntimeProfile",
      }) as Promise<RuntimeProfile>;
    },
    UpdateRuntimeProfile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/runtime-profiles/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "UpdateRuntimeProfile",
      }) as Promise<RuntimeProfile>;
    },
    DeleteRuntimeProfile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/runtime-profiles/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "DeleteRuntimeProfile",
      }) as Promise<wellKnownEmpty>;
    },
    SetActive(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/runtime-profiles/${request.id}/set-active`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "RuntimeProfileService",
        method: "SetActive",
      }) as Promise<RuntimeProfile>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
