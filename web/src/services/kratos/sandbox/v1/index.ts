// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// Sandbox is the admin-facing snapshot of one live sandbox instance (M82).
// Timestamps are RFC3339 strings (empty when unset).
export type Sandbox = {
  id: string | undefined;
  profile: string | undefined;
  agentKey: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  state: string | undefined;
  createdAt: string | undefined;
  deadline: string | undefined;
  lastExecAt: string | undefined;
  execCount: number | undefined;
};

export type ListSandboxesRequest = {
};

export type ListSandboxesResponse = {
  items: Sandbox[] | undefined;
};

// SandboxProfileMetrics is the per-profile water level.
export type SandboxProfileMetrics = {
  profile: string | undefined;
  ready: number | undefined;
  active: number | undefined;
};

// SandboxMetrics is the pool water level + counter snapshot since process
// boot (82-sandbox.design.md §8.1).
export type SandboxMetrics = {
  profiles: SandboxProfileMetrics[] | undefined;
  globalActive: number | undefined;
  acquireWarm: number | undefined;
  acquireCold: number | undefined;
  acquireFail: number | undefined;
  execOk: number | undefined;
  execError: number | undefined;
  execTimeout: number | undefined;
  destroy: { [key: string]: number } | undefined;
  quotaReject: { [key: string]: number } | undefined;
};

export type GetSandboxMetricsRequest = {
};

export type ForceKillSandboxRequest = {
  id: string | undefined;
  reason: string | undefined;
};

export type ForceKillSandboxResponse = {
  id: string | undefined;
};

// SandboxService is the M82 sandbox admin API (design §5.3, ADR-82-3:
// no execution capability over HTTP — only lifecycle management).
export interface SandboxService {
  // ListSandboxes returns all live sandboxes (warm-pool ready + leased).
  ListSandboxes(request: ListSandboxesRequest): Promise<ListSandboxesResponse>;
  // GetSandboxMetrics returns pool water levels and the counter snapshot.
  GetSandboxMetrics(request: GetSandboxMetricsRequest): Promise<SandboxMetrics>;
  // ForceKillSandbox destroys one sandbox immediately (FR-31). The operator
  // is taken from the JWT principal; reason is mandatory and both are
  // written to the audit log (FR-33).
  ForceKillSandbox(request: ForceKillSandboxRequest): Promise<ForceKillSandboxResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createSandboxServiceClient(
  handler: RequestHandler
): SandboxService {
  return {
    ListSandboxes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/sandboxes`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SandboxService",
        method: "ListSandboxes",
      }) as Promise<ListSandboxesResponse>;
    },
    GetSandboxMetrics(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/sandboxes/metrics`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SandboxService",
        method: "GetSandboxMetrics",
      }) as Promise<SandboxMetrics>;
    },
    ForceKillSandbox(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/sandboxes/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.reason) {
        queryParams.push(`reason=${encodeURIComponent(request.reason.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "SandboxService",
        method: "ForceKillSandbox",
      }) as Promise<ForceKillSandboxResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
