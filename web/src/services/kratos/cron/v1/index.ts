// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type CronTask = {
  id: string | undefined;
  taskKey: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  agentId: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
};

export type ListCronTasksResponse = {
  items: CronTask[] | undefined;
};

export type CreateCronTaskRequest = {
  //
  // Behaviors: REQUIRED
  taskKey: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  agentId: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
};

export type GetCronTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateCronTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  task: CronTask | undefined;
};

export type DeleteCronTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CronTaskRun = {
  id: string | undefined;
  taskId: string | undefined;
  taskName: string | undefined;
  status: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  trigger: string | undefined;
  runId: string | undefined;
  outputJson: string | undefined;
  errorMessage: string | undefined;
  createdAt: string | undefined;
};

export type ListCronTaskRunsRequest = {
  cronTaskId: string | undefined;
  status: string | undefined;
  // Deprecated: kept for CLI compatibility; ignored when page_size > 0.
  limit: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListCronTaskRunsResponse = {
  items: CronTaskRun[] | undefined;
  total: number | undefined;
};

export type TriggerCronTaskRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ResetCronTaskFailuresRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export interface CronService {
  ListCronTasks(request: wellKnownEmpty): Promise<ListCronTasksResponse>;
  CreateCronTask(request: CreateCronTaskRequest): Promise<CronTask>;
  GetCronTask(request: GetCronTaskRequest): Promise<CronTask>;
  UpdateCronTask(request: UpdateCronTaskRequest): Promise<CronTask>;
  DeleteCronTask(request: DeleteCronTaskRequest): Promise<wellKnownEmpty>;
  ListCronTaskRuns(request: ListCronTaskRunsRequest): Promise<ListCronTaskRunsResponse>;
  TriggerCronTask(request: TriggerCronTaskRequest): Promise<CronTaskRun>;
  ResetCronTaskFailures(request: ResetCronTaskFailuresRequest): Promise<CronTask>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createCronServiceClient(
  handler: RequestHandler
): CronService {
  return {
    ListCronTasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/cron-tasks`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "CronService",
        method: "ListCronTasks",
      }) as Promise<ListCronTasksResponse>;
    },
    CreateCronTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/cron-tasks`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "CronService",
        method: "CreateCronTask",
      }) as Promise<CronTask>;
    },
    GetCronTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/cron-tasks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "CronService",
        method: "GetCronTask",
      }) as Promise<CronTask>;
    },
    UpdateCronTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/cron-tasks/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.task ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "CronService",
        method: "UpdateCronTask",
      }) as Promise<CronTask>;
    },
    DeleteCronTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/cron-tasks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "CronService",
        method: "DeleteCronTask",
      }) as Promise<wellKnownEmpty>;
    },
    ListCronTaskRuns(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/cron-task-runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.cronTaskId) {
        queryParams.push(`cronTaskId=${encodeURIComponent(request.cronTaskId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "CronService",
        method: "ListCronTaskRuns",
      }) as Promise<ListCronTaskRunsResponse>;
    },
    TriggerCronTask(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/cron-tasks/${request.id}/trigger`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "CronService",
        method: "TriggerCronTask",
      }) as Promise<CronTaskRun>;
    },
    ResetCronTaskFailures(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/cron-tasks/${request.id}/reset-failures`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "CronService",
        method: "ResetCronTaskFailures",
      }) as Promise<CronTask>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
