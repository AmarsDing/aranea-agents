// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Team = {
  id: string | undefined;
  teamKey: string | undefined;
  displayName: string | undefined;
  status: string | undefined;
  isDefault: boolean | undefined;
  definitionJson: string | undefined;
  adkAppName: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
  // OrchestrationSpec.linked_graph_id (stored in definition_json).
  linkedGraphId: string | undefined;
  // True when the team has a running or pending TeamRun (orchestration read-only).
  hasActiveRun: boolean | undefined;
  // Strong-typed OrchestrationSpec v2 (parallel to definition_json; M53 TG-CMP-V2).
  orchestrationSpec: OrchestrationSpec | undefined;
  // Department node ID for team grouping.
  departmentId: string | undefined;
  spiritSessionId: string | undefined;
  taskDescription: string | undefined;
  autoCreated: boolean | undefined;
  dagNodeId: string | undefined;
  dependsOn: string[] | undefined;
  parallelConfigJson: string | undefined;
  // readonly: system teams cannot be deleted.
  readonly: boolean | undefined;
  // source: user | system | imported.
  source: string | undefined;
  // kind: user | system_builtin | ecosystem_preset | marketplace | certified (ownership classification, maps from DB kind column).
  kind: string | undefined;
  // JSON array of team deliverables.
  deliverables: string | undefined;
  // JSON schema for team input contract.
  inputContract: string | undefined;
  // Agent ID of the department lead.
  deptLeadAgentId: string | undefined;
  // JSON array of cross-department member IDs.
  crossDeptMemberIds: string | undefined;
};

export type OrchestrationSpec = {
  version: number | undefined;
  mode: string | undefined;
  members: OrchestrationMember[] | undefined;
  graph: EmbeddedGraph | undefined;
  linkedGraphId: string | undefined;
  failurePolicy: FailurePolicySpec | undefined;
  runtimeEngine: string | undefined;
  turnTimeoutSec: number | undefined;
  firstByteTimeoutSec: number | undefined;
  intentAnchorAgentId: string | undefined;
  description: string | undefined;
  maxConcurrency: number | undefined;
  timeoutSeconds: number | undefined;
  loopMaxIterations: number | undefined;
  enableCheckpoint: boolean | undefined;
};

export type OrchestrationMember = {
  agentId: string | undefined;
  role: string | undefined;
  name: string | undefined;
  taskPrompt: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
};

export type EmbeddedGraph = {
  version: number | undefined;
  layout: string | undefined;
  nodes: EmbeddedGraphNode[] | undefined;
  edges: EmbeddedGraphEdge[] | undefined;
};

export type EmbeddedGraphNode = {
  id: string | undefined;
  type: string | undefined;
  label: string | undefined;
  agentId: string | undefined;
  role: string | undefined;
  interruptBefore: boolean | undefined;
  interruptAfter: boolean | undefined;
  destinations: string[] | undefined;
  taskPrompt: string | undefined;
  enabled?: boolean;
};

export type EmbeddedGraphEdge = {
  id: string | undefined;
  source: string | undefined;
  target: string | undefined;
  label: string | undefined;
  condition: string | undefined;
};

export type FailurePolicySpec = {
  defaultPolicy: string | undefined;
  parallelFail: string | undefined;
  retry: TeamRetryPolicySpec | undefined;
  nodeOverrides: { [key: string]: TeamNodeFailureOverrideSpec } | undefined;
  circuitBreaker: CircuitBreakerSpec | undefined;
  onError: string | undefined;
};

export type TeamRetryPolicySpec = {
  maxAttempts: number | undefined;
  initialIntervalMs: number | undefined;
  backoffFactor: number | undefined;
  maxIntervalMs: number | undefined;
};

export type TeamNodeFailureOverrideSpec = {
  policy: string | undefined;
  retry: TeamRetryPolicySpec | undefined;
  fallbackAgent: string | undefined;
};

export type CircuitBreakerSpec = {
  failureThreshold: number | undefined;
  resetTimeoutSeconds: number | undefined;
  fallbackNode: string | undefined;
};

export type TeamRun = {
  id: string | undefined;
  teamId: string | undefined;
  sessionId: string | undefined;
  messageId: string | undefined;
  mode: string | undefined;
  status: string | undefined;
  inputPreview: string | undefined;
  outputPreview: string | undefined;
  tokenIn: number | undefined;
  tokenOut: number | undefined;
  costMicroUsd: number | undefined;
  durationMs: number | undefined;
  errorMessage: string | undefined;
  topologyJson: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  graphExecutionId: string | undefined;
  // Frozen OrchestrationSpec JSON captured at run start (M53 TG-API-03).
  definitionSnapshotJson: string | undefined;
  // Cross-domain trace id (M53 OPS-TRACE-01).
  traceId: string | undefined;
};

export type TeamRunStep = {
  id: string | undefined;
  runId: string | undefined;
  teamId: string | undefined;
  agentId: string | undefined;
  agentKey: string | undefined;
  agentName: string | undefined;
  role: string | undefined;
  sortOrder: number | undefined;
  status: string | undefined;
  inputPreview: string | undefined;
  outputPreview: string | undefined;
  tokenIn: number | undefined;
  tokenOut: number | undefined;
  costMicroUsd: number | undefined;
  durationMs: number | undefined;
  errorMessage: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  createdAt: string | undefined;
  toolCallCount: number | undefined;
};

export type TeamRunMemberSummary = {
  agentId: string | undefined;
  agentKey: string | undefined;
  agentName: string | undefined;
  role: string | undefined;
  sortOrder: number | undefined;
  status: string | undefined;
  tokenIn: number | undefined;
  tokenOut: number | undefined;
  durationMs: number | undefined;
  costMicroUsd: number | undefined;
  outputPreview: string | undefined;
  toolCallCount: number | undefined;
};

export type TeamRunSummary = {
  runId: string | undefined;
  teamId: string | undefined;
  sessionId: string | undefined;
  mode: string | undefined;
  status: string | undefined;
  durationMs: number | undefined;
  tokenIn: number | undefined;
  tokenOut: number | undefined;
  costMicroUsd: number | undefined;
  memberCount: number | undefined;
  toolCallCount: number | undefined;
  outputPreview: string | undefined;
  errorMessage: string | undefined;
  members: TeamRunMemberSummary[] | undefined;
};

export type GetTeamRunSummaryRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetTeamRunSummaryResponse = {
  summary: TeamRunSummary | undefined;
};

export type ActivitySnapshotView = {
  kind: string | undefined;
  displayLabel: string | undefined;
  toolName: string | undefined;
  status: string | undefined;
  summary: string | undefined;
  argumentsJson: string | undefined;
  resultJson: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  durationMs: number | undefined;
  errorCode: string | undefined;
};

export type AgentNodeStateView = {
  nodeId: string | undefined;
  agentId: string | undefined;
  agentKey: string | undefined;
  agentName: string | undefined;
  role: string | undefined;
  status: string | undefined;
  displayStatus: string | undefined;
  phase: string | undefined;
  retryCount: number | undefined;
  inputPreview: string | undefined;
  outputPreview: string | undefined;
  errorMessage: string | undefined;
  currentActivity: ActivitySnapshotView | undefined;
  activityHistory: ActivitySnapshotView[] | undefined;
};

export type GetTeamRunObservatoryRequest = {
  //
  // Behaviors: REQUIRED
  runId: string | undefined;
};

export type GetTeamRunObservatoryResponse = {
  runId: string | undefined;
  teamId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  mode: string | undefined;
  nodes: AgentNodeStateView[] | undefined;
  // Backend-compiled topology from run snapshot (CompileTeamGraph parity).
  compiledTopology: CompileTeamGraphResponse | undefined;
  // Echo of frozen definition JSON used for bootstrap (empty for legacy runs).
  definitionSnapshotJson: string | undefined;
  // Graph execution id when run uses compiled graph runtime (M54 task board).
  graphExecutionId: string | undefined;
  // Cross-domain trace id for Monitor deep link (M53 OPS-TRACE-01).
  traceId: string | undefined;
};

export type CompileTeamGraphResponse = {
  templateId: string | undefined;
  mode: string | undefined;
  entryPoint: string | undefined;
  finishPoint: string | undefined;
  nodes: CompiledGraphNodeView[] | undefined;
  edges: CompiledGraphEdgeView[] | undefined;
  conditionalEdges: CompiledGraphConditionalEdgeView[] | undefined;
  graphJson: string | undefined;
  issues: CompileTeamGraphValidationIssue[] | undefined;
  valid: boolean | undefined;
};

export type CompiledGraphNodeView = {
  id: string | undefined;
  type: string | undefined;
  agentName: string | undefined;
  role: string | undefined;
  description: string | undefined;
  // Catalog display name resolved from member.agent_id (falls back to description).
  agentDisplayName: string | undefined;
  // Member task_prompt / responsibility text for UI "做什么".
  taskPrompt: string | undefined;
};

export type CompiledGraphEdgeView = {
  from: string | undefined;
  to: string | undefined;
  edgeKind: string | undefined;
};

export type CompiledGraphConditionalEdgeView = {
  from: string | undefined;
  pathMap: { [key: string]: string } | undefined;
};

export type CompileTeamGraphValidationIssue = {
  code: string | undefined;
  nodeId: string | undefined;
  field: string | undefined;
  message: string | undefined;
  warning: boolean | undefined;
};

export type GetTeamRunObservatoryTimelineRequest = {
  //
  // Behaviors: REQUIRED
  runId: string | undefined;
  nodeId?: string;
  limit?: number;
};

export type ActivityTimelineRow = {
  nodeId: string | undefined;
  kind: string | undefined;
  displayLabel: string | undefined;
  status: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  durationMs: number | undefined;
  traceId: string | undefined;
};

export type GetTeamRunObservatoryTimelineResponse = {
  rows: ActivityTimelineRow[] | undefined;
  traceId: string | undefined;
};

export type ResumeTeamRunExecutionRequest = {
  //
  // Behaviors: REQUIRED
  runId: string | undefined;
  resumeValue: wellKnownStruct | undefined;
};

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

export type ResumeTeamRunExecutionResponse = {
  runId: string | undefined;
  graphExecutionId: string | undefined;
  status: string | undefined;
};

export type ListTeamsRequest = {};

export type ListTeamsResponse = {
  items: Team[] | undefined;
};

export type CreateTeamRequest = {
  //
  // Behaviors: REQUIRED
  teamKey: string | undefined;
  //
  // Behaviors: REQUIRED
  displayName: string | undefined;
  status: string | undefined;
  definitionJson: string | undefined;
  adkAppName: string | undefined;
  departmentId: string | undefined;
};

export type GetTeamRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateTeamRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  team: Team | undefined;
};

export type DeleteTeamRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type DuplicateTeamRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ListTeamRunsRequest = {
  teamId: string | undefined;
  limit: number | undefined;
};

export type ListTeamRunsResponse = {
  items: TeamRun[] | undefined;
};

export type ListTeamRunStepsRequest = {
  //
  // Behaviors: REQUIRED
  runId: string | undefined;
};

export type ListTeamRunStepsResponse = {
  items: TeamRunStep[] | undefined;
};

export type GetTeamRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CancelTeamRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type RunTeamTestRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  content: string | undefined;
};

export type RunTeamTestResponse = {
  run: TeamRun | undefined;
  reply: string | undefined;
};

export type UpdateSwarmMembersRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
  addAgentIds: string[] | undefined;
  removeAgentIds: string[] | undefined;
};

export type UpdateSwarmMembersResponse = {
  updated: boolean | undefined;
};

export type ExportTeamStructureRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
};

export type ExportTeamStructureResponse = {
  entryNodeId: string | undefined;
  nodes: StructureNode[] | undefined;
  edges: StructureEdge[] | undefined;
  surfaces: StructureSurface[] | undefined;
};

export type StructureNode = {
  nodeId: string | undefined;
  kind: string | undefined;
  name: string | undefined;
};

export type StructureEdge = {
  fromNodeId: string | undefined;
  toNodeId: string | undefined;
};

export type StructureSurface = {
  nodeId: string | undefined;
  name: string | undefined;
};

export type CompileTeamGraphRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
  // Optional override; when set, compiles this JSON instead of persisted team definition.
  definitionJson: string | undefined;
};

export type TaskDeadLetter = {
  id: string | undefined;
  sourceType: string | undefined;
  sourceId: string | undefined;
  teamId: string | undefined;
  teamRunId: string | undefined;
  sessionId: string | undefined;
  graphExecutionId: string | undefined;
  errorMessage: string | undefined;
  payloadJson: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  resolvedAt: string | undefined;
};

export type ListTaskDeadLettersRequest = {
  sessionId: string | undefined;
  teamId: string | undefined;
  status: string | undefined;
  limit: number | undefined;
};

export type ListTaskDeadLettersResponse = {
  items: TaskDeadLetter[] | undefined;
};

export type ResolveTaskDeadLetterRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ResolveTaskDeadLetterResponse = {
  item: TaskDeadLetter | undefined;
};

export type SpiritMemberView = {
  agentId: string | undefined;
  agentKey: string | undefined;
  displayName: string | undefined;
  role: string | undefined;
  status: string | undefined;
  avatarUrl: string | undefined;
};

export type SpiritTeamView = {
  id: string | undefined;
  teamName: string | undefined;
  taskSummary: string | undefined;
  status: string | undefined;
  mode: string | undefined;
  memberAvatars: string[] | undefined;
  completedSteps: number | undefined;
  totalSteps: number | undefined;
  durationMs: number | undefined;
  spiritSessionId: string | undefined;
  teamSessionId: string | undefined;
  members: SpiritMemberView[] | undefined;
  sharedAgentIds: string[] | undefined;
  dagNodeId: string | undefined;
  graphExecutionId: string | undefined;
  dependsOn: string[] | undefined;
  topologyReason: string | undefined;
  interruptReason: string | undefined;
  tokenIn: number | undefined;
  tokenOut: number | undefined;
};

export type ListSpiritTeamsRequest = {
  //
  // Behaviors: REQUIRED
  spiritSessionId: string | undefined;
};

export type ListSpiritTeamsResponse = {
  teams: SpiritTeamView[] | undefined;
};

export type SynthesizeResultsRequest = {
  //
  // Behaviors: REQUIRED
  spiritSessionId: string | undefined;
  strategy: string | undefined;
};

export type SynthesisTeamResult = {
  teamId: string | undefined;
  teamName: string | undefined;
  status: string | undefined;
  resultSummary: string | undefined;
  keyFindings: string[] | undefined;
};

export type SynthesizeResultsResponse = {
  strategy: string | undefined;
  unifiedSummary: string | undefined;
  teamResults: SynthesisTeamResult[] | undefined;
  successRate: number | undefined;
  partialFailure: boolean | undefined;
  cascadeNote: string | undefined;
};

export type ArchiveTeamRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
};

export type ArchiveTeamResponse = {
  teamId: string | undefined;
  status: string | undefined;
};

export type RetryTeamRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
};

export type RetryTeamResponse = {
  teamId: string | undefined;
  status: string | undefined;
};

// PauseTeamRunRequest pauses a running team run.
// The team transitions to 'paused' state after the current member step completes.
export type PauseTeamRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type PauseTeamRunResponse = {
  runId: string | undefined;
  status: string | undefined;
};

// UnpauseTeamRunRequest resumes a paused team run.
// The team transitions back to 'running' state and continues execution.
// Note: this is distinct from ResumeTeamRunExecution which is used for HITL graph recovery.
export type UnpauseTeamRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UnpauseTeamRunResponse = {
  runId: string | undefined;
  status: string | undefined;
};

// InjectTeamMessageRequest injects a user message into a team run's pending queue.
// The message is processed at the next step boundary during execution.
export type InjectTeamMessageRequest = {
  //
  // Behaviors: REQUIRED
  teamId: string | undefined;
  //
  // Behaviors: REQUIRED
  message: string | undefined;
};

export type InjectTeamMessageResponse = {
  accepted: boolean | undefined;
  queued: boolean | undefined;
  pendingId: string | undefined;
};

export interface TeamService {
  ListTeams(request: ListTeamsRequest): Promise<ListTeamsResponse>;
  CreateTeam(request: CreateTeamRequest): Promise<Team>;
  GetTeam(request: GetTeamRequest): Promise<Team>;
  UpdateTeam(request: UpdateTeamRequest): Promise<Team>;
  DeleteTeam(request: DeleteTeamRequest): Promise<wellKnownEmpty>;
  DuplicateTeam(request: DuplicateTeamRequest): Promise<Team>;
  ListTeamRuns(request: ListTeamRunsRequest): Promise<ListTeamRunsResponse>;
  ListTeamRunSteps(request: ListTeamRunStepsRequest): Promise<ListTeamRunStepsResponse>;
  GetTeamRun(request: GetTeamRunRequest): Promise<TeamRun>;
  CancelTeamRun(request: CancelTeamRunRequest): Promise<TeamRun>;
  RunTeamTest(request: RunTeamTestRequest): Promise<RunTeamTestResponse>;
  UpdateSwarmMembers(request: UpdateSwarmMembersRequest): Promise<UpdateSwarmMembersResponse>;
  ExportTeamStructure(request: ExportTeamStructureRequest): Promise<ExportTeamStructureResponse>;
  GetTeamRunSummary(request: GetTeamRunSummaryRequest): Promise<GetTeamRunSummaryResponse>;
  GetTeamRunObservatory(request: GetTeamRunObservatoryRequest): Promise<GetTeamRunObservatoryResponse>;
  GetTeamRunObservatoryTimeline(
    request: GetTeamRunObservatoryTimelineRequest,
  ): Promise<GetTeamRunObservatoryTimelineResponse>;
  ResumeTeamRunExecution(request: ResumeTeamRunExecutionRequest): Promise<ResumeTeamRunExecutionResponse>;
  CompileTeamGraph(request: CompileTeamGraphRequest): Promise<CompileTeamGraphResponse>;
  ListTaskDeadLetters(request: ListTaskDeadLettersRequest): Promise<ListTaskDeadLettersResponse>;
  ResolveTaskDeadLetter(request: ResolveTaskDeadLetterRequest): Promise<ResolveTaskDeadLetterResponse>;
  // ListSpiritTeams returns teams belonging to a spirit session.
  ListSpiritTeams(request: ListSpiritTeamsRequest): Promise<ListSpiritTeamsResponse>;
  // SynthesizeResults merges results from multiple teams into a unified output.
  SynthesizeResults(request: SynthesizeResultsRequest): Promise<SynthesizeResultsResponse>;
  // ArchiveTeam archives a completed/failed/cancelled team.
  ArchiveTeam(request: ArchiveTeamRequest): Promise<ArchiveTeamResponse>;
  // RetryTeam retries a failed/cancelled team by resetting its status and re-starting.
  RetryTeam(request: RetryTeamRequest): Promise<RetryTeamResponse>;
  // PauseTeamRun pauses a running team run (running → paused).
  // User-initiated pause; the current member step completes before pausing.
  PauseTeamRun(request: PauseTeamRunRequest): Promise<PauseTeamRunResponse>;
  // UnpauseTeamRun resumes a paused team run (paused → running).
  // User-initiated resume; distinct from ResumeTeamRunExecution (HITL graph recovery).
  UnpauseTeamRun(request: UnpauseTeamRunRequest): Promise<UnpauseTeamRunResponse>;
  // InjectTeamMessage injects a user message into a team run's pending queue.
  // The message is processed at the next step boundary during execution.
  InjectTeamMessage(request: InjectTeamMessageRequest): Promise<InjectTeamMessageResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createTeamServiceClient(handler: RequestHandler): TeamService {
  return {
    ListTeams(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/teams`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ListTeams',
        },
      ) as Promise<ListTeamsResponse>;
    },
    CreateTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/teams`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'CreateTeam',
        },
      ) as Promise<Team>;
    },
    GetTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/teams/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'GetTeam',
        },
      ) as Promise<Team>;
    },
    UpdateTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/teams/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.team ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'TeamService',
          method: 'UpdateTeam',
        },
      ) as Promise<Team>;
    },
    DeleteTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/teams/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'TeamService',
          method: 'DeleteTeam',
        },
      ) as Promise<wellKnownEmpty>;
    },
    DuplicateTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/teams/${request.id}/duplicate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'DuplicateTeam',
        },
      ) as Promise<Team>;
    },
    ListTeamRuns(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/team-runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ListTeamRuns',
        },
      ) as Promise<ListTeamRunsResponse>;
    },
    ListTeamRunSteps(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.runId) {
        throw new Error('missing required field request.run_id');
      }
      const path = `v1/team-runs/${request.runId}/steps`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ListTeamRunSteps',
        },
      ) as Promise<ListTeamRunStepsResponse>;
    },
    GetTeamRun(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/team-runs/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'GetTeamRun',
        },
      ) as Promise<TeamRun>;
    },
    CancelTeamRun(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/team-runs/${request.id}/cancel`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'CancelTeamRun',
        },
      ) as Promise<TeamRun>;
    },
    RunTeamTest(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/teams/${request.id}/run-test`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'RunTeamTest',
        },
      ) as Promise<RunTeamTestResponse>;
    },
    UpdateSwarmMembers(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/swarm-members`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'UpdateSwarmMembers',
        },
      ) as Promise<UpdateSwarmMembersResponse>;
    },
    ExportTeamStructure(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/structure`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ExportTeamStructure',
        },
      ) as Promise<ExportTeamStructureResponse>;
    },
    GetTeamRunSummary(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/team-runs/${request.id}/summary`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'GetTeamRunSummary',
        },
      ) as Promise<GetTeamRunSummaryResponse>;
    },
    GetTeamRunObservatory(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.runId) {
        throw new Error('missing required field request.run_id');
      }
      const path = `v1/team-runs/${request.runId}/observatory`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'GetTeamRunObservatory',
        },
      ) as Promise<GetTeamRunObservatoryResponse>;
    },
    GetTeamRunObservatoryTimeline(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.runId) {
        throw new Error('missing required field request.run_id');
      }
      const path = `v1/team-runs/${request.runId}/observatory/timeline`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.nodeId) {
        queryParams.push(`nodeId=${encodeURIComponent(request.nodeId.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'GetTeamRunObservatoryTimeline',
        },
      ) as Promise<GetTeamRunObservatoryTimelineResponse>;
    },
    ResumeTeamRunExecution(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.runId) {
        throw new Error('missing required field request.run_id');
      }
      const path = `v1/team-runs/${request.runId}/resume`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'ResumeTeamRunExecution',
        },
      ) as Promise<ResumeTeamRunExecutionResponse>;
    },
    CompileTeamGraph(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/compile-graph`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'CompileTeamGraph',
        },
      ) as Promise<CompileTeamGraphResponse>;
    },
    ListTaskDeadLetters(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/task-dead-letters`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`);
      }
      if (request.teamId) {
        queryParams.push(`teamId=${encodeURIComponent(request.teamId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ListTaskDeadLetters',
        },
      ) as Promise<ListTaskDeadLettersResponse>;
    },
    ResolveTaskDeadLetter(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/task-dead-letters/${request.id}/resolve`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'ResolveTaskDeadLetter',
        },
      ) as Promise<ResolveTaskDeadLetterResponse>;
    },
    ListSpiritTeams(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.spiritSessionId) {
        throw new Error('missing required field request.spirit_session_id');
      }
      const path = `v1/spirit/${request.spiritSessionId}/teams`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'TeamService',
          method: 'ListSpiritTeams',
        },
      ) as Promise<ListSpiritTeamsResponse>;
    },
    SynthesizeResults(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.spiritSessionId) {
        throw new Error('missing required field request.spirit_session_id');
      }
      const path = `v1/spirit/${request.spiritSessionId}/synthesize`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'SynthesizeResults',
        },
      ) as Promise<SynthesizeResultsResponse>;
    },
    ArchiveTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/archive`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'ArchiveTeam',
        },
      ) as Promise<ArchiveTeamResponse>;
    },
    RetryTeam(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/retry`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'RetryTeam',
        },
      ) as Promise<RetryTeamResponse>;
    },
    PauseTeamRun(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/team-runs/${request.id}/pause`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'PauseTeamRun',
        },
      ) as Promise<PauseTeamRunResponse>;
    },
    UnpauseTeamRun(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/team-runs/${request.id}/unpause`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'UnpauseTeamRun',
        },
      ) as Promise<UnpauseTeamRunResponse>;
    },
    InjectTeamMessage(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.teamId) {
        throw new Error('missing required field request.team_id');
      }
      const path = `v1/teams/${request.teamId}/inject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'TeamService',
          method: 'InjectTeamMessage',
        },
      ) as Promise<InjectTeamMessageResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
