// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// AgentRuntimeSettings mirrors legacy agent_runtime_settings / frontend AgentRuntimeSettings.
export type AgentRuntimeSettings = {
  agentId: string | undefined;
  selfEvolve: boolean | undefined;
  subagentsEnabled: boolean | undefined;
  subagentsMaxConcurrency: number | undefined;
  subagentsMaxGenerationDepth: number | undefined;
  subagentsMaxChildrenPerAgent: number | undefined;
  subagentsArchiveAfterMinutes: number | undefined;
  subagentsMaxRetries: number | undefined;
  subagentsModelOverride: string | undefined;
  toolsEnabled: boolean | undefined;
  toolsProfile: string | undefined;
  toolsToolCallPrefix: string | undefined;
  toolsAllowJson: string | undefined;
  toolsDenyJson: string | undefined;
  toolsConcurrentAllowJson: string | undefined;
  memoryEnabled: boolean | undefined;
  memoryMaxChunkLength: number | undefined;
  memoryMaxResults: number | undefined;
  memoryMinScore: number | undefined;
  heartbeatEnabled: boolean | undefined;
  heartbeatIntervalMinutes: number | undefined;
  evolutionSelfEvolve: boolean | undefined;
  evolutionSkillEvolve: boolean | undefined;
  evolutionMetricsEnabled: boolean | undefined;
  evolutionSuggestionsEnabled: boolean | undefined;
  guardrailMaxChangePerPeriod: number | undefined;
  guardrailMinDataPoints: number | undefined;
  guardrailRollbackOnDeclinePercent: number | undefined;
  l0RecentWindowTurns: number | undefined;
  l0RecentWindowTokens: number | undefined;
  l0SummaryThreshold: number | undefined;
  l0SummaryKeepTurns: number | undefined;
  l0TruncateStrategy: string | undefined;
  l0InjectL1: boolean | undefined;
  l0InjectL3: boolean | undefined;
  l0InjectL4: boolean | undefined;
  l0L3MaxChunks: number | undefined;
  l0L4MaxPaths: number | undefined;
  l0SnapshotMode: string | undefined;
  l0SnapshotEnabled: boolean | undefined;
  l1Enabled: boolean | undefined;
  l1BudgetTokens: number | undefined;
  l1FieldMaxTokens: number | undefined;
  l1HistoryKeepRevisions: number | undefined;
  l1DefaultSchemaId: string | undefined;
  l1ArchiveOnIdleMinutes: number | undefined;
  l2EpisodeEnabled: boolean | undefined;
  l2EpisodeMinImportance: number | undefined;
  l2IndexEnabled: boolean | undefined;
  l2IndexEmbeddingModel: string | undefined;
  l2RecallEnabled: boolean | undefined;
  l2RecallMax: number | undefined;
  l2RetentionDays: number | undefined;
  l2ArchiveAfterDays: number | undefined;
  l3Enabled: boolean | undefined;
  l3RecallTopK: number | undefined;
  l3RecallMinScore: number | undefined;
  l3RecallScopesJson: string | undefined;
  l3EmbeddingModel: string | undefined;
  l3DecayIntervalHours: number | undefined;
  l3ArchiveThreshold: number | undefined;
  l3MaxPerRecallChars: number | undefined;
  l4Enabled: boolean | undefined;
  l4GraphInjectNeighbors: boolean | undefined;
  l4GraphMaxNeighbors: number | undefined;
  l4GraphMaxHops: number | undefined;
  l4IdentityInject: boolean | undefined;
  l4StrategyInject: boolean | undefined;
  evoEnabled: boolean | undefined;
  evoAutoApply: boolean | undefined;
  evoMinEpisodes: number | undefined;
  evoMinNegativeFeedback: number | undefined;
  evoThrottleHours: number | undefined;
  evoProposalTtlDays: number | undefined;
  evoPersonaMaxChars: number | undefined;
  evoSystemPromptMaxAppends: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  skillRuntimeJson: string | undefined;
  intentPassEnabled?: boolean;
  variablesJson: string | undefined;
  modelInstructionsJson: string | undefined;
  contextCompactionEnabled: boolean | undefined;
  memoryCompactEnabled: boolean | undefined;
  toolResultGateEnabled: boolean | undefined;
  sessionSummaryEnabled: boolean | undefined;
  // skill_load_mode controls how skill content is loaded:
  // "progressive" - 3-phase progressive loading (default): L0 manifest only in
  // prompt, L1 body via skill_load tool, L2 refs via skill_select_docs
  // "once"      - load for one model request, then offload
  // "turn"      - load for the current invocation, offload on next
  // "session"   - keep loaded across invocations (legacy)
  skillLoadMode: string | undefined;
  outputSchemaJson: string | undefined;
  modelSelector: string | undefined;
  toolsRetryEnabled: boolean | undefined;
  toolsRetryMaxAttempts: number | undefined;
  toolsRetryInitialIntervalMs: number | undefined;
  toolsRetryBackoffFactor: number | undefined;
  toolsRetryMaxIntervalMs: number | undefined;
  toolsRetryJitter: boolean | undefined;
  toolsParallelEnabled: boolean | undefined;
  toolsStreamingEnabled: boolean | undefined;
  channelId: string | undefined;
  chatId: string | undefined;
  workspace: string | undefined;
  reasoningMode: string | undefined;
  reasoningLevel: string | undefined;
  // planner_kind selects the planning strategy: "" | "builtin" | "react" | "a2ui"
  plannerKind: string | undefined;
  // code_executor_type selects Skill code execution backend: local | docker | e2b | container
  codeExecutorType: string | undefined;
  // planner_config_json holds planner-specific options (shape depends on planner_kind)
  plannerConfigJson: string | undefined;
  // Ralph Loop: iterative run until completion_promise and/or verify_command succeeds
  ralphLoopMaxIterations: number | undefined;
  ralphLoopCompletionPromise: string | undefined;
  ralphLoopVerifyCommand: string | undefined;
  ralphLoopVerifyTimeoutSeconds: number | undefined;
  ralphLoopPromiseTagOpen: string | undefined;
  ralphLoopPromiseTagClose: string | undefined;
  ralphLoopVerifyWorkDir: string | undefined;
  // L0CompressProvider / L0CompressModel: cheaper catalog model for session summarization; empty → chat model.
  l0CompressProvider: string | undefined;
  l0CompressModel: string | undefined;
  // MemoryWorkerProvider / MemoryWorkerModel: LLM for async memory extraction; empty → L0 compress, then chat model.
  memoryWorkerProvider: string | undefined;
  memoryWorkerModel: string | undefined;
  toolsCircuitBreakerEnabled: boolean | undefined;
  toolsCircuitBreakerOverridesJson: string | undefined;
  toolsDeferredJson: string | undefined;
  toolsCommandSafetyEnabled: boolean | undefined;
  // CompressLLMCacheEnabled enables LLM compression response caching (LRU + TTL).
  compressLlmCacheEnabled: boolean | undefined;
  // CompressLLMCacheMaxEntries is the maximum number of cache entries (default 256).
  compressLlmCacheMaxEntries: number | undefined;
  // CompressLLMCacheTTLSec is the cache TTL in seconds (default 600).
  compressLlmCacheTtlSec: number | undefined;
  // CompressionBufferRatio is the fraction of contextWindow reserved as compression buffer (default 0.15, range 0.10–0.25).
  compressionBufferRatio: number | undefined;
  // SoftTriggerRatio is the fraction of effective_budget at which async compression triggers (default 0.70).
  softTriggerRatio: number | undefined;
  // HardTriggerRatio is the fraction of effective_budget at which sync compression triggers (default 0.90).
  hardTriggerRatio: number | undefined;
  // SubagentsStoredResultRunes is the max rune count for stored subagent results (default 4000).
  subagentsStoredResultRunes: number | undefined;
  // SubagentsStoredSummaryRunes is the max rune count for stored subagent summaries (default 240).
  subagentsStoredSummaryRunes: number | undefined;
  // VerificationTruncateChars is the max character count for truncating team output in verification gate prompts (default 2000).
  verificationTruncateChars: number | undefined;
  // L1HistoryEnabled controls whether old field values are archived to memory_l1_field_history on upsert (default false).
  l1HistoryEnabled: boolean | undefined;
  // L3RecallBudgetTokens is the token budget for the fused L2/L3 recall block
  // in the prompt (FR-12/P2). Hits are packed score-descending and the block is
  // truncated once the estimated tokens exceed this budget. 0 = default 800.
  // Preset tiers: 400 (compact) / 800 (standard) / 1600 (generous).
  l3RecallBudgetTokens: number | undefined;
  // L2RecallBudgetTokens is the token budget for the standalone L2 episodic
  // recall block (2026-08-20: split from l3_recall_budget_tokens so the two
  // blocks can be tuned independently). 0 = default 800.
  // Preset tiers: 400 (compact) / 800 (standard) / 1600 (generous).
  l2RecallBudgetTokens: number | undefined;
  // L3InjectProvenance controls whether injected L3 facts carry provenance
  // metadata ([id:…, src:…, conf:…, v…]) in the prompt. Absent → default true
  // (matches DefaultAgentRuntimeSettings and Ent schema default).
  l3InjectProvenance?: boolean;
};

export type AgentPromptFile = {
  id: string | undefined;
  agentId: string | undefined;
  name: string | undefined;
  body: string | undefined;
  sortOrder: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

// A2AProxyConfig connects an a2a_proxy catalog agent to a remote A2A service.
export type A2AProxyConfig = {
  remoteUrl: string | undefined;
  agentCardUrl: string | undefined;
  enableStreaming: boolean | undefined;
  authType: string | undefined;
  authConfigJson: string | undefined;
  timeoutSeconds: number | undefined;
};

export type Agent = {
  id: string | undefined;
  agentKey: string | undefined;
  displayName: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  status: string | undefined;
  isDefault: boolean | undefined;
  isFavorite: boolean | undefined;
  icon: string | undefined;
  agentDescription: string | undefined;
  positionId: string | undefined;
  systemPromptMode: string | undefined;
  contextWindow: number | undefined;
  budgetMonthlyCents: number | undefined;
  configJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
  settings: AgentRuntimeSettings | undefined;
  files: AgentPromptFile[] | undefined;
  // agent_kind: "" | "llm" | "a2a_proxy"
  agentKind: string | undefined;
  a2aProxyConfig: A2AProxyConfig | undefined;
  // a2a_endpoint_enabled: true when A2A AgentCard is enabled (list enrichment).
  a2aEndpointEnabled: boolean | undefined;
  // List enrichment: latest session run lifecycle (runtime.status) or idle/completed.
  lastRunStatus: string | undefined;
  lastRunAt: string | undefined;
  pendingEvolutionCount: number | undefined;
  // Creator user id (stringified auth UserID).
  createdBy: string | undefined;
  // readonly: system agents cannot be deleted.
  readonly: boolean | undefined;
  positionKey: string | undefined;
  agentVariant: string | undefined;
  variantDescription: string | undefined;
  // source: user | system | imported (origin tracking, maps from DB source column).
  source: string | undefined;
  // kind: user | system_builtin | ecosystem_preset | marketplace | certified (ownership classification, maps from DB kind column).
  kind: string | undefined;
};

export type ListAgentsRequest = {
  keyword: string | undefined;
  status: string | undefined;
  provider: string | undefined;
  orgNodeId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // Filter by creator: empty = all; "mine" = current user; otherwise specific user id.
  createdBy: string | undefined;
};

export type AgentCreator = {
  userId: string | undefined;
  label: string | undefined;
};

export type ListAgentCreatorsResponse = {
  items: AgentCreator[] | undefined;
};

export type ListAgentsResponse = {
  items: Agent[] | undefined;
  total: number | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type CreateAgentRequest = {
  //
  // Behaviors: REQUIRED
  agentKey: string | undefined;
  //
  // Behaviors: REQUIRED
  displayName: string | undefined;
  // Required for llm; omit for agent_kind=a2a_proxy (server defaults provider=a2a, model=proxy).
  provider: string | undefined;
  model: string | undefined;
  icon: string | undefined;
  agentDescription: string | undefined;
  positionId: string | undefined;
  systemPromptMode: string | undefined;
  contextWindow: number | undefined;
  budgetMonthlyCents: number | undefined;
  configJson: string | undefined;
  settings: AgentRuntimeSettings | undefined;
  files: AgentPromptFile[] | undefined;
  agentKind: string | undefined;
  a2aProxyConfig: A2AProxyConfig | undefined;
  positionKey: string | undefined;
  agentVariant: string | undefined;
  variantDescription: string | undefined;
};

export type GetAgentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateAgentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  agent: Agent | undefined;
};

export type DeleteAgentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetAgentPromptPreviewRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  mode: string | undefined;
};

export type GetAgentPromptPreviewResponse = {
  preview: string | undefined;
  // instruction is the static build-time system instruction (Description + prompt files + RuntimeCapabilityCue).
  instruction: string | undefined;
  sections: PromptSectionEstimate[] | undefined;
  staticTotalTokens: number | undefined;
  runtimeOverlayEstTokens: number | undefined;
  runtimeNote: string | undefined;
};

export type PromptSectionEstimate = {
  key: string | undefined;
  label: string | undefined;
  estTokens: number | undefined;
  // build = included in instruction; runtime = injected per LLM call when enabled.
  source: string | undefined;
};

// EffectiveAgentTool is one evaluated tool row for an agent (legacy /agents/{id}/tools/effective JSON).
export type EffectiveAgentTool = {
  toolKey: string | undefined;
  displayName: string | undefined;
  category: string | undefined;
  source: string | undefined;
  enabled: boolean | undefined;
  effectiveState: string | undefined;
  reason: string | undefined;
};

export type AgentEffectiveToolsView = {
  toolsEnabled: boolean | undefined;
  profile: string | undefined;
  allow: string[] | undefined;
  deny: string[] | undefined;
  items: EffectiveAgentTool[] | undefined;
};

export type ToggleFavoriteRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetAgentEffectiveToolsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type UpdateAgentToolPolicyRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  toolsEnabled: boolean | undefined;
  profile: string | undefined;
  allow: string[] | undefined;
  deny: string[] | undefined;
  // P1-2：可选策略字段。非 nil 时更新 agent_runtime_settings.tools_execution_timeout_sec
  // （0 = 恢复服务端默认）。该字段由 policyResolver 运行时接管：仅它变化时不触发
  // agent 重建，新调用立即生效；装配字段（enabled/profile/allow/deny）变化仍触发重建。
  executionTimeoutSec?: number;
};

export type CreateAgentPromptFileRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  body: string | undefined;
  sortOrder: number | undefined;
};

export type UpdateAgentPromptFileRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  name: string | undefined;
  body: string | undefined;
  sortOrder: number | undefined;
};

export type DeleteAgentPromptFileRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type EstimateTokensRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type FileTokenEstimate = {
  fileId: string | undefined;
  fileName: string | undefined;
  estimatedTokens: number | undefined;
};

export type EstimateTokensResponse = {
  totalTokens: number | undefined;
  fileEstimates: FileTokenEstimate[] | undefined;
};

export type CheckAgentKeyRequest = {
  //
  // Behaviors: REQUIRED
  agentKey: string | undefined;
};

export type CheckAgentKeyResponse = {
  available: boolean | undefined;
  message: string | undefined;
};

export type GetAgentEvolutionMetricsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  timeRange: string | undefined;
};

export type MetricDataPoint = {
  date: string | undefined;
  value: number | undefined;
};

export type EvolutionMetricsResponse = {
  agentId: string | undefined;
  timeRange: string | undefined;
  toolSuccessRate: number | undefined;
  retrievalQuality: number | undefined;
  totalEpisodes: number | undefined;
  negativeFeedback: number | undefined;
  toolSuccessSeries: MetricDataPoint[] | undefined;
  retrievalQualitySeries: MetricDataPoint[] | undefined;
  // S-05: true when metrics are incomplete due to partial query failures.
  partial: boolean | undefined;
  // S-08: which sub-queries failed (for observability).
  partialErrors: string[] | undefined;
};

export type GetAgentEvolutionSuggestionsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  status: string | undefined;
};

export type EvolutionSuggestion = {
  id: string | undefined;
  agentId: string | undefined;
  type: string | undefined;
  title: string | undefined;
  content: string | undefined;
  status: string | undefined;
  diffPreview: string | undefined;
  createdAt: string | undefined;
  appliedAt: string | undefined;
  // applicable 为 true 时前端才显示「应用」按钮：仅 persona/prompt
  // 且 metadata 携带 apply_payload 的建议可应用；通知类建议为 false。
  applicable: boolean | undefined;
};

export type ListEvolutionSuggestionsResponse = {
  items: EvolutionSuggestion[] | undefined;
};

export type ApplyEvolutionSuggestionRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  suggestionId: string | undefined;
};

export type RejectEvolutionSuggestionRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  suggestionId: string | undefined;
  // 拒绝原因（可选），持久化到 metadata.rejection_reason 供审计。
  reason: string | undefined;
};

export type RollbackEvolutionSuggestionRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  suggestionId: string | undefined;
};

export type EditPromptFileByAIRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  fileId: string | undefined;
  //
  // Behaviors: REQUIRED
  instruction: string | undefined;
};

export type EditPromptFileByAIResponse = {
  file: AgentPromptFile | undefined;
};

export type AgentTemplate = {
  key: string | undefined;
  label: string | undefined;
  icon: string | undefined;
  description: string | undefined;
  displayName: string | undefined;
  provider: string | undefined;
  model: string | undefined;
};

export type ListAgentTemplatesResponse = {
  items: AgentTemplate[] | undefined;
};

export type DuplicateAgentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// BatchUpdateAgentsRequest is LIST-04 bulk enable/disable/delete.
// status and delete are mutually exclusive; exactly one must be set.
export type BatchUpdateAgentsRequest = {
  //
  // Behaviors: REQUIRED
  ids: string[] | undefined;
  status: string | undefined;
  delete: boolean | undefined;
};

export type BatchUpdateAgentsResponse = {
  affected: number | undefined;
};

export interface AgentService {
  ListAgents(request: ListAgentsRequest): Promise<ListAgentsResponse>;
  CreateAgent(request: CreateAgentRequest): Promise<Agent>;
  // Static sub-resource GET paths MUST be registered before /v1/agents/{id}
  // to avoid gorilla/mux matching the variable segment first
  // (e.g. "creators" as {id}).
  ListAgentCreators(request: wellKnownEmpty): Promise<ListAgentCreatorsResponse>;
  GetAgent(request: GetAgentRequest): Promise<Agent>;
  UpdateAgent(request: UpdateAgentRequest): Promise<Agent>;
  DeleteAgent(request: DeleteAgentRequest): Promise<wellKnownEmpty>;
  ToggleFavorite(request: ToggleFavoriteRequest): Promise<Agent>;
  GetAgentPromptPreview(request: GetAgentPromptPreviewRequest): Promise<GetAgentPromptPreviewResponse>;
  GetAgentEffectiveTools(request: GetAgentEffectiveToolsRequest): Promise<AgentEffectiveToolsView>;
  UpdateAgentToolPolicy(request: UpdateAgentToolPolicyRequest): Promise<AgentEffectiveToolsView>;
  CreateAgentPromptFile(request: CreateAgentPromptFileRequest): Promise<AgentPromptFile>;
  UpdateAgentPromptFile(request: UpdateAgentPromptFileRequest): Promise<AgentPromptFile>;
  DeleteAgentPromptFile(request: DeleteAgentPromptFileRequest): Promise<wellKnownEmpty>;
  EstimateTokens(request: EstimateTokensRequest): Promise<EstimateTokensResponse>;
  EditPromptFileByAI(request: EditPromptFileByAIRequest): Promise<EditPromptFileByAIResponse>;
  ListAgentTemplates(request: wellKnownEmpty): Promise<ListAgentTemplatesResponse>;
  DuplicateAgent(request: DuplicateAgentRequest): Promise<Agent>;
  BatchUpdateAgents(request: BatchUpdateAgentsRequest): Promise<BatchUpdateAgentsResponse>;
  CheckAgentKey(request: CheckAgentKeyRequest): Promise<CheckAgentKeyResponse>;
  GetAgentEvolutionMetrics(request: GetAgentEvolutionMetricsRequest): Promise<EvolutionMetricsResponse>;
  GetAgentEvolutionSuggestions(request: GetAgentEvolutionSuggestionsRequest): Promise<ListEvolutionSuggestionsResponse>;
  ApplyEvolutionSuggestion(request: ApplyEvolutionSuggestionRequest): Promise<EvolutionSuggestion>;
  RejectEvolutionSuggestion(request: RejectEvolutionSuggestionRequest): Promise<EvolutionSuggestion>;
  RollbackEvolutionSuggestion(request: RollbackEvolutionSuggestionRequest): Promise<EvolutionSuggestion>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createAgentServiceClient(
  handler: RequestHandler
): AgentService {
  return {
    ListAgents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agents`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.keyword) {
        queryParams.push(`keyword=${encodeURIComponent(request.keyword.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.provider) {
        queryParams.push(`provider=${encodeURIComponent(request.provider.toString())}`)
      }
      if (request.orgNodeId) {
        queryParams.push(`orgNodeId=${encodeURIComponent(request.orgNodeId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      if (request.createdBy) {
        queryParams.push(`createdBy=${encodeURIComponent(request.createdBy.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "ListAgents",
      }) as Promise<ListAgentsResponse>;
    },
    CreateAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agents`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "CreateAgent",
      }) as Promise<Agent>;
    },
    ListAgentCreators(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agents/creators`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "ListAgentCreators",
      }) as Promise<ListAgentCreatorsResponse>;
    },
    GetAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "GetAgent",
      }) as Promise<Agent>;
    },
    UpdateAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.agent ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "AgentService",
        method: "UpdateAgent",
      }) as Promise<Agent>;
    },
    DeleteAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "AgentService",
        method: "DeleteAgent",
      }) as Promise<wellKnownEmpty>;
    },
    ToggleFavorite(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}/favorite`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "AgentService",
        method: "ToggleFavorite",
      }) as Promise<Agent>;
    },
    GetAgentPromptPreview(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}/system-prompt/preview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.mode) {
        queryParams.push(`mode=${encodeURIComponent(request.mode.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "GetAgentPromptPreview",
      }) as Promise<GetAgentPromptPreviewResponse>;
    },
    GetAgentEffectiveTools(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/tools/effective`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "GetAgentEffectiveTools",
      }) as Promise<AgentEffectiveToolsView>;
    },
    UpdateAgentToolPolicy(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/tools/policy`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "AgentService",
        method: "UpdateAgentToolPolicy",
      }) as Promise<AgentEffectiveToolsView>;
    },
    CreateAgentPromptFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/files`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "CreateAgentPromptFile",
      }) as Promise<AgentPromptFile>;
    },
    UpdateAgentPromptFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.agentId}/files/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "AgentService",
        method: "UpdateAgentPromptFile",
      }) as Promise<AgentPromptFile>;
    },
    DeleteAgentPromptFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.agentId}/files/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "AgentService",
        method: "DeleteAgentPromptFile",
      }) as Promise<wellKnownEmpty>;
    },
    EstimateTokens(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/files/estimate-tokens`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "EstimateTokens",
      }) as Promise<EstimateTokensResponse>;
    },
    EditPromptFileByAI(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.fileId) {
        throw new Error("missing required field request.file_id");
      }
      const path = `v1/agents/${request.agentId}/files/${request.fileId}/ai-edit`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "EditPromptFileByAI",
      }) as Promise<EditPromptFileByAIResponse>;
    },
    ListAgentTemplates(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agent-templates`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "ListAgentTemplates",
      }) as Promise<ListAgentTemplatesResponse>;
    },
    DuplicateAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/agents/${request.id}/duplicate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "DuplicateAgent",
      }) as Promise<Agent>;
    },
    BatchUpdateAgents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agents:batchUpdate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "BatchUpdateAgents",
      }) as Promise<BatchUpdateAgentsResponse>;
    },
    CheckAgentKey(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agent-keys/check`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentKey) {
        queryParams.push(`agentKey=${encodeURIComponent(request.agentKey.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "CheckAgentKey",
      }) as Promise<CheckAgentKeyResponse>;
    },
    GetAgentEvolutionMetrics(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/metrics`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.timeRange) {
        queryParams.push(`timeRange=${encodeURIComponent(request.timeRange.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "GetAgentEvolutionMetrics",
      }) as Promise<EvolutionMetricsResponse>;
    },
    GetAgentEvolutionSuggestions(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/suggestions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "AgentService",
        method: "GetAgentEvolutionSuggestions",
      }) as Promise<ListEvolutionSuggestionsResponse>;
    },
    ApplyEvolutionSuggestion(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.suggestionId) {
        throw new Error("missing required field request.suggestion_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/suggestions/${request.suggestionId}/apply`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "ApplyEvolutionSuggestion",
      }) as Promise<EvolutionSuggestion>;
    },
    RejectEvolutionSuggestion(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.suggestionId) {
        throw new Error("missing required field request.suggestion_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/suggestions/${request.suggestionId}/reject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "RejectEvolutionSuggestion",
      }) as Promise<EvolutionSuggestion>;
    },
    RollbackEvolutionSuggestion(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      if (!request.suggestionId) {
        throw new Error("missing required field request.suggestion_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/suggestions/${request.suggestionId}/rollback`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "AgentService",
        method: "RollbackEvolutionSuggestion",
      }) as Promise<EvolutionSuggestion>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
