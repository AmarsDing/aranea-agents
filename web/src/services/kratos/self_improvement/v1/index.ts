// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ListRunsRequest = {
  status: string | undefined;
  riskLevel: string | undefined;
  triggerSource: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListRunsResponse = {
  items: SelfImprovementRunMsg[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

// SelfImprovementRunMsg is the console view of one self-improvement run.
// ListRuns omits diff/diagnosis/verification_report/critic_report/governance
// (heavy fields); GetRun populates them.
export type SelfImprovementRunMsg = {
  id: string | undefined;
  suggestionId: string | undefined;
  status: string | undefined;
  triggerSource: string | undefined;
  patchKind: string | undefined;
  riskLevel: string | undefined;
  baseRef: string | undefined;
  branch: string | undefined;
  diffStats: DiffStatsMsg | undefined;
  attempts: number | undefined;
  approvedBy: string | undefined;
  appliedCommit: string | undefined;
  observeUntil: wellKnownTimestamp | undefined;
  closedReason: string | undefined;
  createdAt: wellKnownTimestamp | undefined;
  updatedAt: wellKnownTimestamp | undefined;
  // ── detail-only fields (GetRun) ──
  diff: string | undefined;
  diagnosis: DiagnosisMsg | undefined;
  verificationReport: SandboxGateResultMsg[] | undefined;
  criticReport: CriticReportMsg | undefined;
  governance: GovernanceDecisionMsg | undefined;
};

export type DiffStatsMsg = {
  files: number | undefined;
  additions: number | undefined;
  deletions: number | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

// DiagnosisMsg is the Analyst Agent structured output (D5).
export type DiagnosisMsg = {
  rootCause: string | undefined;
  affectedFiles: string[] | undefined;
  impactScope: string | undefined;
  fixStrategy: string | undefined;
  confidence: number | undefined;
};

// SandboxGateResultMsg is one verification gate outcome (G1-G3/G5).
export type SandboxGateResultMsg = {
  gate: string | undefined;
  passed: boolean | undefined;
  output: string | undefined;
  durationMs: number | undefined;
};

// CriticReportMsg is the Critic Agent structured review (G4).
export type CriticReportMsg = {
  isSafe: boolean | undefined;
  riskLevel: string | undefined;
  concerns: string[] | undefined;
  suggestion: string | undefined;
};

// GovernanceDecisionMsg is the RiskClassifier output (D6).
export type GovernanceDecisionMsg = {
  riskLevel: string | undefined;
  channel: string | undefined;
  ruleHits: string[] | undefined;
};

export type GetRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetRunResponse = {
  run: SelfImprovementRunMsg | undefined;
};

export type ApproveRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reason: string | undefined;
};

export type ApproveRunResponse = {
};

export type RejectRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  reason: string | undefined;
};

export type RejectRunResponse = {
};

export type RollbackRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reason: string | undefined;
};

export type RollbackRunResponse = {
};

export type CloseRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reason: string | undefined;
};

export type CloseRunResponse = {
};

export type ControlRunRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  // pause | skip_retry | rollback (see biz.ParseSIControlCommand)
  //
  // Behaviors: REQUIRED
  command: string | undefined;
};

export type ControlRunResponse = {
};

export type GetOutcomeStatsRequest = {
};

export type GetOutcomeStatsResponse = {
  total: number | undefined;
  effective: number | undefined;
  neutral: number | undefined;
  regressed: number | undefined;
  effectiveRate: number | undefined;
  rollbackRate: number | undefined;
  byTrigger: TriggerOutcomeStatsMsg[] | undefined;
};

export type TriggerOutcomeStatsMsg = {
  triggerSource: string | undefined;
  total: number | undefined;
  effective: number | undefined;
  neutral: number | undefined;
  regressed: number | undefined;
};

export type GetStatusRequest = {
};

// GetStatusResponse reports whether the self-improvement pipeline is enabled
// and whether its hard prerequisites are satisfied. All fields are best-effort
// (a failing subsystem degrades to false/empty, never to an error).
export type GetStatusResponse = {
  enabled: boolean | undefined;
  refineLlmConfigured: boolean | undefined;
  refineLlmProvider: string | undefined;
  refineLlmModel: string | undefined;
  repoRoot: string | undefined;
  repoRootValid: boolean | undefined;
};

// RiskRulesMsg is one view of the risk-classification rule set. Numeric 0 /
// empty globs mean "inherit the code default".
export type RiskRulesMsg = {
  lowMaxLines: number | undefined;
  mediumMaxLines: number | undefined;
  corePathGlobs: string[] | undefined;
  dailyAutoQuota: number | undefined;
};

export type GetRiskRulesRequest = {
};

export type GetRiskRulesResponse = {
  configured: RiskRulesMsg | undefined;
  effective: RiskRulesMsg | undefined;
};

export type UpdateRiskRulesRequest = {
  lowMaxLines: number | undefined;
  mediumMaxLines: number | undefined;
  corePathGlobs: string[] | undefined;
  dailyAutoQuota: number | undefined;
};

export type UpdateRiskRulesResponse = {
  configured: RiskRulesMsg | undefined;
  effective: RiskRulesMsg | undefined;
};

// SelfImprovementService exposes the platform self-improvement console API
// (73-self-iteration-v3, design §七). All endpoints require admin auth.
export interface SelfImprovementService {
  // ListRuns returns runs filtered by status / risk_level / trigger_source
  // with pagination. Heavy fields (diff/diagnosis/report) are omitted.
  ListRuns(request: ListRunsRequest): Promise<ListRunsResponse>;
  // GetRun returns the full run detail including diff/diagnosis/reports.
  GetRun(request: GetRunRequest): Promise<GetRunResponse>;
  // ApproveRun approves a high-risk run (awaiting_governance → applying).
  ApproveRun(request: ApproveRunRequest): Promise<ApproveRunResponse>;
  // RejectRun rejects an awaiting_governance run (reason required).
  RejectRun(request: RejectRunRequest): Promise<RejectRunResponse>;
  // RollbackRun manually rolls back an applied/observing run.
  RollbackRun(request: RollbackRunRequest): Promise<RollbackRunResponse>;
  // CloseRun closes an observing run early (confirmed effective).
  CloseRun(request: CloseRunRequest): Promise<CloseRunResponse>;
  // ControlRun issues a user-intervention command to an in-flight run
  // (pause / skip_retry / rollback). The command is consumed asynchronously
  // by the pipeline at stage boundaries; only runs in
  // detected/diagnosing/patching/verifying accept commands.
  ControlRun(request: ControlRunRequest): Promise<ControlRunResponse>;
  // GetOutcomeStats returns Learn-stage attribution statistics
  // (verdict distribution + per-trigger breakdown).
  GetOutcomeStats(request: GetOutcomeStatsRequest): Promise<GetOutcomeStatsResponse>;
  // GetStatus returns the feature availability + prerequisite preflight view
  // (master switch, DefaultRefineLLM readiness, sandbox repo-root validity).
  // Unlike the other endpoints it does NOT depend on the pipeline usecase, so
  // it answers even when the feature is disabled — the console uses it to
  // render the disabled empty state / missing-prerequisite guidance.
  GetStatus(request: GetStatusRequest): Promise<GetStatusResponse>;
  // GetRiskRules returns the configured risk-classification rules
  // (0/empty fields inherit the code defaults; effective carries the
  // normalized values actually consumed by the pipeline/router).
  GetRiskRules(request: GetRiskRulesRequest): Promise<GetRiskRulesResponse>;
  // UpdateRiskRules persists a new risk-classification rule set
  // (design §七; takes effect for newly-classified runs).
  UpdateRiskRules(request: UpdateRiskRulesRequest): Promise<UpdateRiskRulesResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createSelfImprovementServiceClient(
  handler: RequestHandler
): SelfImprovementService {
  return {
    ListRuns(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/self-improvement/runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.riskLevel) {
        queryParams.push(`riskLevel=${encodeURIComponent(request.riskLevel.toString())}`)
      }
      if (request.triggerSource) {
        queryParams.push(`triggerSource=${encodeURIComponent(request.triggerSource.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SelfImprovementService",
        method: "ListRuns",
      }) as Promise<ListRunsResponse>;
    },
    GetRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SelfImprovementService",
        method: "GetRun",
      }) as Promise<GetRunResponse>;
    },
    ApproveRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}/approve`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SelfImprovementService",
        method: "ApproveRun",
      }) as Promise<ApproveRunResponse>;
    },
    RejectRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}/reject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SelfImprovementService",
        method: "RejectRun",
      }) as Promise<RejectRunResponse>;
    },
    RollbackRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}/rollback`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SelfImprovementService",
        method: "RollbackRun",
      }) as Promise<RollbackRunResponse>;
    },
    CloseRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}/close`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SelfImprovementService",
        method: "CloseRun",
      }) as Promise<CloseRunResponse>;
    },
    ControlRun(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/self-improvement/runs/${request.id}/control`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SelfImprovementService",
        method: "ControlRun",
      }) as Promise<ControlRunResponse>;
    },
    GetOutcomeStats(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/self-improvement/outcome-stats`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SelfImprovementService",
        method: "GetOutcomeStats",
      }) as Promise<GetOutcomeStatsResponse>;
    },
    GetStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/self-improvement/status`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SelfImprovementService",
        method: "GetStatus",
      }) as Promise<GetStatusResponse>;
    },
    GetRiskRules(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/self-improvement/risk-rules`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SelfImprovementService",
        method: "GetRiskRules",
      }) as Promise<GetRiskRulesResponse>;
    },
    UpdateRiskRules(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/self-improvement/risk-rules`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "SelfImprovementService",
        method: "UpdateRiskRules",
      }) as Promise<UpdateRiskRulesResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
