// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// ProviderModel is one row of llm_provider_models (legacy "llm-provider-models").
export type ProviderModel = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  provider: string | undefined;
  model: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
  capabilities: ModelCapabilities | undefined;
  pricingConfigured: boolean | undefined;
};

export type ModelCapabilities = {
  text: boolean | undefined;
  vision: boolean | undefined;
  audio: boolean | undefined;
  file: boolean | undefined;
  toolCall: boolean | undefined;
  cache: boolean | undefined;
  thinking: boolean | undefined;
  textOnly: boolean | undefined;
};

export type ListProviderModelsResponse = {
  items: ProviderModel[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type CreateProviderModelRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  provider: string | undefined;
  model: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  capabilities: ModelCapabilities | undefined;
};

export type GetProviderModelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateProviderModelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  providerModel: ProviderModel | undefined;
};

export type DeleteProviderModelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// ValidatePairRequest matches POST /api/v1/agents/validate-model body.
export type ValidateProviderPairRequest = {
  provider: string | undefined;
  model: string | undefined;
};

export type ValidateProviderPairResponse = {
  ok: boolean | undefined;
  message: string | undefined;
};

export type InspectProviderModelRequest = {
  resourceId: string | undefined;
  providerCode: string | undefined;
  providerType: string | undefined;
  modelApiId: string | undefined;
  apiBaseUrl: string | undefined;
  apiKey: string | undefined;
  variant: string | undefined;
  secretId: string | undefined;
  secretKey: string | undefined;
  awsRegion: string | undefined;
};

export type RevealProviderModelCredentialsRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type HACandidateCredential = {
  name: string | undefined;
  apiKey: string | undefined;
};

export type RevealProviderModelCredentialsResponse = {
  apiKey: string | undefined;
  secretKey: string | undefined;
  hasApiKey: boolean | undefined;
  hasSecretKey: boolean | undefined;
  haCandidates: HACandidateCredential[] | undefined;
};

export type InspectProviderModelResponse = {
  ok: boolean | undefined;
  message: string | undefined;
  providerCode: string | undefined;
  providerType: string | undefined;
  modelApiId: string | undefined;
  modelDisplayName: string | undefined;
  modelSizeLabel: string | undefined;
  contextWindowK: number | undefined;
  maxOutputTokens: number | undefined;
  inputPriceMicroUsdPer1k: number | undefined;
  outputPriceMicroUsdPer1k: number | undefined;
  cachedInputPriceMicroUsdPer1k: number | undefined;
  reasoningPriceMicroUsdPer1k: number | undefined;
  embeddingPriceMicroUsdPer1k: number | undefined;
  source: string | undefined;
  rawMetadataJson: string | undefined;
  variant: string | undefined;
  enableTokenTailoring: boolean | undefined;
  supportsCache: boolean | undefined;
  supportsThinking: boolean | undefined;
};

export interface LlmProviderModelService {
  ListProviderModels(request: wellKnownEmpty): Promise<ListProviderModelsResponse>;
  CreateProviderModel(request: CreateProviderModelRequest): Promise<ProviderModel>;
  GetProviderModel(request: GetProviderModelRequest): Promise<ProviderModel>;
  // RevealProviderModelCredentials returns decrypted API secrets for admin console edit UI only.
  // Requires the same authentication as other admin APIs; access is audit-logged (no secret values in logs).
  RevealProviderModelCredentials(request: RevealProviderModelCredentialsRequest): Promise<RevealProviderModelCredentialsResponse>;
  UpdateProviderModel(request: UpdateProviderModelRequest): Promise<ProviderModel>;
  DeleteProviderModel(request: DeleteProviderModelRequest): Promise<wellKnownEmpty>;
  InspectProviderModel(request: InspectProviderModelRequest): Promise<InspectProviderModelResponse>;
  ValidateProviderPair(request: ValidateProviderPairRequest): Promise<ValidateProviderPairResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createLlmProviderModelServiceClient(
  handler: RequestHandler
): LlmProviderModelService {
  return {
    ListProviderModels(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/llm-provider-models`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "ListProviderModels",
      }) as Promise<ListProviderModelsResponse>;
    },
    CreateProviderModel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/llm-provider-models`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "CreateProviderModel",
      }) as Promise<ProviderModel>;
    },
    GetProviderModel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/llm-provider-models/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "GetProviderModel",
      }) as Promise<ProviderModel>;
    },
    RevealProviderModelCredentials(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/llm-provider-models/${request.id}/credentials`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "RevealProviderModelCredentials",
      }) as Promise<RevealProviderModelCredentialsResponse>;
    },
    UpdateProviderModel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/llm-provider-models/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.providerModel ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "UpdateProviderModel",
      }) as Promise<ProviderModel>;
    },
    DeleteProviderModel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/llm-provider-models/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "DeleteProviderModel",
      }) as Promise<wellKnownEmpty>;
    },
    InspectProviderModel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/llm-provider-models/inspect`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "InspectProviderModel",
      }) as Promise<InspectProviderModelResponse>;
    },
    ValidateProviderPair(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/agents/validate-model`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "LlmProviderModelService",
        method: "ValidateProviderPair",
      }) as Promise<ValidateProviderPairResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
