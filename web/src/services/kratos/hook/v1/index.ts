// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type Hook = {
  id: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
};

export type ListHooksResponse = {
  items: Hook[] | undefined;
};

export type CreateHookRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
};

export type GetHookRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateHookRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  hook: Hook | undefined;
};

export type DeleteHookRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type HookDelivery = {
  id: string | undefined;
  hookKey: string | undefined;
  hookId: string | undefined;
  webhookUrl: string | undefined;
  payloadJson: string | undefined;
  status: string | undefined;
  attemptCount: number | undefined;
  maxAttempts: number | undefined;
  lastError: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  idempotencyKey: string | undefined;
};

export type ListHookDeliveriesRequest = {
  hookKey: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListHookDeliveriesResponse = {
  items: HookDelivery[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export interface HookService {
  ListHooks(request: wellKnownEmpty): Promise<ListHooksResponse>;
  CreateHook(request: CreateHookRequest): Promise<Hook>;
  // Static sub-resource GET paths MUST be registered before /v1/hooks/{id}
  // to avoid gorilla/mux matching the variable segment first
  // (e.g. "deliveries" as {id}).
  ListHookDeliveries(request: ListHookDeliveriesRequest): Promise<ListHookDeliveriesResponse>;
  GetHook(request: GetHookRequest): Promise<Hook>;
  UpdateHook(request: UpdateHookRequest): Promise<Hook>;
  DeleteHook(request: DeleteHookRequest): Promise<wellKnownEmpty>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createHookServiceClient(handler: RequestHandler): HookService {
  return {
    ListHooks(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/hooks`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'HookService',
          method: 'ListHooks',
        },
      ) as Promise<ListHooksResponse>;
    },
    CreateHook(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/hooks`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'HookService',
          method: 'CreateHook',
        },
      ) as Promise<Hook>;
    },
    ListHookDeliveries(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/hooks/deliveries`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.hookKey) {
        queryParams.push(`hookKey=${encodeURIComponent(request.hookKey.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`);
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`);
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`);
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'HookService',
          method: 'ListHookDeliveries',
        },
      ) as Promise<ListHookDeliveriesResponse>;
    },
    GetHook(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/hooks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'HookService',
          method: 'GetHook',
        },
      ) as Promise<Hook>;
    },
    UpdateHook(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/hooks/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.hook ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'HookService',
          method: 'UpdateHook',
        },
      ) as Promise<Hook>;
    },
    DeleteHook(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/hooks/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'HookService',
          method: 'DeleteHook',
        },
      ) as Promise<wellKnownEmpty>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
