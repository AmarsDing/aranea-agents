// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ListDecisionRecordsRequest = {
  // category 过滤：hitl_approval / planner_orchestration / system_guard /
  // knowledge_arbitration / evolution_applied；空串 = 全部。
  category: string | undefined;
  actorKey: string | undefined;
  // 实体过滤：related_entities @> [{"type":..,"key":..}]，二者需同时给。
  entityType: string | undefined;
  entityKey: string | undefined;
  // source_ref.run_id 过滤。
  sourceRunId: string | undefined;
  // source_ref.session_id 过滤（T5）：读侧 COALESCE 兼容旧记录（session_id
  // 仅在 metadata 的时期）。
  sourceSessionId: string | undefined;
  // 时间窗（created_at，含端点）；空 = 不限。
  timeFrom: wellKnownTimestamp | undefined;
  timeTo: wellKnownTimestamp | undefined;
  page: number | undefined;
  // page_size ≤ 100，0/越界由服务端收敛为默认值。
  pageSize: number | undefined;
};

// Encoded using RFC 3339, where generated output will always be Z-normalized
// and uses 0, 3, 6 or 9 fractional digits.
// Offsets other than "Z" are also accepted.
type wellKnownTimestamp = string;

export type ListDecisionRecordsResponse = {
  items: DecisionRecord[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

// DecisionRecord 字段与 decision_records 表一一对应（设计 §4.1）；
// entities/source_ref/metadata 用 Struct 承载双方言 JSON 文本列。
export type DecisionRecord = {
  id: number | undefined;
  decisionKey: string | undefined;
  category: string | undefined;
  scenario: string | undefined;
  reasoning: string | undefined;
  outcome: string | undefined;
  // optional：confidence 可空（未置置信度的类别）。
  confidence?: number;
  actorType: string | undefined;
  actorKey: string | undefined;
  // optional：单父链指针（ADR-3）；根节点为空。
  parentDecisionId?: number;
  // related_entities 是 JSON 数组（[{type,key}]），用 ListValue 承载；
  // source_ref/metadata 是 JSON 对象，用 Struct 承载。
  relatedEntities: wellKnownListValue | undefined;
  sourceRef: wellKnownStruct | undefined;
  metadata: wellKnownStruct | undefined;
  workspaceId: string | undefined;
  createdAt: wellKnownTimestamp | undefined;
  updatedAt: wellKnownTimestamp | undefined;
  // virtual_parent：链查询兜底补链标记（设计 §5）——同 run 内时间最近的
  // 前置 planner 决策作为虚拟父节点返回时不回写，仅此节点置 true。
  virtualParent: boolean | undefined;
};

type wellKnownListValue = wellKnownValue[];

// Any JSON value.
type wellKnownStruct = Record<string, unknown>;

export type GetDecisionRecordRequest = {
  //
  // Behaviors: REQUIRED
  decisionKey: string | undefined;
};

export type GetDecisionRecordResponse = {
  record: DecisionRecord | undefined;
};

export type GetDecisionChainRequest = {
  //
  // Behaviors: REQUIRED
  decisionKey: string | undefined;
  // direction: up / down / both（默认 both）。
  direction: string | undefined;
  // max_depth ≤ 20，0/越界由服务端收敛为默认值（设计 §5）。
  maxDepth: number | undefined;
};

export type GetDecisionChainResponse = {
  root: DecisionRecord | undefined;
  upstream: DecisionRecord[] | undefined;
  downstream: DecisionRecord[] | undefined;
};

export type GetSessionGateStatsRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
};

// SessionGateStats 字段与 decision.RunGateStats 一一对应（聚合维度从 run
// 换成会话，字段语义不变； TeamRunStats 内嵌闸字段同口径）。
export type SessionGateStats = {
  // loop_guard_blocked 记录条数。
  loopGuardBlocks: number | undefined;
  // 是否触发过 token_budget_tripped。
  budgetTripped: boolean | undefined;
  // 是否触发过 no_progress_tripped。
  noProgressTripped: boolean | undefined;
  // 被剪枝 tool result 总条数（observed_value 求和）。
  pruneCount: number | undefined;
  // 被剪枝原文总字节数（metadata.prune_bytes 求和）。
  pruneBytes: number | undefined;
  // 终审压缩触发次数。
  compactCount: number | undefined;
  // 工具参数门禁 deny 记录条数。
  paramRuleDenies: number | undefined;
  // 输入级确定性安全扫描命中记录条数（input_risk_flagged，2026-08-28 方案② S3）。
  inputRiskFlagged: number | undefined;
};

export type GetSessionGateStatsResponse = {
  stats: SessionGateStats | undefined;
};

// DecisionRecordService 是 M80 决策智能 Phase 1 的统一查询面
// （设计 80-decision-intelligence.design.md §4.1）：五类高价值决策
// （HITL 人工审批 / planner 策略路由 / 系统闸动作 / 知识仲裁裁决 /
// 进化建议应用）归一后的只读查询 API。FindSimilarDecisions 属 Phase 2
// 先例检索，不在本文件（避免预留死代码）。
export interface DecisionRecordService {
  ListDecisionRecords(request: ListDecisionRecordsRequest): Promise<ListDecisionRecordsResponse>;
  GetDecisionRecord(request: GetDecisionRecordRequest): Promise<GetDecisionRecordResponse>;
  GetDecisionChain(request: GetDecisionChainRequest): Promise<GetDecisionChainResponse>;
  // GetSessionGateStats 是 T5 chat 侧闸事件聚合面（2026-08-27）：按会话聚
  // 合 system_guard 记录（loop_guard/param_rule/prune/compact/预算/无进展
  // 各闸计数），与 team 侧 TeamRunStats 的 run 级聚合同型、维度换成会话。
  GetSessionGateStats(request: GetSessionGateStatsRequest): Promise<GetSessionGateStatsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createDecisionRecordServiceClient(
  handler: RequestHandler
): DecisionRecordService {
  return {
    ListDecisionRecords(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/decisions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.category) {
        queryParams.push(`category=${encodeURIComponent(request.category.toString())}`)
      }
      if (request.actorKey) {
        queryParams.push(`actorKey=${encodeURIComponent(request.actorKey.toString())}`)
      }
      if (request.entityType) {
        queryParams.push(`entityType=${encodeURIComponent(request.entityType.toString())}`)
      }
      if (request.entityKey) {
        queryParams.push(`entityKey=${encodeURIComponent(request.entityKey.toString())}`)
      }
      if (request.sourceRunId) {
        queryParams.push(`sourceRunId=${encodeURIComponent(request.sourceRunId.toString())}`)
      }
      if (request.sourceSessionId) {
        queryParams.push(`sourceSessionId=${encodeURIComponent(request.sourceSessionId.toString())}`)
      }
      if (request.timeFrom) {
        queryParams.push(`timeFrom=${encodeURIComponent(request.timeFrom.toString())}`)
      }
      if (request.timeTo) {
        queryParams.push(`timeTo=${encodeURIComponent(request.timeTo.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "DecisionRecordService",
        method: "ListDecisionRecords",
      }) as Promise<ListDecisionRecordsResponse>;
    },
    GetDecisionRecord(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.decisionKey) {
        throw new Error("missing required field request.decision_key");
      }
      const path = `v1/decisions/${request.decisionKey}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "DecisionRecordService",
        method: "GetDecisionRecord",
      }) as Promise<GetDecisionRecordResponse>;
    },
    GetDecisionChain(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.decisionKey) {
        throw new Error("missing required field request.decision_key");
      }
      const path = `v1/decisions/${request.decisionKey}/chain`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.direction) {
        queryParams.push(`direction=${encodeURIComponent(request.direction.toString())}`)
      }
      if (request.maxDepth) {
        queryParams.push(`maxDepth=${encodeURIComponent(request.maxDepth.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "DecisionRecordService",
        method: "GetDecisionChain",
      }) as Promise<GetDecisionChainResponse>;
    },
    GetSessionGateStats(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/sessions/${request.sessionId}/gate-stats`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "DecisionRecordService",
        method: "GetSessionGateStats",
      }) as Promise<GetSessionGateStatsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
