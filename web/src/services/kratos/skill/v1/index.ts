// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type SkillTag = {
  name: string | undefined;
  source: string | undefined;
};

// SkillTagInfo 是字典标签 + 实时使用计数。
export type SkillTagInfo = {
  name: string | undefined;
  // `:` 前缀维度（file_type/domain），无维度为空串。
  dimension: string | undefined;
  // system | user | orphan（orphan = 使用中但未收录进字典）。
  source: string | undefined;
  usedCount: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListSkillTagsResponse = {
  items: SkillTagInfo[] | undefined;
};

export type CreateSkillTagRequest = {
  name: string | undefined;
};

export type RenameSkillTagRequest = {
  oldName: string | undefined;
  newName: string | undefined;
};

export type RenameSkillTagResponse = {
  // 重写引用该标签的 skill 条数。
  rewritten: number | undefined;
};

export type DeleteSkillTagRequest = {
  name: string | undefined;
};

export type DeleteSkillTagResponse = {
  rewritten: number | undefined;
};

export type SkillVersionSummary = {
  id: string | undefined;
  version: string | undefined;
  validationStatus: string | undefined;
  publishedAt: string | undefined;
};

export type SkillPermissions = {
  canEdit: boolean | undefined;
  canDelete: boolean | undefined;
  canToggleEnabled: boolean | undefined;
  canDuplicate: boolean | undefined;
};

export type Skill = {
  id: string | undefined;
  name: string | undefined;
  slug: string | undefined;
  description: string | undefined;
  tags: SkillTag[] | undefined;
  extendsSkillId: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  currentVersion: SkillVersionSummary | undefined;
  invokeCount: number | undefined;
  successCount: number | undefined;
  failureCount: number | undefined;
  usageCount7d: number | undefined;
  avgDurationMs: number | undefined;
  lastAgentId: string | undefined;
  lastAgentDisplayName: string | undefined;
  lastInvokedAt: string | undefined;
  lastDurationMs: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  permissions: SkillPermissions | undefined;
  filesystemMissing: boolean | undefined;
  // filesystem | import | manual
  syncOrigin: string | undefined;
  // system | workspace | agent | private | public
  visibility: string | undefined;
  defaultConfigJson: string | undefined;
};

export type SkillFilesystemHealth = {
  rootAccessible: boolean | undefined;
  resolvedRoot: string | undefined;
  missingCount: number | undefined;
  pendingFilesystemCount: number | undefined;
};

export type ListSkillsRequest = {
  search: string | undefined;
  tags: string | undefined;
  enabled: string | undefined;
  status: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
  filesystemMissing: string | undefined;
  syncOrigin: string | undefined;
};

export type ListSkillsResponse = {
  items: Skill[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ToggleSkillEnabledRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  enabled: boolean | undefined;
};

export type DuplicateSkillRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type DeleteSkillRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ListSkillFilesRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type SkillFile = {
  path: string | undefined;
  name: string | undefined;
  language: string | undefined;
  size: number | undefined;
  updatedAt: string | undefined;
};

export type ListSkillFilesResponse = {
  items: SkillFile[] | undefined;
};

export type GetSkillFileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  path: string | undefined;
};

export type SkillFileContent = {
  path: string | undefined;
  content: string | undefined;
  language: string | undefined;
};

export type UpdateSkillFileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  path: string | undefined;
  //
  // Behaviors: REQUIRED
  content: string | undefined;
};

export type SkillInvocationPermissions = {
  canViewDetail: boolean | undefined;
};

export type SkillInvocation = {
  id: string | undefined;
  skillId: string | undefined;
  skillName: string | undefined;
  skillVersion: string | undefined;
  agentId: string | undefined;
  agentDisplayName: string | undefined;
  userId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  durationMs: number | undefined;
  startedAt: string | undefined;
  endedAt: string | undefined;
  inputPreview: string | undefined;
  inputHash: string | undefined;
  outputPreview: string | undefined;
  errorCode: string | undefined;
  errorMessage: string | undefined;
  permissions: SkillInvocationPermissions | undefined;
  source: string | undefined;
  activationId: string | undefined;
  messageId: string | undefined;
};

export type ListSkillRunsRequest = {
  skillId: string | undefined;
  agentId: string | undefined;
  sessionId: string | undefined;
  status: string | undefined;
  from: string | undefined;
  to: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListSkillRunsResponse = {
  items: SkillInvocation[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type GetSkillRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type GetSkillResponse = {
  skill: Skill | undefined;
  bodyMarkdown: string | undefined;
};

export type CreateSkillRequest = {
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  //
  // Behaviors: REQUIRED
  slug: string | undefined;
  //
  // Behaviors: REQUIRED
  description: string | undefined;
  //
  // Behaviors: REQUIRED
  bodyMarkdown: string | undefined;
  tags: SkillTag[] | undefined;
};

export type UpdateSkillRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  name?: string;
  description?: string;
  replaceTags: boolean | undefined;
  tags: SkillTag[] | undefined;
  bodyMarkdown?: string;
};

export type PublishSkillRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type DeleteSkillFileRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  path: string | undefined;
};

export type PreviewSkillRuntimeRequest = {
  // Optional: preview for a specific agent (applies Layer A/B policy).
  agentId: string | undefined;
  // Optional: simulate a user query for intent routing (Layer B).
  userQuery: string | undefined;
};

export type PreviewSkillRuntimeResponse = {
  resolvedStorageRoot: string | undefined;
  enabledPublishedCount: number | undefined;
  enabledSkillSlugs: string[] | undefined;
  // Per-slug reason: why each skill was included or excluded.
  reasons: { [key: string]: string } | undefined;
};

// Skill import (ZIP batch). JSON field names match web/src/features/skills/types.ts (snake_case).
export type SkillImportIssue = {
  type: string | undefined;
  message: string | undefined;
};

export type SkillImportCandidate = {
  candidateId: string | undefined;
  name: string | undefined;
  slug: string | undefined;
  description: string | undefined;
  bodyPreview: string | undefined;
  targetDir: string | undefined;
  validationStatus: string | undefined;
  statusIcon: string | undefined;
  warnings: SkillImportIssue[] | undefined;
  blocks: SkillImportIssue[] | undefined;
};

export type SkillSimilarityMetrics = {
  similarityScore: number | undefined;
  nameSimilarity: number | undefined;
  descriptionSimilarity: number | undefined;
  bodySimilarity: number | undefined;
  triggerSimilarity: number | undefined;
  toolSimilarity: number | undefined;
  conflictRisk: string | undefined;
  recommendation: string | undefined;
  confidence: number | undefined;
};

export type SkillSimilaritySource = {
  id: string | undefined;
  name: string | undefined;
  slug: string | undefined;
  description: string | undefined;
  version: string | undefined;
  bodyPreview: string | undefined;
};

export type SkillConflictGroup = {
  groupId: string | undefined;
  highestSimilarityScore: number | undefined;
  metrics: SkillSimilarityMetrics | undefined;
  reason: string | undefined;
  evidence: string[] | undefined;
  candidateIds: string[] | undefined;
  existingSkills: SkillSimilaritySource[] | undefined;
  canRefine: boolean | undefined;
};

export type SkillImportJob = {
  jobId: string | undefined;
  status: string | undefined;
  validationStatus: string | undefined;
  storageRoot: string | undefined;
  candidates: SkillImportCandidate[] | undefined;
  conflictGroups: SkillConflictGroup[] | undefined;
  message: string | undefined;
};

export type ImportSkillZipResponse = {
  jobId: string | undefined;
};

export type GetSkillImportJobRequest = {
  //
  // Behaviors: REQUIRED
  jobId: string | undefined;
};

export type SkillImportDecision = {
  candidateId: string | undefined;
  groupId: string | undefined;
  //
  // Behaviors: REQUIRED
  action: string | undefined;
  mergedName: string | undefined;
  mergedDescription: string | undefined;
  mergedBody: string | undefined;
  mergedTags: SkillTag[] | undefined;
  // merge_group_with_ai: archive the conflict group's existing skills after
  // a successful merge apply (status=archived, enabled=false).
  retireSources: boolean | undefined;
};

export type ApplySkillImportRequest = {
  //
  // Behaviors: REQUIRED
  jobId: string | undefined;
  decisions: SkillImportDecision[] | undefined;
};

export type SkillImportApplyResult = {
  createdSkillIds: string[] | undefined;
  skippedCandidateIds: string[] | undefined;
  message: string | undefined;
};

export type RefineSkillImportConflictRequest = {
  //
  // Behaviors: REQUIRED
  jobId: string | undefined;
  //
  // Behaviors: REQUIRED
  groupId: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  instructions: string | undefined;
};

export type SkillRefineResult = {
  mergedName: string | undefined;
  mergedDescription: string | undefined;
  mergedBody: string | undefined;
  mergedTags: SkillTag[] | undefined;
  sourceCandidateIds: string[] | undefined;
  sourceExistingSkillIds: string[] | undefined;
};

export type SkillVersionDetail = {
  id: string | undefined;
  skillId: string | undefined;
  version: string | undefined;
  status: string | undefined;
  contentMarkdown: string | undefined;
  validationStatus: string | undefined;
  publishedAt: string | undefined;
  createdAt: string | undefined;
  fileManifestJson: string | undefined;
};

export type GetSkillVersionsRequest = {
  //
  // Behaviors: REQUIRED
  skillId: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type GetSkillVersionsResponse = {
  items: SkillVersionDetail[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type RollbackSkillVersionRequest = {
  //
  // Behaviors: REQUIRED
  skillId: string | undefined;
  //
  // Behaviors: REQUIRED
  versionId: string | undefined;
};

export type GetSkillHealthRequest = {
  //
  // Behaviors: REQUIRED
  skillId: string | undefined;
};

export type SkillHealthMetric = {
  skillId: string | undefined;
  totalInvocations7d: number | undefined;
  successCount7d: number | undefined;
  successRate7d: number | undefined;
  p95DurationMs7d: number | undefined;
  totalInvocations30d: number | undefined;
  successCount30d: number | undefined;
  successRate30d: number | undefined;
  p95DurationMs30d: number | undefined;
  dailyMetrics: SkillHealthDailyMetric[] | undefined;
  routeHitRate7d: number | undefined;
  routeHitRate30d: number | undefined;
};

export type SkillHealthDailyMetric = {
  date: string | undefined;
  invocations: number | undefined;
  successes: number | undefined;
  avgDurationMs: number | undefined;
  routedCount: number | undefined;
  loadedCount: number | undefined;
};

export interface SkillService {
  ListSkills(request: ListSkillsRequest): Promise<ListSkillsResponse>;
  GetSkillFilesystemHealth(request: wellKnownEmpty): Promise<SkillFilesystemHealth>;
  GetSkill(request: GetSkillRequest): Promise<GetSkillResponse>;
  CreateSkill(request: CreateSkillRequest): Promise<Skill>;
  UpdateSkill(request: UpdateSkillRequest): Promise<Skill>;
  PublishSkill(request: PublishSkillRequest): Promise<Skill>;
  PreviewSkillRuntime(request: PreviewSkillRuntimeRequest): Promise<PreviewSkillRuntimeResponse>;
  ToggleSkillEnabled(request: ToggleSkillEnabledRequest): Promise<Skill>;
  DuplicateSkill(request: DuplicateSkillRequest): Promise<Skill>;
  DeleteSkill(request: DeleteSkillRequest): Promise<wellKnownEmpty>;
  ListSkillFiles(request: ListSkillFilesRequest): Promise<ListSkillFilesResponse>;
  GetSkillFile(request: GetSkillFileRequest): Promise<SkillFileContent>;
  UpdateSkillFile(request: UpdateSkillFileRequest): Promise<SkillFileContent>;
  DeleteSkillFile(request: DeleteSkillFileRequest): Promise<wellKnownEmpty>;
  ListSkillRuns(request: ListSkillRunsRequest): Promise<ListSkillRunsResponse>;
  // ImportSkillZip accepts a ZIP file via multipart/form-data at POST /v1/skills/import.
  // The actual handler is registered by RegisterSkillImportMultipart (not proto codegen)
  // because proto cannot express multipart uploads. This RPC declaration exists solely
  // to include the endpoint in the generated OpenAPI spec.
  ImportSkillZip(request: wellKnownEmpty): Promise<ImportSkillZipResponse>;
  GetSkillImportJob(request: GetSkillImportJobRequest): Promise<SkillImportJob>;
  ApplySkillImport(request: ApplySkillImportRequest): Promise<SkillImportApplyResult>;
  RefineSkillImportConflict(request: RefineSkillImportConflictRequest): Promise<SkillRefineResult>;
  GetSkillVersions(request: GetSkillVersionsRequest): Promise<GetSkillVersionsResponse>;
  RollbackSkillVersion(request: RollbackSkillVersionRequest): Promise<Skill>;
  GetSkillHealth(request: GetSkillHealthRequest): Promise<SkillHealthMetric>;
  // 标签字典：治理 + 预建 + 实时使用计数。
  // 路由用独立前缀 /v1/skill-tags，避免被 /v1/skills/{id} 吞掉。
  ListSkillTags(request: wellKnownEmpty): Promise<ListSkillTagsResponse>;
  CreateSkillTag(request: CreateSkillTagRequest): Promise<SkillTagInfo>;
  RenameSkillTag(request: RenameSkillTagRequest): Promise<RenameSkillTagResponse>;
  DeleteSkillTag(request: DeleteSkillTagRequest): Promise<DeleteSkillTagResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createSkillServiceClient(
  handler: RequestHandler
): SkillService {
  return {
    ListSkills(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skills`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.search) {
        queryParams.push(`search=${encodeURIComponent(request.search.toString())}`)
      }
      if (request.tags) {
        queryParams.push(`tags=${encodeURIComponent(request.tags.toString())}`)
      }
      if (request.enabled) {
        queryParams.push(`enabled=${encodeURIComponent(request.enabled.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      if (request.filesystemMissing) {
        queryParams.push(`filesystemMissing=${encodeURIComponent(request.filesystemMissing.toString())}`)
      }
      if (request.syncOrigin) {
        queryParams.push(`syncOrigin=${encodeURIComponent(request.syncOrigin.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "ListSkills",
      }) as Promise<ListSkillsResponse>;
    },
    GetSkillFilesystemHealth(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skills/filesystem-health`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkillFilesystemHealth",
      }) as Promise<SkillFilesystemHealth>;
    },
    GetSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkill",
      }) as Promise<GetSkillResponse>;
    },
    CreateSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skills`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "CreateSkill",
      }) as Promise<Skill>;
    },
    UpdateSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "SkillService",
        method: "UpdateSkill",
      }) as Promise<Skill>;
    },
    PublishSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/publish`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "PublishSkill",
      }) as Promise<Skill>;
    },
    PreviewSkillRuntime(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-runtime-preview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.userQuery) {
        queryParams.push(`userQuery=${encodeURIComponent(request.userQuery.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "PreviewSkillRuntime",
      }) as Promise<PreviewSkillRuntimeResponse>;
    },
    ToggleSkillEnabled(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/enabled`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PATCH",
        body,
      }, {
        service: "SkillService",
        method: "ToggleSkillEnabled",
      }) as Promise<Skill>;
    },
    DuplicateSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/duplicate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "DuplicateSkill",
      }) as Promise<Skill>;
    },
    DeleteSkill(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "SkillService",
        method: "DeleteSkill",
      }) as Promise<wellKnownEmpty>;
    },
    ListSkillFiles(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/files`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "ListSkillFiles",
      }) as Promise<ListSkillFilesResponse>;
    },
    GetSkillFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/file`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.path) {
        queryParams.push(`path=${encodeURIComponent(request.path.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkillFile",
      }) as Promise<SkillFileContent>;
    },
    UpdateSkillFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/file`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "SkillService",
        method: "UpdateSkillFile",
      }) as Promise<SkillFileContent>;
    },
    DeleteSkillFile(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skills/${request.id}/files:delete`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "DeleteSkillFile",
      }) as Promise<wellKnownEmpty>;
    },
    ListSkillRuns(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-runs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.skillId) {
        queryParams.push(`skillId=${encodeURIComponent(request.skillId.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.from) {
        queryParams.push(`from=${encodeURIComponent(request.from.toString())}`)
      }
      if (request.to) {
        queryParams.push(`to=${encodeURIComponent(request.to.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "ListSkillRuns",
      }) as Promise<ListSkillRunsResponse>;
    },
    ImportSkillZip(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skills/import`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "ImportSkillZip",
      }) as Promise<ImportSkillZipResponse>;
    },
    GetSkillImportJob(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.jobId) {
        throw new Error("missing required field request.job_id");
      }
      const path = `v1/skills/import/${request.jobId}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkillImportJob",
      }) as Promise<SkillImportJob>;
    },
    ApplySkillImport(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.jobId) {
        throw new Error("missing required field request.job_id");
      }
      const path = `v1/skills/import/${request.jobId}/apply`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "ApplySkillImport",
      }) as Promise<SkillImportApplyResult>;
    },
    RefineSkillImportConflict(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.jobId) {
        throw new Error("missing required field request.job_id");
      }
      if (!request.groupId) {
        throw new Error("missing required field request.group_id");
      }
      const path = `v1/skills/import/${request.jobId}/conflict-groups/${request.groupId}/refine`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "RefineSkillImportConflict",
      }) as Promise<SkillRefineResult>;
    },
    GetSkillVersions(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.skillId) {
        throw new Error("missing required field request.skill_id");
      }
      const path = `v1/skills/${request.skillId}/versions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkillVersions",
      }) as Promise<GetSkillVersionsResponse>;
    },
    RollbackSkillVersion(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.skillId) {
        throw new Error("missing required field request.skill_id");
      }
      if (!request.versionId) {
        throw new Error("missing required field request.version_id");
      }
      const path = `v1/skills/${request.skillId}/versions/${request.versionId}/rollback`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "RollbackSkillVersion",
      }) as Promise<Skill>;
    },
    GetSkillHealth(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.skillId) {
        throw new Error("missing required field request.skill_id");
      }
      const path = `v1/skills/${request.skillId}/health`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "GetSkillHealth",
      }) as Promise<SkillHealthMetric>;
    },
    ListSkillTags(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-tags`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillService",
        method: "ListSkillTags",
      }) as Promise<ListSkillTagsResponse>;
    },
    CreateSkillTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-tags`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "CreateSkillTag",
      }) as Promise<SkillTagInfo>;
    },
    RenameSkillTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-tags:rename`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillService",
        method: "RenameSkillTag",
      }) as Promise<RenameSkillTagResponse>;
    },
    DeleteSkillTag(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.name) {
        throw new Error("missing required field request.name");
      }
      const path = `v1/skill-tags/${request.name}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "SkillService",
        method: "DeleteSkillTag",
      }) as Promise<DeleteSkillTagResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;


// @@protoc_insertion_point(typescript-http-eof)
