// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// OrganizationNode represents a node in the organization hierarchy.
// Level values: "company" | "department" | "position".
export type OrganizationNode = {
  id: string | undefined;
  orgKey: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  parentId: string | undefined;
  // Level: "company" | "department" | "position"
  level: string | undefined;
  workspaceId: string | undefined;
  ownerUserId: string | undefined;
  isSystem: boolean | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
  // Department-level fields
  deptLeadAgentId: string | undefined;
  deptLeadConfigJson: string | undefined;
};

export type ListOrganizationResponse = {
  items: OrganizationNode[] | undefined;
};

export type ListOrganizationTreeResponse = {
  items: OrganizationTreeNode[] | undefined;
};

export type OrganizationTreeNode = {
  node: OrganizationNode | undefined;
  children: OrganizationTreeNode[] | undefined;
};

export type CreateOrganizationRequest = {
  //
  // Behaviors: REQUIRED
  orgKey: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled?: boolean;
  sortOrder: number | undefined;
  parentId: string | undefined;
  // Level: "company" | "department" | "position"
  level: string | undefined;
  workspaceId: string | undefined;
  ownerUserId: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  // Department-level fields
  deptLeadAgentId: string | undefined;
  deptLeadConfigJson: string | undefined;
};

export type GetOrganizationRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type UpdateOrganizationRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  node: OrganizationNode | undefined;
};

export type DeleteOrganizationRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ReorderOrganizationRequest = {
  ids: string[] | undefined;
};

export type ReorderOrganizationResponse = {};

export interface OrganizationService {
  ListOrganization(request: wellKnownEmpty): Promise<ListOrganizationResponse>;
  ListOrganizationTree(request: wellKnownEmpty): Promise<ListOrganizationTreeResponse>;
  CreateOrganization(request: CreateOrganizationRequest): Promise<OrganizationNode>;
  GetOrganization(request: GetOrganizationRequest): Promise<OrganizationNode>;
  UpdateOrganization(request: UpdateOrganizationRequest): Promise<OrganizationNode>;
  DeleteOrganization(request: DeleteOrganizationRequest): Promise<wellKnownEmpty>;
  ReorderOrganization(request: ReorderOrganizationRequest): Promise<ReorderOrganizationResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createOrganizationServiceClient(handler: RequestHandler): OrganizationService {
  return {
    ListOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/organization`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'ListOrganization',
        },
      ) as Promise<ListOrganizationResponse>;
    },
    ListOrganizationTree(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/organization/tree`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'ListOrganizationTree',
        },
      ) as Promise<ListOrganizationTreeResponse>;
    },
    CreateOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/organization`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'CreateOrganization',
        },
      ) as Promise<OrganizationNode>;
    },
    GetOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/organization/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'GetOrganization',
        },
      ) as Promise<OrganizationNode>;
    },
    UpdateOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/organization/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request?.node ?? {});
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'UpdateOrganization',
        },
      ) as Promise<OrganizationNode>;
    },
    DeleteOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/organization/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'DeleteOrganization',
        },
      ) as Promise<wellKnownEmpty>;
    },
    ReorderOrganization(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/organization/reorder`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'OrganizationService',
          method: 'ReorderOrganization',
        },
      ) as Promise<ReorderOrganizationResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
