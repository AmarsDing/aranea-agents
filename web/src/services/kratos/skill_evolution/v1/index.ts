// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type SkillProposal = {
  id: string | undefined;
  agentId: string | undefined;
  patternHash: string | undefined;
  patternDesc: string | undefined;
  skillName: string | undefined;
  skillMd: string | undefined;
  status: string | undefined;
  approvedBy: string | undefined;
  rejectedBy: string | undefined;
  createdAt: string | undefined;
  approvedAt: string | undefined;
};

export type ListSkillProposalsRequest = {
  agentId: string | undefined;
  status: string | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type ListSkillProposalsResponse = {
  items: SkillProposal[] | undefined;
  total: number | undefined;
  page: number | undefined;
  pageSize: number | undefined;
};

export type GetSkillProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ApproveSkillProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  approvedBy: string | undefined;
};

export type RejectSkillProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  rejectedBy: string | undefined;
};

export type RegisterSkillProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type TriggerSkillDetectionRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type TriggerSkillDetectionResponse = {
  proposals: SkillProposal[] | undefined;
};

export interface SkillEvolutionService {
  ListSkillProposals(request: ListSkillProposalsRequest): Promise<ListSkillProposalsResponse>;
  GetSkillProposal(request: GetSkillProposalRequest): Promise<SkillProposal>;
  ApproveSkillProposal(request: ApproveSkillProposalRequest): Promise<SkillProposal>;
  RejectSkillProposal(request: RejectSkillProposalRequest): Promise<SkillProposal>;
  RegisterSkillProposal(request: RegisterSkillProposalRequest): Promise<SkillProposal>;
  TriggerSkillDetection(request: TriggerSkillDetectionRequest): Promise<TriggerSkillDetectionResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createSkillEvolutionServiceClient(
  handler: RequestHandler
): SkillEvolutionService {
  return {
    ListSkillProposals(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/skill-evolution/proposals`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.page) {
        queryParams.push(`page=${encodeURIComponent(request.page.toString())}`)
      }
      if (request.pageSize) {
        queryParams.push(`pageSize=${encodeURIComponent(request.pageSize.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "ListSkillProposals",
      }) as Promise<ListSkillProposalsResponse>;
    },
    GetSkillProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skill-evolution/proposals/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "GetSkillProposal",
      }) as Promise<SkillProposal>;
    },
    ApproveSkillProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skill-evolution/proposals/${request.id}/approve`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "ApproveSkillProposal",
      }) as Promise<SkillProposal>;
    },
    RejectSkillProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skill-evolution/proposals/${request.id}/reject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "RejectSkillProposal",
      }) as Promise<SkillProposal>;
    },
    RegisterSkillProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/skill-evolution/proposals/${request.id}/register`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "RegisterSkillProposal",
      }) as Promise<SkillProposal>;
    },
    TriggerSkillDetection(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/skill-evolution/agents/${request.agentId}/detect`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "SkillEvolutionService",
        method: "TriggerSkillDetection",
      }) as Promise<TriggerSkillDetectionResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
