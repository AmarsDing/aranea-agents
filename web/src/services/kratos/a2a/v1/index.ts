// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// A2ACapability describes one capability advertised by an agent.
export type A2ACapability = {
  name: string | undefined;
  description: string | undefined;
  inputSchemaJson: string | undefined;
  outputSchemaJson: string | undefined;
};

// A2AAgentCard is the public metadata published by an A2A-enabled agent.
export type A2AAgentCard = {
  agentId: string | undefined;
  displayName: string | undefined;
  workspace: string | undefined;
  enabled: boolean | undefined;
  capabilities: A2ACapability[] | undefined;
  updatedAt: string | undefined;
  // source classifies discover/gateway entries: local | remote
  source: string | undefined;
  // endpoint_url is the public A2A URL when source=local and endpoint is enabled.
  endpointUrl: string | undefined;
  // remote_url is set when source=remote.
  remoteUrl: string | undefined;
};

// A2AInvokeRequest sends a request from one agent to another.
export type A2AInvokeRequest = {
  //
  // Behaviors: REQUIRED
  calleeAgentId: string | undefined;
  //
  // Behaviors: REQUIRED
  capability: string | undefined;
  //
  // Behaviors: REQUIRED
  payloadJson: string | undefined;
  // caller_session_id is the originating session for audit.
  callerSessionId: string | undefined;
  // timeout_seconds overrides the default invocation timeout.
  timeoutSeconds: number | undefined;
  // workspace scopes Admin Invoke to a workspace (must match callee card workspace).
  workspace: string | undefined;
};

// A2ARemoteAgent is a workspace-scoped registry entry for an external A2A service.
export type A2ARemoteAgent = {
  id: string | undefined;
  workspace: string | undefined;
  displayName: string | undefined;
  remoteUrl: string | undefined;
  agentCardUrl: string | undefined;
  authType: string | undefined;
  // auth_config_json is opaque JSON (api_key, token, mtls cert paths); never returned on read.
  enabled: boolean | undefined;
  discoveredCard: A2AAgentCard | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  // Health fields populated by the gateway health probe runner.
  healthy: boolean | undefined;
  lastHealthAt: string | undefined;
};

export type RegisterRemoteAgentRequest = {
  workspace: string | undefined;
  //
  // Behaviors: REQUIRED
  remoteUrl: string | undefined;
  agentCardUrl: string | undefined;
  displayName: string | undefined;
  authType: string | undefined;
  authConfigJson: string | undefined;
  enabled?: boolean;
};

export type ListRemoteAgentsRequest = {
  workspace: string | undefined;
};

export type ListRemoteAgentsResponse = {
  items: A2ARemoteAgent[] | undefined;
};

export type DeleteRemoteAgentRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type DiscoverRemoteAgentRequest = {
  //
  // Behaviors: REQUIRED
  remoteUrl: string | undefined;
  authType: string | undefined;
  authConfigJson: string | undefined;
};

// A2AInvokeResponse returns the result from the callee agent.
export type A2AInvokeResponse = {
  invokeId: string | undefined;
  status: string | undefined;
  resultJson: string | undefined;
  errorMessage: string | undefined;
  durationMs: number | undefined;
};

// A2AAuditEntry is one record in the A2A audit log.
export type A2AAuditEntry = {
  id: string | undefined;
  invokeId: string | undefined;
  callerAgentId: string | undefined;
  calleeAgentId: string | undefined;
  capability: string | undefined;
  status: string | undefined;
  durationMs: number | undefined;
  workspace: string | undefined;
  createdAt: string | undefined;
};

export type UpdateAgentCardRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  enabled: boolean | undefined;
  capabilities: A2ACapability[] | undefined;
};

export type GetAgentCardRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type DiscoverRequest = {
  workspace: string | undefined;
  capability: string | undefined;
};

export type DiscoverResponse = {
  agents: A2AAgentCard[] | undefined;
};

// A2AGatewayEntry aggregates local endpoints and remote registry rows for federation.
export type A2AGatewayEntry = {
  card: A2AAgentCard | undefined;
  source: string | undefined;
  registryId: string | undefined;
  endpointUrl: string | undefined;
  remoteUrl: string | undefined;
  healthy: boolean | undefined;
};

export type GatewayDiscoverRequest = {
  workspace: string | undefined;
  capability: string | undefined;
  // check_health probes remote AgentCard URLs when true.
  checkHealth: boolean | undefined;
};

export type GatewayDiscoverResponse = {
  items: A2AGatewayEntry[] | undefined;
};

// A2ARuntimeConfig exposes effective deployment settings for the A2A admin UI.
export type A2ARuntimeConfig = {
  publicBaseUrl: string | undefined;
  // env | config | derived
  publicBaseUrlSource: string | undefined;
};

export type ListAuditRequest = {
  callerAgentId: string | undefined;
  calleeAgentId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListAuditResponse = {
  items: A2AAuditEntry[] | undefined;
  total: number | undefined;
};

export interface A2AService {
  // Discover A2A-enabled agents in a workspace.
  Discover(request: DiscoverRequest): Promise<DiscoverResponse>;
  // Invoke a capability on a target agent.
  Invoke(request: A2AInvokeRequest): Promise<A2AInvokeResponse>;
  // Manage the agent's A2A card (capabilities + enabled flag).
  UpdateAgentCard(request: UpdateAgentCardRequest): Promise<A2AAgentCard>;
  GetAgentCard(request: GetAgentCardRequest): Promise<A2AAgentCard>;
  // Audit log
  ListAudit(request: ListAuditRequest): Promise<ListAuditResponse>;
  // RegisterRemoteAgent adds an external A2A agent to the workspace registry.
  RegisterRemoteAgent(request: RegisterRemoteAgentRequest): Promise<A2ARemoteAgent>;
  ListRemoteAgents(request: ListRemoteAgentsRequest): Promise<ListRemoteAgentsResponse>;
  DeleteRemoteAgent(request: DeleteRemoteAgentRequest): Promise<wellKnownEmpty>;
  // DiscoverRemoteAgent fetches AgentCard from a URL without persisting.
  DiscoverRemoteAgent(request: DiscoverRemoteAgentRequest): Promise<A2AAgentCard>;
  // GatewayDiscover lists federated local endpoints + remote registry entries.
  GatewayDiscover(request: GatewayDiscoverRequest): Promise<GatewayDiscoverResponse>;
  // GetA2AConfig returns read-only runtime configuration (public URL source, etc.).
  GetA2AConfig(request: wellKnownEmpty): Promise<A2ARuntimeConfig>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createA2AServiceClient(
  handler: RequestHandler
): A2AService {
  return {
    Discover(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/discover`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.workspace) {
        queryParams.push(`workspace=${encodeURIComponent(request.workspace.toString())}`)
      }
      if (request.capability) {
        queryParams.push(`capability=${encodeURIComponent(request.capability.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "Discover",
      }) as Promise<DiscoverResponse>;
    },
    Invoke(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/invoke`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "A2AService",
        method: "Invoke",
      }) as Promise<A2AInvokeResponse>;
    },
    UpdateAgentCard(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/a2a/agents/${request.agentId}/card`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "A2AService",
        method: "UpdateAgentCard",
      }) as Promise<A2AAgentCard>;
    },
    GetAgentCard(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/a2a/agents/${request.agentId}/card`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "GetAgentCard",
      }) as Promise<A2AAgentCard>;
    },
    ListAudit(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/audit`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.callerAgentId) {
        queryParams.push(`callerAgentId=${encodeURIComponent(request.callerAgentId.toString())}`)
      }
      if (request.calleeAgentId) {
        queryParams.push(`calleeAgentId=${encodeURIComponent(request.calleeAgentId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "ListAudit",
      }) as Promise<ListAuditResponse>;
    },
    RegisterRemoteAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/remote-agents`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "A2AService",
        method: "RegisterRemoteAgent",
      }) as Promise<A2ARemoteAgent>;
    },
    ListRemoteAgents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/remote-agents`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.workspace) {
        queryParams.push(`workspace=${encodeURIComponent(request.workspace.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "ListRemoteAgents",
      }) as Promise<ListRemoteAgentsResponse>;
    },
    DeleteRemoteAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/a2a/remote-agents/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "A2AService",
        method: "DeleteRemoteAgent",
      }) as Promise<wellKnownEmpty>;
    },
    DiscoverRemoteAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/remote-discover`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "A2AService",
        method: "DiscoverRemoteAgent",
      }) as Promise<A2AAgentCard>;
    },
    GatewayDiscover(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/gateway/discover`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.workspace) {
        queryParams.push(`workspace=${encodeURIComponent(request.workspace.toString())}`)
      }
      if (request.capability) {
        queryParams.push(`capability=${encodeURIComponent(request.capability.toString())}`)
      }
      if (request.checkHealth) {
        queryParams.push(`checkHealth=${encodeURIComponent(request.checkHealth.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "GatewayDiscover",
      }) as Promise<GatewayDiscoverResponse>;
    },
    GetA2AConfig(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/config`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "A2AService",
        method: "GetA2AConfig",
      }) as Promise<A2ARuntimeConfig>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// FederationOrg is a registered organization in the A2A federation network.
// auth_config_json is write-only: reads never return the plaintext config
// (design F.8); auth_config_set reports whether one is stored.
export type FederationOrg = {
  id: string | undefined;
  name: string | undefined;
  domain: string | undefined;
  publicBaseUrl: string | undefined;
  // trust_level: untrusted | neutral | trusted
  trustLevel: string | undefined;
  // auth_type: none | api_key | bearer | mtls
  authType: string | undefined;
  authConfigSet: boolean | undefined;
  // status: active | suspended
  status: string | undefined;
  joinedAt: string | undefined;
  updatedAt: string | undefined;
};

// FederationPolicy controls outbound calls for one org pair.
export type FederationPolicy = {
  id: string | undefined;
  // caller_org_id = "local" means this organization (outbound policy).
  callerOrgId: string | undefined;
  calleeOrgId: string | undefined;
  // action: allow | deny (approval reserved, treated as deny this iteration)
  action: string | undefined;
  // max_per_min: per-minute invocation cap; 0 = unlimited
  maxPerMin: number | undefined;
  // daily_quota: daily cap on decision=allowed calls; 0 = unlimited
  dailyQuota: number | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

// FederationAgentEntry is one directory row: the org plus its registered
// remote agent and cached agent card.
export type FederationAgentEntry = {
  org: FederationOrg | undefined;
  remoteAgent: A2ARemoteAgent | undefined;
  card: A2AAgentCard | undefined;
};

// FederationAuditEntry is one cross-organization invocation decision + result.
export type FederationAuditEntry = {
  id: string | undefined;
  // direction: outbound (inbound reserved for future iteration)
  direction: string | undefined;
  callerOrgId: string | undefined;
  calleeOrgId: string | undefined;
  callerAgentId: string | undefined;
  calleeAgentId: string | undefined;
  capability: string | undefined;
  // decision: allowed | denied_trust | denied_policy | denied_quota
  decision: string | undefined;
  // status: pending | success | error | timeout
  status: string | undefined;
  latencyMs: number | undefined;
  errorMessage: string | undefined;
  createdAt: string | undefined;
};

export type RegisterFederationOrgRequest = {
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  // domain is the unique upsert key: re-registering the same domain updates
  // the existing org in place (identity + joined_at preserved).
  //
  // Behaviors: REQUIRED
  domain: string | undefined;
  publicBaseUrl: string | undefined;
  trustLevel: string | undefined;
  authType: string | undefined;
  authConfigJson: string | undefined;
};

export type ListFederationOrgsRequest = {
};

export type ListFederationOrgsResponse = {
  items: FederationOrg[] | undefined;
};

export type DeleteFederationOrgRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type SetFederationTrustLevelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  trustLevel: string | undefined;
};

export type UpsertFederationPolicyRequest = {
  //
  // Behaviors: REQUIRED
  callerOrgId: string | undefined;
  //
  // Behaviors: REQUIRED
  calleeOrgId: string | undefined;
  action: string | undefined;
  maxPerMin: number | undefined;
  dailyQuota: number | undefined;
};

export type DiscoverFederationAgentsRequest = {
  capability: string | undefined;
  orgId: string | undefined;
};

export type DiscoverFederationAgentsResponse = {
  items: FederationAgentEntry[] | undefined;
};

export type InvokeFederatedAgentRequest = {
  //
  // Behaviors: REQUIRED
  orgId: string | undefined;
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  //
  // Behaviors: REQUIRED
  capability: string | undefined;
  //
  // Behaviors: REQUIRED
  payloadJson: string | undefined;
  // timeout_seconds overrides the default invocation timeout.
  timeoutSeconds: number | undefined;
  // workspace scopes remote-agent resolution to the caller's workspace.
  workspace: string | undefined;
  // caller_agent_id is recorded on the audit entry (optional).
  callerAgentId: string | undefined;
};

export type InvokeFederatedAgentResponse = {
  // audit_id correlates the response with the federation audit entry.
  auditId: string | undefined;
  status: string | undefined;
  resultJson: string | undefined;
  errorMessage: string | undefined;
  latencyMs: number | undefined;
};

export type QueryFederationAuditLogsRequest = {
  callerOrgId: string | undefined;
  calleeOrgId: string | undefined;
  decision: string | undefined;
  status: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type QueryFederationAuditLogsResponse = {
  items: FederationAuditEntry[] | undefined;
  total: number | undefined;
};

// FederationService governs the cross-organization A2A federation network:
// org registry, trust levels, call policies, directory, invocation and audit.
export interface FederationService {
  // RegisterFederationOrg creates or updates an org (upsert by domain).
  RegisterFederationOrg(request: RegisterFederationOrgRequest): Promise<FederationOrg>;
  // ListFederationOrgs lists all registered orgs.
  ListFederationOrgs(request: ListFederationOrgsRequest): Promise<ListFederationOrgsResponse>;
  // DeleteFederationOrg removes an org; remote agents are only disassociated
  // (org_id cleared), not cascade-deleted.
  DeleteFederationOrg(request: DeleteFederationOrgRequest): Promise<wellKnownEmpty>;
  // SetFederationTrustLevel sets the trust level of an org.
  SetFederationTrustLevel(request: SetFederationTrustLevelRequest): Promise<FederationOrg>;
  // UpsertFederationPolicy configures the call policy for one org pair.
  UpsertFederationPolicy(request: UpsertFederationPolicyRequest): Promise<FederationPolicy>;
  // DiscoverFederationAgents searches the federation directory (cached cards,
  // no live pull); filters by capability and/or org_id.
  DiscoverFederationAgents(request: DiscoverFederationAgentsRequest): Promise<DiscoverFederationAgentsResponse>;
  // InvokeFederatedAgent invokes a remote agent of a federated org through
  // the governance chain (trust -> policy -> quota -> audit -> invoke).
  InvokeFederatedAgent(request: InvokeFederatedAgentRequest): Promise<InvokeFederatedAgentResponse>;
  // QueryFederationAuditLogs queries audit entries with filters + pagination.
  QueryFederationAuditLogs(request: QueryFederationAuditLogsRequest): Promise<QueryFederationAuditLogsResponse>;
}

export function createFederationServiceClient(
  handler: RequestHandler
): FederationService {
  return {
    RegisterFederationOrg(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/orgs`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "FederationService",
        method: "RegisterFederationOrg",
      }) as Promise<FederationOrg>;
    },
    ListFederationOrgs(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/orgs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "FederationService",
        method: "ListFederationOrgs",
      }) as Promise<ListFederationOrgsResponse>;
    },
    DeleteFederationOrg(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/a2a/federation/orgs/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "DELETE",
        body,
      }, {
        service: "FederationService",
        method: "DeleteFederationOrg",
      }) as Promise<wellKnownEmpty>;
    },
    SetFederationTrustLevel(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/a2a/federation/orgs/${request.id}/trust`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "FederationService",
        method: "SetFederationTrustLevel",
      }) as Promise<FederationOrg>;
    },
    UpsertFederationPolicy(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/policies`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "FederationService",
        method: "UpsertFederationPolicy",
      }) as Promise<FederationPolicy>;
    },
    DiscoverFederationAgents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/agents`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.capability) {
        queryParams.push(`capability=${encodeURIComponent(request.capability.toString())}`)
      }
      if (request.orgId) {
        queryParams.push(`orgId=${encodeURIComponent(request.orgId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "FederationService",
        method: "DiscoverFederationAgents",
      }) as Promise<DiscoverFederationAgentsResponse>;
    },
    InvokeFederatedAgent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/invoke`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "FederationService",
        method: "InvokeFederatedAgent",
      }) as Promise<InvokeFederatedAgentResponse>;
    },
    QueryFederationAuditLogs(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/a2a/federation/audits`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.callerOrgId) {
        queryParams.push(`callerOrgId=${encodeURIComponent(request.callerOrgId.toString())}`)
      }
      if (request.calleeOrgId) {
        queryParams.push(`calleeOrgId=${encodeURIComponent(request.calleeOrgId.toString())}`)
      }
      if (request.decision) {
        queryParams.push(`decision=${encodeURIComponent(request.decision.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "FederationService",
        method: "QueryFederationAuditLogs",
      }) as Promise<QueryFederationAuditLogsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
