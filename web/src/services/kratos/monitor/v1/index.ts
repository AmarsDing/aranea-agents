// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type AuditLog = {
  id: string | undefined;
  action: string | undefined;
  resource: string | undefined;
  resourceId: string | undefined;
  requestId: string | undefined;
  detail: string | undefined;
  createdAt: string | undefined;
  actor: string | undefined;
  ip: string | undefined;
  userAgent: string | undefined;
  severity: string | undefined;
  metadataJson: string | undefined;
};

export type ListAuditLogsRequest = {
  limit: number | undefined;
  offset: number | undefined;
  action: string | undefined;
  resource: string | undefined;
  actor: string | undefined;
  keyword: string | undefined;
};

export type ListAuditLogsResponse = {
  items: AuditLog[] | undefined;
  total: number | undefined;
};

// Mirrors legacy domain.PlatformResource JSON for monitor-events / monitor-traces rows.
export type MonitorPlatformRow = {
  id: string | undefined;
  resource: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  parentId: string | undefined;
  level: string | undefined;
  agentId: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
};

export type ListMonitorEventsRequest = {
  limit: number | undefined;
  offset: number | undefined;
  eventType: string | undefined;
  agentId: string | undefined;
  status: string | undefined;
};

export type ListMonitorEventsResponse = {
  items: MonitorPlatformRow[] | undefined;
  total: number | undefined;
};

export type GetMonitorEventRequest = {
  id: string | undefined;
};

export type ListMonitorTracesRequest = {
  limit: number | undefined;
  offset: number | undefined;
  agentId: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  status: string | undefined;
};

export type ListMonitorTracesResponse = {
  items: MonitorPlatformRow[] | undefined;
  total: number | undefined;
};

export type MonitorTraceDetail = {
  trace: MonitorPlatformRow | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  spansJson: string | undefined;
};

export type GetMonitorTraceRequest = {
  id: string | undefined;
};

export type MonitorLogLine = {
  id: string | undefined;
  time: string | undefined;
  level: string | undefined;
  message: string | undefined;
  source: string | undefined;
  createdAt: string | undefined;
};

export type GetMonitorLogsResponse = {
  items: MonitorLogLine[] | undefined;
  // Mirrors server.monitor.process_log_enabled (Gateway process log / EnvelopeTypeLog).
  enabled: boolean | undefined;
  message: string | undefined;
};

export type GetMonitorLogsRequest = {};

export type FlowLogEntry = {
  id: string | undefined;
  traceId: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  teamId: string | undefined;
  domain: string | undefined;
  agentKey: string | undefined;
  stepId: string | undefined;
  flowPhase: string | undefined;
  severity: string | undefined;
  title: string | undefined;
  message: string | undefined;
  payloadJson: string | undefined;
  createdAt: string | undefined;
};

export type ListFlowLogsRequest = {
  traceId: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  severity: string | undefined;
  domain: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // RFC3339 bounds (optional).
  since: string | undefined;
  until: string | undefined;
};

export type ListFlowLogsResponse = {
  items: FlowLogEntry[] | undefined;
  total: number | undefined;
};

export type MonitorAlertRule = {
  id: string | undefined;
  name: string | undefined;
  metricKey: string | undefined;
  threshold: number | undefined;
  windowMinutes: number | undefined;
  enabled: boolean | undefined;
  severity: string | undefined;
  notifyWebhookUrl: string | undefined;
  notifyChannelId: string | undefined;
  cooldownMinutes: number | undefined;
};

export type ListMonitorAlertRulesResponse = {
  items: MonitorAlertRule[] | undefined;
};

export type PutMonitorAlertRulesRequest = {
  items: MonitorAlertRule[] | undefined;
};

export type PutMonitorAlertRulesResponse = {
  items: MonitorAlertRule[] | undefined;
};

export type GetRunnerMetricsRequest = {
  windowMinutes: number | undefined;
};

export type RunnerMetricsSummary = {
  windowMinutes: number | undefined;
  totalRuns: number | undefined;
  errorRuns: number | undefined;
  errorRate: number | undefined;
  successRate: number | undefined;
  avgDurationMs: number | undefined;
  p50DurationMs: number | undefined;
  p95DurationMs: number | undefined;
  p99DurationMs: number | undefined;
};

export type CodeExecutorCapability = {
  type: string | undefined;
  available: boolean | undefined;
  reason: string | undefined;
};

export type GetCodeExecutorCapabilitiesResponse = {
  backends: CodeExecutorCapability[] | undefined;
};

export type GenerateDiagnosticBundleRequest = {
  triggerType: string | undefined;
  traceId: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  stepId: string | undefined;
  contextMinutes: number | undefined;
};

export type GenerateDiagnosticBundleResponse = {
  bundleId: string | undefined;
  manifestJson: string | undefined;
  totalEntries: number | undefined;
  flowJsonl: string | undefined;
  traceJson: string | undefined;
  usageJson: string | undefined;
  alertsJsonl: string | undefined;
};

export type DiagnoseAndHealRequest = {
  triggerType: string | undefined;
  traceId: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  stepId: string | undefined;
  contextMinutes: number | undefined;
};

export type AutoHealedCondition = {
  autoHealed: boolean | undefined;
  healStrategy: string | undefined;
};

export type HealAttemptsCondition = {
  attempts: number | undefined;
  maxAttempts: number | undefined;
  lastStrategy: string | undefined;
};

export type SelfCheckStatusCondition = {
  checkName: string | undefined;
  status: string | undefined;
  message: string | undefined;
};

export type RootCauseCondition = {
  autoHealed?: AutoHealedCondition;
  healAttempts?: HealAttemptsCondition;
  selfCheckStatus?: SelfCheckStatusCondition;
};

export type DiagnoseAndHealResponse = {
  healId: string | undefined;
  ruleId: string | undefined;
  status: string | undefined;
  reason: string | undefined;
  confidence: number | undefined;
  fixActionType: string | undefined;
  fixActionMaxAttempts: number | undefined;
  fixActionParamsJson: string | undefined;
  createdAt: string | undefined;
  rootCauseCondition: RootCauseCondition | undefined;
  runtimeAutoHealed: boolean | undefined;
  runtimeHealAttempts: number | undefined;
};

export type SelfCheckResultEntry = {
  checkId: string | undefined;
  checker: string | undefined;
  status: string | undefined;
  message: string | undefined;
  detailsJson: string | undefined;
  checkedAt: string | undefined;
};

export type RepairActionEntry = {
  success: boolean | undefined;
  action: string | undefined;
  message: string | undefined;
};

export type SelfCheckReportEntry = {
  id: string | undefined;
  checkResults: SelfCheckResultEntry[] | undefined;
  overallStatus: string | undefined;
  repairActions: RepairActionEntry[] | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  durationMs: number | undefined;
};

export type TriggerSelfCheckRequest = {};

export type TriggerSelfCheckResponse = {
  report: SelfCheckReportEntry | undefined;
};

export type ListSelfCheckReportsRequest = {
  limit: number | undefined;
  offset: number | undefined;
};

export type ListSelfCheckReportsResponse = {
  items: SelfCheckReportEntry[] | undefined;
  total: number | undefined;
};

export type HealStatsRequest = {};

export type HealStatsResponse = {
  totalHeals: number | undefined;
  successRate: number | undefined;
  topFailRules: RuleFailCount[] | undefined;
};

export type RuleFailCount = {
  ruleId: string | undefined;
  count: number | undefined;
};

export type ListHealRecordsRequest = {
  limit: number | undefined;
  offset: number | undefined;
  ruleId: string | undefined;
  status: string | undefined;
  sessionId: string | undefined;
};

export type HealRecordEntry = {
  id: string | undefined;
  ruleId: string | undefined;
  triggerType: string | undefined;
  traceId: string | undefined;
  sessionId: string | undefined;
  stepId: string | undefined;
  fixActionType: string | undefined;
  confidence: number | undefined;
  status: string | undefined;
  runtimeAutoHealed: boolean | undefined;
  runtimeHealAttempts: number | undefined;
  reason: string | undefined;
  createdAt: string | undefined;
};

export type ListHealRecordsResponse = {
  items: HealRecordEntry[] | undefined;
  total: number | undefined;
};

export interface MonitorService {
  ListAuditLogs(request: ListAuditLogsRequest): Promise<ListAuditLogsResponse>;
  ListMonitorEvents(request: ListMonitorEventsRequest): Promise<ListMonitorEventsResponse>;
  GetMonitorEvent(request: GetMonitorEventRequest): Promise<MonitorPlatformRow>;
  ListMonitorTraces(request: ListMonitorTracesRequest): Promise<ListMonitorTracesResponse>;
  GetMonitorTrace(request: GetMonitorTraceRequest): Promise<MonitorTraceDetail>;
  // Snapshot hint only; live lines use WebSocket GET /v1/ws (session_id=* or session-scoped, channel=monitor, type=log).
  GetMonitorLogs(request: GetMonitorLogsRequest): Promise<GetMonitorLogsResponse>;
  ListFlowLogs(request: ListFlowLogsRequest): Promise<ListFlowLogsResponse>;
  ListMonitorAlertRules(request: GetMonitorLogsRequest): Promise<ListMonitorAlertRulesResponse>;
  PutMonitorAlertRules(request: PutMonitorAlertRulesRequest): Promise<PutMonitorAlertRulesResponse>;
  GetRunnerMetrics(request: GetRunnerMetricsRequest): Promise<RunnerMetricsSummary>;
  GetCodeExecutorCapabilities(request: GetMonitorLogsRequest): Promise<GetCodeExecutorCapabilitiesResponse>;
  GenerateDiagnosticBundle(request: GenerateDiagnosticBundleRequest): Promise<GenerateDiagnosticBundleResponse>;
  DiagnoseAndHeal(request: DiagnoseAndHealRequest): Promise<DiagnoseAndHealResponse>;
  TriggerSelfCheck(request: TriggerSelfCheckRequest): Promise<TriggerSelfCheckResponse>;
  ListSelfCheckReports(request: ListSelfCheckReportsRequest): Promise<ListSelfCheckReportsResponse>;
  GetHealStats(request: HealStatsRequest): Promise<HealStatsResponse>;
  ListHealRecords(request: ListHealRecordsRequest): Promise<ListHealRecordsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createMonitorServiceClient(handler: RequestHandler): MonitorService {
  return {
    ListAuditLogs(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/audit`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.action) {
        queryParams.push(`action=${encodeURIComponent(request.action.toString())}`);
      }
      if (request.resource) {
        queryParams.push(`resource=${encodeURIComponent(request.resource.toString())}`);
      }
      if (request.actor) {
        queryParams.push(`actor=${encodeURIComponent(request.actor.toString())}`);
      }
      if (request.keyword) {
        queryParams.push(`keyword=${encodeURIComponent(request.keyword.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListAuditLogs',
        },
      ) as Promise<ListAuditLogsResponse>;
    },
    ListMonitorEvents(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/events`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.eventType) {
        queryParams.push(`eventType=${encodeURIComponent(request.eventType.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListMonitorEvents',
        },
      ) as Promise<ListMonitorEventsResponse>;
    },
    GetMonitorEvent(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/monitor/events/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetMonitorEvent',
        },
      ) as Promise<MonitorPlatformRow>;
    },
    ListMonitorTraces(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/traces`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`);
      }
      if (request.provider) {
        queryParams.push(`provider=${encodeURIComponent(request.provider.toString())}`);
      }
      if (request.model) {
        queryParams.push(`model=${encodeURIComponent(request.model.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListMonitorTraces',
        },
      ) as Promise<ListMonitorTracesResponse>;
    },
    GetMonitorTrace(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/monitor/traces/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetMonitorTrace',
        },
      ) as Promise<MonitorTraceDetail>;
    },
    GetMonitorLogs(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/logs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetMonitorLogs',
        },
      ) as Promise<GetMonitorLogsResponse>;
    },
    ListFlowLogs(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/flow-logs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.traceId) {
        queryParams.push(`traceId=${encodeURIComponent(request.traceId.toString())}`);
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`);
      }
      if (request.runId) {
        queryParams.push(`runId=${encodeURIComponent(request.runId.toString())}`);
      }
      if (request.severity) {
        queryParams.push(`severity=${encodeURIComponent(request.severity.toString())}`);
      }
      if (request.domain) {
        queryParams.push(`domain=${encodeURIComponent(request.domain.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.since) {
        queryParams.push(`since=${encodeURIComponent(request.since.toString())}`);
      }
      if (request.until) {
        queryParams.push(`until=${encodeURIComponent(request.until.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListFlowLogs',
        },
      ) as Promise<ListFlowLogsResponse>;
    },
    ListMonitorAlertRules(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/alert-rules`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListMonitorAlertRules',
        },
      ) as Promise<ListMonitorAlertRulesResponse>;
    },
    PutMonitorAlertRules(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/alert-rules`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'MonitorService',
          method: 'PutMonitorAlertRules',
        },
      ) as Promise<PutMonitorAlertRulesResponse>;
    },
    GetRunnerMetrics(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/runner-metrics`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.windowMinutes) {
        queryParams.push(`windowMinutes=${encodeURIComponent(request.windowMinutes.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetRunnerMetrics',
        },
      ) as Promise<RunnerMetricsSummary>;
    },
    GetCodeExecutorCapabilities(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/code-executor-capabilities`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetCodeExecutorCapabilities',
        },
      ) as Promise<GetCodeExecutorCapabilitiesResponse>;
    },
    GenerateDiagnosticBundle(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/diagnostic-bundle`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GenerateDiagnosticBundle',
        },
      ) as Promise<GenerateDiagnosticBundleResponse>;
    },
    DiagnoseAndHeal(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/diagnose-and-heal`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'MonitorService',
          method: 'DiagnoseAndHeal',
        },
      ) as Promise<DiagnoseAndHealResponse>;
    },
    TriggerSelfCheck(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/self-check`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'MonitorService',
          method: 'TriggerSelfCheck',
        },
      ) as Promise<TriggerSelfCheckResponse>;
    },
    ListSelfCheckReports(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/self-check-reports`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListSelfCheckReports',
        },
      ) as Promise<ListSelfCheckReportsResponse>;
    },
    GetHealStats(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/heal-stats`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'GetHealStats',
        },
      ) as Promise<HealStatsResponse>;
    },
    ListHealRecords(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/monitor/heal-records`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.ruleId) {
        queryParams.push(`ruleId=${encodeURIComponent(request.ruleId.toString())}`);
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`);
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'MonitorService',
          method: 'ListHealRecords',
        },
      ) as Promise<ListHealRecordsResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
