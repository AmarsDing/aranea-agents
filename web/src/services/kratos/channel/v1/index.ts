// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// Aligns with legacy PlatformResource JSON for resource "channels".
export type Channel = {
  id: string | undefined;
  resource: string | undefined;
  key: string | undefined;
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  parentId: string | undefined;
  level: string | undefined;
  agentId: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  deletedAt: string | undefined;
};

export type ChannelTypeItem = {
  type: string | undefined;
  label: string | undefined;
  description: string | undefined;
  group: string | undefined;
  receiveModes: string[] | undefined;
  icon: string | undefined;
  bundled: boolean | undefined;
  supportsTest: boolean | undefined;
  supportsWebhook: boolean | undefined;
  configSchemaJson: string | undefined;
  credentialSchemaJson: string | undefined;
  uiHintsJson: string | undefined;
  sortOrder: number | undefined;
};

export type ChannelCredential = {
  id: string | undefined;
  channelId: string | undefined;
  credentialKey: string | undefined;
  status: string | undefined;
  metadataJson: string | undefined;
  configured: boolean | undefined;
  maskedPreview: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ChannelCredentialInput = {
  //
  // Behaviors: REQUIRED
  credentialKey: string | undefined;
  secret: string | undefined;
  secretRef: string | undefined;
  status: string | undefined;
  metadataJson: string | undefined;
};

export type ChannelDelivery = {
  id: string | undefined;
  channelId: string | undefined;
  agentId: string | undefined;
  status: string | undefined;
  payloadJson: string | undefined;
  errorMessage: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ChannelTestResult = {
  ok: boolean | undefined;
  status: string | undefined;
  message: string | undefined;
  detailsJson: string | undefined;
};

export type ListChannelTypesResponse = {
  items: ChannelTypeItem[] | undefined;
};

export type ListChannelsResponse = {
  items: Channel[] | undefined;
};

export type GetChannelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type CreateChannelRequest = {
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  credentials: ChannelCredentialInput[] | undefined;
};

export type UpdateChannelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  key: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  description: string | undefined;
  status: string | undefined;
  enabled: boolean | undefined;
  sortOrder: number | undefined;
  configJson: string | undefined;
  metadataJson: string | undefined;
  credentials: ChannelCredentialInput[] | undefined;
};

export type DeleteChannelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ToggleChannelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  enabled: boolean | undefined;
};

export type TestChannelRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ListChannelCredentialsResponse = {
  items: ChannelCredential[] | undefined;
};

export type UpsertChannelCredentialsRequest = {
  //
  // Behaviors: REQUIRED
  channelId: string | undefined;
  credentials: ChannelCredentialInput[] | undefined;
};

export type DeleteChannelCredentialRequest = {
  //
  // Behaviors: REQUIRED
  channelId: string | undefined;
  //
  // Behaviors: REQUIRED
  credentialKey: string | undefined;
};

export type ListChannelDeliveriesRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  limit: number | undefined;
};

export type ListChannelDeliveriesResponse = {
  items: ChannelDelivery[] | undefined;
};

export type ChannelTurnJob = {
  id: string | undefined;
  channelId: string | undefined;
  sessionId: string | undefined;
  peerId: string | undefined;
  peerKey: string | undefined;
  idempotencyKey: string | undefined;
  status: string | undefined;
  previewMessageId: string | undefined;
  contentPreview: string | undefined;
  asyncTargetType: string | undefined;
  asyncTargetId: string | undefined;
  errorMessage: string | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListChannelTurnJobsRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  limit: number | undefined;
};

export type ListChannelTurnJobsResponse = {
  items: ChannelTurnJob[] | undefined;
};

export interface ChannelService {
  ListChannelTypes(request: wellKnownEmpty): Promise<ListChannelTypesResponse>;
  ListChannels(request: wellKnownEmpty): Promise<ListChannelsResponse>;
  GetChannel(request: GetChannelRequest): Promise<Channel>;
  CreateChannel(request: CreateChannelRequest): Promise<Channel>;
  UpdateChannel(request: UpdateChannelRequest): Promise<Channel>;
  DeleteChannel(request: DeleteChannelRequest): Promise<wellKnownEmpty>;
  ToggleChannel(request: ToggleChannelRequest): Promise<Channel>;
  TestChannel(request: TestChannelRequest): Promise<ChannelTestResult>;
  ListChannelCredentials(request: GetChannelRequest): Promise<ListChannelCredentialsResponse>;
  UpsertChannelCredentials(request: UpsertChannelCredentialsRequest): Promise<ListChannelCredentialsResponse>;
  DeleteChannelCredential(request: DeleteChannelCredentialRequest): Promise<wellKnownEmpty>;
  ListChannelDeliveries(request: ListChannelDeliveriesRequest): Promise<ListChannelDeliveriesResponse>;
  ListChannelTurnJobs(request: ListChannelTurnJobsRequest): Promise<ListChannelTurnJobsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createChannelServiceClient(handler: RequestHandler): ChannelService {
  return {
    ListChannelTypes(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/channels/catalog`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ListChannelTypes',
        },
      ) as Promise<ListChannelTypesResponse>;
    },
    ListChannels(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/channels`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ListChannels',
        },
      ) as Promise<ListChannelsResponse>;
    },
    GetChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'GetChannel',
        },
      ) as Promise<Channel>;
    },
    CreateChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/channels`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ChannelService',
          method: 'CreateChannel',
        },
      ) as Promise<Channel>;
    },
    UpdateChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PATCH',
          body,
        },
        {
          service: 'ChannelService',
          method: 'UpdateChannel',
        },
      ) as Promise<Channel>;
    },
    DeleteChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'ChannelService',
          method: 'DeleteChannel',
        },
      ) as Promise<wellKnownEmpty>;
    },
    ToggleChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}/toggle`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ToggleChannel',
        },
      ) as Promise<Channel>;
    },
    TestChannel(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}/test`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ChannelService',
          method: 'TestChannel',
        },
      ) as Promise<ChannelTestResult>;
    },
    ListChannelCredentials(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}/credentials`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ListChannelCredentials',
        },
      ) as Promise<ListChannelCredentialsResponse>;
    },
    UpsertChannelCredentials(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.channelId) {
        throw new Error('missing required field request.channel_id');
      }
      const path = `v1/channels/${request.channelId}/credentials`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'PUT',
          body,
        },
        {
          service: 'ChannelService',
          method: 'UpsertChannelCredentials',
        },
      ) as Promise<ListChannelCredentialsResponse>;
    },
    DeleteChannelCredential(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.channelId) {
        throw new Error('missing required field request.channel_id');
      }
      if (!request.credentialKey) {
        throw new Error('missing required field request.credential_key');
      }
      const path = `v1/channels/${request.channelId}/credentials/${request.credentialKey}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'ChannelService',
          method: 'DeleteChannelCredential',
        },
      ) as Promise<wellKnownEmpty>;
    },
    ListChannelDeliveries(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}/deliveries`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ListChannelDeliveries',
        },
      ) as Promise<ListChannelDeliveriesResponse>;
    },
    ListChannelTurnJobs(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/channels/${request.id}/turn-jobs`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ChannelService',
          method: 'ListChannelTurnJobs',
        },
      ) as Promise<ListChannelTurnJobsResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
