// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

// ArtifactMeta holds metadata for a stored artifact (no binary payload).
export type ArtifactMeta = {
  id: string | undefined;
  sessionId: string | undefined;
  name: string | undefined;
  mimeType: string | undefined;
  size: number | undefined;
  sha256: string | undefined;
  storageKind: string | undefined;
  storageUri: string | undefined;
  version: number | undefined;
  createdAt: string | undefined;
};

// ArtifactData bundles metadata with the raw binary encoded as base64.
export type ArtifactData = {
  meta: ArtifactMeta | undefined;
  // data_base64 is the artifact payload encoded in standard base64.
  dataBase64: string | undefined;
};

export type UploadArtifactRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  name: string | undefined;
  mimeType: string | undefined;
  // data_base64 is the artifact payload encoded in standard base64.
  //
  // Behaviors: REQUIRED
  dataBase64: string | undefined;
};

export type GetArtifactRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  // version selects a specific version; 0 or omitted means latest.
  version: number | undefined;
};

export type ListArtifactsRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // query filters by name, session_id, or mime_type (case-insensitive substring).
  query: string | undefined;
  // mime_type_prefix filters by MIME prefix, e.g. "image/" or "text/".
  mimeTypePrefix: string | undefined;
};

export type ListArtifactsResponse = {
  items: ArtifactMeta[] | undefined;
  total: number | undefined;
};

export type DeleteArtifactRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

// DeleteArtifactVersionRequest removes exactly one version of a logical artifact.
// Use the ID of any version as the lookup handle, and specify the version number
// to delete. If it is the only remaining version the logical artifact is gone.
export type DeleteArtifactVersionRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  //
  // Behaviors: REQUIRED
  version: number | undefined;
};

export type PreviewArtifactRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  version: number | undefined;
};

export type PreviewArtifactResponse = {
  meta: ArtifactMeta | undefined;
  // preview_kind: text | image | pdf | binary
  previewKind: string | undefined;
  // text_content for text previews; empty for binary kinds.
  textContent: string | undefined;
  // data_base64 for image/pdf inline preview.
  dataBase64: string | undefined;
};

export type SignDownloadUrlRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  version: number | undefined;
  // ttl_seconds overrides default expiry (max 86400).
  ttlSeconds: number | undefined;
};

export type SignDownloadUrlResponse = {
  url: string | undefined;
  expiresAt: string | undefined;
};

export type ListArtifactVersionsRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type ListArtifactVersionsResponse = {
  items: ArtifactMeta[] | undefined;
};

export interface ArtifactService {
  UploadArtifact(request: UploadArtifactRequest): Promise<ArtifactMeta>;
  GetArtifact(request: GetArtifactRequest): Promise<ArtifactData>;
  ListArtifacts(request: ListArtifactsRequest): Promise<ListArtifactsResponse>;
  DeleteArtifact(request: DeleteArtifactRequest): Promise<wellKnownEmpty>;
  // DeleteArtifactVersion removes a single version of a logical artifact.
  // DELETE /v1/artifacts/{id}/versions/{version}
  DeleteArtifactVersion(request: DeleteArtifactVersionRequest): Promise<wellKnownEmpty>;
  PreviewArtifact(request: PreviewArtifactRequest): Promise<PreviewArtifactResponse>;
  SignDownloadUrl(request: SignDownloadUrlRequest): Promise<SignDownloadUrlResponse>;
  ListArtifactVersions(request: ListArtifactVersionsRequest): Promise<ListArtifactVersionsResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string; method: string }) => Promise<unknown>;

export function createArtifactServiceClient(handler: RequestHandler): ArtifactService {
  return {
    UploadArtifact(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/artifacts`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'UploadArtifact',
        },
      ) as Promise<ArtifactMeta>;
    },
    GetArtifact(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/artifacts/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.version) {
        queryParams.push(`version=${encodeURIComponent(request.version.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'GetArtifact',
        },
      ) as Promise<ArtifactData>;
    },
    ListArtifacts(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/artifacts`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`);
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`);
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`);
      }
      if (request.query) {
        queryParams.push(`query=${encodeURIComponent(request.query.toString())}`);
      }
      if (request.mimeTypePrefix) {
        queryParams.push(`mimeTypePrefix=${encodeURIComponent(request.mimeTypePrefix.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'ListArtifacts',
        },
      ) as Promise<ListArtifactsResponse>;
    },
    DeleteArtifact(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/artifacts/${request.id}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'DeleteArtifact',
        },
      ) as Promise<wellKnownEmpty>;
    },
    DeleteArtifactVersion(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      if (!request.version) {
        throw new Error('missing required field request.version');
      }
      const path = `v1/artifacts/${request.id}/versions/${request.version}`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'DELETE',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'DeleteArtifactVersion',
        },
      ) as Promise<wellKnownEmpty>;
    },
    PreviewArtifact(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/artifacts/${request.id}/preview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.version) {
        queryParams.push(`version=${encodeURIComponent(request.version.toString())}`);
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'PreviewArtifact',
        },
      ) as Promise<PreviewArtifactResponse>;
    },
    SignDownloadUrl(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/artifacts/${request.id}/sign-download`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'POST',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'SignDownloadUrl',
        },
      ) as Promise<SignDownloadUrlResponse>;
    },
    ListArtifactVersions(request) {
      // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error('missing required field request.id');
      }
      const path = `v1/artifacts/${request.id}/versions`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join('&')}`;
      }
      return handler(
        {
          path: uri,
          method: 'GET',
          body,
        },
        {
          service: 'ArtifactService',
          method: 'ListArtifactVersions',
        },
      ) as Promise<ListArtifactVersionsResponse>;
    },
  };
}
// An empty JSON object
type wellKnownEmpty = Record<never, never>;

// @@protoc_insertion_point(typescript-http-eof)
