// Code generated by protoc-gen-typescript-http. DO NOT EDIT.
/* eslint-disable camelcase */
// @ts-nocheck

export type ListL0SnapshotsRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  limit: number | undefined;
  agentId: string | undefined;
};

export type ListL0SnapshotsResponse = {
  items: L0AssemblySnapshot[] | undefined;
};

export type L0AssemblySnapshot = {
  id: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  turnId: string | undefined;
  spanId: string | undefined;
  agentId: string | undefined;
  teamId: string | undefined;
  provider: string | undefined;
  model: string | undefined;
  contextWindowTokens: number | undefined;
  budgetTokens: number | undefined;
  recentWindowTurns: number | undefined;
  recentWindowTokens: number | undefined;
  summaryTokenEstimate: number | undefined;
  l1FieldCount: number | undefined;
  l1TokenEstimate: number | undefined;
  l3ChunkCount: number | undefined;
  l3TokenEstimate: number | undefined;
  l4PathCount: number | undefined;
  l4TokenEstimate: number | undefined;
  promptTokenEstimate: number | undefined;
  promptTokenActual: number | undefined;
  usedRatio: number | undefined;
  truncateStrategy: string | undefined;
  truncatedMessageCount: number | undefined;
  summarizedTurnFrom: number | undefined;
  summarizedTurnTo: number | undefined;
  segmentsJson: string | undefined;
  warningCodesJson: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
};

export type ListL1TasksRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  agentId: string | undefined;
  status: string | undefined;
  includeEnded: string | undefined;
};

export type ListL1TasksResponse = {
  items: L1Task[] | undefined;
};

export type L1Task = {
  id: string | undefined;
  sessionId: string | undefined;
  runId: string | undefined;
  teamId: string | undefined;
  agentId: string | undefined;
  taskKey: string | undefined;
  taskTitle: string | undefined;
  taskGoal: string | undefined;
  status: string | undefined;
  schemaVersion: number | undefined;
  budgetTokens: number | undefined;
  usedTokens: number | undefined;
  parentTaskId: string | undefined;
  sharedWithJson: string | undefined;
  startedAt: string | undefined;
  endedAt: string | undefined;
  archivedAt: string | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListL1FieldsRequest = {
  //
  // Behaviors: REQUIRED
  sessionId: string | undefined;
  //
  // Behaviors: REQUIRED
  taskId: string | undefined;
  includeInternal: string | undefined;
  agentId: string | undefined;
};

export type ListL1FieldsResponse = {
  items: L1Field[] | undefined;
};

export type L1Field = {
  id: string | undefined;
  taskId: string | undefined;
  sessionId: string | undefined;
  agentId: string | undefined;
  fieldPath: string | undefined;
  fieldKind: string | undefined;
  visibility: string | undefined;
  pinToPrompt: boolean | undefined;
  isRequired: boolean | undefined;
  valueText: string | undefined;
  valueJson: string | undefined;
  valueRef: string | undefined;
  preview: string | undefined;
  tokenEstimate: number | undefined;
  source: string | undefined;
  sourceRef: string | undefined;
  ttlSeconds: number | undefined;
  expiresAt: string | undefined;
  revision: number | undefined;
  lastReadAt: string | undefined;
  readCount: number | undefined;
  metadataJson: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListMemoryFactsRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  kind: string | undefined;
  status: string | undefined;
  keyword: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // optional: filter by ORIGINATING agent (memory_facts.agent_id) across all
  // scopes — AND-combined with the scope namespace filters. Used by the memory
  // center so the L3 browse tab matches the panorama card count (which counts
  // facts an agent produced into session/user scopes as well).
  agentId: string | undefined;
};

export type ListMemoryFactsResponse = {
  items: MemoryFact[] | undefined;
  total: number | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // active_count / archived_count are status-based counts over the FULL filter
  // set (scope/kind/keyword/agent) ignoring the status filter itself — used by
  // the memory-center knowledge panel statistics row.
  activeCount: number | undefined;
  archivedCount: number | undefined;
  // filtered_count matches the returned items' caliber (same WHERE including
  // the status filter, default 'active') — the server-side pagination total.
  filteredCount: number | undefined;
};

export type MemoryFact = {
  id: string | undefined;
  scopeType: string | undefined;
  scopeId: string | undefined;
  workspaceId: string | undefined;
  userId: string | undefined;
  teamId: string | undefined;
  agentId: string | undefined;
  statement: string | undefined;
  detailsMarkdown: string | undefined;
  factKind: string | undefined;
  tagsJson: string | undefined;
  confidence: number | undefined;
  importance: number | undefined;
  useCount: number | undefined;
  hitCount: number | undefined;
  positiveFeedbackCount: number | undefined;
  negativeFeedbackCount: number | undefined;
  conflictCount: number | undefined;
  sourceKind: string | undefined;
  sourceEpisodeId: string | undefined;
  sourceSessionId: string | undefined;
  sourceMessageId: string | undefined;
  version: number | undefined;
  status: string | undefined;
  piiFlag: boolean | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
  qualityScore: number | undefined;
  piiTypesJson: string | undefined;
  // FR-12.6 three-stage counters. use_count (14) is legacy and no longer
  // maintained; these carry the precise semantics:
  // recalled_count  — entered a recall result set
  // injected_count  — actually written into the LLM prompt
  // cited_count     — explicitly referenced by the assistant reply
  recalledCount: number | undefined;
  injectedCount: number | undefined;
  citedCount: number | undefined;
};

export type ListConflictingFactsRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
  // agent_id filters conflicts by the ORIGINATING agent across ALL scopes
  // (session/user/agent). H2: facts live in session/user scopes (F1), so a
  // scope-only query undercounts. When set, scope_type may be left empty.
  agentId: string | undefined;
};

export type ListConflictingFactsResponse = {
  items: MemoryFact[] | undefined;
  total: number | undefined;
};

export type ListMemoryEntitiesRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  workspaceId: string | undefined;
  userId: string | undefined;
  entityType: string | undefined;
  status: string | undefined;
  keyword: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListMemoryEntitiesResponse = {
  items: MemoryEntity[] | undefined;
  total: number | undefined;
};

export type MemoryEntity = {
  id: string | undefined;
  scopeType: string | undefined;
  scopeId: string | undefined;
  workspaceId: string | undefined;
  userId: string | undefined;
  entityType: string | undefined;
  name: string | undefined;
  nameNormalized: string | undefined;
  aliases: string[] | undefined;
  description: string | undefined;
  importance: number | undefined;
  confidence: number | undefined;
  useCount: number | undefined;
  sourceKind: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type MemoryRelation = {
  id: string | undefined;
  sourceId: string | undefined;
  targetId: string | undefined;
  relationType: string | undefined;
  weight: number | undefined;
  confidence: number | undefined;
  status: string | undefined;
  validFrom: string | undefined;
  validTo: string | undefined;
};

export type GetMemoryNeighborhoodRequest = {
  //
  // Behaviors: REQUIRED
  centerId: string | undefined;
  hops: number | undefined;
  maxNodes: number | undefined;
  queryAt: string | undefined;
};

export type GraphNeighborhood = {
  center: MemoryEntity | undefined;
  hops: number | undefined;
  entities: MemoryEntity[] | undefined;
  relations: MemoryRelation[] | undefined;
  queryAt: string | undefined;
};

export type SpreadingActivationRequest = {
  //
  // Behaviors: REQUIRED
  centerId: string | undefined;
  hops: number | undefined;
  topK: number | undefined;
};

export type ActivationPathStep = {
  fromNodeId: string | undefined;
  toNodeId: string | undefined;
  edgeWeight: number | undefined;
  relationType: string | undefined;
};

export type ActivationResult = {
  nodeId: string | undefined;
  activation: number | undefined;
  hopCount: number | undefined;
  activationPath: ActivationPathStep[] | undefined;
};

export type SpreadingActivationResponse = {
  centerId: string | undefined;
  hops: number | undefined;
  topK: number | undefined;
  items: ActivationResult[] | undefined;
};

export type GetAgentIdentityRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type AgentIdentity = {
  agentId: string | undefined;
  persona: string | undefined;
  values: string[] | undefined;
  tone: string | undefined;
  domains: string[] | undefined;
  userExpectations: string | undefined;
  currentPhase: string | undefined;
  version: number | undefined;
};

export type GetAgentStrategyRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
};

export type AgentStrategyProfile = {
  agentId: string | undefined;
  exploration: number | undefined;
  conciseness: number | undefined;
  caution: number | undefined;
  delegation: number | undefined;
  toolPreference: { [key: string]: number } | undefined;
  toolBlacklist: string[] | undefined;
  providerPreference: { [key: string]: number } | undefined;
  modelPreference: { [key: string]: number } | undefined;
  version: number | undefined;
};

export type ListEvolutionProposalsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  status: string | undefined;
  limit: number | undefined;
};

export type ListEvolutionProposalsResponse = {
  items: EvolutionProposal[] | undefined;
};

export type EvolutionProposal = {
  id: string | undefined;
  agentId: string | undefined;
  proposalKind: string | undefined;
  kind: string | undefined;
  targetField: string | undefined;
  rationale: string | undefined;
  expectedImpact: string | undefined;
  riskLevel: string | undefined;
  status: string | undefined;
  createdAt: string | undefined;
};

export type ListEvolutionEventsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  limit: number | undefined;
};

export type ListEvolutionEventsResponse = {
  items: EvolutionEvent[] | undefined;
};

export type EvolutionEvent = {
  id: string | undefined;
  agentId: string | undefined;
  eventKind: string | undefined;
  kind: string | undefined;
  targetField: string | undefined;
  reason: string | undefined;
  reverted: boolean | undefined;
  createdAt: string | undefined;
};

export type GetEvolutionMetricsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  range: string | undefined;
};

export type AgentSkillStat = {
  agentId: string | undefined;
  toolKey: string | undefined;
  invocations: number | undefined;
  successes: number | undefined;
  failures: number | undefined;
  preferenceScore: number | undefined;
  lastUsedAt: string | undefined;
};

export type EvolutionMetricsReport = {
  eventsTotal: number | undefined;
  eventsReverted: number | undefined;
  proposalsTotal: number | undefined;
  proposalsByStatus: { [key: string]: number } | undefined;
  skillStats: AgentSkillStat[] | undefined;
};

// UpsertMemoryFactRequest persists or updates one L3 fact (dedupe via scope + fingerprint of statement).
export type UpsertMemoryFactRequest = {
  //
  // Behaviors: REQUIRED
  fact: MemoryFact | undefined;
};

export type UpsertMemoryFactResponse = {
  fact: MemoryFact | undefined;
};

// ReviewMemoryFact applies a single-fact user governance action with a precise
// column-targeted UPDATE (memory.md §9.4). Unlike UpsertMemoryFact it never
// touches links/keywords/metadata/quality_score, so feedback actions cannot
// silently wipe graph linkages.
export type ReviewMemoryFactRequest = {
  //
  // Behaviors: REQUIRED
  factId: string | undefined;
  // confirm | reject | archive | dispute | deprecate | refine
  //
  // Behaviors: REQUIRED
  action: string | undefined;
  // refine-only replacement fields. statement is required for refine; the
  // remaining fields overwrite unconditionally (callers merge current values).
  statement: string | undefined;
  detailsMarkdown: string | undefined;
  factKind: string | undefined;
  tagsJson: string | undefined;
};

export type ReviewMemoryFactResponse = {
  fact: MemoryFact | undefined;
};

// AppendEvolutionEventRequest registers a persisted evolution timeline row (SQLite agent_evolution_events).
export type AppendEvolutionEventRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  workspaceId: string | undefined;
  // event_kind identifies the lineage record (preferred over kind).
  //
  // Behaviors: REQUIRED
  eventKind: string | undefined;
  // kind mirrors legacy list JSON when different from event_kind; optional duplicate of event_kind.
  kind: string | undefined;
  targetField: string | undefined;
  reason: string | undefined;
  //
  // Behaviors: REQUIRED
  triggerKind: string | undefined;
  triggerSource: string | undefined;
  metadataJson: string | undefined;
};

export type AppendEvolutionEventResponse = {
  event: EvolutionEvent | undefined;
};

export type CascadeAffectedEntity = {
  entityId: string | undefined;
  entityName: string | undefined;
  entityType: string | undefined;
  relationType: string | undefined;
  hops: number | undefined;
};

export type CascadeProposal = {
  id: string | undefined;
  agentId: string | undefined;
  workspaceId: string | undefined;
  triggerEntityId: string | undefined;
  triggerEntityName: string | undefined;
  triggerAttribute: string | undefined;
  oldValue: string | undefined;
  newValue: string | undefined;
  affectedEntities: CascadeAffectedEntity[] | undefined;
  status: string | undefined;
  riskLevel: string | undefined;
  rationale: string | undefined;
  reviewedBy: string | undefined;
  reviewedAt: string | undefined;
  expiresAt: string | undefined;
  createdAt: string | undefined;
  updatedAt: string | undefined;
};

export type ListCascadeProposalsRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  status: string | undefined;
  limit: number | undefined;
};

export type ListCascadeProposalsResponse = {
  items: CascadeProposal[] | undefined;
};

export type ApproveCascadeProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewer: string | undefined;
};

export type ApproveCascadeProposalResponse = {
  proposal: CascadeProposal | undefined;
};

export type RejectCascadeProposalRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewer: string | undefined;
  reason: string | undefined;
};

export type RejectCascadeProposalResponse = {
  proposal: CascadeProposal | undefined;
};

export type CascadeFactDiff = {
  factId: string | undefined;
  beforeStatement: string | undefined;
  afterStatement: string | undefined;
  scope: string | undefined;
};

export type CascadeEntityRename = {
  entityId: string | undefined;
  entityType: string | undefined;
  oldName: string | undefined;
  newName: string | undefined;
};

export type CascadePreview = {
  affectedEntitiesCount: number | undefined;
  affectedFactsCount: number | undefined;
  factDiffs: CascadeFactDiff[] | undefined;
  entityRenames: CascadeEntityRename[] | undefined;
};

export type PreviewCascadeApproveRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
};

export type PreviewCascadeApproveResponse = {
  preview: CascadePreview | undefined;
};

export type CascadeSagaStep = {
  id: string | undefined;
  proposalId: string | undefined;
  stepIndex: number | undefined;
  stepName: string | undefined;
  state: string | undefined;
  isCritical: boolean | undefined;
  attempts: number | undefined;
  startedAt: string | undefined;
  finishedAt: string | undefined;
  payloadJson: string | undefined;
  resultJson: string | undefined;
  error: string | undefined;
};

export type GetCascadeSagaStepsRequest = {
  //
  // Behaviors: REQUIRED
  proposalId: string | undefined;
};

export type GetCascadeSagaStepsResponse = {
  steps: CascadeSagaStep[] | undefined;
};

export type RetryCascadeApproveRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewer: string | undefined;
};

export type RetryCascadeApproveResponse = {
  proposal: CascadeProposal | undefined;
};

export type CompensateCascadeApproveRequest = {
  //
  // Behaviors: REQUIRED
  id: string | undefined;
  reviewer: string | undefined;
};

export type CompensateCascadeApproveResponse = {
  proposal: CascadeProposal | undefined;
};

export type MemoryRecallScoreBreakdown = {
  keyword: number | undefined;
  vector: number | undefined;
  importance: number | undefined;
  recency: number | undefined;
  crossEncoder: number | undefined;
  total: number | undefined;
  qualityScore: number | undefined;
};

export type MemoryRecallHit = {
  layer: string | undefined;
  id: string | undefined;
  title: string | undefined;
  summary: string | undefined;
  statement: string | undefined;
  scores: MemoryRecallScoreBreakdown | undefined;
};

export type DebugMemoryRecallRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  sessionId: string | undefined;
  userId: string | undefined;
  query: string | undefined;
  l2Limit: number | undefined;
  l3Limit: number | undefined;
};

export type DebugMemoryRecallResponse = {
  l2Hits: MemoryRecallHit[] | undefined;
  l3Hits: MemoryRecallHit[] | undefined;
};

export type CompositeSearchHit = {
  layer: string | undefined;
  id: string | undefined;
  text: string | undefined;
  score: number | undefined;
};

export type CompositeSearchMemoriesRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  sessionId: string | undefined;
  userId: string | undefined;
  //
  // Behaviors: REQUIRED
  query: string | undefined;
  limit: number | undefined;
};

export type CompositeSearchMemoriesResponse = {
  items: CompositeSearchHit[] | undefined;
};

export type GetMemoryWorkerStatusRequest = {
};

export type MemoryWorkerStatus = {
  jobsDone: number | undefined;
  jobsDead: number | undefined;
  llmFallbackTotal: number | undefined;
  avgExtractionSeconds: number | undefined;
  episodeBackfillTotal: number | undefined;
  // MEM-OPT-01: index health
  factIndexStaleCount: number | undefined;
  factIndexDisabledCount: number | undefined;
  queueHigh: MemoryWorkerStatus_QueueStats | undefined;
  queueNormal: MemoryWorkerStatus_QueueStats | undefined;
  queueLow: MemoryWorkerStatus_QueueStats | undefined;
  oldestPendingAgeMs: number | undefined;
  // DB health: false when underlying store queries fail (counts may be stale/zero).
  dbAvailable: boolean | undefined;
  // Global dead-letter pending count (not queue-specific).
  deadLetterPending: number | undefined;
};

// MEM-OPT-03: queue stats
export type MemoryWorkerStatus_QueueStats = {
  capacity: number | undefined;
  inFlight: number | undefined;
  droppedTotal: number | undefined;
  debouncedTotal: number | undefined;
};

export type MemoryPlatformSettings = {
  policyStrict: boolean | undefined;
  episodeBackfillDisabled: boolean | undefined;
  // Read-only: env MEMORY_POLICY_STRICT overrides DB when true.
  envPolicyStrictOverride: boolean | undefined;
  // Read-only: env MEMORY_EPISODE_BACKFILL_DISABLED overrides DB when true.
  envEpisodeBackfillDisabledOverride: boolean | undefined;
};

export type GetMemoryPlatformSettingsRequest = {
};

export type UpdateMemoryPlatformSettingsRequest = {
  policyStrict: boolean | undefined;
  episodeBackfillDisabled: boolean | undefined;
};

export type MemoryDeadLetterEntry = {
  id: number | undefined;
  enqueuedAt: string | undefined;
  failedAt: string | undefined;
  sessionId: string | undefined;
  appName: string | undefined;
  dropReason: string | undefined;
  priority: number | undefined;
  attempts: number | undefined;
  state: string | undefined;
  lastError: string | undefined;
};

export type ListMemoryDeadLettersRequest = {
  state: string | undefined;
  limit: number | undefined;
};

export type ListMemoryDeadLettersResponse = {
  items: MemoryDeadLetterEntry[] | undefined;
};

export type ReplayMemoryDeadLetterRequest = {
  //
  // Behaviors: REQUIRED
  id: number | undefined;
};

export type ReplayMemoryDeadLetterResponse = {
  entry: MemoryDeadLetterEntry | undefined;
};

export type AbandonMemoryDeadLetterRequest = {
  //
  // Behaviors: REQUIRED
  id: number | undefined;
  reason: string | undefined;
};

export type AbandonMemoryDeadLetterResponse = {
  entry: MemoryDeadLetterEntry | undefined;
};

export type ListPIIFlaggedFactsRequest = {
  scopeType: string | undefined;
  scopeId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type ListPIIFlaggedFactsResponse = {
  items: MemoryFact[] | undefined;
  total: number | undefined;
};

export type ReviewPIIFactRequest = {
  //
  // Behaviors: REQUIRED
  factId: string | undefined;
  //
  // Behaviors: REQUIRED
  action: string | undefined;
};

export type ReviewPIIFactResponse = {
  fact: MemoryFact | undefined;
};

export type GetMemoryLayerOverviewRequest = {
  agentId: string | undefined;
  sessionId: string | undefined;
};

export type MemoryLayerStat = {
  layer: string | undefined;
  itemCount: number | undefined;
  todayAdded: number | undefined;
  recallHits: number | undefined;
  health: string | undefined;
  headlineJson: string | undefined;
};

export type MemoryActionItem = {
  kind: string | undefined;
  count: number | undefined;
  targetTab: string | undefined;
};

export type MemoryActivityItem = {
  ts: string | undefined;
  kind: string | undefined;
  layerFrom: string | undefined;
  layerTo: string | undefined;
  summary: string | undefined;
};

export type GetMemoryLayerOverviewResponse = {
  layers: MemoryLayerStat[] | undefined;
  actionItems: MemoryActionItem[] | undefined;
  activityFeed: MemoryActivityItem[] | undefined;
};

export type GetUnifiedMemoryGraphRequest = {
  agentId: string | undefined;
  focus: string | undefined;
  hops: number | undefined;
  minWeight: number | undefined;
  layers: string[] | undefined;
};

export type UnifiedGraphNode = {
  id: string | undefined;
  layer: string | undefined;
  kind: string | undefined;
  label: string | undefined;
  weight: number | undefined;
  metaJson: string | undefined;
};

export type UnifiedGraphEdge = {
  source: string | undefined;
  target: string | undefined;
  type: string | undefined;
  label: string | undefined;
  weight: number | undefined;
  polarity: string | undefined;
};

export type GetUnifiedMemoryGraphResponse = {
  focus: string | undefined;
  nodes: UnifiedGraphNode[] | undefined;
  edges: UnifiedGraphEdge[] | undefined;
  nodeCount: number | undefined;
  edgeCount: number | undefined;
  filteredEdgeCount: number | undefined;
  emptyReason: string | undefined;
};

export type ListMemoryEpisodesRequest = {
  //
  // Behaviors: REQUIRED
  agentId: string | undefined;
  sessionId: string | undefined;
  limit: number | undefined;
  offset: number | undefined;
};

export type MemoryEpisode = {
  id: string | undefined;
  sessionId: string | undefined;
  agentId: string | undefined;
  episodeKind: string | undefined;
  title: string | undefined;
  outcomeSummary: string | undefined;
  importance: number | undefined;
  consolidationStatus: string | undefined;
  consolidatedL3Count: number | undefined;
  endedAt: string | undefined;
  createdAt: string | undefined;
};

export type ListMemoryEpisodesResponse = {
  items: MemoryEpisode[] | undefined;
  total: number | undefined;
};

export interface MemoryService {
  ListL0Snapshots(request: ListL0SnapshotsRequest): Promise<ListL0SnapshotsResponse>;
  ListL1Tasks(request: ListL1TasksRequest): Promise<ListL1TasksResponse>;
  ListL1Fields(request: ListL1FieldsRequest): Promise<ListL1FieldsResponse>;
  ListMemoryFacts(request: ListMemoryFactsRequest): Promise<ListMemoryFactsResponse>;
  ListConflictingFacts(request: ListConflictingFactsRequest): Promise<ListConflictingFactsResponse>;
  ListMemoryEntities(request: ListMemoryEntitiesRequest): Promise<ListMemoryEntitiesResponse>;
  GetMemoryNeighborhood(request: GetMemoryNeighborhoodRequest): Promise<GraphNeighborhood>;
  SpreadingActivation(request: SpreadingActivationRequest): Promise<SpreadingActivationResponse>;
  GetAgentIdentity(request: GetAgentIdentityRequest): Promise<AgentIdentity>;
  GetAgentStrategy(request: GetAgentStrategyRequest): Promise<AgentStrategyProfile>;
  ListEvolutionProposals(request: ListEvolutionProposalsRequest): Promise<ListEvolutionProposalsResponse>;
  ListEvolutionEvents(request: ListEvolutionEventsRequest): Promise<ListEvolutionEventsResponse>;
  GetEvolutionMetrics(request: GetEvolutionMetricsRequest): Promise<EvolutionMetricsReport>;
  UpsertMemoryFact(request: UpsertMemoryFactRequest): Promise<UpsertMemoryFactResponse>;
  ReviewMemoryFact(request: ReviewMemoryFactRequest): Promise<ReviewMemoryFactResponse>;
  AppendEvolutionEvent(request: AppendEvolutionEventRequest): Promise<AppendEvolutionEventResponse>;
  ListCascadeProposals(request: ListCascadeProposalsRequest): Promise<ListCascadeProposalsResponse>;
  ApproveCascadeProposal(request: ApproveCascadeProposalRequest): Promise<ApproveCascadeProposalResponse>;
  RejectCascadeProposal(request: RejectCascadeProposalRequest): Promise<RejectCascadeProposalResponse>;
  PreviewCascadeApprove(request: PreviewCascadeApproveRequest): Promise<PreviewCascadeApproveResponse>;
  GetCascadeSagaSteps(request: GetCascadeSagaStepsRequest): Promise<GetCascadeSagaStepsResponse>;
  RetryCascadeApprove(request: RetryCascadeApproveRequest): Promise<RetryCascadeApproveResponse>;
  CompensateCascadeApprove(request: CompensateCascadeApproveRequest): Promise<CompensateCascadeApproveResponse>;
  DebugMemoryRecall(request: DebugMemoryRecallRequest): Promise<DebugMemoryRecallResponse>;
  CompositeSearchMemories(request: CompositeSearchMemoriesRequest): Promise<CompositeSearchMemoriesResponse>;
  GetMemoryWorkerStatus(request: GetMemoryWorkerStatusRequest): Promise<MemoryWorkerStatus>;
  GetMemoryPlatformSettings(request: GetMemoryPlatformSettingsRequest): Promise<MemoryPlatformSettings>;
  UpdateMemoryPlatformSettings(request: UpdateMemoryPlatformSettingsRequest): Promise<MemoryPlatformSettings>;
  ListMemoryDeadLetters(request: ListMemoryDeadLettersRequest): Promise<ListMemoryDeadLettersResponse>;
  ReplayMemoryDeadLetter(request: ReplayMemoryDeadLetterRequest): Promise<ReplayMemoryDeadLetterResponse>;
  AbandonMemoryDeadLetter(request: AbandonMemoryDeadLetterRequest): Promise<AbandonMemoryDeadLetterResponse>;
  ListPIIFlaggedFacts(request: ListPIIFlaggedFactsRequest): Promise<ListPIIFlaggedFactsResponse>;
  ReviewPIIFact(request: ReviewPIIFactRequest): Promise<ReviewPIIFactResponse>;
  GetMemoryLayerOverview(request: GetMemoryLayerOverviewRequest): Promise<GetMemoryLayerOverviewResponse>;
  GetUnifiedMemoryGraph(request: GetUnifiedMemoryGraphRequest): Promise<GetUnifiedMemoryGraphResponse>;
  ListMemoryEpisodes(request: ListMemoryEpisodesRequest): Promise<ListMemoryEpisodesResponse>;
}

type RequestType = {
  path: string;
  method: string;
  body: string | null;
};

type RequestHandler = (request: RequestType, meta: { service: string, method: string }) => Promise<unknown>;

export function createMemoryServiceClient(
  handler: RequestHandler
): MemoryService {
  return {
    ListL0Snapshots(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/sessions/${request.sessionId}/l0/snapshots`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListL0Snapshots",
      }) as Promise<ListL0SnapshotsResponse>;
    },
    ListL1Tasks(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      const path = `v1/sessions/${request.sessionId}/l1/tasks`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.includeEnded) {
        queryParams.push(`includeEnded=${encodeURIComponent(request.includeEnded.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListL1Tasks",
      }) as Promise<ListL1TasksResponse>;
    },
    ListL1Fields(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.sessionId) {
        throw new Error("missing required field request.session_id");
      }
      if (!request.taskId) {
        throw new Error("missing required field request.task_id");
      }
      const path = `v1/sessions/${request.sessionId}/l1/tasks/${request.taskId}/fields`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.includeInternal) {
        queryParams.push(`includeInternal=${encodeURIComponent(request.includeInternal.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListL1Fields",
      }) as Promise<ListL1FieldsResponse>;
    },
    ListMemoryFacts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l3/facts`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.scopeType) {
        queryParams.push(`scopeType=${encodeURIComponent(request.scopeType.toString())}`)
      }
      if (request.scopeId) {
        queryParams.push(`scopeId=${encodeURIComponent(request.scopeId.toString())}`)
      }
      if (request.kind) {
        queryParams.push(`kind=${encodeURIComponent(request.kind.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.keyword) {
        queryParams.push(`keyword=${encodeURIComponent(request.keyword.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListMemoryFacts",
      }) as Promise<ListMemoryFactsResponse>;
    },
    ListConflictingFacts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l3/facts/conflicts`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.scopeType) {
        queryParams.push(`scopeType=${encodeURIComponent(request.scopeType.toString())}`)
      }
      if (request.scopeId) {
        queryParams.push(`scopeId=${encodeURIComponent(request.scopeId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListConflictingFacts",
      }) as Promise<ListConflictingFactsResponse>;
    },
    ListMemoryEntities(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l4/entities`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.scopeType) {
        queryParams.push(`scopeType=${encodeURIComponent(request.scopeType.toString())}`)
      }
      if (request.scopeId) {
        queryParams.push(`scopeId=${encodeURIComponent(request.scopeId.toString())}`)
      }
      if (request.workspaceId) {
        queryParams.push(`workspaceId=${encodeURIComponent(request.workspaceId.toString())}`)
      }
      if (request.userId) {
        queryParams.push(`userId=${encodeURIComponent(request.userId.toString())}`)
      }
      if (request.entityType) {
        queryParams.push(`entityType=${encodeURIComponent(request.entityType.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.keyword) {
        queryParams.push(`keyword=${encodeURIComponent(request.keyword.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListMemoryEntities",
      }) as Promise<ListMemoryEntitiesResponse>;
    },
    GetMemoryNeighborhood(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.centerId) {
        throw new Error("missing required field request.center_id");
      }
      const path = `v1/memory/l4/entities/${request.centerId}/neighborhood`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.hops) {
        queryParams.push(`hops=${encodeURIComponent(request.hops.toString())}`)
      }
      if (request.maxNodes) {
        queryParams.push(`maxNodes=${encodeURIComponent(request.maxNodes.toString())}`)
      }
      if (request.queryAt) {
        queryParams.push(`queryAt=${encodeURIComponent(request.queryAt.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetMemoryNeighborhood",
      }) as Promise<GraphNeighborhood>;
    },
    SpreadingActivation(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l4/spreading-activation`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.centerId) {
        queryParams.push(`centerId=${encodeURIComponent(request.centerId.toString())}`)
      }
      if (request.hops) {
        queryParams.push(`hops=${encodeURIComponent(request.hops.toString())}`)
      }
      if (request.topK) {
        queryParams.push(`topK=${encodeURIComponent(request.topK.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "SpreadingActivation",
      }) as Promise<SpreadingActivationResponse>;
    },
    GetAgentIdentity(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/identity`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetAgentIdentity",
      }) as Promise<AgentIdentity>;
    },
    GetAgentStrategy(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/strategy`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetAgentStrategy",
      }) as Promise<AgentStrategyProfile>;
    },
    ListEvolutionProposals(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/proposals`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListEvolutionProposals",
      }) as Promise<ListEvolutionProposalsResponse>;
    },
    ListEvolutionEvents(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/events`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListEvolutionEvents",
      }) as Promise<ListEvolutionEventsResponse>;
    },
    GetEvolutionMetrics(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/metrics`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.range) {
        queryParams.push(`range=${encodeURIComponent(request.range.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetEvolutionMetrics",
      }) as Promise<EvolutionMetricsReport>;
    },
    UpsertMemoryFact(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l3/facts`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "UpsertMemoryFact",
      }) as Promise<UpsertMemoryFactResponse>;
    },
    ReviewMemoryFact(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.factId) {
        throw new Error("missing required field request.fact_id");
      }
      const path = `v1/memory/l3/facts/${request.factId}/review`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "ReviewMemoryFact",
      }) as Promise<ReviewMemoryFactResponse>;
    },
    AppendEvolutionEvent(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.agentId) {
        throw new Error("missing required field request.agent_id");
      }
      const path = `v1/agents/${request.agentId}/evolution/events`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "AppendEvolutionEvent",
      }) as Promise<AppendEvolutionEventResponse>;
    },
    ListCascadeProposals(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/cascade/proposals`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.status) {
        queryParams.push(`status=${encodeURIComponent(request.status.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListCascadeProposals",
      }) as Promise<ListCascadeProposalsResponse>;
    },
    ApproveCascadeProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/cascade/proposals/${request.id}/approve`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "ApproveCascadeProposal",
      }) as Promise<ApproveCascadeProposalResponse>;
    },
    RejectCascadeProposal(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/cascade/proposals/${request.id}/reject`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "RejectCascadeProposal",
      }) as Promise<RejectCascadeProposalResponse>;
    },
    PreviewCascadeApprove(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/cascade/proposals/${request.id}/preview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "PreviewCascadeApprove",
      }) as Promise<PreviewCascadeApproveResponse>;
    },
    GetCascadeSagaSteps(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.proposalId) {
        throw new Error("missing required field request.proposal_id");
      }
      const path = `v1/memory/cascade/proposals/${request.proposalId}/saga-steps`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetCascadeSagaSteps",
      }) as Promise<GetCascadeSagaStepsResponse>;
    },
    RetryCascadeApprove(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/cascade/proposals/${request.id}/retry`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "RetryCascadeApprove",
      }) as Promise<RetryCascadeApproveResponse>;
    },
    CompensateCascadeApprove(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/cascade/proposals/${request.id}/compensate`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "CompensateCascadeApprove",
      }) as Promise<CompensateCascadeApproveResponse>;
    },
    DebugMemoryRecall(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/recall/debug`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "DebugMemoryRecall",
      }) as Promise<DebugMemoryRecallResponse>;
    },
    CompositeSearchMemories(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/search/composite`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "CompositeSearchMemories",
      }) as Promise<CompositeSearchMemoriesResponse>;
    },
    GetMemoryWorkerStatus(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/worker/status`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetMemoryWorkerStatus",
      }) as Promise<MemoryWorkerStatus>;
    },
    GetMemoryPlatformSettings(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/platform/settings`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetMemoryPlatformSettings",
      }) as Promise<MemoryPlatformSettings>;
    },
    UpdateMemoryPlatformSettings(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/platform/settings`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "PUT",
        body,
      }, {
        service: "MemoryService",
        method: "UpdateMemoryPlatformSettings",
      }) as Promise<MemoryPlatformSettings>;
    },
    ListMemoryDeadLetters(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/worker/dead-letters`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.state) {
        queryParams.push(`state=${encodeURIComponent(request.state.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListMemoryDeadLetters",
      }) as Promise<ListMemoryDeadLettersResponse>;
    },
    ReplayMemoryDeadLetter(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/worker/dead-letters/${request.id}/replay`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "ReplayMemoryDeadLetter",
      }) as Promise<ReplayMemoryDeadLetterResponse>;
    },
    AbandonMemoryDeadLetter(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      if (!request.id) {
        throw new Error("missing required field request.id");
      }
      const path = `v1/memory/worker/dead-letters/${request.id}/abandon`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "AbandonMemoryDeadLetter",
      }) as Promise<AbandonMemoryDeadLetterResponse>;
    },
    ListPIIFlaggedFacts(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l3/facts/pii`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.scopeType) {
        queryParams.push(`scopeType=${encodeURIComponent(request.scopeType.toString())}`)
      }
      if (request.scopeId) {
        queryParams.push(`scopeId=${encodeURIComponent(request.scopeId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListPIIFlaggedFacts",
      }) as Promise<ListPIIFlaggedFactsResponse>;
    },
    ReviewPIIFact(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/l3/facts/pii/review`; // eslint-disable-line quotes
      const body = JSON.stringify(request);
      const queryParams: string[] = [];
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "POST",
        body,
      }, {
        service: "MemoryService",
        method: "ReviewPIIFact",
      }) as Promise<ReviewPIIFactResponse>;
    },
    GetMemoryLayerOverview(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/layer-overview`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetMemoryLayerOverview",
      }) as Promise<GetMemoryLayerOverviewResponse>;
    },
    GetUnifiedMemoryGraph(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/graph/unified`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.focus) {
        queryParams.push(`focus=${encodeURIComponent(request.focus.toString())}`)
      }
      if (request.hops) {
        queryParams.push(`hops=${encodeURIComponent(request.hops.toString())}`)
      }
      if (request.minWeight) {
        queryParams.push(`minWeight=${encodeURIComponent(request.minWeight.toString())}`)
      }
      if (request.layers) {
        request.layers.forEach((x) => {
          queryParams.push(`layers=${encodeURIComponent(x.toString())}`)
        })
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "GetUnifiedMemoryGraph",
      }) as Promise<GetUnifiedMemoryGraphResponse>;
    },
    ListMemoryEpisodes(request) { // eslint-disable-line @typescript-eslint/no-unused-vars
      const path = `v1/memory/episodes`; // eslint-disable-line quotes
      const body = null;
      const queryParams: string[] = [];
      if (request.agentId) {
        queryParams.push(`agentId=${encodeURIComponent(request.agentId.toString())}`)
      }
      if (request.sessionId) {
        queryParams.push(`sessionId=${encodeURIComponent(request.sessionId.toString())}`)
      }
      if (request.limit) {
        queryParams.push(`limit=${encodeURIComponent(request.limit.toString())}`)
      }
      if (request.offset) {
        queryParams.push(`offset=${encodeURIComponent(request.offset.toString())}`)
      }
      let uri = path;
      if (queryParams.length > 0) {
        uri += `?${queryParams.join("&")}`
      }
      return handler({
        path: uri,
        method: "GET",
        body,
      }, {
        service: "MemoryService",
        method: "ListMemoryEpisodes",
      }) as Promise<ListMemoryEpisodesResponse>;
    },
  };
}

// @@protoc_insertion_point(typescript-http-eof)
