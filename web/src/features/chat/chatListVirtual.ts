/** Threshold above which the chat timeline uses Quasar virtual scroll. */
export const CHAT_VIRTUAL_SCROLL_THRESHOLD = 40;

/** Estimated row height (px) for q-virtual-scroll item sizing. */
export const CHAT_VIRTUAL_ROW_ESTIMATE = 148;

/** Max messages rendered before virtual scroll (safety cap for v-for fallback). */
export const CHAT_MESSAGE_RENDER_CAP = 300;
