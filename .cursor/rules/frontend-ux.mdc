---
description: Aranea 前端 UI/UX 约束。改样式、布局、主题、组件视觉时必须遵守。
globs:
  - web/src/**/*.vue
  - web/src/css/**/*
---

# Frontend UX Guardrail

改 UI 前必须先读：

1. `docs/frontend/UX.md` — token、玻璃材质、昼夜规则（唯一权威）
2. `docs/guides/frontend-guide.md` — 样式落点、红线
3. `.cursor/skills/aranea-frontend-ux/SKILL.md` — 项目专用 skill

## 必须

- 颜色/字号/间距用 `var(--*)`；新 token 写入 `web/src/css/theme/_css-vars-*.sass`
- 布局壳改动优先 `MainLayout.vue` + `app-global.sass`
- 浮层必须 `backdrop-filter` + `-webkit-backdrop-filter` 成对
- 主强调：`--color-accent`（昼 `#E9A23B`，夜 `#00E5FF` 仅焦点/边）
- 改完跑 `cd web && pnpm stylelint && pnpm build`
- 视觉验收：`http://localhost:9001/dev/theme-preview`（dev only）

## 禁止

- 在 `.vue` 硬编码 hex/rgb（token 定义文件除外）
- 日间使用夜间霓虹青紫作默认强调
- 与 `app-global.sass` 平行的第二套全局 CSS 入口
- 运行时用脚本改 `quasar-variables`
- 靠重 box-shadow 堆层级（昼）；玻璃层混用实体大卡

## 代表路由（浏览器抽检，不必全站截图）

`/chat` · `/overview` · `/agents` · `/graphs` · `/monitor/logs` · `/settings` · `/plugins` · `/models` · `/dev/theme-preview`

## 实体页排错（复用经验）

改 Registry 列表、Provider/Team 弹窗、趋势图时，先读 skill 内 **「实体页 / Registry / Dialog 优化 Playbook」**（`.cursor/skills/aranea-frontend-ux/SKILL.md`）：

- 列表空 → 先查组件 `import`；列错位 → 共用 grid `--*-col-template` + ListHeader
- 弹窗突兀 → 不用饱和 Stepper/实心 primary；玻璃向导 + 底部统一操作栏
- 图表占位 → 复用 `usageEcharts` / `useUsageChart`，KPI + 240px 图 + detail grid
