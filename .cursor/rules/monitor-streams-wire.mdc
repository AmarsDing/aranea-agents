---
description: 监控 Audit / Logs / Events 分流约定；SSE 与 Wire 纪律
alwaysApply: false
---

# 监控流与 Wire（精简）

## 三件事分流

| 类型 | 用途 | 入口 / 落点 |
|------|------|-------------|
| Audit | 管理侧可追溯操作 | `GET /v1/monitor/audit` → `audit_logs`；`monitor.go` / `data/monitor.go` |
| Logs | 运维文本流 | SSE `/monitor/logs/stream`，`event: log` → **`MonitorLogBroker.Publish`** |
| Events | Team/Agent 运行时 JSON | SSE `/team-run-events`，`internal/server/team_run_sse.go` |

- **Audit**：重要配置变更写 **`audit_logs`**，`detail` 不脱敏密钥；别把瞬时运维句子塞进 Audit。
- **Logs / Events**：编排侧事实走 **`TeamRunEventBroker.Publish`**；需要上「日志」页的运维句再走 **`MonitorLogBroker`**（可与 `log` 并存）。
- **`TeamRunEvent`**：`run` / `step` 字段 **snake_case**（`team_types.go`）；扩展进 **`payload`**；尽量带 **`session_id`**。

## 前端与开发代理（改监控 SSE 时记着）

- **`getSseBaseURL()`**：开发环境固定 **`/sse`**（见 `web/src/config/runtime.ts`）；生产跨端口用 **`runtime-config.json` 的 `sseOrigin`**。
- **Vite**：`/sse` 代理只用 **`rewrite`** 剥前缀，**不要用 webpack 的 `pathRewrite`**（`web/quasar.config.ts`）。
- **`EventSource`**：连接建立靠 **`onopen`** 表示「已连通」；仅靠注释行不会触发命名事件（见 `web/src/features/monitor/api.ts`）。

## Wire

- **`cmd/admin/wire_gen.go` 禁止手改**；注入变更后 **`make wire-admin`**（或 **`make all`**），再 **`go build ./...`**。

细则与排障写在 **`docs/需求/18 monitor.md`**，不必抄进本 rule。
